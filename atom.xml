<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>楼兰</title>
  
  
  <link href="https://trevoewu.github.io/atom.xml" rel="self"/>
  
  <link href="https://trevoewu.github.io/"/>
  <updated>2023-06-19T06:04:16.635Z</updated>
  <id>https://trevoewu.github.io/</id>
  
  <author>
    <name>楼兰</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>实践课程笔记</title>
    <link href="https://trevoewu.github.io/2023/06/19/%E5%AE%9E%E8%B7%B5%E8%AF%BE%E7%A8%8B%E7%AC%94%E8%AE%B0/"/>
    <id>https://trevoewu.github.io/2023/06/19/%E5%AE%9E%E8%B7%B5%E8%AF%BE%E7%A8%8B%E7%AC%94%E8%AE%B0/</id>
    <published>2023-06-19T05:56:52.000Z</published>
    <updated>2023-06-19T06:04:16.635Z</updated>
    
    <content type="html"><![CDATA[<h1>实践课程笔记</h1><h2 id="第一课，软件工程概论">第一课，软件工程概论</h2><p>时间：6-19</p><p>地点：新主楼E1129</p><ol><li>软件的概念</li><li>软件的特点</li><li>软件的分类</li><li>软件的发展</li></ol><h3 id="软件的概念">软件的概念</h3><p>软件是指计算机系统中的可执行程序或程序集合，它们以一定的算法和逻辑规则组织起来，用于实现特定的功能或完成特定的任务。</p><p>软件可以分为系统软件和应用软件两大类。</p><ol><li><p>系统软件：系统软件是直接运行在计算机硬件上，为上层应用软件提供基本的支持和管理功能。常见的系统软件包括操作系统（如Windows、Linux、macOS）、编译器、链接器、调试器、设备驱动程序等。系统软件的主要作用是管理计算机的硬件资源，提供各种服务和接口，以便应用软件能够运行和使用硬件设备。</p></li><li><p>应用软件：应用软件是针对特定任务或领域开发的软件，用于满足用户的具体需求。应用软件可以涵盖各种领域，如办公软件（如Microsoft Office、OpenOffice）、图形图像处理软件（如Adobe Photoshop、GIMP）、数据库管理软件（如MySQL、Oracle）、游戏软件、音视频播放器等。应用软件的目标是通过特定的功能和界面，为用户提供实际的工具和应用，帮助用户完成各种任务或娱乐活动。</p></li></ol><p>软件开发通常经历需求分析、设计、编码、测试和部署等阶段。在编码过程中，程序员使用编程语言将问题解决方案转化为计算机可执行的代码。测试阶段用于验证软件的正确性和稳定性，确保软件能够按照预期的方式工作。</p><p>软件的概念涵盖了广泛的范围，从底层的系统软件到上层的应用软件，涉及了计算机科学、软件工程和用户体验等多个领域。软件在现代社会中扮演着重要的角色，促进了信息技术的发展和人类活动的智能化。</p><h3 id="软件的特点">软件的特点</h3><p>软件具有以下几个主要特点：</p><ol><li><p>可编程性：软件是可编程的，它们由一系列指令和算法组成，可以根据需求进行灵活的修改和扩展。程序员可以使用编程语言编写、调试和修改软件代码，以实现特定的功能或解决问题。</p></li><li><p>抽象性：软件通过对问题进行抽象和建模，将复杂的现实世界问题转化为计算机可理解和处理的形式。通过抽象，软件可以忽略不必要的细节，集中关注问题的核心，提高问题解决的效率和可靠性。</p></li><li><p>可重用性：软件具有可重用性，即已经开发完成的软件模块可以在不同的项目或应用中被重复利用。通过软件复用，可以提高开发效率、降低成本，并促进软件工程的进一步发展。</p></li><li><p>可移植性：软件可以在不同的计算机平台和操作系统上运行，具有一定的可移植性。通过合理设计和编写软件，可以使其具备跨平台的能力，提高软件的适应性和灵活性。</p></li><li><p>可靠性：软件应该具备良好的可靠性，即能够在各种条件下正常运行，并产生正确的结果。为了提高软件的可靠性，需要进行充分的测试和调试，并采用合适的软件工程方法和技术。</p></li><li><p>可变性：软件易于修改和更新，可以根据用户需求和业务变化进行快速调整。相对于硬件而言，软件的变更成本较低，能够更好地适应不断变化的环境和需求。</p></li><li><p>可扩展性：软件具有可扩展性，即可以通过添加新的功能模块或组件来扩展现有的软件系统，以满足不断变化的需求和用户期望。</p></li></ol><p>总之，软件的特点包括可编程性、抽象性、可重用性、可移植性、可靠性、可变性和可扩展性。这些特点使软件成为现代计算机系统中不可或缺的组成部分，并在各个领域中发挥着重要的作用。</p><h3 id="软件的分类">软件的分类</h3><p>软件可以按照不同的分类标准进行分类。下面是一些常见的软件分类方式：</p><ol><li><p>操作系统软件：操作系统是计算机系统中最基本的软件，用于管理和控制计算机硬件资源，提供用户与计算机硬件的接口。常见的操作系统包括Windows、Linux、macOS等。</p></li><li><p>应用软件：应用软件是为满足特定任务或领域需求而开发的软件。它可以进一步细分为以下几类：</p><ul><li>办公软件：用于处理文档、制表、演示等办公任务，如Microsoft Office、OpenOffice等。</li><li>图形图像处理软件：用于图像编辑、图形设计等任务，如Adobe Photoshop、GIMP等。</li><li>数据库管理软件：用于管理和处理数据库，如MySQL、Oracle等。</li><li>游戏软件：用于娱乐和游戏的软件，如电脑游戏、手机游戏等。</li><li>多媒体软件：用于音频、视频播放、编辑和处理等任务，如Windows Media Player、VLC媒体播放器等。</li><li>网络通信软件：用于网络通信和协作，如网页浏览器、电子邮件客户端等。</li></ul></li><li><p>开发工具和编程软件：这类软件用于开发其他软件，包括编程语言、集成开发环境（IDE）、文本编辑器、调试器等。</p></li><li><p>系统工具软件：这类软件用于管理和优化计算机系统，包括系统维护工具、安全工具、磁盘清理工具、防病毒软件等。</p></li><li><p>嵌入式软件：嵌入式软件是被嵌入到电子设备或系统中的软件，用于控制、管理和协调硬件和设备的功能。例如，手机、汽车、家电等设备中的软件。</p></li><li><p>算法和数据分析软件：这类软件用于进行数据分析、模型建立和算法实现，支持科学计算、统计分析和机器学习等任务。</p></li></ol><p>此外，还可以根据软件的许可证类型进行分类，如开源软件和商业软件；根据软件的平台或操作系统进行分类，如Windows软件、Linux软件、iOS软件等；根据软件的功能和领域进行分类，如金融软件、医疗软件、教育软件等。</p><p>这些分类方式只是对软件进行粗略划分，实际上软件领域非常广泛，涵盖了各种类型和领域的软件。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;实践课程笔记&lt;/h1&gt;
&lt;h2 id=&quot;第一课，软件工程概论&quot;&gt;第一课，软件工程概论&lt;/h2&gt;
&lt;p&gt;时间：6-19&lt;/p&gt;
&lt;p&gt;地点：新主楼E1129&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;软件的概念&lt;/li&gt;
&lt;li&gt;软件的特点&lt;/li&gt;
&lt;li&gt;软件的分类&lt;/li&gt;
&lt;li&gt;</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>学习杂谈：持久层框架那些链接数据库的操作</title>
    <link href="https://trevoewu.github.io/2023/06/10/%E5%AD%A6%E4%B9%A0%E6%9D%82%E8%B0%88%EF%BC%9A%E6%8C%81%E4%B9%85%E5%B1%82%E6%A1%86%E6%9E%B6%E9%82%A3%E4%BA%9B%E9%93%BE%E6%8E%A5%E6%95%B0%E6%8D%AE%E5%BA%93%E7%9A%84%E6%93%8D%E4%BD%9C/"/>
    <id>https://trevoewu.github.io/2023/06/10/%E5%AD%A6%E4%B9%A0%E6%9D%82%E8%B0%88%EF%BC%9A%E6%8C%81%E4%B9%85%E5%B1%82%E6%A1%86%E6%9E%B6%E9%82%A3%E4%BA%9B%E9%93%BE%E6%8E%A5%E6%95%B0%E6%8D%AE%E5%BA%93%E7%9A%84%E6%93%8D%E4%BD%9C/</id>
    <published>2023-06-10T11:26:53.000Z</published>
    <updated>2023-06-11T11:53:02.194Z</updated>
    
    <content type="html"><![CDATA[<p>为了进行数据的持久化，和数据库操作是必须的操作。 本文探讨不同的持久层框架链接数据库的操作。</p><span id="more"></span><h2 id="JDBC">JDBC</h2><p>JDBC 为多个不同的数据库实现提供了同一的接口，使得我们可以通过JDBC来操作不同的数据库，而不需要担心数据库的具体实现。</p><p>要在Java项目中使用JDBC，首先需要引进jar包。假设我们需要链接的数据库是mysql，则需要引进mysql相关的jar包。</p><p>使用Maven是进行包管理是相当便捷的做法，要使用maven，首先我们需要新建一个Maven的项目或者模块。这里就用Java唯一指定IDE IntlliJ IDEA 来演示。</p><pre><code class="language-xml">&lt;!--        mysql --&gt;&lt;dependency&gt;  &lt;groupId&gt;mysql&lt;/groupId&gt;  &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;  &lt;version&gt;8.0.28&lt;/version&gt;&lt;/dependency&gt;</code></pre><p>要链接数据库，自然少不了要链接数据库的参数，比如Driver，URL，以及Username，Password等等。所以我们需要新建一个properties 来存放这些数据，而不是写死在Java代码中。</p><pre><code class="language-properties">DRIVER=com.mysql.cj.jdbc.DriverURL=mysql://localhost:3306/demo?useUnicode=true&amp;characterEncoding=utf-8USERNAME=rootPASSWORD=root</code></pre><ol><li>加载驱动</li></ol><p>数据库有着不同的实现，mysql，oracle，monogDB，SQL Server等等，而每个数据库都提供一个驱动程序，驱动程序是与数据库系统进行通信的软件组件。每种数据库系统都有自己的驱动程序，这是因为不同数据库系统采用不同的协议和通信方式。</p><p><strong>驱动程序充当Java程序与数据库之间的桥梁</strong>，负责处理与数据库的连接、执行SQL语句以及获取和处理查询结果等任务。它们提供了一组标准的接口和方法，使得Java程序能够通过统一的方式与不同数据库进行交互，而不需要了解底层数据库的具体实现细节。</p><p>加载数据库驱动程序的常用方式是使用<code>Class.forName()</code>方法，例如：</p><p>在这里，我们需要加载mysql的驱动程序，MySQL数据库的驱动程序类名为<code>com.mysql.jdbc.Driver</code>（旧版）或<code>com.mysql.cj.jdbc.Driver</code>（新版）。</p><pre><code class="language-java">Class.forName(&quot;com.mysql.cj.jdbc.Driver&quot;); // 加载MySQL驱动程序</code></pre><p>一旦数据库驱动程序加载成功，就可以通过JDBC建立与数据库的连接并执行相关操作。</p><ol start="2"><li>建立数据库连接</li></ol><p>要和数据库进程操作，我们需要和数据库建立一个会话关系，就像在web中一样，在web中，这个会话被称为httpSession, 而在数据库中，也同样存在着这个会话。</p><p>一个会话代表了客户端与数据库之间的通信通道，它允许客户端应用程序与数据库服务器进行交互。</p><pre><code class="language-java">// 2.连接数据库Connection connection = DriverManager.getConnection(url, username, password);</code></pre><p>在JDBC中，通过<code>DriverManager.getConnection()</code>方法建立数据库连接时，实际上是在建立一个会话。该方法返回一个<code>Connection</code>对象，表示与数据库的连接。这个<code>Connection</code>对象封装了与数据库服务器的底层通信细节，包括连接信息、会话状态和数据传输等。</p><p>通过会话，可以在连接期间执行多个SQL语句，保持数据库连接的状态。这样可以避免每次执行SQL语句都重新建立连接，提高性能和效率。当不再需要连接时，可以调用<code>Connection</code>对象的<code>close()</code>方法关闭连接，释放相关资源。</p><ol start="3"><li>创建执行SQL语句的对象</li></ol><p>有了链接，我们就可以通过<code>Connection</code>对象的<code>createStatement()</code>方法创建<code>Statement</code>对象或<code>prepareStatement()</code>方法创建<code>PreparedStatement</code>对象。</p><p><code>Statement</code>用于执行静态的SQL语句，而<code>PreparedStatement</code>用于执行预编译的SQL语句，可以提高性能和安全性，防止SQL注入。</p><ol start="4"><li>执行SQL语句：</li></ol><p>通过<code>Statement</code>或<code>PreparedStatement</code>对象的<code>executeQuery()</code>方法执行查询语句，返回一个<code>ResultSet</code>对象，其中包含查询结果；通过<code>executeUpdate()</code>方法执行更新语句（如插入、更新、删除），返回受影响的行数。</p><ol start="5"><li>处理结果集：</li></ol><p>但是仅仅是往数据库中传SQL语句，显然是不够的，我们还需要接受数据库返回的数据。</p><p>数据库的操作有四种，我们将其称为CRUD，即CREATE, READ, UPDATE and DELETE，也就是我们常说的增删改查。其中查询操作返回一个结果集Result，其他的操作都返回一个int类型的整数，代表受影响的行数。</p><p>所以，我们只需要编写两个函数，处理这两种不同的操作。</p><p>对于查询操作</p><pre><code class="language-java">//执行查询, 返回结果集public static ResultSet executeQuery(Connection conn,String query,Object[] params) throws SQLException, ClassNotFoundException&#123;    ResultSet resultSet = null;    if(conn != null &amp;&amp; !query.isEmpty() &amp;&amp; params != null)&#123;        //预处理        PreparedStatement statement = conn.prepareStatement(query);        for (int i = 0; i &lt; params.length;i++)&#123;            //设置SQL 语句中的参数            statement.setObject(i+1,params[i]);        &#125;        //执行        resultSet = statement.executeQuery();    &#125;    return resultSet;&#125;</code></pre><p>对于更新删除，增加操作：</p><pre><code class="language-java">//执行更新， 返回影响行数public static int executeUpdate(Connection conn,String query,Object[] params) throws SQLException &#123;    int affectedRows = 0;    if(conn != null &amp;&amp; !query.isEmpty() &amp;&amp; params != null)&#123;        //预处理        PreparedStatement statement = conn.prepareStatement(query);        for (int i = 0; i &lt; params.length;i++)&#123;            //设置SQL 语句中的参数            statement.setObject(i+1,params[i]);        &#125;        //执行         affectedRows = statement.executeUpdate();    &#125;    return affectedRows;&#125;</code></pre><ol start="6"><li>关闭连接和资源</li></ol><p>使用完<code>ResultSet</code>、<code>Statement</code>和<code>Connection</code>等对象后，需要调用它们的<code>close()</code>方法来关闭连接和释放资源，以免造成内存泄漏和资源浪费。</p><p>我们可以编写一个函数来专门关闭这些资源，但这不是必须的，你也可以手动关闭。</p><pre><code class="language-java">//释放链接资源public static void close(Connection conn, PreparedStatement pstmt, ResultSet resSet) throws SQLException &#123;    if(resSet != null)resSet.close();    if(pstmt != null)pstmt.close();    if(conn != null) conn.close();&#125;</code></pre><p>果然好麻烦，但是还没有结束，查询操作返回了一个结果集，我们还需要对这个结果集进行ORM处理，即Object Relationship Mapping。把数据库中的entity映射到一个bean上，这是最麻烦的。因为我们需要一一个处理这些字段和属性的对应关系。</p><p>下面这段代码就是一个一个例子，可以看到，对于每一个对象的属性值，我们需要一一的找到对应的字段值。这还只是查询一张表，在多表联合查询的过程中，我们需要封装的对象数量更多。我们不应该花太多时间处理这些重复而又无意义的事。</p><p>下面介绍的持久层框架，就是对JDBC的二次封装，免去了我们编写ORM的代码。</p><pre><code class="language-java">public User findById(int id) &#123;    Object[] params = &#123;id&#125;;    ResultSet set;    sql = &quot;SELECT * FROM smbms_user,smbms_role Where smbms_user.userRole = smbms_role.id and smbms_user.id = ?&quot;;    try &#123;        conn = DB.getConnection();        set = DB.executeQuery(conn, sql,params);        if(set != null)&#123;            user = new User();            while (set.next()) &#123;                user.setUserRoleName(set.getString(&quot;roleName&quot;));                user.setUserPassword(set.getString(&quot;userPassword&quot;));                user.setId(set.getInt(&quot;id&quot;));                user.setUserCode(set.getString(&quot;userCode&quot;));                user.setUserName(set.getString(&quot;userName&quot;));                user.setAddress(set.getString(&quot;address&quot;));                user.setBirthday((Date) set.getObject(&quot;birthday&quot;));                user.setGender(set.getInt(&quot;gender&quot;));                user.setPhone(set.getString(&quot;phone&quot;));                user.setIdPicPath(set.getString(&quot;idPicPath&quot;));                user.setUserRole(set.getInt(&quot;userRole&quot;));                user.setCreatedBy(set.getInt(&quot;createdBy&quot;));                user.setCreationDate( set.getDate(&quot;creationDate&quot;));                user.setModifyBy(set.getInt(&quot;modifyBy&quot;));                user.setModifyDate(set.getDate(&quot;modifyDate&quot;));                user.setWorkPicPath(&quot;workPicPath&quot;);            &#125;        &#125; else &#123;            return null;        &#125;        //关闭链接和结果集        DB.close(conn,null,set);    &#125; catch (SQLException e) &#123;        throw new RuntimeException(e);    &#125; catch (ClassNotFoundException e) &#123;        throw new RuntimeException(e);    &#125;    return user;&#125;</code></pre><h2 id="MyBatis">MyBatis</h2><p>MyBatis是一个优秀的持久层框架，是对JDBC的二次封装。该框架大大简化了ORM过程，MyBatis可以通过字段和属性名自动进行ORM操作。</p><ol><li>要使用MyBatis，当然需要引入相关的jar包：</li></ol><pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;org.mybatis&lt;/groupId&gt;    &lt;artifactId&gt;mybatis&lt;/artifactId&gt;    &lt;version&gt;3.5.13&lt;/version&gt;&lt;/dependency&gt;</code></pre><ol start="2"><li>编写MyBatis配置文件，这个配置文件我们可以从官网中拿到对应的模版，这是mybatis的核心配置文件，通过编写配置文件，我们可以得到mybatis的不同表现。</li></ol><p>最好将这个配置文件编写在根路径下，放在资源文件有是再好不过的选择</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE configuration        PUBLIC &quot;-//mybatis.org//DTD Config 3.0//EN&quot;        &quot;https://mybatis.org/dtd/mybatis-3-config.dtd&quot;&gt;&lt;configuration&gt;&lt;!--    从db.properties读取数据库属性--&gt;    &lt;properties resource=&quot;db.properties&quot;/&gt;    &lt;environments default=&quot;development&quot;&gt;        &lt;environment id=&quot;development&quot;&gt;            &lt;transactionManager type=&quot;JDBC&quot;/&gt;            &lt;dataSource type=&quot;POOLED&quot;&gt;                &lt;property name=&quot;driver&quot; value=&quot;$&#123;DRIVER&#125;&quot;/&gt;                &lt;property name=&quot;url&quot; value=&quot;$&#123;URL&#125;&quot;/&gt;                &lt;property name=&quot;username&quot; value=&quot;$&#123;USERNAME&#125;&quot;/&gt;                &lt;property name=&quot;password&quot; value=&quot;&quot;/&gt;            &lt;/dataSource&gt;        &lt;/environment&gt;    &lt;/environments&gt;&lt;/configuration&gt;</code></pre><p>在这个配置文件中，我们只配置的数据源，后续的配置随着讲解的深入再一一添加</p><ol start="2"><li><p>编写bean</p><pre><code class="language-java">package cn.loulan.bean;import lombok.Data;@Datapublic class Student &#123;    private int id;    private String name;    private String sex;    private String major;    private int age;&#125;</code></pre></li><li><p>编写接口</p></li></ol><p>和JDBC一样，我们需要编写接口，在接口中定义方法。面向接口编程可以大大降低层与层之间的耦合度。</p><pre><code class="language-java">package cn.loulan.dao;import cn.loulan.bean.Student;import org.apache.ibatis.annotations.Param;import java.util.List;public interface StudentMapper &#123;    int insert(Student student);    int update(Student student);    Student getStudent(int id);    List&lt;Student&gt; getAll();    int delete(@Param(&quot;id&quot;) int id);&#125;</code></pre><p>不同的是，由于不需要编写接口实现类了，Mybatis会通过反射的方式在程序运行时生成实现类。所以我们需要使用<code>@Param</code>注解来指定参数，如果参数只有一个则不需要显式的给出，参数类型是复杂类型如java bean时也不需要显式的给出。</p><ol start="4"><li>编写mapper</li></ol><p>没有了实现了类，但是我们还需要编写mapper文件，其实也可以直接使用注解的方式定义SQL语句，但是这样对于较为复杂的SQL语句代码的可读性将大大降低。所以我们还是基于xml的的方式。</p><p>mapper使用xml格式，且需要保证与接口名称相同。 在mapper中，要指定命名空间，这个命名空间指向接口类，使用接口类的全限定名。</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE mapper        PUBLIC &quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;        &quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;&gt;&lt;mapper namespace=&quot;cn.loulan.dao.StudentMapper&quot;&gt;    &lt;select id=&quot;getStudent&quot; resultType=&quot;cn.loulan.bean.Student&quot;&gt;        select * from student where id=#&#123;id&#125;;    &lt;/select&gt;&lt;/mapper&gt;</code></pre><p>和JDBC一样，我们需要和数据库建立连接，拿到会话，在Mybatis中，这个会话被称为Sql Session要拿到这个SqlSession, 还有点麻烦，这里Mybatis使用了一个设计模式，叫做建造者模式。这是GOF23种设计模式的创造型设计模式之一。要理解建造者模式，我们需要首先了解工厂模式。</p><p>工厂模式在spring中有着大量的运用，比如动态代理中Bean的实例化就是工厂模式的一个运用。每一个bean都对应一个工厂，用来生成这个bean。这里的工厂模式确实的说是静态工厂模式，也叫做简单工厂模式，这个设计模式并不在GOF23种设计模式之内，但是运用的也比较多，而且简单工厂模式其实算得上是工厂模式的一个特例。</p><p>要理解工厂模式，首先我们需要知道为什么需要工厂模式？</p><p>面向对象有七大设计原则，第一个便是单一职责原则。顾名思义就是一个类只负责一个职责。我们使用一个类，不需要担心这个是如何被创建出来的，就像在生活中我们买一台电脑不需要关心这个电脑是如何生产出来的。电脑的生产由生产这个电脑的工厂负责。相较于自己造一台电脑，显然去买一台工厂生产的电脑更为现实。</p><p>简单工厂模式就是这样思想的一种体验，在软件开发过程中，我们往往需要实例化一些来着同一个父类的对象, 这个父类定义了这个工厂下所以对象的通用方法。为此专门提供一个工厂来帮我们实例化这个对象，我们只需要提供不同的参数，就可以得到不同的实例化对象，在Java开发中，我们常常把工厂类的实例化这个对象的方法定义为一个静态方法，这样，我们就不需要实例化这个类就可以调用这个方法了，这也就是静态工厂模式名字的来源。</p><p>简单工厂模式包含三个重要的角色：</p><ol><li>工厂 Factory:  工厂模式的核心角色，这个角色提供了一个静态方法，这个方法负责实例化对象并返回，返回类型为抽象产品。</li><li>抽象产品 Product： 抽象产品是工厂建造的所以具体对象的父类，负责描述所以实例化对象的公共接口。它的引入使得系统更加灵活，在工厂类中只需要定义一个方法，因为所以的具体产品对象都是其子类对象。</li><li>具体产品 ConcreteProduct： 简单工厂模式的创建目标，是抽象产品的具体表现，需要实现抽象产品中定义的方法。</li></ol><!--to do... 补充工厂模式，建造者模式--><p>在Mybatis中，便使用工厂模式这一思想。要拿到sqlsession，我们需要先拿到工厂对象，通过读取配置文件，MyBatis会创建一个SqlSessionFactory对象。通过工厂对象的openSession我们就可以拿到这个Sql Session了。SqlSession是与数据库进行交互的主要接口，它封装了数据库会话和执行SQL操作的方法。</p><pre><code class="language-java">package cn.loulan.dao;import cn.loulan.bean.Student;import org.apache.ibatis.session.SqlSession;import org.apache.ibatis.session.SqlSessionFactory;import org.apache.ibatis.session.SqlSessionFactoryBuilder;import org.junit.Test;import java.io.InputStream;public class TestStudent &#123;    @Test    public void testGetStudent() throws Exception &#123;        //1. 加载mybatis配置文件        InputStream resourceAsStream = TestStudent.class.getClassLoader().getResourceAsStream(&quot;mybatis.xml&quot;);      //2. 拿到工厂对象        SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(resourceAsStream);      //3. sqlSession对象        SqlSession sqlSession = sqlSessionFactory.openSession();      //4. 拿到接口实现类        StudentMapper mapper = sqlSession.getMapper(StudentMapper.class);      //5. 关闭sql session释放资源        sqlSession.close();      //6. 输出查询结果        Student student = mapper.getStudent(1);        System.out.println(student);    &#125;&#125;</code></pre><!--饿了，吃饭去了--><!--我来了，继续继续。哎，越来越搞不懂自己了，一躺在床上就会想瑟瑟。感觉午觉睡了根没睡一样，很奇怪啊，一躺下就忍不住想Moon。我也不知道我喜欢她什么，高中时期的暗恋对象能持续这么久的影响我。她都邀请我去她家玩了，虽然确实很想去，但是自己想去的动机总感觉不纯。而且也怪不好意思的，她姐姐我也不熟，去她家玩的话自己也会很难办。且不说往返的机票钱就不是一个小数目，为什么要跑到这么远上大学呢，我又开始怀疑自己了。要是在上海读书的话就方便很多了。哎～加上还有准备考研，准备考研，一直都在准备，准备着准备着，到目前也没什么进度，感觉就像默认自己已经要二战了一样。好可悲啊，哪有还没考就想着下一次的啊，怎么说呢，也不能讨厌这样的自己。要和自己和平相处，不要和自己为敌。假期把CSAPP搞定，一定要加油！--><p>到这里Mybatis链接数据库的操作就结束了，虽然还有很多没有设计，比如事务管理，对象关系映射啊，但是这又不是Mybatis教程，要看mybatis教程的话移步博客文章mybatis。</p><p>总得来说，mybatis确实相较于直接用JDBC方便很多，自己再也不用处理令人头疼的结果集了，只需要写些配置文件就好了，效率提高了很多。但是听说到了spring boot就更方便了，总感觉学不完啊。</p><h2 id="Spring集成Mybatis框架">Spring集成Mybatis框架</h2><p>接下来就是spring集成mybatis框架了，集成spring可以通过依赖注入（Dependency Injection）来降低层与层之间的耦合度，具体来说就是对象不需要我们自己new了，对象间关系的维护也不需要我们自己维护了，全部交给Spring来管理。</p><p>spring集成mybatis框架感觉就是把mybatis配置移到了Spring的配置中去，以前是mybatis通过反射实例化对象，变成了spring和mybatis共同完成了。</p><ol><li>MyBatis部分：MyBatis通过Mapper接口的代理方式来执行SQL操作。在MyBatis中，通过配置映射文件（Mapper XML）来定义SQL语句和映射关系。MyBatis会根据映射文件中的配置，动态生成Mapper接口的代理对象，实现了Mapper接口中定义的方法。</li><li>Spring部分：Spring负责管理和配置应用程序中的各个组件，包括Mapper接口的实例化和依赖注入。在集成MyBatis时，Spring会扫描指定的包路径，检测并实例化Mapper接口的代理对象。Spring提供了<code>@MapperScan</code>注解来自动扫描和实例化Mapper接口。</li></ol><p>当Spring实例化Mapper接口时，它会使用MyBatis提供的<code>MapperProxyFactory</code>类来创建Mapper接口的代理对象。该代理对象在执行方法时，会委托给MyBatis的底层实现来执行相应的SQL操作。</p><p>通过Spring和MyBatis的协作，实现了Mapper接口的实例化和使用，使得开发者可以在Spring中轻松地使用MyBatis进行数据库操作。同时，Spring还负责管理事务、数据源配置等与数据库相关的事务处理和连接管理工作，与MyBatis相结合提供了更完整的数据库访问解决方案。</p><p>以上内容全由chatGPT生成，我是不会的。</p><p>继续。</p><p>照例，先引入jar包</p><pre><code class="language-xml">&lt;dependency&gt;  &lt;groupId&gt;org.mybatis&lt;/groupId&gt;  &lt;artifactId&gt;mybatis-spring&lt;/artifactId&gt;  &lt;version&gt;3.0.0&lt;/version&gt;&lt;/dependency&gt;&lt;dependency&gt;  &lt;groupId&gt;com.alibaba&lt;/groupId&gt;  &lt;artifactId&gt;druid&lt;/artifactId&gt;  &lt;version&gt;1.2.16&lt;/version&gt;&lt;/dependency&gt;&lt;dependency&gt;  &lt;groupId&gt;org.springframework&lt;/groupId&gt;  &lt;artifactId&gt;spring-tx&lt;/artifactId&gt;  &lt;version&gt;6.0.8&lt;/version&gt;&lt;/dependency&gt;&lt;dependency&gt;  &lt;groupId&gt;org.springframework&lt;/groupId&gt;  &lt;artifactId&gt;spring-jdbc&lt;/artifactId&gt;  &lt;version&gt;6.0.8&lt;/version&gt;&lt;/dependency&gt;&lt;!--        MyBatis--&gt;&lt;dependency&gt;    &lt;groupId&gt;org.mybatis&lt;/groupId&gt;    &lt;artifactId&gt;mybatis&lt;/artifactId&gt;    &lt;version&gt;3.5.13&lt;/version&gt;&lt;/dependency&gt;&lt;dependency&gt;    &lt;groupId&gt;org.springframework&lt;/groupId&gt;    &lt;artifactId&gt;spring-context&lt;/artifactId&gt;    &lt;version&gt;6.0.8&lt;/version&gt;&lt;/dependency&gt;</code></pre><p>差不多就这些了，主要包括spring框架的依赖，mybatis依赖，spring集成mybatis的依赖，德鲁伊的依赖。</p><p>累了，不想写了。下次补充。。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;为了进行数据的持久化，和数据库操作是必须的操作。 本文探讨不同的持久层框架链接数据库的操作。&lt;/p&gt;</summary>
    
    
    
    <category term="杂谈" scheme="https://trevoewu.github.io/categories/%E6%9D%82%E8%B0%88/"/>
    
    
    <category term="杂谈" scheme="https://trevoewu.github.io/tags/%E6%9D%82%E8%B0%88/"/>
    
    <category term="mybatis" scheme="https://trevoewu.github.io/tags/mybatis/"/>
    
    <category term="JDBC" scheme="https://trevoewu.github.io/tags/JDBC/"/>
    
    <category term="JDBCtemplate" scheme="https://trevoewu.github.io/tags/JDBCtemplate/"/>
    
  </entry>
  
  <entry>
    <title>手写简易Spring框架</title>
    <link href="https://trevoewu.github.io/2023/06/09/%E6%89%8B%E5%86%99%E7%AE%80%E6%98%93Spring%E6%A1%86%E6%9E%B6/"/>
    <id>https://trevoewu.github.io/2023/06/09/%E6%89%8B%E5%86%99%E7%AE%80%E6%98%93Spring%E6%A1%86%E6%9E%B6/</id>
    <published>2023-06-09T06:10:22.000Z</published>
    <updated>2023-06-09T06:11:33.799Z</updated>
    
    <content type="html"><![CDATA[<h2 id="手写Spring框架">手写Spring框架</h2><p>Spring底层是通过反射机制来实例化对象，以及属性注入的，在手写Spring框架之前，我们需要了解反射机制。</p><blockquote><p>JAVA反射机制是在运行状态中，对于任意一个类，都能够知道这个类的所有属性和方法；对于任意一个对象，都能够调用它的任意一个方法和属性；这种动态获取的信息以及动态调用对象的方法的功能称为java语言的反射机制。Java反射机制在框架设计中极为广泛，需要深入理解。</p></blockquote><p>通过反射，我们可以拿到类的属性，方法。 回顾Spring框架，我们需要编写一个spring的配置文件例如：spring.xml，在这个配置文件中，我们注册一个类，并注入属性值。</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;beans&gt;    &lt;beans&gt;        &lt;bean name=&quot;user&quot; class=&quot;bean.User&quot;&gt;            &lt;property name=&quot;id&quot; value=&quot;1&quot;/&gt;            &lt;property name=&quot;username&quot; value=&quot;sonetto&quot;/&gt;            &lt;property name=&quot;password&quot; value=&quot;123456&quot;/&gt;            &lt;property name=&quot;points&quot; value=&quot;12.3&quot;/&gt;            &lt;property name=&quot;isActive&quot; value=&quot;true&quot;/&gt;        &lt;/bean&gt;        &lt;bean name=&quot;userDao&quot; class=&quot;bean.UserDao&quot;/&gt;        &lt;bean name=&quot;userService&quot; class=&quot;bean.UserService&quot;&gt;            &lt;property name=&quot;userDao&quot; ref=&quot;userDao&quot;/&gt;        &lt;/bean&gt;    &lt;/beans&gt;&lt;/beans&gt;</code></pre><p>在实例化一个对象时，我们通过<code>ClassPathXmlApplicationContext()</code>，传入我们的配置文件，拿到一个<code>ApplicationContext</code>对象，通过<code>ApplicationContext</code>对象调用<code>getBean（）</code>方法来获取对象, 最终调用对象发方法，来使用Bean。</p><pre><code class="language-java">ApplicationContext context = new ClassPathXmlApplicationContext(&quot;mySpring.xml&quot;);        User user = (User) context.getBean(&quot;user&quot;);        System.out.println(user);        UserService userService = (UserService) context.getBean(&quot;userService&quot;);        userService.createUser();</code></pre><p>我们的最终目的就是调用对象的方法，而一个指定一个方法需要四要素： 什么对象（对象的全限定类名是什么？）什么方法（方法名），什么参数（参数的类型和数量），返回什么值。</p><p>指定来这方法的四个要素，我们就可以唯一的确定一个方法，然后调用该方法。</p><p>在spring的配置文件中，注册一个Bean时，我们需要提供这个Bean的全限定类名，也就是Bean标签内的class属性的内容。有了全限定类名，我们就可以通过反射机制调用<code>class.forName()</code>拿到这个对象的类。更进一步，通过这个类拿到构造方法，实例化对象。拿到我们需要的目标方法，最终调用这个目标方法。</p><p>有了整个大概的全景图，我们就可以开始编写我们自己的Spring框架了。</p><ol><li>首先，让我们先编写一个<code>ApplicationContext</code> 接口类，这个类定义了<code>getBean()</code>方法</li></ol><pre><code class="language-java">package org.loulan.spring;public interface ApplicationContext &#123;    Object getBean(String name);&#125;</code></pre><ol start="2"><li>然后编写该接口的实现类<code>ClassPathXmlApplicationContext</code> 我们正是通过这个类来实例化一个<code>ApplicationContext</code> 对象。回顾<code>ApplicationContext context = new ClassPathXmlApplicationContext(&quot;mySpring.xml&quot;);</code> 这段代码，实例化这个类时，我们需要一个参数——Spring配置文件，这个参数对应一个构造方法，所以我们的<code>ClassPathXmlApplicationContext</code>也需要提供这个构造方法。</li></ol><pre><code class="language-java">public ClassPathXmlApplicationContext(String resource) &#123;&#125;</code></pre><ol start="3"><li><p>我们需要读取使用这个框架的配置文件，并拿到所有的Bean标签。要拿到这个xml文件并读取里面的标签已经属性，我们可以借助<code>SAXReader</code> 这个类来完成。通过Maven我们可以轻松的引入这个jar包。同时，我们还需要解析这个xml文件，通过<code>Document</code> 来完成。所以我还需要dom4j的依赖。</p><pre><code class="language-xml">&lt;!--        dom4j依赖--&gt;        &lt;dependency&gt;            &lt;groupId&gt;org.dom4j&lt;/groupId&gt;            &lt;artifactId&gt;dom4j&lt;/artifactId&gt;            &lt;version&gt;2.1.4&lt;/version&gt;        &lt;/dependency&gt;&lt;!--        jaxen的依赖，因为要使用它解析XML文件--&gt;        &lt;dependency&gt;            &lt;groupId&gt;jaxen&lt;/groupId&gt;            &lt;artifactId&gt;jaxen&lt;/artifactId&gt;            &lt;version&gt;2.0.0&lt;/version&gt;        &lt;/dependency&gt;</code></pre><p>接下来就是愉快的编写代码啦！</p><pre><code class="language-java">package org.loulan.spring;import org.dom4j.Document;import org.dom4j.DocumentException;import org.dom4j.Element;import org.dom4j.Node;import org.dom4j.io.SAXReader;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import java.lang.reflect.Constructor;import java.lang.reflect.Field;import java.lang.reflect.Method;import java.util.HashMap;import java.util.List;import java.util.Map;public class ClassPathXmlApplicationContext implements ApplicationContext&#123;    private Map&lt;String,Object&gt;beans = new HashMap&lt;String,Object&gt;();    private Logger logger = LoggerFactory.getLogger(ClassPathXmlApplicationContext.class);    public ClassPathXmlApplicationContext(String resource) &#123;        //1. 拿到这个xml配置文件        SAXReader reader = new SAXReader();        Document document = null;        try &#123;            document = reader.read(ClassLoader.getSystemClassLoader().getResourceAsStream(resource));            // 获取所有的bean标签            List&lt;Node&gt; beanNodes = document.selectNodes(&quot;//bean&quot;);            // 遍历集合            beanNodes.forEach(beanNode -&gt; &#123;                Element beanElt = (Element) beanNode;                // 获取id                String id = beanElt.attributeValue(&quot;name&quot;);                // 获取className                String className = beanElt.attributeValue(&quot;class&quot;);                logger.info(&quot;id-&gt;&quot;+id+&quot; className-&gt;&quot;+className);                try &#123;                    //通过反射拿到class                    Class&lt;?&gt; clazz = Class.forName(className);                    // 获得class的构造器                    Constructor&lt;?&gt; constructor = clazz.getConstructor();                    // 实例化对象                    Object o = constructor.newInstance();                    logger.info(&quot;实例化对象-&gt;&quot;+o);                    // 暴露bean，放在map（缓存）                    beans.put(id,o);                &#125; catch (Exception e) &#123;                    e.printStackTrace();                &#125;            &#125;);            //再次遍历bean，给属性赋值（DI）            beanNodes.forEach(beanNode -&gt; &#123;                Element beanElt = (Element) beanNode;                // 获取id                String id = beanElt.attributeValue(&quot;name&quot;);                // 获取className                String className = beanElt.attributeValue(&quot;class&quot;);                // 获取实例化对象                Object bean = beans.get(id);                logger.info(&quot;获取实例化对象&quot;+bean.toString());                //拿到properties                List&lt;Node&gt; properties = beanElt.selectNodes(&quot;property&quot;);                properties.forEach(property -&gt;&#123;                    logger.info(&quot;获取property-&gt; &quot; + property);                    Element element = (Element) property;                    String name = element.attributeValue(&quot;name&quot;);                    String arg = element.attributeValue(&quot;value&quot;);                    String ref = element.attributeValue(&quot;ref&quot;);                    String methodName = &quot;set&quot;+name.toUpperCase().charAt(0)+name.substring(1);                    logger.info(&quot;methodName=&quot;+methodName+&quot; arg=&quot;+arg);                    Object actualValue = null;                    try &#123;                        //获得属性                        Field field = bean.getClass().getDeclaredField(name);                        Method method = bean.getClass().getMethod(methodName, field.getType());                        logger.info(&quot;参数类型-&gt;&quot;+field.getType().getSimpleName());                        if(ref != null)&#123;                            //如果参数是引用数据类型                            method.invoke(bean,beans.get(ref));                        &#125; else if( arg != null)&#123;                            //参数是简单数据类型                            switch (field.getType().getSimpleName())&#123;                                case &quot;boolean&quot;,&quot;Boolean&quot;: actualValue = Boolean.parseBoolean(arg); break;                                case &quot;byte&quot;,&quot;Byte&quot;: actualValue = Byte.parseByte(arg); break;                                case &quot;short&quot;,&quot;Short&quot;: actualValue = Short.parseShort(arg); break;                                case &quot;int&quot;,&quot;Integer&quot;: actualValue = Integer.parseInt(arg); break;                                case &quot;long&quot;, &quot;Long&quot;: actualValue = Long.parseLong(arg); break;                                case &quot;float&quot;,&quot;Float&quot;: actualValue = Float.parseFloat(arg); break;                                case &quot;double&quot;,&quot;Double&quot;: actualValue = Double.parseDouble(arg); break;                                case &quot;char&quot;,&quot;Character&quot;: actualValue = arg.charAt(0); break;                                case &quot;String&quot;: actualValue = arg; break;                            &#125;                            method.invoke(bean,actualValue);                        &#125;                    &#125; catch (Exception e) &#123;                        e.printStackTrace();                    &#125;                &#125;);            &#125;);        &#125; catch (Exception e) &#123;            e.printStackTrace();        &#125;    &#125;    @Override    public Object getBean(String name) &#123;        return beans.get(name);    &#125;&#125;</code></pre></li></ol><p>代码很长，别急我们一步步分析：</p><p>这段代码做三件事：</p><ol><li><p>通过 <code>document = reader.read(ClassLoader.getSystemClassLoader().getResourceAsStream(resource));</code>拿到配置文件。</p></li><li><p>拿到所以的bean标签,获取标签的name属性和和class属性值，name属性值是这个bean的id，class是这个类的全限定类名。</p></li><li><p>通过反射机制实例化对象，并放到map集合中。</p><p>这一步相当于把bean放入二级缓存中，提前暴露bean。map集合就是二级缓存，其中key存放对象的name或id，value存放这个实例化的对象。</p></li></ol><pre><code class="language-java">//1. 拿到这个xml配置文件SAXReader reader = new SAXReader();Document document = null;try &#123;    document = reader.read(ClassLoader.getSystemClassLoader().getResourceAsStream(resource));    // 获取所有的bean标签    List&lt;Node&gt; beanNodes = document.selectNodes(&quot;//bean&quot;);    // 遍历集合    beanNodes.forEach(beanNode -&gt; &#123;        Element beanElt = (Element) beanNode;        // 获取id        String id = beanElt.attributeValue(&quot;name&quot;);        // 获取className        String className = beanElt.attributeValue(&quot;class&quot;);        logger.info(&quot;id-&gt;&quot;+id+&quot; className-&gt;&quot;+className);        try &#123;            //通过反射拿到class            Class&lt;?&gt; clazz = Class.forName(className);            // 获得class的构造器            Constructor&lt;?&gt; constructor = clazz.getConstructor();            // 实例化对象            Object o = constructor.newInstance();            logger.info(&quot;实例化对象-&gt;&quot;+o);            // 暴露bean，放在map（缓存）            beans.put(id,o);        &#125; catch (Exception e) &#123;            e.printStackTrace();        &#125;    &#125;);</code></pre><p>实例化bean之后，接下里就是依赖注入了（Dependency Injection）。我们需要再次遍历每一个bean节点，一个bean节点相当于一个bean对象。对于每一个bean对象，再次遍历bean对象的<property>标签，这对于这些bena对象的属性值，对于这些属性值，我们需要通过set注入的方法给属性赋值。</p><p>这时候就是调用对象的set方法了，还记得调用方法的四要素吗？没错，我们需要方法所属的对象，方法名，方法参数，以及返回值类型。</p><p>其中，对象我们可以通过二级缓存拿到，也就是上一部分的map集合。方法参数也是属性值，在property标签的value属性值指明。返回值就更轻松了，set方法的返回值都是void。</p><p>那么剩下的，就是方法名了，按照Java命名规范规范，set方法名有set加上属性名的首字母大小组成，符合驼峰命名规范。我们拿到了属性值，就可以通过拼接字符串的方法来得到方法名。</p><p>但是还有一个问题，属性值有普通类型和引用类型两种，对应引用类型，我们的做法很简单，通过二级缓存拿到引用对象，再通过set方法注入就好啦。但是普通类型就比较麻烦了。</p><p>首先，普通类型太多，而我们从配置文件中拿到的数据都是字符串类型，如果目标属性是String类型的话很直接，直接调用set方法即可。但是如果属性类型是int类型，或者bool类型，我们就需要把String类型转换成对应的类型。</p><p>好在这些普通类都有对应的包装类型，通包装类型我们可以轻松的实现字符串类型的转换，所以我们需要的就是匹配每一个普通类型，对这样的类型做类型转换即可。</p><p>普通类型太多，出于学习的目的，假设我们的Spring框架只支持其中部分类型。</p><pre><code class="language-java">//再次遍历bean，给属性赋值（DI）beanNodes.forEach(beanNode -&gt; &#123;  Element beanElt = (Element) beanNode;  // 获取id  String id = beanElt.attributeValue(&quot;name&quot;);  // 获取className  String className = beanElt.attributeValue(&quot;class&quot;);  // 获取实例化对象  Object bean = beans.get(id);  logger.info(&quot;获取实例化对象&quot;+bean.toString());  //拿到properties  List&lt;Node&gt; properties = beanElt.selectNodes(&quot;property&quot;);  properties.forEach(property -&gt;&#123;      logger.info(&quot;获取property-&gt; &quot; + property);      Element element = (Element) property;      String name = element.attributeValue(&quot;name&quot;);      String arg = element.attributeValue(&quot;value&quot;);      String ref = element.attributeValue(&quot;ref&quot;);      String methodName = &quot;set&quot;+name.toUpperCase().charAt(0)+name.substring(1);      logger.info(&quot;methodName=&quot;+methodName+&quot; arg=&quot;+arg);      Object actualValue = null;      try &#123;          //获得属性          Field field = bean.getClass().getDeclaredField(name);          Method method = bean.getClass().getMethod(methodName, field.getType());          logger.info(&quot;参数类型-&gt;&quot;+field.getType().getSimpleName());          if(ref != null)&#123;              //如果参数是引用数据类型              method.invoke(bean,beans.get(ref));          &#125; else if( arg != null)&#123;              //参数是简单数据类型              switch (field.getType().getSimpleName())&#123;                  case &quot;boolean&quot;,&quot;Boolean&quot;: actualValue = Boolean.parseBoolean(arg); break;                  case &quot;byte&quot;,&quot;Byte&quot;: actualValue = Byte.parseByte(arg); break;                  case &quot;short&quot;,&quot;Short&quot;: actualValue = Short.parseShort(arg); break;                  case &quot;int&quot;,&quot;Integer&quot;: actualValue = Integer.parseInt(arg); break;                  case &quot;long&quot;, &quot;Long&quot;: actualValue = Long.parseLong(arg); break;                  case &quot;float&quot;,&quot;Float&quot;: actualValue = Float.parseFloat(arg); break;                  case &quot;double&quot;,&quot;Double&quot;: actualValue = Double.parseDouble(arg); break;                  case &quot;char&quot;,&quot;Character&quot;: actualValue = arg.charAt(0); break;                  case &quot;String&quot;: actualValue = arg; break;              &#125;              method.invoke(bean,actualValue);          &#125;      &#125; catch (Exception e) &#123;          e.printStackTrace();      &#125;</code></pre><p>最后，提供一个getBean方法的接口，用于活动实例化的bean</p><pre><code class="language-java">@Overridepublic Object getBean(String name) &#123;    return beans.get(name);&#125;</code></pre><p>到这里，我们的Spring框架就算编写完了，打包发布吧！</p><img src="手写简易Spring框架/image-20230609140316257.png" alt="image-20230609140316257" style="zoom:30%;" / loading="lazy"><h3 id="测试自己的Spring框架">测试自己的Spring框架</h3><p>创建一个Maven模块，并引入刚刚打包好的jar包吧</p><pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;cn.loulan&lt;/groupId&gt;    &lt;artifactId&gt;MySpring&lt;/artifactId&gt;    &lt;version&gt;1.0-SNAPSHOT&lt;/version&gt;&lt;/dependency&gt;</code></pre><p>首先，我们需要一个Bean类</p><pre><code class="language-java">package bean;import lombok.Data;@Datapublic class User &#123;    private int id;    private String username;    private String password;    private float points;    private boolean isActive;    public void setIsActive(boolean active) &#123;        isActive = active;    &#125;&#125;</code></pre><p>然后是Spring配置文件</p><pre><code class="language-java">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;beans&gt;    &lt;beans&gt;        &lt;bean name=&quot;user&quot; class=&quot;bean.User&quot;&gt;            &lt;property name=&quot;id&quot; value=&quot;1&quot;/&gt;            &lt;property name=&quot;username&quot; value=&quot;sonetto&quot;/&gt;            &lt;property name=&quot;password&quot; value=&quot;123456&quot;/&gt;            &lt;property name=&quot;points&quot; value=&quot;12.3&quot;/&gt;            &lt;property name=&quot;isActive&quot; value=&quot;true&quot;/&gt;        &lt;/bean&gt;        &lt;bean name=&quot;userDao&quot; class=&quot;bean.UserDao&quot;/&gt;        &lt;bean name=&quot;userService&quot; class=&quot;bean.UserService&quot;&gt;            &lt;property name=&quot;userDao&quot; ref=&quot;userDao&quot;/&gt;        &lt;/bean&gt;    &lt;/beans&gt;&lt;/beans&gt;</code></pre><p>编写测试类：</p><pre><code class="language-java">package bean;import org.junit.Test;import org.loulan.spring.ApplicationContext;import org.loulan.spring.ClassPathXmlApplicationContext;import org.springframework.beans.factory.support.DefaultSingletonBeanRegistry;public class TestBean &#123;    @Test    public void test()&#123;        ApplicationContext context = new ClassPathXmlApplicationContext(&quot;mySpring.xml&quot;);        User user = (User) context.getBean(&quot;user&quot;);        System.out.println(user);    &#125;&#125;</code></pre><p><img src="%E6%89%8B%E5%86%99%E7%AE%80%E6%98%93Spring%E6%A1%86%E6%9E%B6/image-20230609140900802.png" alt="image-20230609140900802" loading="lazy"></p><p>跑起来了！</p>]]></content>
    
    
    <summary type="html">在实例化一个对象时，我们通过`ClassPathXmlApplicationContext()`，传入我们的配置文件，拿到一个`ApplicationContext`对象，通过`ApplicationContext`对象调用`getBean（）`方法来获取对象, 最终调用对象发方法，来使用Bean。</summary>
    
    
    
    <category term="spring" scheme="https://trevoewu.github.io/categories/spring/"/>
    
    <category term="java" scheme="https://trevoewu.github.io/categories/spring/java/"/>
    
    <category term="javaweb" scheme="https://trevoewu.github.io/categories/spring/java/javaweb/"/>
    
    
    <category term="spring" scheme="https://trevoewu.github.io/tags/spring/"/>
    
  </entry>
  
  <entry>
    <title>Spring 学习笔记</title>
    <link href="https://trevoewu.github.io/2023/06/03/Spring-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <id>https://trevoewu.github.io/2023/06/03/Spring-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</id>
    <published>2023-06-03T07:49:54.000Z</published>
    <updated>2023-06-10T08:43:20.933Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Spring概述">Spring概述</h2><ul><li><p><strong>Spring框架</strong>是<a href="https://zh.wikipedia.org/wiki/Java">Java</a>平台的一个<a href="https://zh.wikipedia.org/wiki/%E5%BC%80%E6%94%BE%E6%BA%90%E4%BB%A3%E7%A0%81">开源</a>的全栈（<a href="https://zh.wikipedia.org/wiki/%E5%85%A8%E6%A0%88">full-stack</a>）<a href="https://zh.wikipedia.org/wiki/%E8%BD%AF%E4%BB%B6%E6%A1%86%E6%9E%B6">应用程式框架</a>和<a href="https://zh.wikipedia.org/wiki/%E6%8E%A7%E5%88%B6%E5%8F%8D%E8%BD%AC">控制反转</a>容器实现，一般被直接称为Spring。</p></li><li><p><strong>Spring</strong>是一个轻量级的控制<strong>反转(IoC)<strong>和面向</strong>切面(AOP)<strong>的</strong>容器框架</strong>。</p></li><li><p>Spring最初的出现是为了解决EJB臃肿的设计，以及难以测试等问题。</p></li><li><p>Spring为简化开发而生，让程序员只需关注核心业务的实现，尽可能的不再关注非业务逻辑代码（事务控制，安全日志等）。</p></li></ul><h2 id="Spring特点">Spring特点</h2><ol><li><p>轻量</p></li><li><ol><li>从大小与开销两方面而言Spring都是轻量的。完整的Spring框架可以在一个大小只有1MB多的JAR文件里发布。并且Spring所需的处理开销也是微不足道的。</li><li>Spring<strong>是非侵入式</strong>的：Spring应用中的对象不依赖于Spring的特定类。</li></ol></li><li><p><strong>控制反转</strong></p></li><li><ol><li>Spring通过一种称作控制反转（IoC）的技术促进了松耦合。当应用了IoC，一个对象依赖的其它对象会通过被动的方式传递进来，而不是这个对象自己创建或者查找依赖对象。你可以认为IoC与JNDI相反——不是对象从容器中查找依赖，而是容器在对象初始化时不等对象请求就主动将依赖传递给它。</li></ol></li><li><p><strong>面向切面</strong></p></li><li><ol><li>Spring提供了面向切面编程的丰富支持，允许通过分离应用的业务逻辑与系统级服务（例如审计（auditing）和事务（transaction）管理）进行内聚性的开发。应用对象只实现它们应该做的——完成业务逻辑——仅此而已。它们并不负责（甚至是意识）其它的系统级关注点，例如日志或事务支持。</li></ol></li><li><p>容器</p></li><li><ol><li>Spring包含并管理应用对象的配置和生命周期，在这个意义上它是一种容器，你可以配置你的每个bean如何被创建——基于一个可配置原型（prototype），你的bean可以创建一个单独的实例或者每次需要时都生成一个新的实例——以及它们是如何相互关联的。然而，Spring不应该被混同于传统的重量级的EJB容器，它们经常是庞大与笨重的，难以使用。</li></ol></li><li><p>框架</p></li><li><ol><li>Spring可以将简单的组件配置、组合成为复杂的应用。在Spring中，应用对象被声明式地组合，典型地是在一个XML文件里。Spring也提供了很多基础功能（事务管理、持久化框架集成等等），将应用逻辑的开发留给了你。</li></ol></li></ol><p>所有Spring的这些特征使你能够编写更干净、更可管理、并且更易于测试的代码。它们也为Spring中的各种模块提供了基础支持。</p><h2 id="Spring对IOC的实现">Spring对IOC的实现</h2><ul><li><blockquote><p>控制反转（Inversion of Control，IoC）是面向对象编程中的一种设计思想，可以用来降低代码之间的耦合度，符合<strong>依赖倒置原则</strong>。</p><p>控制反转的核心是：<strong>将对象的创建权交出去，将对象和对象之间关系的管理权交出去，由第三方容器来负责创建与维护</strong>。</p><p>控制反转常见的实现方式：依赖注入（Dependency Injection，简称DI）</p></blockquote></li><li></li></ul><p><strong>Spring通过依赖注入的方式来完成Bean管理的。</strong></p><p><strong>Bean管理说的是：Bean对象的创建，以及Bean对象中属性的赋值（或者叫做Bean对象之间关系的维护）。</strong></p><p>依赖注入：</p><ul><li><p>依赖指的是对象和对象之间的关联关系。</p></li><li><p>注入指的是一种数据传递行为，通过注入行为来让对象和对象产生关系。</p><ul><li>依赖注入常见的实现方式包括两种：set注入 构造注入</li></ul><h3 id="set注入">set注入</h3><p><strong>原理：</strong></p><ol><li><p>通过<code>property</code>标签获取到属性名：userDao</p></li><li><p>通过属性名推断出set方法名：setUserDao</p></li><li><p>通过反射机制调用setUserDao()方法给属性赋值</p></li></ol><p><code>property</code>标签的name是属性名。</p><p><code>property</code>标签的ref是要注入的bean对象的id。</p><p><strong>(通过ref属性来完成bean的装配，这是bean最简单的一种装配方式。装配指的是：创建系统组件之间关联的动作)</strong></p><p>==set注入的核心实现原理：通过反射机制调用set方法来给属性赋值，让两个对象之间产生关系。==</p><p>TeacherService往往需要依赖TeacherMapper/Dao 对象， 为了降低类与类，层与层之间的耦合度，我们通常采用面向接口编程。把对象的创建权交出去，交给Spring容器来管理。由Spring容器来创建对象和维护对象与对象之间的关系。</p><p>通过在spring配置文件（如spring-config.xml）中，注册Bean，以及Bean与Bean的关系，Bean的属性值，来让Spring去创建和注入Bean。</p><pre><code class="language-xml">&lt;bean id=&quot;teacherMapper&quot; class=&quot;mapper.teacher.TeacherMapperImpl&quot;&gt;&lt;/bean&gt;&lt;!--    基于setter的外部Bean注入--&gt;&lt;!--    &lt;property&gt; 标签内的name属性是对应setter方法的方法名去掉setter，首字母小写--&gt;&lt;!--    在规范的Bean setter 方法中，这个值就等于Bean的属性名称--&gt;&lt;bean id=&quot;teacherService&quot; class=&quot;service.teacher.TeacherService&quot;&gt;  &lt;property name=&quot;teacherMapper&quot; ref=&quot;teacherMapper&quot;/&gt;&lt;/bean&gt;</code></pre><p>在这个配置文件中，我们注册了两个Bean，<code>teacherMapper</code>和<code>teacherService</code> ，并通过set注入来给属性负责。</p><pre><code class="language-java">public class TestTeacherService &#123;  public void testTeacher()&#123;          ApplicationContext applicationContext = new ClassPathXmlApplicationContext(&quot;spring.xml&quot;);          TeacherService teacherService = applicationContext.getBean(&quot;teacherService&quot;, TeacherService.class);          teacherService.getTeachers();      &#125;&#125;</code></pre><p>这样，我们就可以通过<code>applicatContext</code>对象得到，<code>applicationContext</code>对象通过读取配置文件得到。</p><p>调用<code>applicatonContext.getBean(&quot;Bean id&quot;)</code>我们就可以得到实例化的Bean了。</p><h3 id="构造注入">构造注入</h3><p>==通过调用构造方法来给属性赋值。==</p><pre><code class="language-xml">&lt;!--    基于构造方法注入--&gt;&lt;!--    要求对应Bean中要有对应的构造方法--&gt;&lt;!--    可以通过&lt;constructor-arg&gt;标签 注入属性值--&gt;&lt;!--    其中，name属性通过属性名完成注入，值为value--&gt;&lt;!--    index 使用属性值下标完成注入，从0开始--&gt;&lt;bean id=&quot;blog&quot; class=&quot;bean.Blog&quot;&gt;  &lt;constructor-arg name=&quot;title&quot; value=&quot;unveil a new cd&quot;/&gt;  &lt;constructor-arg name=&quot;author&quot; value=&quot;regulus&quot;/&gt;  &lt;constructor-arg name=&quot;description&quot; value=&quot;new cd&quot;/&gt;  &lt;constructor-arg name=&quot;id&quot; value=&quot;1&quot;/&gt;&lt;/bean&gt;</code></pre></li></ul><h2 id="set注入专题">set注入专题</h2><h3 id="注入外部Bean和内部Bean">注入外部Bean和内部Bean</h3><pre><code class="language-xml">&lt;!--    基于setter的外部Bean注入--&gt;&lt;!--    &lt;property&gt; 标签内的name属性是对应setter方法的方法名去掉setter，首字母小写--&gt;&lt;!--    在规范的Bean setter 方法中，这个值就等于Bean的属性名称--&gt;    &lt;bean id=&quot;teacherService&quot; class=&quot;service.teacher.TeacherService&quot;&gt;        &lt;property name=&quot;teacherMapper&quot; ref=&quot;teacherMapper&quot;/&gt;    &lt;/bean&gt;&lt;!--    同样，我们可以基于内部Bean注入--&gt;&lt;!--    内部Bean就是在&lt;bean&gt; 标签里嵌套定义的Bean--&gt;    &lt;bean id=&quot;teacherService2&quot; class=&quot;service.teacher.TeacherService&quot;&gt;        &lt;property name=&quot;teacherMapper&quot;&gt;            &lt;bean id=&quot;teacherMapper&quot; class=&quot;mapper.teacher.TeacherMapperImpl&quot;&gt;&lt;/bean&gt;        &lt;/property&gt;    &lt;/bean&gt;</code></pre><h3 id="注入简单类型">注入简单类型</h3><p>==如果给简单类型赋值，使用value属性或value标签。而不是ref。==</p><pre><code class="language-java">package bean;import lombok.Data;@Datapublic class Person &#123;    private String name;    private int age;    private String gender;&#125;</code></pre><pre><code class="language-xml">&lt;bean id=&quot;person&quot; class=&quot;bean.Person&quot;&gt;    &lt;property name=&quot;age&quot; value=&quot;20&quot;/&gt;    &lt;property name=&quot;gender&quot; value=&quot;female&quot;/&gt;    &lt;property name=&quot;name&quot; value=&quot;sonetto&quot;/&gt;&lt;/bean</code></pre><p><strong>简单类型包括哪些呢？</strong></p><p>可以通过Spring的源码来分析一下：BeanUtils类</p><pre><code class="language-java">public class BeanUtils&#123;  //.......public static boolean isSimpleValueType(Class&lt;?&gt; type) &#123;return (Void.class != type &amp;&amp; void.class != type &amp;&amp;(ClassUtils.isPrimitiveOrWrapper(type) ||Enum.class.isAssignableFrom(type) ||CharSequence.class.isAssignableFrom(type) ||Number.class.isAssignableFrom(type) ||Date.class.isAssignableFrom(type) ||Temporal.class.isAssignableFrom(type) ||URI.class == type ||URL.class == type ||Locale.class == type ||Class.class == type));&#125;        //........&#125;</code></pre><p>通过源码分析得知，简单类型包括：</p><ul><li>基本数据类型(int,char)</li><li>基本数据类型对应的包装类(String, Interger)</li><li>String或其他的CharSequence子类</li><li>Number子类</li><li>Date子类</li><li>Enum子类</li><li>URI</li><li>URL</li><li>Temporal子类</li><li>Locale</li><li>Class</li><li>另外还包括以上简单值类型对应的数组类型。</li></ul><h3 id="注入集合数组">注入集合数组</h3><h4 id="数组">数组</h4><pre><code class="language-java">package bean;import lombok.Data;@Datapublic class Article &#123;    private String tags[];    private Person person[];&#125;</code></pre><pre><code class="language-xml">&lt;!--    注入数组--&gt;&lt;!--    1. 数组元素是简单类型--&gt;    &lt;bean id=&quot;article&quot; class=&quot;bean.Article&quot;&gt;        &lt;property name=&quot;tags&quot;&gt;            &lt;array &gt;                &lt;value&gt;mac&lt;/value&gt;                &lt;value&gt;c++&lt;/value&gt;                &lt;value&gt;app recommend&lt;/value&gt;            &lt;/array&gt;        &lt;/property&gt;&lt;!--        数组元素是引用类型--&gt;        &lt;property name=&quot;person&quot;&gt;            &lt;array&gt;                &lt;ref bean=&quot;person&quot;/&gt;            &lt;/array&gt;        &lt;/property&gt;    &lt;/bean&gt;</code></pre><p><strong>要点：</strong></p><ul><li><strong>如果数组中是简单类型，使用value标签。</strong></li><li><strong>如果数组中是非简单类型，使用ref标签。</strong></li></ul><h4 id="集合">集合</h4><pre><code class="language-java">import lombok.Data;import java.util.List;import java.util.Set;@Datapublic class Library &#123;    private List&lt;Book&gt; bookList;    private List&lt;String&gt; classifications;    private Set&lt;Book&gt; bookSet;&#125;</code></pre><pre><code class="language-xml">&lt;!--    注入集合--&gt;    &lt;bean name=&quot;library&quot; class=&quot;bean.Library&quot;&gt;&lt;!--        注入List集合--&gt;        &lt;property name=&quot;bookList&quot;&gt;            &lt;list&gt;                &lt;bean class=&quot;bean.Book&quot;&gt;                    &lt;property name=&quot;bookName&quot; value=&quot;爱丽丝梦游仙境&quot;/&gt;                    &lt;property name=&quot;ISBN&quot; value=&quot;1192-2313-31-13&quot;/&gt;                &lt;/bean&gt;                &lt;bean class=&quot;bean.Book&quot;&gt;                    &lt;property name=&quot;bookName&quot; value=&quot;追风筝的人&quot;/&gt;                    &lt;property name=&quot;ISBN&quot; value=&quot;1192-2343-34-33&quot;/&gt;                &lt;/bean&gt;            &lt;/list&gt;        &lt;/property&gt;&lt;!--        注入set集合--&gt;        &lt;property name=&quot;bookSet&quot;&gt;           &lt;set&gt;               &lt;bean class=&quot;bean.Book&quot;&gt;                    &lt;property name=&quot;bookName&quot; value=&quot;爱丽丝梦游仙境&quot;/&gt;                    &lt;property name=&quot;ISBN&quot; value=&quot;1192-2313-31-13&quot;/&gt;                &lt;/bean&gt;                &lt;bean class=&quot;bean.Book&quot;&gt;                    &lt;property name=&quot;bookName&quot; value=&quot;追风筝的人&quot;/&gt;                    &lt;property name=&quot;ISBN&quot; value=&quot;1192-2343-34-33&quot;/&gt;                &lt;/bean&gt;           &lt;/set&gt;        &lt;/property&gt;&lt;!--        注入简单类型 String--&gt;        &lt;property name=&quot;classifications&quot;&gt;            &lt;array&gt;                &lt;value&gt;小说&lt;/value&gt;                &lt;value&gt;教育&lt;/value&gt;            &lt;/array&gt;        &lt;/property&gt;    &lt;/bean&gt;</code></pre><p><strong>注入List集合的时候使用list标签，如果List集合中是简单类型使用value标签，反之使用ref标签。</strong></p><p><strong>set集合中元素是简单类型的使用value标签，反之使用ref标签。</strong></p><h4 id="map">map</h4><p>==使用<map>标签和<entry key="" value="">嵌套==</p><pre><code class="language-java">package bean;import lombok.Data;import java.util.Map;@Datapublic class Color &#123;    private Map&lt;String,String&gt; colorMap;&#125;</code></pre><pre><code class="language-xml">&lt;!--    注入map--&gt;    &lt;bean name=&quot;color&quot; class=&quot;bean.Color&quot;&gt;        &lt;property name=&quot;colorMap&quot;&gt;            &lt;map&gt;                &lt;entry key=&quot;RED&quot; value=&quot;255,0,255&quot;/&gt;                &lt;entry key=&quot;GREEN&quot; value=&quot;0,255,0&quot;/&gt;                &lt;entry key=&quot;BLUE&quot; value=&quot;0,0,255&quot;/&gt;            &lt;/map&gt;        &lt;/property&gt;    &lt;/bean&gt;</code></pre><h3 id="注入Propertis">注入Propertis</h3><p>==使用<props>标签嵌套<prop>标签完成。==</p><pre><code class="language-java">package bean;import lombok.Data;import java.util.Properties;@Datapublic class JDBC &#123;    private Properties properties;&#125;</code></pre><pre><code class="language-xml">&lt;!--注入properties--&gt;&lt;bean name=&quot;JDBC&quot; class=&quot;bean.JDBC&quot;&gt;  &lt;property name=&quot;properties&quot; &gt;      &lt;props&gt;          &lt;prop key=&quot;DRIVER&quot;&gt;com.mysql.jdbc.Driver&lt;/prop&gt;          &lt;prop key=&quot;URL&quot;&gt;jdbc:mysql://localhost:3306/demo?useUnicode=true&amp;amp;characterEncoding=utf-8&lt;/prop&gt;          &lt;prop key=&quot;USERNAME&quot;&gt;root&lt;/prop&gt;          &lt;prop key=&quot;PASSWORD&quot;&gt;password&lt;/prop&gt;      &lt;/props&gt;  &lt;/property&gt;&lt;/bean&gt;</code></pre><h3 id="注入空字符串和NULL">注入空字符串和NULL</h3><p>不注入就是<code>null</code>，空字符串可以设置<code>value=&quot;&quot;</code>注入</p><p>除此之外，还可以使用<code>&lt;value/&gt;</code>这个自闭和标签注入空字符串，以及使用<code>&lt;null/&gt;</code>标签注入null。</p><pre><code class="language-xml">&lt;!--空串的第一种方式--&gt;&lt;property name=&quot;email&quot; value=&quot;&quot;/&gt;&lt;!--空串的第二种方式--&gt;&lt;property name=&quot;email&quot;&gt;  &lt;value/&gt;&lt;/property&gt;&lt;!--注入null--&gt;&lt;bean id=&quot;vipBean&quot; class=&quot;com.powernode.spring6.beans.Vip&quot;&gt;  &lt;property name=&quot;email&quot;&gt;      &lt;null/&gt;  &lt;/property&gt;&lt;/bean&gt;</code></pre><h3 id="注入特殊符号">注入特殊符号</h3><p>XML中有5个特殊字符，分别是：&lt;、&gt;、'、&quot;、&amp;</p><p>以上5个特殊符号在XML中会被特殊对待，会被当做XML语法的一部分进行解析，如果这些特殊符号直接出现在注入的字符串当中，会报错。</p><p>解决方案包括两种：</p><ul><li>第一种：特殊符号使用转义字符代替。</li><li>第二种：将含有特殊符号的字符串放到：<![CDATA[]]> 当中。因为放在CDATA区中的数据不会被XML文件解析器解析。</li></ul><p>5个特殊字符对应的转义字符分别是：</p><table><thead><tr><th><strong>特殊字符</strong></th><th><strong>转义字符</strong></th></tr></thead><tbody><tr><td>&gt;</td><td>&amp;gt;</td></tr><tr><td>&lt;</td><td>&amp;lt;</td></tr><tr><td>’</td><td>&amp;apos;</td></tr><tr><td>&quot;</td><td>&amp;quot;</td></tr><tr><td>&amp;</td><td>&amp;amp;</td></tr></tbody></table><h2 id="Bean的实例化方式">Bean的实例化方式</h2><ol><li><p>通过构造方法实例化</p></li><li><p>通过简单工厂模式实例化</p></li><li><p><strong>通过factory-bean实例化</strong></p></li><li><p>通过factoryBean接口实例化</p></li></ol><h3 id="通过构造方法实例化Bean">通过构造方法实例化Bean</h3><p>通过构造方法实例化Bean时，默认情况下Spring会在加载配置文件的时候，也就是我们调用</p><pre><code class="language-java">ApplicationContext applicationContext = new ClassPathXmlApplicationContext(&quot;spring.xml&quot;);</code></pre><p>这段代码时，Spring调用目标Bean的无参构造方法实例化Bean。</p><h3 id="通过简单工厂模式实例化Bean">通过简单工厂模式实例化Bean</h3><p>在Spring框架中，你可以使用简单工厂模式来实例化Bean。</p><ol><li>创建一个工厂类，该类负责实例化Bean并返回实例化后的对象。这个工厂类可以是一个<strong>普通的Java类，不需要实现特定的接口</strong>。</li></ol><pre><code class="language-java">public class BeanFactory &#123;    public static MyBean createBean() &#123;        // 在这里进行实例化Bean的逻辑        MyBean bean = new MyBean();        // 可以进行一些初始化操作        bean.setName(&quot;Example Bean&quot;);        // 返回实例化后的Bean对象        return bean;    &#125;&#125;</code></pre><p>在上面的示例中，<code>MyBean</code>是要实例化的Bean类，你可以根据实际情况替换为你自己的Bean类。在<code>createBean()</code>方法中，你可以根据需要进行Bean的实例化，并对其进行一些初始化操作。</p><ol start="2"><li>在你的Spring配置文件中，可以使用工厂方法来创建Bean的实例。通过在配置文件中声明一个工厂方法，Spring将调用该方法来获取Bean的实例。</li></ol><pre><code class="language-xml">&lt;bean id=&quot;myBean&quot; class=&quot;com.example.BeanFactory&quot; factory-method=&quot;createBean&quot; /&gt;</code></pre><p>在上面的示例中，<code>id</code>属性指定了Bean的ID，<code>class</code>属性指定了工厂类的全限定名，<code>factory-method</code>属性指定了工厂类中用于创建Bean实例的方法名。</p><p>现在，当Spring容器启动时，它将使用工厂方法来实例化<code>myBean</code>。你可以通过从容器中获取该Bean来使用它。</p><pre><code class="language-java">MyBean bean = (MyBean) applicationContext.getBean(&quot;myBean&quot;);</code></pre><p>通过这种方式，你可以使用简单工厂模式来实例化Bean，并将其集成到Spring框架中。</p><blockquote><p>简单工厂模式（Simple Factory Pattern）是一种创建型设计模式，它提供了一种集中化的方法来创建对象，而不需要在客户端代码中直接实例化具体的对象。</p><p>简单工厂模式由三个主要组件组成：</p><ol><li>工厂类（Factory Class）：负责创建对象的类。它包含一个或多个创建对象的静态方法，这些方法根据传入的参数或条件确定要创建的具体对象类型，并返回相应的对象实例。</li><li>产品类（Product Class）：被工厂类创建的对象。它们共享一个共同的父类或接口，并定义了一组共同的行为和属性。</li><li>客户端（Client）：使用工厂类创建对象的代码。客户端通过调用工厂类的方法来获取所需的对象，而无需直接实例化具体的产品类。</li></ol><p>简单工厂模式的工作原理如下：</p><ol><li>客户端通过调用工厂类的方法请求创建对象。</li><li>工厂类根据传入的参数或条件确定要创建的对象类型。</li><li>工厂类实例化相应的对象，并返回给客户端。</li><li>客户端使用返回的对象进行后续操作。</li></ol><p>简单工厂模式的优点包括：</p><ul><li>将对象的创建逻辑集中在工厂类中，客户端无需知道具体的对象实现。</li><li>简化了客户端代码，客户端只需要关注如何使用对象，而不需要关注对象的创建过程。</li><li>通过工厂类可以轻松添加新的产品类，无需修改客户端代码。</li></ul><p>然而，简单工厂模式也有一些局限性：</p><ul><li>如果需要添加新的产品类，需要修改工厂类的代码，违背了开闭原则。</li><li>工厂类的职责较重，可能会导致类的职责不够单一。</li><li>客户端只能通过工厂类提供的方法来创建对象，缺乏灵活性。</li></ul><p>简单工厂模式适用于对象的创建逻辑相对简单、对象类型较少且不经常变化的情况。对于复杂的对象创建逻辑或对象类型频繁变化的情况，可以考虑使用其他创建型设计模式，如工厂方法模式或抽象工厂模式。</p></blockquote><h3 id="Bean的生命周期">Bean<strong>的生命周期</strong></h3><ol><li>五步<ol><li>实例化Bean。默认使用无参数构造方法new一个Bean</li><li>注入Bean。通过Settet方法给属性赋值</li><li>初始化Bean。调用Bean的init方法，这个方法需要程序员自己编写，下destoy方法同。</li><li>使用Bean。</li><li>销毁Bean。调用bean的destroy方法。</li></ol></li></ol><pre><code class="language-java">package bean;public class Cat &#123;    private String name;    public Cat() &#123;        System.out.println(&quot;1. new cat is instantiated&quot;);    &#125;    public void miao()&#123;        System.out.println(&quot;4. cat: miao miao miao....&quot;);    &#125;    public void setName(String name) &#123;        System.out.println(&quot;2. bean resign properties&quot;);        this.name = name;    &#125;    public void init()&#123;        System.out.println(&quot;3. cat is initialized&quot;);    &#125;    public void destroy() &#123;        System.out.println(&quot;5. cat is destroyed&quot;);    &#125;&#125;</code></pre><pre><code class="language-xml">&lt;bean name=&quot;cat&quot; class=&quot;bean.Cat&quot; init-method=&quot;init&quot; destroy-method=&quot;destroy&quot;&gt;    &lt;property name=&quot;name&quot; value=&quot;Mio&quot;/&gt;&lt;/bean&gt;</code></pre><p><img src="Spring-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-20230605164041553.png" alt="image-20230605164041553" loading="lazy"></p><ol><li><p><strong>七步（需要掌握）</strong></p><ol><li>实例化Bean。默认使用无参数构造方法new一个Bean</li><li>注入Bean。通过Settet方法给属性赋值</li><li>==Bean后处理器Before方法执行==</li><li>初始化Bean。调用Bean的init方法，这个方法需要程序员自己编写，下destoy方法同。==←在这里插入==</li><li>==Bean后处理器的After方法执行==</li><li>使用Bean。</li><li>销毁Bean。调用bean的destroy方法。</li></ol><p>在以上的5步中，第3步是初始化Bean，如果你还想在初始化前和初始化后添加代码，可以加入“Bean后处理器”。</p><p>编写一个类实现BeanPostProcessor类，并且重写before和after方法：</p><pre><code class="language-java">package bean;import org.springframework.beans.BeansException;import org.springframework.beans.factory.config.BeanPostProcessor;public class PostProcessor implements BeanPostProcessor &#123;    @Override    public Object postProcessBeforeInitialization(Object bean, String beanName) throws BeansException &#123;        System.out.println(&quot;------------------------&gt;beforeInitialization&quot;);        return BeanPostProcessor.super.postProcessBeforeInitialization(bean, beanName);    &#125;    @Override    public Object postProcessAfterInitialization(Object bean, String beanName) throws BeansException &#123;        System.out.println(&quot;------------------------&gt;afterInitialization&quot;);        return BeanPostProcessor.super.postProcessAfterInitialization(bean, beanName);    &#125;&#125;</code></pre><p>在spring.xml文件中配置“Bean后处理器”：</p><pre><code class="language-xml">&lt;!--    在spring.xml文件中配置“Bean后处理器”：--&gt;&lt;bean class=&quot;bean.PostProcessor&quot;/&gt;</code></pre><p>==一定要注意：在spring.xml文件中配置的Bean后处理器将作用于当前配置文件中所有的Bean。==</p></li><li><p>十步</p><ol><li><p>实例化Bean。默认使用无参数构造方法new一个Bean</p></li><li><p>注入Bean。通过Settet方法给属性赋值</p></li><li><p>==检查Bean是否实现了Aware相关接口，并设置相关依赖。==</p></li><li><p>Bean后处理器Before方法执行</p></li><li><p>==检查Bean是否实现了InitializingBean接口 &amp; 调用接口方法。==</p></li><li><p>初始化Bean。调用Bean的init方法，这个方法需要程序员自己编写，下destoy方法同。</p></li><li><p>Bean后处理器的After方法执行</p></li><li><p>使用Bean。</p></li><li><p>==检查Bean是否实现了DisposableBean接口，并调用接口方法==</p></li><li><p>销毁Bean。调用bean的destroy方法。</p></li></ol></li></ol><p>Aware相关的接口包括：BeanNameAware、BeanClassLoaderAware、BeanFactoryAware</p><ul><li>当Bean实现了BeanNameAware，Spring会将Bean的名字传递给Bean。</li><li>当Bean实现了BeanClassLoaderAware，Spring会将加载该Bean的类加载器传递给Bean。</li><li>当Bean实现了BeanFactoryAware，Spring会将Bean工厂对象传递给Bean。</li></ul><p>测试以上10步，可以让User类实现5个接口，并实现所有方法：</p><ul><li>BeanNameAware</li><li>BeanClassLoaderAware</li><li>BeanFactoryAware</li><li>InitializingBean</li><li>DisposableBean</li></ul><h2 id="Bean的循环依赖问题">Bean的循环依赖问题</h2><h3 id="Overview">Overview</h3><p>Bean的循环依赖指的是在Spring容器中存在相互依赖的Bean实例。这种循环依赖会导致Bean的创建过程陷入死循环或无法完成，从而导致应用程序无法正常启动或出现其他异常。</p><p>循环依赖通常发生在以下情况下：</p><ol><li><strong>构造函数循环依赖</strong>：一个Bean的构造函数参数依赖于另一个Bean，而另一个Bean的构造函数参数又依赖于第一个Bean。这种情况下，当容器尝试实例化其中一个Bean时，它需要先实例化另一个Bean，而后者又依赖于前者，从而导致循环依赖。</li><li><strong>属性循环依赖</strong>：一个Bean的属性依赖于另一个Bean，而另一个Bean的属性又依赖于第一个Bean。这种情况下，当容器尝试为一个Bean注入属性时，它需要先实例化另一个Bean，而后者又依赖于前者，从而导致循环依赖。</li></ol><p>Spring框架通过使用<strong>三级缓存</strong>来解决Bean的循环依赖问题。在实例化Bean的过程中，如果发现循环依赖，Spring会先创建一个尚未完成初始化的早期对象并缓存起来，然后继续完成当前Bean的实例化和初始化。当其他Bean需要访问当前Bean时，将返回该早期对象。待当前Bean初始化完成后，Spring会将其设置到早期对象中，从而解决循环依赖。</p><p>然而，需要注意的是，循环依赖可能会导致潜在的问题，如死锁或不确定性状态。因此，应该尽量避免循环依赖的产生，合理设计Bean之间的依赖关系，或者考虑通过重构代码来消除循环依赖。</p><h3 id="Spring的三级缓存">Spring的三级缓存</h3><p>Spring为什么可以解决set + singleton模式下循环依赖？</p><p>==根本的原因在于：这种方式可以做到将“实例化Bean”和“给Bean属性赋值”这两个动作分开去完成。==</p><p>实例化Bean的时候：调用无参数构造方法来完成。<strong>此时可以先不给属性赋值，可以提前将该Bean对象“曝光”给外界。</strong></p><p>给Bean属性赋值的时候：调用setter方法来完成。</p><p>两个步骤是完全可以分离开去完成的，并且这两步不要求在同一个时间点上完成。</p><p>也就是说，Bean都是单例的，我们可以先把所有的单例Bean实例化出来，放到一个集合当中（我们可以称之为缓存），所有的单例Bean全部实例化完成之后，以后我们再慢慢的调用setter方法给属性赋值。这样就解决了循环依赖的问题。</p><p>那么在Spring框架底层源码级别上是如何实现的呢？请看：</p><pre><code class="language-java">public class DefaultSingletonBeanRegistry extends SimpleAliasRegistry implements SingletonBeanRegistry &#123;   /** Maximum number of suppressed exceptions to preserve. */   private static final int SUPPRESSED_EXCEPTIONS_LIMIT = 100;// 1. 一级缓存 单例对象缓存池   /** Cache of singleton objects: bean name to bean instance. */   private final Map&lt;String, Object&gt; singletonObjects = new ConcurrentHashMap&lt;&gt;(256);// 3. 三级缓存 原始对象缓存池   /** Cache of singleton factories: bean name to ObjectFactory. */   private final Map&lt;String, ObjectFactory&lt;?&gt;&gt; singletonFactories = new HashMap&lt;&gt;(16);// 2. 二级缓存 早期对象缓存池   /** Cache of early singleton objects: bean name to bean instance. */   private final Map&lt;String, Object&gt; earlySingletonObjects = new ConcurrentHashMap&lt;&gt;(16);&#125;</code></pre><p>Spring的三级缓存（Three-Level Cache）是Spring框架在处理Bean实例化和循环依赖时使用的一种机制。它是为了解决Bean的循环依赖问题而引入的。</p><p>三级缓存在Spring容器的Bean生命周期中扮演重要角色，它包含了以下三个层次的缓存：</p><ol><li><p>第一级缓存：单例对象缓存池（Singleton Cache）。</p><ul><li>第一级缓存是Spring容器中的单例Bean缓存。</li><li><strong>当Spring容器创建一个单例Bean时，会首先尝试从第一级缓存中获取已经创建的实例。</strong></li><li>如果能够找到实例，则直接返回给调用者，避免重复实例化相同的单例Bean。</li><li>第一级缓存的键值对结构为 <code>&lt;Bean名称, Bean实例&gt;</code>。</li></ul></li><li><p>第二级缓存：早期对象缓存池（Early-Stage Object Cache）。</p><ul><li><strong>第二级缓存用于解决Bean的循环依赖问题。</strong></li><li>当Bean实例化过程中存在循环依赖时，Spring会将已经创建但尚未完成初始化的Bean对象放入第二级缓存中。</li><li>早期对象指的是在Bean实例化过程中已经创建出来但尚未完全初始化的对象。</li><li>通过第二级缓存，Spring能够在后续的Bean初始化阶段中解决循环依赖，将依赖的Bean设置到早期对象中。</li><li>第二级缓存的键值对结构为 <code>&lt;Bean名称, 早期对象&gt;</code>。</li></ul></li><li><p>第三级缓存：原始对象缓存池（Raw Object Cache）。</p><ul><li><p>第三级缓存是最底层的缓存，用于存储尚未经过任何处理的原始Bean对象。</p></li><li><p>当容器创建Bean时，如果无法从第一级和第二级缓存中获取Bean实例，Spring会将正在创建的Bean放入第三级缓存中，作为缓存的最后一道防线。</p></li><li><p>原始对象指的是尚未经过任何处理的初始对象，尚未进行依赖注入等操作。</p></li><li><p>通过第三级缓存，Spring可以确保在解决循环依赖后，将原始对象进行进一步的初始化操作。</p></li><li><p>第三级缓存的键值对结构为 <code>&lt;Bean名称, 原始对象&gt;</code>。</p></li></ul></li></ol><p>在Bean的实例化过程中，Spring框架会依次尝试从三级缓存中获取Bean的实例，以满足依赖注入和循环依赖的需求。这种缓存机制使得Spring能够高效地处理复杂的Bean依赖关系，保证了Bean的正确创建和初始化。</p><p>需要注意的是，<strong>三级缓存仅在处理单例（Singleton）作用域的Bean时使用，对于其他作用域（如原型Prototype）的Bean，不会使用三级缓存。每次请求原型Bean时，Spring都会创建一个新的实例，避免了循环依赖的问题。</strong></p><h2 id="手写Spring框架">手写Spring框架</h2><p>Spring底层是通过反射机制来实例化对象，以及属性注入的，在手写Spring框架之前，我们需要了解反射机制。</p><blockquote><p>JAVA反射机制是在运行状态中，对于任意一个类，都能够知道这个类的所有属性和方法；对于任意一个对象，都能够调用它的任意一个方法和属性；这种动态获取的信息以及动态调用对象的方法的功能称为java语言的反射机制。Java反射机制在框架设计中极为广泛，需要深入理解。</p></blockquote><p>通过反射，我们可以拿到类的属性，方法。 回顾Spring框架，我们需要编写一个spring的配置文件例如：spring.xml，在这个配置文件中，我们注册一个类，并注入属性值。</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;beans&gt;    &lt;beans&gt;        &lt;bean name=&quot;user&quot; class=&quot;bean.User&quot;&gt;            &lt;property name=&quot;id&quot; value=&quot;1&quot;/&gt;            &lt;property name=&quot;username&quot; value=&quot;sonetto&quot;/&gt;            &lt;property name=&quot;password&quot; value=&quot;123456&quot;/&gt;            &lt;property name=&quot;points&quot; value=&quot;12.3&quot;/&gt;            &lt;property name=&quot;isActive&quot; value=&quot;true&quot;/&gt;        &lt;/bean&gt;        &lt;bean name=&quot;userDao&quot; class=&quot;bean.UserDao&quot;/&gt;        &lt;bean name=&quot;userService&quot; class=&quot;bean.UserService&quot;&gt;            &lt;property name=&quot;userDao&quot; ref=&quot;userDao&quot;/&gt;        &lt;/bean&gt;    &lt;/beans&gt;&lt;/beans&gt;</code></pre><p>在实例化一个对象时，我们通过<code>ClassPathXmlApplicationContext()</code>，传入我们的配置文件，拿到一个<code>ApplicationContext</code>对象，通过<code>ApplicationContext</code>对象调用<code>getBean（）</code>方法来获取对象, 最终调用对象发方法，来使用Bean。</p><pre><code class="language-java">ApplicationContext context = new ClassPathXmlApplicationContext(&quot;mySpring.xml&quot;);        User user = (User) context.getBean(&quot;user&quot;);        System.out.println(user);        UserService userService = (UserService) context.getBean(&quot;userService&quot;);        userService.createUser();</code></pre><p>我们的最终目的就是调用对象的方法，而一个指定一个方法需要四要素： 什么对象（对象的全限定类名是什么？）什么方法（方法名），什么参数（参数的类型和数量），返回什么值。</p><p>指定来这方法的四个要素，我们就可以唯一的确定一个方法，然后调用该方法。</p><p>在spring的配置文件中，注册一个Bean时，我们需要提供这个Bean的全限定类名，也就是Bean标签内的class属性的内容。有了全限定类名，我们就可以通过反射机制调用<code>class.forName()</code>拿到这个对象的类。更进一步，通过这个类拿到构造方法，实例化对象。拿到我们需要的目标方法，最终调用这个目标方法。</p><p>有了整个大概的全景图，我们就可以开始编写我们自己的Spring框架了。</p><ol><li>首先，让我们先编写一个<code>ApplicationContext</code> 接口类，这个类定义了<code>getBean()</code>方法</li></ol><pre><code class="language-java">package org.loulan.spring;public interface ApplicationContext &#123;    Object getBean(String name);&#125;</code></pre><ol start="2"><li>然后编写该接口的实现类<code>ClassPathXmlApplicationContext</code> 我们正是通过这个类来实例化一个<code>ApplicationContext</code> 对象。回顾<code>ApplicationContext context = new ClassPathXmlApplicationContext(&quot;mySpring.xml&quot;);</code> 这段代码，实例化这个类时，我们需要一个参数——Spring配置文件，这个参数对应一个构造方法，所以我们的<code>ClassPathXmlApplicationContext</code>也需要提供这个构造方法。</li></ol><pre><code class="language-java">public ClassPathXmlApplicationContext(String resource) &#123;&#125;</code></pre><ol start="3"><li><p>我们需要读取使用这个框架的配置文件，并拿到所有的Bean标签。要拿到这个xml文件并读取里面的标签已经属性，我们可以借助<code>SAXReader</code> 这个类来完成。通过Maven我们可以轻松的引入这个jar包。同时，我们还需要解析这个xml文件，通过<code>Document</code> 来完成。所以我还需要dom4j的依赖。</p><pre><code class="language-xml">&lt;!--        dom4j依赖--&gt;        &lt;dependency&gt;            &lt;groupId&gt;org.dom4j&lt;/groupId&gt;            &lt;artifactId&gt;dom4j&lt;/artifactId&gt;            &lt;version&gt;2.1.4&lt;/version&gt;        &lt;/dependency&gt;&lt;!--        jaxen的依赖，因为要使用它解析XML文件--&gt;        &lt;dependency&gt;            &lt;groupId&gt;jaxen&lt;/groupId&gt;            &lt;artifactId&gt;jaxen&lt;/artifactId&gt;            &lt;version&gt;2.0.0&lt;/version&gt;        &lt;/dependency&gt;</code></pre><p>接下来就是愉快的编写代码啦！</p><pre><code class="language-java">package org.loulan.spring;import org.dom4j.Document;import org.dom4j.DocumentException;import org.dom4j.Element;import org.dom4j.Node;import org.dom4j.io.SAXReader;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import java.lang.reflect.Constructor;import java.lang.reflect.Field;import java.lang.reflect.Method;import java.util.HashMap;import java.util.List;import java.util.Map;public class ClassPathXmlApplicationContext implements ApplicationContext&#123;    private Map&lt;String,Object&gt;beans = new HashMap&lt;String,Object&gt;();    private Logger logger = LoggerFactory.getLogger(ClassPathXmlApplicationContext.class);    public ClassPathXmlApplicationContext(String resource) &#123;        //1. 拿到这个xml配置文件        SAXReader reader = new SAXReader();        Document document = null;        try &#123;            document = reader.read(ClassLoader.getSystemClassLoader().getResourceAsStream(resource));            // 获取所有的bean标签            List&lt;Node&gt; beanNodes = document.selectNodes(&quot;//bean&quot;);            // 遍历集合            beanNodes.forEach(beanNode -&gt; &#123;                Element beanElt = (Element) beanNode;                // 获取id                String id = beanElt.attributeValue(&quot;name&quot;);                // 获取className                String className = beanElt.attributeValue(&quot;class&quot;);                logger.info(&quot;id-&gt;&quot;+id+&quot; className-&gt;&quot;+className);                try &#123;                    //通过反射拿到class                    Class&lt;?&gt; clazz = Class.forName(className);                    // 获得class的构造器                    Constructor&lt;?&gt; constructor = clazz.getConstructor();                    // 实例化对象                    Object o = constructor.newInstance();                    logger.info(&quot;实例化对象-&gt;&quot;+o);                    // 暴露bean，放在map（缓存）                    beans.put(id,o);                &#125; catch (Exception e) &#123;                    e.printStackTrace();                &#125;            &#125;);            //再次遍历bean，给属性赋值（DI）            beanNodes.forEach(beanNode -&gt; &#123;                Element beanElt = (Element) beanNode;                // 获取id                String id = beanElt.attributeValue(&quot;name&quot;);                // 获取className                String className = beanElt.attributeValue(&quot;class&quot;);                // 获取实例化对象                Object bean = beans.get(id);                logger.info(&quot;获取实例化对象&quot;+bean.toString());                //拿到properties                List&lt;Node&gt; properties = beanElt.selectNodes(&quot;property&quot;);                properties.forEach(property -&gt;&#123;                    logger.info(&quot;获取property-&gt; &quot; + property);                    Element element = (Element) property;                    String name = element.attributeValue(&quot;name&quot;);                    String arg = element.attributeValue(&quot;value&quot;);                    String ref = element.attributeValue(&quot;ref&quot;);                    String methodName = &quot;set&quot;+name.toUpperCase().charAt(0)+name.substring(1);                    logger.info(&quot;methodName=&quot;+methodName+&quot; arg=&quot;+arg);                    Object actualValue = null;                    try &#123;                        //获得属性                        Field field = bean.getClass().getDeclaredField(name);                        Method method = bean.getClass().getMethod(methodName, field.getType());                        logger.info(&quot;参数类型-&gt;&quot;+field.getType().getSimpleName());                        if(ref != null)&#123;                            //如果参数是引用数据类型                            method.invoke(bean,beans.get(ref));                        &#125; else if( arg != null)&#123;                            //参数是简单数据类型                            switch (field.getType().getSimpleName())&#123;                                case &quot;boolean&quot;,&quot;Boolean&quot;: actualValue = Boolean.parseBoolean(arg); break;                                case &quot;byte&quot;,&quot;Byte&quot;: actualValue = Byte.parseByte(arg); break;                                case &quot;short&quot;,&quot;Short&quot;: actualValue = Short.parseShort(arg); break;                                case &quot;int&quot;,&quot;Integer&quot;: actualValue = Integer.parseInt(arg); break;                                case &quot;long&quot;, &quot;Long&quot;: actualValue = Long.parseLong(arg); break;                                case &quot;float&quot;,&quot;Float&quot;: actualValue = Float.parseFloat(arg); break;                                case &quot;double&quot;,&quot;Double&quot;: actualValue = Double.parseDouble(arg); break;                                case &quot;char&quot;,&quot;Character&quot;: actualValue = arg.charAt(0); break;                                case &quot;String&quot;: actualValue = arg; break;                            &#125;                            method.invoke(bean,actualValue);                        &#125;                    &#125; catch (Exception e) &#123;                        e.printStackTrace();                    &#125;                &#125;);            &#125;);        &#125; catch (Exception e) &#123;            e.printStackTrace();        &#125;    &#125;    @Override    public Object getBean(String name) &#123;        return beans.get(name);    &#125;&#125;</code></pre></li></ol><p>代码很长，别急我们一步步分析：</p><p>这段代码做三件事：</p><ol><li><p>通过 <code>document = reader.read(ClassLoader.getSystemClassLoader().getResourceAsStream(resource));</code>拿到配置文件。</p></li><li><p>拿到所以的bean标签,获取标签的name属性和和class属性值，name属性值是这个bean的id，class是这个类的全限定类名。</p></li><li><p>通过反射机制实例化对象，并放到map集合中。</p><p>这一步相当于把bean放入二级缓存中，提前暴露bean。map集合就是二级缓存，其中key存放对象的name或id，value存放这个实例化的对象。</p></li></ol><pre><code class="language-java">//1. 拿到这个xml配置文件SAXReader reader = new SAXReader();Document document = null;try &#123;    document = reader.read(ClassLoader.getSystemClassLoader().getResourceAsStream(resource));    // 获取所有的bean标签    List&lt;Node&gt; beanNodes = document.selectNodes(&quot;//bean&quot;);    // 遍历集合    beanNodes.forEach(beanNode -&gt; &#123;        Element beanElt = (Element) beanNode;        // 获取id        String id = beanElt.attributeValue(&quot;name&quot;);        // 获取className        String className = beanElt.attributeValue(&quot;class&quot;);        logger.info(&quot;id-&gt;&quot;+id+&quot; className-&gt;&quot;+className);        try &#123;            //通过反射拿到class            Class&lt;?&gt; clazz = Class.forName(className);            // 获得class的构造器            Constructor&lt;?&gt; constructor = clazz.getConstructor();            // 实例化对象            Object o = constructor.newInstance();            logger.info(&quot;实例化对象-&gt;&quot;+o);            // 暴露bean，放在map（缓存）            beans.put(id,o);        &#125; catch (Exception e) &#123;            e.printStackTrace();        &#125;    &#125;);</code></pre><p>实例化bean之后，接下里就是依赖注入了（Dependency Injection）。我们需要再次遍历每一个bean节点，一个bean节点相当于一个bean对象。对于每一个bean对象，再次遍历bean对象的<property>标签，这对于这些bena对象的属性值，对于这些属性值，我们需要通过set注入的方法给属性赋值。</p><p>这时候就是调用对象的set方法了，还记得调用方法的四要素吗？没错，我们需要方法所属的对象，方法名，方法参数，以及返回值类型。</p><p>其中，对象我们可以通过二级缓存拿到，也就是上一部分的map集合。方法参数也是属性值，在property标签的value属性值指明。返回值就更轻松了，set方法的返回值都是void。</p><p>那么剩下的，就是方法名了，按照Java命名规范规范，set方法名有set加上属性名的首字母大小组成，符合驼峰命名规范。我们拿到了属性值，就可以通过拼接字符串的方法来得到方法名。</p><p>但是还有一个问题，属性值有普通类型和引用类型两种，对应引用类型，我们的做法很简单，通过二级缓存拿到引用对象，再通过set方法注入就好啦。但是普通类型就比较麻烦了。</p><p>首先，普通类型太多，而我们从配置文件中拿到的数据都是字符串类型，如果目标属性是String类型的话很直接，直接调用set方法即可。但是如果属性类型是int类型，或者bool类型，我们就需要把String类型转换成对应的类型。</p><p>好在这些普通类都有对应的包装类型，通包装类型我们可以轻松的实现字符串类型的转换，所以我们需要的就是匹配每一个普通类型，对这样的类型做类型转换即可。</p><p>普通类型太多，出于学习的目的，假设我们的Spring框架只支持其中部分类型。</p><pre><code class="language-java">//再次遍历bean，给属性赋值（DI）beanNodes.forEach(beanNode -&gt; &#123;  Element beanElt = (Element) beanNode;  // 获取id  String id = beanElt.attributeValue(&quot;name&quot;);  // 获取className  String className = beanElt.attributeValue(&quot;class&quot;);  // 获取实例化对象  Object bean = beans.get(id);  logger.info(&quot;获取实例化对象&quot;+bean.toString());  //拿到properties  List&lt;Node&gt; properties = beanElt.selectNodes(&quot;property&quot;);  properties.forEach(property -&gt;&#123;      logger.info(&quot;获取property-&gt; &quot; + property);      Element element = (Element) property;      String name = element.attributeValue(&quot;name&quot;);      String arg = element.attributeValue(&quot;value&quot;);      String ref = element.attributeValue(&quot;ref&quot;);      String methodName = &quot;set&quot;+name.toUpperCase().charAt(0)+name.substring(1);      logger.info(&quot;methodName=&quot;+methodName+&quot; arg=&quot;+arg);      Object actualValue = null;      try &#123;          //获得属性          Field field = bean.getClass().getDeclaredField(name);          Method method = bean.getClass().getMethod(methodName, field.getType());          logger.info(&quot;参数类型-&gt;&quot;+field.getType().getSimpleName());          if(ref != null)&#123;              //如果参数是引用数据类型              method.invoke(bean,beans.get(ref));          &#125; else if( arg != null)&#123;              //参数是简单数据类型              switch (field.getType().getSimpleName())&#123;                  case &quot;boolean&quot;,&quot;Boolean&quot;: actualValue = Boolean.parseBoolean(arg); break;                  case &quot;byte&quot;,&quot;Byte&quot;: actualValue = Byte.parseByte(arg); break;                  case &quot;short&quot;,&quot;Short&quot;: actualValue = Short.parseShort(arg); break;                  case &quot;int&quot;,&quot;Integer&quot;: actualValue = Integer.parseInt(arg); break;                  case &quot;long&quot;, &quot;Long&quot;: actualValue = Long.parseLong(arg); break;                  case &quot;float&quot;,&quot;Float&quot;: actualValue = Float.parseFloat(arg); break;                  case &quot;double&quot;,&quot;Double&quot;: actualValue = Double.parseDouble(arg); break;                  case &quot;char&quot;,&quot;Character&quot;: actualValue = arg.charAt(0); break;                  case &quot;String&quot;: actualValue = arg; break;              &#125;              method.invoke(bean,actualValue);          &#125;      &#125; catch (Exception e) &#123;          e.printStackTrace();      &#125;</code></pre><p>最后，提供一个getBean方法的接口，用于活动实例化的bean</p><pre><code class="language-java">@Overridepublic Object getBean(String name) &#123;    return beans.get(name);&#125;</code></pre><p>到这里，我们的Spring框架就算编写完了，打包发布吧！</p><img src="Spring-学习笔记/image-20230609140316257.png" alt="image-20230609140316257" style="zoom:30%;" / loading="lazy"><h3 id="测试自己的Spring框架">测试自己的Spring框架</h3><p>创建一个Maven模块，并引入刚刚打包好的jar包吧</p><pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;cn.loulan&lt;/groupId&gt;    &lt;artifactId&gt;MySpring&lt;/artifactId&gt;    &lt;version&gt;1.0-SNAPSHOT&lt;/version&gt;&lt;/dependency&gt;</code></pre><p>首先，我们需要一个Bean类</p><pre><code class="language-java">package bean;import lombok.Data;@Datapublic class User &#123;    private int id;    private String username;    private String password;    private float points;    private boolean isActive;    public void setIsActive(boolean active) &#123;        isActive = active;    &#125;&#125;</code></pre><p>然后是Spring配置文件</p><pre><code class="language-java">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;beans&gt;    &lt;beans&gt;        &lt;bean name=&quot;user&quot; class=&quot;bean.User&quot;&gt;            &lt;property name=&quot;id&quot; value=&quot;1&quot;/&gt;            &lt;property name=&quot;username&quot; value=&quot;sonetto&quot;/&gt;            &lt;property name=&quot;password&quot; value=&quot;123456&quot;/&gt;            &lt;property name=&quot;points&quot; value=&quot;12.3&quot;/&gt;            &lt;property name=&quot;isActive&quot; value=&quot;true&quot;/&gt;        &lt;/bean&gt;        &lt;bean name=&quot;userDao&quot; class=&quot;bean.UserDao&quot;/&gt;        &lt;bean name=&quot;userService&quot; class=&quot;bean.UserService&quot;&gt;            &lt;property name=&quot;userDao&quot; ref=&quot;userDao&quot;/&gt;        &lt;/bean&gt;    &lt;/beans&gt;&lt;/beans&gt;</code></pre><p>编写测试类：</p><pre><code class="language-java">package bean;import org.junit.Test;import org.loulan.spring.ApplicationContext;import org.loulan.spring.ClassPathXmlApplicationContext;import org.springframework.beans.factory.support.DefaultSingletonBeanRegistry;public class TestBean &#123;    @Test    public void test()&#123;        ApplicationContext context = new ClassPathXmlApplicationContext(&quot;mySpring.xml&quot;);        User user = (User) context.getBean(&quot;user&quot;);        System.out.println(user);    &#125;&#125;</code></pre><p><img src="Spring-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-20230609140900802.png" alt="image-20230609140900802" loading="lazy"></p><p>跑起来了！</p><h2 id="Spring-集成MyBatis框架">Spring 集成MyBatis框架</h2><h3 id="Overview-2">Overview</h3><ol><li><p>需要的依赖</p><ol><li>Spring-context(关联引入Spring-aop)</li><li>Spring-MyBatis（Spring 集成MyBatis）</li><li>Mysql-connetor(Mysql驱动)</li><li>Spring-jdbc（链接数据库）</li><li>MyBatis</li><li>Junit（单元测试）</li><li>druid(数据源)</li><li>Lombok</li></ol></li><li><p>配置数据库</p><ol><li>account表，字段act_no,banlance</li></ol></li><li><p>项目结构搭建</p><ol><li><p>Bean(pojo)</p><ol><li>Account</li></ol></li><li><p>mapper</p><ol><li>AccountMapper</li><li>AccountMapper.xml</li></ol></li><li><p>Service</p><ol><li>AccountService</li><li>AccountServiceImpl</li></ol></li><li><p>resources</p><ol><li><p>Spring核心配置文件Spring.xml</p><ol><li>组建扫描</li><li>事务管理器</li></ol></li><li><p>MyBatis核心配置文件MyBatis.xml</p><ol><li>注册Mapper</li><li>类别名</li><li>设置日志实现</li></ol></li><li><ul><li>该文件可以没有，大部分的配置可以转移到spring配置文件中。</li><li>如果遇到mybatis相关的系统级配置，还是需要这个文件。</li></ul></li><li><p>DB.propertis</p><ol><li>Driver→com.mysql.jdbc.Driver</li><li>url→jdbc:mysql://localhost:3307/class?useUnicode=true&amp;characterEncoding=utf-8</li><li>username→root</li><li>Password→root</li></ol></li><li><p>编写测试类</p></li></ol></li></ol></li></ol><h3 id="编写mapper">编写mapper</h3><pre><code class="language-java">package cn.loulan.mapper;import cn.loulan.bean.Account;import java.util.List;public interface AccountMapper &#123;    int insert(Account account);    int delete(String actNo);    int update(Account account);    Account getById(String actNo);    List&lt;Account&gt; getAll();&#125;</code></pre><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE mapper        PUBLIC &quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;        &quot;https://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;&gt;&lt;mapper namespace=&quot;cn.loulan.mapper.AccountMapper&quot;&gt;    &lt;insert id=&quot;insert&quot;&gt;        Insert into account values(#&#123;actNo&#125;,#&#123;balance&#125;)    &lt;/insert&gt;    &lt;update id=&quot;update&quot; parameterType=&quot;Account&quot;&gt;        update account set balance = #&#123;balance&#125; where act_no = #&#123;actNo&#125;    &lt;/update&gt;    &lt;delete id=&quot;delete&quot; &gt;        delete account where act_no = #&#123;actNo&#125;;    &lt;/delete&gt;    &lt;select id=&quot;getById&quot; resultType=&quot;Account&quot;&gt;        select * from account where act_no =#&#123;actNo&#125;    &lt;/select&gt;    &lt;select id=&quot;getAll&quot; resultType=&quot;Account&quot;&gt;        select * from account;    &lt;/select&gt;&lt;/mapper&gt;</code></pre><h3 id="Service">Service</h3><pre><code class="language-java">package cn.loulan.service;import cn.loulan.bean.Account;import java.util.List;public interface AccountService &#123;    boolean save(Account account);    boolean delete(String actNo);    boolean modify(Account account);    Account getById(String actNo);    List&lt;Account&gt; getAllAccounts();    boolean transaction(String from, String to,double amount);&#125;</code></pre><pre><code class="language-java">package cn.loulan.service;import cn.loulan.bean.Account;import cn.loulan.mapper.AccountMapper;import lombok.Data;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.stereotype.Service;import org.springframework.transaction.annotation.Transactional;import java.util.List;@Service(&quot;accountService&quot;)@Transactionalpublic class AccountServiceImpl implements AccountService &#123;    private AccountMapper accountMapper;    @Autowired    public void setMapper(AccountMapper mapper) &#123;        this.accountMapper = mapper;    &#125;    @Override    public boolean save(Account account) &#123;       accountMapper.insert(account);        return true;    &#125;    @Override    public boolean delete(String actNo) &#123;        accountMapper.delete(actNo);        return true;    &#125;    @Override    public boolean modify(Account account) &#123;        accountMapper.update(account);        return false;    &#125;    @Override    public Account getById(String actNo) &#123;        return accountMapper.getById(actNo);    &#125;    @Override    public List&lt;Account&gt; getAllAccounts() &#123;        return accountMapper.getAll();    &#125;    @Override    public boolean transaction(String from, String to, double amount) &#123;        Account fromAccount = accountMapper.getById(from);        Account toAccount = accountMapper.getById(to);        if(fromAccount.getBalance() &lt; amount) &#123;            throw new RuntimeException(&quot;余额不足&quot;);        &#125;        fromAccount.setBalance(fromAccount.getBalance() -  amount);        toAccount.setBalance(toAccount.getBalance() + amount);        int count = accountMapper.update(fromAccount);         count += accountMapper.update(toAccount);        if (count != 2) throw new RuntimeException(&quot;异常&quot;);        return count == 2 ? true : false;    &#125;&#125;</code></pre><h3 id="resources">resources</h3><p>数据配置文件</p><pre><code class="language-properties">#DRIVER=com.mysql.jdbc.DriverDRIVER=com.mysql.cj.jdbc.DriverURL=jdbc:mysql://localhost:3306/demo?useUnicode=true&amp;characterEncoding=utf-8USERNAME=rootPASSWORD=root</code></pre><p>MyBatis核心配置文件：</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE configuration        PUBLIC &quot;-//mybatis.org//DTD Config 3.0//EN&quot;        &quot;https://mybatis.org/dtd/mybatis-3-config.dtd&quot;&gt;&lt;configuration&gt;    &lt;!--日志实现--&gt;    &lt;settings&gt;        &lt;setting name=&quot;logImpl&quot; value=&quot;STDOUT_LOGGING&quot;/&gt;    &lt;/settings&gt;    &lt;typeAliases&gt;        &lt;typeAlias alias=&quot;Account&quot; type=&quot;cn.loulan.bean.Account&quot;/&gt;    &lt;/typeAliases&gt;&lt;/configuration&gt;</code></pre><p>Spring核心配置文件</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;beans xmlns=&quot;http://www.springframework.org/schema/beans&quot;       xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;       xmlns:context=&quot;http://www.springframework.org/schema/context&quot; xmlns:tx=&quot;http://www.springframework.org/schema/tx&quot;       xsi:schemaLocation=&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd&quot;&gt;&lt;!--开启组件自动扫描--&gt;    &lt;context:component-scan base-package=&quot;cn.loulan.service&quot;/&gt;&lt;!--    加载数据库配置文件--&gt;    &lt;context:property-placeholder location=&quot;DB.properties&quot;/&gt;&lt;!--    配置数据源--&gt;    &lt;bean id=&quot;dataSource&quot; class=&quot;com.alibaba.druid.pool.DruidDataSource&quot;&gt;        &lt;property name=&quot;driverClassName&quot; value=&quot;$&#123;DRIVER&#125;&quot;/&gt;        &lt;property name=&quot;url&quot; value=&quot;$&#123;URL&#125;&quot;/&gt;        &lt;property name=&quot;username&quot; value=&quot;$&#123;USERNAME&#125;&quot;/&gt;        &lt;property name=&quot;password&quot; value=&quot;$&#123;PASSWORD&#125;&quot;/&gt;    &lt;/bean&gt;    &lt;!--Mapper扫描器--&gt;    &lt;bean class=&quot;org.mybatis.spring.mapper.MapperScannerConfigurer&quot;&gt;        &lt;property name=&quot;basePackage&quot; value=&quot;cn.loulan.mapper&quot;/&gt;    &lt;/bean&gt;    &lt;!--SqlSessionFactoryBean--&gt;    &lt;bean class=&quot;org.mybatis.spring.SqlSessionFactoryBean&quot;&gt;        &lt;!--mybatis核心配置文件路径--&gt;        &lt;property name=&quot;configLocation&quot; value=&quot;mybatis.xml&quot;/&gt;        &lt;!--注入数据源--&gt;        &lt;property name=&quot;dataSource&quot; ref=&quot;dataSource&quot;/&gt;        &lt;!--起别名--&gt;        &lt;property name=&quot;typeAliasesPackage&quot; value=&quot;cn.loulan.bean&quot;/&gt;    &lt;/bean&gt;    &lt;!--事务管理器--&gt;    &lt;bean id=&quot;txManager&quot; class=&quot;org.springframework.jdbc.datasource.DataSourceTransactionManager&quot;&gt;        &lt;property name=&quot;dataSource&quot; ref=&quot;dataSource&quot;/&gt;    &lt;/bean&gt;    &lt;!--开启事务注解--&gt;    &lt;tx:annotation-driven transaction-manager=&quot;txManager&quot;/&gt;&lt;/beans&gt;</code></pre><h3 id="编写测试类">编写测试类</h3><pre><code class="language-java">package cn.loulan.service;import cn.loulan.bean.Account;import cn.loulan.mapper.AccountMapper;import cn.loulan.util.MybatisUtil;import org.apache.ibatis.session.SqlSession;import org.junit.Test;import org.springframework.context.ApplicationContext;import org.springframework.context.support.ClassPathXmlApplicationContext;import java.util.List;public class ServiceTest &#123;    @Test    public void mapper()&#123;        SqlSession sqlSession = MybatisUtil.getSqlSession();        AccountMapper mapper = sqlSession.getMapper(AccountMapper.class);//        mapper.insert(new Account(&quot;123&quot;,500.00));        mapper.insert(new Account(&quot;234&quot;,500.00));        sqlSession.commit();        sqlSession.close();    &#125;    @Test    public void testSpring()throws Exception &#123;        ApplicationContext applicationContext = new ClassPathXmlApplicationContext(&quot;spring.xml&quot;);        AccountService accountService = applicationContext.getBean(&quot;accountService&quot;, AccountService.class);        List&lt;Account&gt; allAccounts = accountService.getAllAccounts();        allAccounts.forEach(account -&gt;&#123;            System.out.println(account);        &#125;);    &#125;    @Test    public void testTransaction()&#123;        ApplicationContext applicationContext = new ClassPathXmlApplicationContext(&quot;spring.xml&quot;);        AccountService accountService = applicationContext.getBean(&quot;accountService&quot;, AccountService.class);        accountService.transaction(&quot;1&quot;,&quot;234&quot;,100);    &#125;&#125;</code></pre><h3 id="总结">总结</h3><p>配置文件还是要写好多，而且完全靠记忆嘛。简单整理一下这些配置文件，主要分为两部分，spring的配置文件和mybatis的配置文件。spring配置文件中主要的配置有：</p><ol><li>注册bean，在用注解开发时开启组建自动扫描</li><li>加载数据库配置文件。</li><li>配置数据源。包括数据库的一下配置信息，驱动器啊url之类的。</li><li>Mapper扫描器。相当于在mybatis中注册mapper，使用包名扫描这个包下的所有mapper。</li><li>配置sqlSessionFactoryBean在这个bean中价值mybatis的核心配置文件，以及价值数据源，类型别名等等可以在mybatis中配置的内容都可以通过这个bean来配置。</li><li>配置事务管理器</li><li>开启事务注解</li></ol><p>这样在MyBatis配置文件的内容就少很多了，甚至可以不配或者没有。这个案例中，我们配置了日志实现和类型别名。</p><p>感觉好乱，课程的这一段讲得太快了，老杜是不是赶着吃饭啊。</p><p>Spring框架下的mybatis还是和直接用mybatis有很大区别。Spring中，我们不需要一个工具类用于价值mybatis配置文件以便获取sqlsession对象了。事务的处理也不需要管了，虽然简化了很多代码，但是总有一种项目莫名其妙就跑起来的感觉，终极还是我了解得不够多了吧。</p>]]></content>
    
    
    <summary type="html">spring 学习笔记</summary>
    
    
    
    <category term="java EE" scheme="https://trevoewu.github.io/categories/java-EE/"/>
    
    
    <category term="Spring" scheme="https://trevoewu.github.io/tags/Spring/"/>
    
    <category term="Java" scheme="https://trevoewu.github.io/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>MyBatis学习笔记</title>
    <link href="https://trevoewu.github.io/2023/05/27/MyBatis%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <id>https://trevoewu.github.io/2023/05/27/MyBatis%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</id>
    <published>2023-05-27T10:14:52.000Z</published>
    <updated>2023-06-09T04:41:45.731Z</updated>
    
    <content type="html"><![CDATA[<h2 id="入门之环境搭建">入门之环境搭建</h2><ol><li><p>导入依赖的jar包</p><pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;org.mybatis&lt;/groupId&gt;    &lt;artifactId&gt;mybatis&lt;/artifactId&gt;    &lt;version&gt;3.5.13&lt;/version&gt;&lt;/dependency&gt;</code></pre></li><li><p>*解决Maven静态资源过滤问题 导致生成的target文件不包含xml，导致IO错误</p><pre><code class="language-xml">&lt;resources&gt;    &lt;resource&gt;        &lt;directory&gt;src/main/java&lt;/directory&gt;        &lt;includes&gt;            &lt;include&gt;**/*.properties&lt;/include&gt;            &lt;include&gt;**/*.xml&lt;/include&gt;        &lt;/includes&gt;        &lt;filtering&gt;false&lt;/filtering&gt;    &lt;/resource&gt;    &lt;resource&gt;        &lt;directory&gt;src/main/resources&lt;/directory&gt;        &lt;includes&gt;            &lt;include&gt;**/*.properties&lt;/include&gt;            &lt;include&gt;**/*.xml&lt;/include&gt;        &lt;/includes&gt;        &lt;filtering&gt;false&lt;/filtering&gt;    &lt;/resource&gt;  &lt;/resources&gt;</code></pre></li><li><p>新建MyBatis配置文件→<code>mybatis-config.xml</code>, 放在<code>resource</code>目录下.</p><p>理论上放在哪里都可以, 文件名叫什么也随意, 但是按照规范来能让代码更规范, 更具有可读性.</p><p>标签:   <code> &lt;properties resource=&quot;db.properties&quot;/&gt;</code>指代数据库的属性, 用于读取<code>properties</code>, 这样我们就配置数据库的属性比如Driver, URL时直接使用配置文件中的变量.</p><p>注意 XML 头部的声明，它用来验证 XML 文档的正确性。</p><p>environment 元素体中包含了事务管理和连接池的配置。mappers 元素则包含了一组映射器（mapper），这些映射器的 XML 映射文件包含了 SQL 代码和映射定义信息。</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE configuration        PUBLIC &quot;-//mybatis.org//DTD Config 3.0//EN&quot;        &quot;https://mybatis.org/dtd/mybatis-3-config.dtd&quot;&gt;&lt;configuration&gt;&lt;!--    从db.properties读取数据库属性--&gt;    &lt;properties resource=&quot;db.properties&quot;/&gt;    &lt;environments default=&quot;development&quot;&gt;        &lt;environment id=&quot;development&quot;&gt;            &lt;transactionManager type=&quot;JDBC&quot;/&gt;            &lt;dataSource type=&quot;POOLED&quot;&gt;                &lt;property name=&quot;driver&quot; value=&quot;$&#123;DRIVER&#125;&quot;/&gt;                &lt;property name=&quot;url&quot; value=&quot;$&#123;URL&#125;&quot;/&gt;                &lt;property name=&quot;username&quot; value=&quot;$&#123;USERNAME&#125;&quot;/&gt;                &lt;property name=&quot;password&quot; value=&quot;$&#123;PASSWORD&#125;&quot;/&gt;            &lt;/dataSource&gt;        &lt;/environment&gt;    &lt;/environments&gt;    &lt;mappers&gt;      &lt;mapper class=&quot;dao.book.BookMapper&quot;&gt;&lt;/mapper&gt;  &lt;/mappers&gt;&lt;/configuration&gt;</code></pre></li><li><p>编写实体类</p></li><li><p>编写dao层接口</p><pre><code class="language-java">public interface BookMapper &#123;    Book getRelativeBook( @Param(&quot;id&quot;) int id, @Param(&quot;bookDesc&quot;) String bookDesc);&#125;</code></pre></li></ol><p>这段代码用于模糊查询同类书籍, <code>@Param</code>注解用于指明参数, 指明参数也可以通过xml文档在指明</p><ol start="5"><li><p>编写SQL</p><p>一个语句既可以通过 XML 定义，也可以通过注解定义。我们先看看 XML 定义语句的方式</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE mapper        PUBLIC &quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;        &quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;&gt;&lt;!--namespace：绑定一个对应的Dao/Mapper接口--&gt;&lt;!--        dao.book.BookMapper--&gt;&lt;mapper namespace=&quot;dao.book.BookMapper&quot;&gt;    &lt;!--select查询语句--&gt;    &lt;select id=&quot;getRelativeBook&quot; resultType=&quot;Bean.Book&quot;&gt;        select * from smbms_provider where proDesc like #&#123;bookDesc&#125;  and id != #&#123;id&#125; limit 1;    &lt;/select&gt;&lt;/mapper&gt;</code></pre><ul><li><p><code>Namespace</code>: 指明接口类的命名空间, 用接口类的权限定名</p><p>命名空间的作用有两个，一个是利用更长的全限定名来将不同的语句隔离开来，同时实现了接口绑定。 将命名空间置于合适的 Java 包命名空间之中，你的代码会变得更加整洁，也有利于你更方便地使用 MyBatis。</p></li><li><p><code>select</code>便签: 用于查询语句</p></li><li><p><code>id</code>: 在命名空间中唯一的标识符，可以被用来引用这条语句. 对应接口类的方法名, <strong>必须和接口类的方法名一致</strong></p></li><li><p><code>resultType</code>: 期望从这条语句中返回结果的<strong>类全限定名或别名</strong>。 注意，<strong>如果返回的是集合，那应该设置为集合包含的类型，而不是集合本身的类型</strong>。 resultType 和 resultMap 之间只能同时使用一个.</p></li><li><p><code>parameterType</code>: 将会传入这条语句的参数的<strong>类全限定名或别名</strong>。这个属性是<strong>可选</strong>的，因为 MyBatis 可以根据语句中实际传入的参数计算出应该使用的类型处理器（TypeHandler），默认值为未设置（unset）。</p></li></ul><p>为了这个简单的例子，我们似乎写了不少配置，但其实并不多。<strong>在一个 XML 映射文件中，可以定义无数个映射语句</strong>，这样一来，XML 头部和文档类型声明部分就显得微不足道了。文档的其它部分很直白，容易理解。 它在命名空间 <code>dao.book.BookMapper </code>中定义了一个名为 <code>selectBlog</code> 的映射语句</p></li><li><p>注册Mapper</p><p>在mybatis-config.xml中注册Mapper</p><pre><code class="language-xml"> &lt;mappers&gt;&lt;!--        方式1： 通过resource注册mapper--&gt;    &lt;mapper resource=&quot;dao/book/BookMapper.xml&quot;&gt;&lt;/mapper&gt;&lt;/mappers&gt;</code></pre><ol start="7"><li><p>编写获取数据库连接的工具类Util</p><pre><code class="language-java">public class MybatisUtil &#123;    static SqlSessionFactory sqlSessionFactory;    static &#123;        InputStream inputStream = null;        try &#123;//          从 XML 文件中构建 SqlSessionFactory 的实例            String resource = &quot;mybatis-config.xml&quot;;            inputStream = Resources.getResourceAsStream(resource);            sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);        &#125; catch (IOException e) &#123;            throw new RuntimeException(e);        &#125;    &#125;    public static SqlSession getSqlSession() &#123;        return sqlSessionFactory.openSession();    &#125;&#125;</code></pre><p>每个基于 MyBatis 的应用都是以一个<code>SqlSessionFactory</code>的实例为核心的。<code>SqlSessionFactory </code>的实例可以通过 <code>SqlSessionFactoryBuilder</code> 的<code>build</code>方法获得。而<code>SqlSessionFactoryBuilder</code>则可以从 XML 配置文件或一个预先配置的 Configuration 实例来构建出 <code>SqlSessionFactory</code> 实例。(<strong>工厂模式</strong>)</p><blockquote><p>工厂模式是一种创建型设计模式，用于封装对象的创建过程。它通过定义一个共同的接口或基类来创建对象，但具体的对象创建逻辑被移到工厂类中。这样，客户端代码只需要与工厂类交互，而不需要直接创建对象，从而降低了代码的耦合性。</p><p>通过工厂模式，代码实现了对象的创建与使用的分离，使得代码更具可维护性和扩展性。如果将来需要修改 <code>SqlSessionFactory</code> 对象的创建逻辑，只需修改 <code>MybatisUtil</code> 类中的代码，而不需要修改客户端代码。同时，工厂模式也隐藏了具体对象的创建细节，使得客户端代码更加简洁和可读。</p></blockquote><p>代码通过静态代码块在类加载时初始化了一个全局的 <code>SqlSessionFactory</code> 对象，并提供了一个公共的静态方法用于获取 <code>SqlSession</code> 实例。这样其他类在使用 MyBatis 进行数据库操作时，可以通过调用 <code>MybatisUtil.getSqlSession()</code> 方法来获取 <code>SqlSession</code> 对象，从而简化了 MyBatis 的配置和使用过程。</p><p><code>SqlSession </code>提供了在数据库执行 SQL 命令所需的所有方法。你可以通过 SqlSession 实例来直接执行已映射的 SQL 语句.</p></li><li><p>编写测试类</p><pre><code class="language-java">@Testpublic void testGetRelativeBook()&#123;     SqlSession sqlSession = MybatisUtil.getSqlSession();     BookMapper mapper = sqlSession.getMapper(BookMapper.class);     Book relativeBook = mapper.getRelativeBook(12, &quot;%计算机%&quot;);     System.out.println(relativeBook.toString()); &#125;</code></pre></li></ol></li></ol><h3 id="参数">参数</h3><ol><li><p>对于大多数简单的使用场景，你都不需要使用复杂的参数，比如：</p><pre><code class="language-xml">&lt;select id=&quot;selectUsers&quot; resultType=&quot;User&quot;&gt;  select id, username, password from users where id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>参数类型（parameterType）会被自动设置为 <code>int</code>，这个参数可以<strong>随意命名</strong>。原始类型或简单数据类型（比如 <code>Integer</code> 和 <code>String</code>）因为没有其它属性，会用它们的值来作为参数.</p></li><li><p>然而，如果传入一个<strong>复杂的对象</strong>，行为就会有点不一样了。比如</p><pre><code class="language-xml">&lt;insert id=&quot;insertUser&quot; parameterType=&quot;User&quot;&gt;  insert into users (id, username, password)  values (#&#123;id&#125;, #&#123;username&#125;, #&#123;password&#125;)&lt;/insert&gt;</code></pre><p>如果 User 类型的参数对象传递到了语句中，会查找 id、username 和 password 属性，然后将它们的值传入预处理语句的参数中。<strong>如果参数类型是具体的类, 参数可以直接通过属性名直接得到, 即使属性是私有的.</strong></p></li><li><p>参数类型是HashMap</p><p>这时候, 参数名会被映射为Map中的key.</p></li><li><p>@Param注解用于给方法参数起一个名字。以下是总结的使用原则：</p><ul><li>在方法只接受一个参数的情况下，可以不使用@Param。</li><li>在方法接受多个参数的情况下，建议一定要使用@Param注解给参数命名。</li><li><strong>如果参数是 JavaBean ， 则不能使用@Param。</strong></li><li>不使用@Param注解时，参数只能有一个，并且是Javabean。</li></ul></li></ol><h4 id="字符串替换">字符串替换</h4><p>默认情况下，使用 <code>#&#123;&#125;</code> 参数语法时，MyBatis 会创建 <code>PreparedStatement</code> 参数占位符，并通过占位符安全地设置参数（就像使用 <code>?</code> 一样）。 这样做更安全，更迅速，通常也是首选做法，不过有时你就是想直接在 SQL 语句中直接插入一个不转义的字符串。 比如 ORDER BY 子句，这时候你可以：</p><pre><code>ORDER BY $&#123;columnName&#125;</code></pre><p>这样，MyBatis 就不会修改或转义该字符串了。</p><p>当 SQL 语句中的元数据（如表名或列名）是动态生成的时候，字符串替换将会非常有用。 举个例子，如果你想 <code>select</code> 一个表任意一列的数据时，不需要这样写：</p><pre><code class="language-java">@Select(&quot;select * from user where id = #&#123;id&#125;&quot;)User findById(@Param(&quot;id&quot;) long id);@Select(&quot;select * from user where name = #&#123;name&#125;&quot;)User findByName(@Param(&quot;name&quot;) String name);@Select(&quot;select * from user where email = #&#123;email&#125;&quot;)User findByEmail(@Param(&quot;email&quot;) String email);// 其它的 &quot;findByXxx&quot; 方法</code></pre><p>而是可以只写这样一个方法：</p><pre><code class="language-java">@Select(&quot;select * from user where $&#123;column&#125; = #&#123;value&#125;&quot;)User findByColumn(@Param(&quot;column&quot;) String column, @Param(&quot;value&quot;) String value);</code></pre><p>其中 <code>$&#123;column&#125;</code> 会被直接替换，而 <code>#&#123;value&#125;</code> 会使用 <code>?</code> 预处理。 这样，就能完成同样的任务：</p><pre><code class="language-java">User userOfId1 = userMapper.findByColumn(&quot;id&quot;, 1L);User userOfNameKid = userMapper.findByColumn(&quot;name&quot;, &quot;kid&quot;);User userOfEmail = userMapper.findByColumn(&quot;email&quot;, &quot;noone@nowhere.com&quot;);</code></pre><p>这种方式也同样适用于替换表名的情况。</p><blockquote><p><strong>提示</strong> :</p><p>用这种方式接受用户的输入，并用作语句参数是不安全的，会导致潜在的 SQL 注入攻击。因此，要么不允许用户输入这些字段，要么自行转义并检验这些参数。</p></blockquote><h2 id="XML配置解析">XML配置解析</h2><p>MyBatis 的配置文件包含了会深深影响 MyBatis 行为的设置和属性信息。</p><ul><li>能配置的内容如下：</li></ul><pre><code class="language-properties">configuration（配置）properties（属性）settings（设置）typeAliases（类型别名）typeHandlers（类型处理器）objectFactory（对象工厂）plugins（插件）environments（环境配置）environment（环境变量）transactionManager（事务管理器）dataSource（数据源）databaseIdProvider（数据库厂商标识）mappers（映射器）&lt;!-- 注意元素节点的顺序！顺序不对会报错 --&gt;</code></pre><h3 id="environments">environments</h3><p>environments 元素定义了如何配置环境。</p><pre><code class="language-xml">&lt;environments default=&quot;development&quot;&gt;  &lt;environment id=&quot;development&quot;&gt;    &lt;transactionManager type=&quot;JDBC&quot;&gt;      &lt;property name=&quot;...&quot; value=&quot;...&quot;/&gt;    &lt;/transactionManager&gt;    &lt;dataSource type=&quot;POOLED&quot;&gt;      &lt;property name=&quot;driver&quot; value=&quot;$&#123;driver&#125;&quot;/&gt;      &lt;property name=&quot;url&quot; value=&quot;$&#123;url&#125;&quot;/&gt;      &lt;property name=&quot;username&quot; value=&quot;$&#123;username&#125;&quot;/&gt;      &lt;property name=&quot;password&quot; value=&quot;$&#123;password&#125;&quot;/&gt;    &lt;/dataSource&gt;  &lt;/environment&gt;&lt;/environments&gt;</code></pre><p>注意一些关键点:</p><ul><li><p>默认使用的环境 ID（比如：default=“development”）。</p><p>环境，将SQL映射到多个不同的数据库上，**不过要记住：尽管可以配置多个环境，但每个 SqlSessionFactory 实例只能选择一种环境。**必须指定其中一个为默认运行环境（通过default指定）</p><p>环境可以随意命名，但务必保证默认的环境 ID 要匹配其中一个环境 ID。</p></li><li><p>事务管理器的配置（比如：type=“JDBC”）。</p><blockquote><p>在 MyBatis 中有两种类型的事务管理器（也就是 type=“[JDBC|MANAGED]”）：</p><ul><li>JDBC – 这个配置直接使用了 JDBC 的提交和回滚功能</li><li>MANAGED – 这个配置几乎没做什么。它从不提交或回滚一个连接，而是让容器来管理事务的整个生命周期（比如 JEE 应用服务器的上下文）。 默认情况下它会关闭连接。</li></ul><p>如果你正在使用 Spring + MyBatis，则没有必要配置事务管理器，因为 Spring 模块会使用自带的管理器来覆盖前面的配置。</p></blockquote></li><li><p>数据源的配置（比如：type=“POOLED”）。</p><p>dataSource 元素使用标准的 JDBC 数据源接口来配置 JDBC 连接对象的资源。</p></li><li><p>数据源是必须配置的。</p></li><li><p>有三种内建的数据源类型</p><pre><code>type=&quot;[UNPOOLED|POOLED|JNDI]&quot;）</code></pre></li><li><p>unpooled：</p><p>这个数据源的实现会每次请求时打开和关闭连接。虽然有点慢，但对那些数据库连接可用性要求不高的简单应用程序来说，是一个很好的选择。 性能表现则依赖于使用的数据库，对某些数据库来说，使用连接池并不重要，这个配置就很适合这种情形。</p></li><li><p><strong>pooled</strong>：</p><p>这种数据源的实现利用“池”的概念将 JDBC 连接对象组织起来 , 避免了创建新的连接实例时所必需的初始化和认证时间。 这种处理方式很流行，能使并发 Web 应用快速响应请求。</p></li><li><p>jndi：</p><p>这个数据源的实现是为了能在如 Spring 或应用服务器这类容器中使用，容器可以集中或在外部配置数据源，然后放置一个 JNDI 上下文的引用。</p></li></ul><h3 id="映射器（mappers）">映射器（mappers）</h3><p>我们需要告诉 MyBatis 到哪里去找到这些语句。 在自动查找资源方面，Java 并没有提供一个很好的解决方案，所以最好的办法是直接告诉 MyBatis 到哪里去找映射文件。</p><pre><code class="language-xml">&lt;!-- 使用相对于类路径的资源引用 --&gt;&lt;mappers&gt;  &lt;mapper resource=&quot;org/mybatis/builder/AuthorMapper.xml&quot;/&gt;  &lt;mapper resource=&quot;org/mybatis/builder/BlogMapper.xml&quot;/&gt;  &lt;mapper resource=&quot;org/mybatis/builder/PostMapper.xml&quot;/&gt;&lt;/mappers&gt;&lt;!-- 使用完全限定资源定位符（URL） --&gt;&lt;mappers&gt;  &lt;mapper url=&quot;file:///var/mappers/AuthorMapper.xml&quot;/&gt;  &lt;mapper url=&quot;file:///var/mappers/BlogMapper.xml&quot;/&gt;  &lt;mapper url=&quot;file:///var/mappers/PostMapper.xml&quot;/&gt;&lt;/mappers&gt;&lt;!-- 使用映射器接口实现类的完全限定类名 --&gt;&lt;mappers&gt;  &lt;mapper class=&quot;org.mybatis.builder.AuthorMapper&quot;/&gt;  &lt;mapper class=&quot;org.mybatis.builder.BlogMapper&quot;/&gt;  &lt;mapper class=&quot;org.mybatis.builder.PostMapper&quot;/&gt;&lt;/mappers&gt;&lt;!-- 将包内的映射器接口全部注册为映射器 --&gt;&lt;mappers&gt;  &lt;package name=&quot;org.mybatis.builder&quot;/&gt;&lt;/mappers&gt;</code></pre><p>⚠️注意: 使用将包内的映射器接口全部注册为映射器时, 必须保证接口和对应的mapper.xml文件名一致</p><h3 id="类型别名（typeAliases）">类型别名（typeAliases）</h3><p>类型别名可为 Java 类型设置一个缩写名字。 它仅用于 XML 配置，用于降低冗余的全限定类名书写。例如：</p><pre><code class="language-xml">&lt;typeAliases&gt;  &lt;typeAlias alias=&quot;Author&quot; type=&quot;domain.blog.Author&quot;/&gt;  &lt;typeAlias alias=&quot;Blog&quot; type=&quot;domain.blog.Blog&quot;/&gt;  &lt;typeAlias alias=&quot;Comment&quot; type=&quot;domain.blog.Comment&quot;/&gt;  &lt;typeAlias alias=&quot;Post&quot; type=&quot;domain.blog.Post&quot;/&gt;  &lt;typeAlias alias=&quot;Section&quot; type=&quot;domain.blog.Section&quot;/&gt;  &lt;typeAlias alias=&quot;Tag&quot; type=&quot;domain.blog.Tag&quot;/&gt;&lt;/typeAliases&gt;</code></pre><p>当这样配置时，<code>Blog</code> 可以用在任何使用 <code>domain.blog.Blog</code> 的地方。</p><p>也可以指定一个包名，MyBatis 会在包名下面搜索需要的 Java Bean，比如：</p><pre><code class="language-xml">&lt;typeAliases&gt;  &lt;package name=&quot;domain.blog&quot;/&gt;&lt;/typeAliases&gt;</code></pre><p>每一个在包 <code>domain.blog</code> 中的 Java Bean，<strong>在没有注解的情况下，会使用 Bean 的首字母小写的非限定类名来作为它的别名</strong>。 比如 <code>domain.blog.Author</code> 的别名为 <code>author</code>；若有注解，则别名为其注解值.</p><h2 id="作用域（Scope）和生命周期">作用域（Scope）和生命周期</h2><p>作用域和生命周期类别是至关重要的，因为错误的使用会导致非常严重的<strong>并发问题。</strong></p><h3 id="SqlSessionFactoryBuilder">SqlSessionFactoryBuilder</h3><p>这个类可以被实例化、使用和丢弃，<strong>一旦创建了 SqlSessionFactory，就不再需要它了</strong>。因此 SqlSessionFactoryBuilder 实例的最佳作用域是方法作用域（也就是<strong>局部方法变量</strong>）。</p><h3 id="SqlSessionFactory">SqlSessionFactory</h3><p><strong>SqlSessionFactory 一旦被创建就应该在应用的运行期间一直存在</strong>，<strong>没有任何理由丢弃它或重新创建另一个实例</strong>。 使用 SqlSessionFactory 的最佳实践是在应用运行期间不要重复创建多次，多次重建 SqlSessionFactory 被视为一种代码“坏习惯”。因此 SqlSessionFactory 的最佳作用域是<strong>应用作用域(Application)</strong>。 有很多方法可以做到，最简单的就是使用<strong>单例模式</strong>或者<strong>静态单例模式。</strong></p><blockquote><p>单例模式是一种创建型设计模式，用于确保一个类只有一个实例，并提供一个全局访问点来访问该实例。它通常适用于需要在整个应用程序中共享状态或资源的情况，以及需要对实例进行严格控制的情况。</p><p>实现单例模式的关键是将类的构造函数私有化，这样就无法通过常规的方式创建类的实例。然后，通过一个静态方法或属性来创建并返回类的唯一实例。每次调用这个方法时，都会返回同一个实例。</p><p>静态单例模式是单例模式的一种变体，它使用静态变量来保存单例实例，并提供一个静态方法来获取该实例。在静态单例模式中，实例的创建和获取方法都是静态的，因此可以通过类名直接调用，无需实例化类。</p><p><strong>单例模式示例：</strong></p><pre><code class="language-java">javaCopy codepublic class Singleton &#123;   private static Singleton instance;   private Singleton() &#123;       // 私有化构造函数   &#125;   public static Singleton getInstance() &#123;       if (instance == null) &#123;           instance = new Singleton();       &#125;       return instance;   &#125;   // 其他方法...&#125;</code></pre></blockquote><h3 id="SqlSession">SqlSession</h3><p><strong>每个线程都应该有它自己的 SqlSession 实例</strong>。SqlSession 的实例不是线程安全的，因此是不能被共享的，所以<strong>它的最佳的作用域是请求或方法作用域</strong>。</p><p>如果说 SqlSessionFactory 相当于数据库连接池，那么 SqlSession 就相当于一个数据库连接（Connection 对象），你可以在一个事务里面执行多条 SQL，然后通过它的 commit、rollback 等方法，提交或者回滚事务。<strong>所以它应该存活在一个业务请求中，处理完整个请求后，应该关闭这条连接，让它归还给 SqlSessionFactory，否则数据库资源就很快被耗费精光，系统就会瘫痪</strong>，所以用 try…catch…finally… 语句来保证其正确关闭。</p><p>比如 Servlet 框架中的 HttpSession, 每次收到 HTTP 请求，就可以打开一个 SqlSession，返回一个响应后，就关闭它。</p><h3 id="映射器-Mapper-实例">映射器(Mapper)实例</h3><p>映射器实例的生命周期与 SqlSession 相关联，作用域是每个 SqlSession。它们在每个 SqlSession 中创建，并在 SqlSession 关闭时销毁。可以通过依赖注入等方式实现映射器实例在不同 SqlSession 中的重用。这样可以确保映射器实例的正确性和资源的有效利用。</p><h2 id="ResultMap">ResultMap</h2><h4 id="问题引出">问题引出</h4><p>类属性名property和数据库字段名column名不一致的问题:</p><p>如果我们更改实体类的属性名把proName改为BookName, 再次执行查询我们会得到值为null.</p><p>这是因为mybatis会根据这些查询的列名(会将列名转化为小写,数据库不区分大小写) , 去对应的实体类中查找相应列名的set方法设值 , 由于找不到setProName() , 所以返回null .</p><p><img src="MyBatis%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-20230527214201597.png" alt="image-20230527214201597" loading="lazy"></p><p>下面介绍ResultMap来解决这个问题:</p><h4 id="解决">解决</h4><p>**<code>resultMap</code> 元素是 MyBatis 中最重要最强大的元素。它可以让你从 90% 的 JDBC <code>ResultSets</code> 数据提取代码中解放出来。**实际上，在为一些比如连接的复杂语句编写映射代码的时候，一份 <code>resultMap</code> 能够代替实现同等功能的长达数千行的代码.</p><p>ResultMap 的设计思想是，对于简单的语句根本不需要配置显式的结果映射，而对于复杂一点的语句只需要描述它们的关系就行了。</p><p>你已经见过简单映射语句的示例了，但并没有显式指定 <code>resultMap</code>。比如：</p><pre><code>&lt;select id=&quot;selectUserById&quot; resultType=&quot;map&quot;&gt;select id , name , pwd  from user  where id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>上述语句只是简单地将所有的列映射到 <code>HashMap</code> 的键上，这由 <code>resultType</code> 属性指定。虽然在大部分情况下都够用，但是 HashMap 不是一个很好的模型。你的程序更可能会使用 JavaBean 或 POJO（Plain Old Java Objects，普通老式 Java 对象）作为模型。</p><p><code>ResultMap</code> 最优秀的地方在于，虽然你已经对它相当了解了，但是根本就不需要显式地用到他们。看看下面这个 JavaBean：</p><pre><code class="language-java">public class User &#123;  private int id;  private String username;  private String hashedPassword;/** setter and getter...*/&#125;</code></pre><p>基于 JavaBean 的规范，上面这个类有 3 个属性：id，username 和 hashedPassword。<strong>这些属性会对应到 select 语句中的列名</strong>。</p><p>这样的一个 JavaBean 可以被映射到 <code>ResultSet</code>，就像映射到 <code>HashMap</code> 一样简单。</p><pre><code class="language-xml">&lt;select id=&quot;selectUsers&quot; resultType=&quot;com.someapp.model.User&quot;&gt;  select id, username, hashedPassword  from some_table  where id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>类型别名是你的好帮手。使用它们，你就可以不用输入类的全限定名了。比如：</p><pre><code class="language-xml">&lt;!-- mybatis-config.xml 中 --&gt;&lt;typeAlias type=&quot;com.someapp.model.User&quot; alias=&quot;User&quot;/&gt;&lt;!-- SQL 映射 XML 中 --&gt;&lt;select id=&quot;selectUsers&quot; resultType=&quot;User&quot;&gt;  select id, username, hashedPassword  from some_table  where id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>在这些情况下，MyBatis 会在幕后自动创建一个 <code>ResultMap</code>，再根据属性名来映射列到 JavaBean 的属性上。如果列名和属性名不能匹配上，可以在 SELECT 语句中设置列别名（这是一个基本的 SQL 特性）来完成匹配。比如：</p><pre><code class="language-xml">&lt;select id=&quot;selectUsers&quot; resultType=&quot;User&quot;&gt;  select    user_id             as &quot;id&quot;,    user_name           as &quot;userName&quot;,    hashed_password     as &quot;hashedPassword&quot;  from some_table  where id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>在学习了上面的知识后，你会发现上面的例子没有一个需要显式配置 <code>ResultMap</code>，这就是 <code>ResultMap</code> 的优秀之处——你完全可以不用显式地配置它们。 虽然上面的例子不用显式配置 <code>ResultMap</code>。 但为了讲解，我们来看看如果在刚刚的示例中，显式使用外部的 <code>resultMap</code> 会怎样，这也是解决列名不匹配的另外一种方式。</p><pre><code class="language-xml">&lt;resultMap id=&quot;userResultMap&quot; type=&quot;User&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;user_id&quot; /&gt;  &lt;result property=&quot;username&quot; column=&quot;user_name&quot;/&gt;  &lt;result property=&quot;password&quot; column=&quot;hashed_password&quot;/&gt;&lt;/resultMap&gt;</code></pre><p>然后在引用它的语句中设置 <code>resultMap</code> 属性就行了（注意我们去掉了 <code>resultType</code> 属性）。比如:</p><pre><code class="language-xml">&lt;select id=&quot;selectUsers&quot; resultMap=&quot;userResultMap&quot;&gt;  select user_id, user_name, hashed_password  from some_table  where id = #&#123;id&#125;&lt;/select&gt;</code></pre><p><strong>如果这个世界总是这么简单就好了。</strong></p><h2 id="日志工厂">日志工厂</h2><p>日志记录是任何优秀软件开发过程的重要组成部分。通过日志记录，您可以检测和修复问题、了解用户的行为. 如果一个 数据库相关的操作出现了问题，我们可以根据输出的SQL语句快速排查问题。</p><p>对于以往的开发过程，我们会经常使用到debug模式来调节，跟踪我们的代码执行过程。但是现在使用Mybatis是基于接口，配置文件的源代码执行过程。因此，我们必须选择日志工具来作为我们开发，调节程序的工具。</p><p>Mybatis内置的日志工厂提供日志功能，具体的日志实现有以下几种工具：</p><ul><li>SLF4J</li><li>Apache Commons Logging</li><li><strong>Log4j 2</strong></li><li>Log4j (3.5.9 起废弃）</li><li>JDK logging</li><li>STDOUT_LOGGING</li><li>NO_LOGGING</li></ul><p>具体选择哪个日志实现工具由MyBatis的内置日志工厂确定。它会使用最先找到的（按上文列举的顺序查找）。如果一个都未找到，日志功能就会被禁用。</p><h3 id="标准日志实现-STDOUT-LOGGING">**标准日志实现 **STDOUT_LOGGING</h3><p>指定 MyBatis 应该使用哪个日志记录实现。如果此设置不存在，则会自动发现日志记录实现。</p><pre><code class="language-xml">&lt;settings&gt;       &lt;setting name=&quot;logImpl&quot; value=&quot;STDOUT_LOGGING&quot;/&gt;&lt;/settings&gt;</code></pre><p>测试，可以看到控制台有大量的输出！我们可以通过这些输出来判断程序到底哪里出了Bug</p><h3 id="Log4j2"><strong>Log4j2</strong></h3><p><strong>简介：</strong></p><ul><li><a href="https://www.scalyr.com/blog/get-started-quickly-java-logging/">Log4j2 是Java</a>语言的日志记录框架，由 Apache 基金会维护。Log4j2 是对其前身 Log4j 的改进。它的主要好处是：<ul><li>API独立于实现</li><li>更好地支持并发</li><li>通过构建自定义组件轻松扩展</li></ul></li></ul><p><strong>使用步骤：</strong></p><p>1、导入log4j2的包</p><pre><code class="language-xml">&lt;!--      log4j-core- 核心Log4j实现类。--&gt;      &lt;dependency&gt;          &lt;groupId&gt;org.apache.logging.log4j&lt;/groupId&gt;          &lt;artifactId&gt;log4j-core&lt;/artifactId&gt;          &lt;version&gt;2.20.0&lt;/version&gt;      &lt;/dependency&gt;&lt;!--      log4j-api- 提供实现者创建日志实现所需的适配器组件。--&gt;      &lt;dependency&gt;          &lt;groupId&gt;org.apache.logging.log4j&lt;/groupId&gt;          &lt;artifactId&gt;log4j-api&lt;/artifactId&gt;          &lt;version&gt;2.20.0&lt;/version&gt;      &lt;/dependency&gt;</code></pre><p>2、配置文件编写</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;Configuration status=&quot;INFO&quot;&gt;    &lt;Appenders&gt;        &lt;Console name=&quot;ConsoleAppender&quot; target=&quot;SYSTEM_OUT&quot;&gt;            &lt;PatternLayout pattern=&quot;%d&#123;HH:mm:ss.SSS&#125; [%t] %-5level %logger&#123;36&#125; - %msg%n&quot; /&gt;        &lt;/Console&gt;        &lt;File name=&quot;FileAppender&quot; fileName=&quot;application-$&#123;date:yyyyMMdd&#125;.log&quot; immediateFlush=&quot;false&quot; append=&quot;true&quot;&gt;            &lt;PatternLayout pattern=&quot;%d&#123;yyy-MM-dd HH:mm:ss.SSS&#125; [%t] %-5level %logger&#123;36&#125; - %msg%n&quot;/&gt;        &lt;/File&gt;    &lt;/Appenders&gt;    &lt;Loggers&gt;        &lt;Root level=&quot;debug&quot;&gt;            &lt;AppenderRef ref=&quot;ConsoleAppender&quot; /&gt;            &lt;AppenderRef ref=&quot;FileAppender&quot;/&gt;        &lt;/Root&gt;    &lt;/Loggers&gt;&lt;/Configuration&gt;</code></pre><p>仔细看看我们的配置文件。它首先定义两个 appender，一个称为“FileAppender”，另一个称为“ConsoleAppender”appender 是有效地将日志消息写入其目的地的组件。</p><p>第一个 appender 将 stdout 定义为其目标，而第二个 appender 定义文件名的模式。</p><p>换句话说，第一个 appender 记录到标准输出，而第二个 appender 记录到一个文件。</p><p>然后该文件继续定义记录器。记录器负责实际记录传递给它的消息。每个记录器都可以引用一个或多个附加程序，这正是这里发生的事情。记录器引用了两个附加程序，因此当它被调用时，它将同时记录到标准输出和日志文件中。</p><p>3、setting设置日志实现</p><pre><code>&lt;settings&gt;   &lt;setting name=&quot;logImpl&quot; value=&quot;LOG4J2&quot;/&gt;&lt;/settings&gt;</code></pre><p>4、在程序中使用Log4j进行输出！</p><pre><code class="language-java">import org.apache.logging.log4j.LogManager;import org.apache.logging.log4j.Logger;public class App &#123;    private static final Logger logger = LogManager.getLogger(App.class); ;    public static void main( String[] args )    &#123;        System.out.println( &quot;Hello World!&quot; );        logger.info( &quot;又浪费了一早上， 都在弄这个破log4j fuck!&quot; );        logger.trace(&quot;We've just greeted the user!&quot;);        logger.debug(&quot;We've just greeted the user!&quot;);        logger.info(&quot;We've just greeted the user!&quot;);        logger.warn(&quot;We've just greeted the user!&quot;);        logger.error(&quot;We've just greeted the user!&quot;);        logger.fatal(&quot;We've just greeted the user!&quot;);    &#125;&#125;</code></pre><p>5、测试，看控制台输出！</p><ul><li><p>使用Log4j 2输出日志</p><p><img src="MyBatis%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-20230530173545142.png" alt="image-20230530173545142" loading="lazy"></p></li><li><p>可以看到还生成了一个日志的文件 【需要修改file的日志级别】</p></li></ul><p><img src="MyBatis%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-20230530173652533.png" alt="image-20230530173652533" loading="lazy"></p><p>Ref:</p><ul><li><a href="https://www.docs4dev.com/docs/zh/log4j2/2.x/all/manual-webapp.html#Servlet-2.5">在 Web 应用程序中使用 Log4j 2</a></li><li><a href="https://www.dataset.com/blog/maven-log4j2-project/">Log4j2 in a Maven Project: How to Setup, Configure, and Use</a></li></ul><h2 id="多对一处理之高级结果映射">多对一处理之高级结果映射</h2><p>MyBatis 创建时的一个思想是：</p><p>数据库不可能永远是你所想或所需的那个样子。 我们希望每个数据库都具备良好的第三范式或 BCNF 范式，可惜它们并不都是那样。 如果能有一种数据库映射模式，完美适配所有的应用程序，那就太好了，但可惜也没有。 而 ResultMap 就是 MyBatis 对这个问题的答案。</p><blockquote><ol><li><p>第一范式（1NF）：表中的每个属性都是原子的，即不可再分。每个属性只包含单个值，不包含多个值或重复的组合值。</p></li><li><p>第二范式（2NF）：表中的非主键属性必须完全依赖于主键。换句话说，表中的每个非主键属性必须完全依赖于表中的主键，而不是部分依赖于主键的某个子集。</p></li><li><p>第三范式（3NF）：表中的非主键属性之间不应该存在传递依赖关系。换句话说，表中的每个非主键属性应该直接依赖于主键，而不是依赖于其他非主键属性。</p></li></ol><p>通过将表设计为第三范式，可以避免数据冗余和不一致性，提高数据存储的效率和一致性。这样的设计有助于减少数据修改的复杂性，提高数据查询和更新的性能，并支持更好的数据一致性维护。</p><p>然而，需要注意的是，严格遵循第三范式并不一定是最佳选择。在某些情况下，为了满足特定的性能需求或简化复杂查询，可能需要在设计中引入一些冗余数据或非范式化的结构。在数据库设计过程中，需要根据具体需求和性能考虑，权衡范式化和非范式化的设计选择。</p></blockquote><p>先从简单的开始, 老师和学生的关系, 多个学生存在同属于一个老师, 这就是多对一关系, 我们设计以下Java Bean</p><pre><code class="language-java">import lombok.AllArgsConstructor;import lombok.Data;import lombok.NoArgsConstructor;@Data@AllArgsConstructor@NoArgsConstructorpublic class Student &#123;    private int id;    private String name;    private int tId;    private Teacher teacher;&#125;</code></pre><pre><code class="language-java">import lombok.AllArgsConstructor;import lombok.Data;import lombok.NoArgsConstructor;@Data@NoArgsConstructor@AllArgsConstructorpublic class Teacher &#123;    private int id;    private String name;&#125;</code></pre><p>如果我们需要查询所有的学生信息以及学生对应的老师, 我们编写SQL</p><pre><code class="language-sql">select  s.id, s.name as sName, tid,t.name as tName from students s, teachers t where s.tid = t.id;</code></pre><p>执行SQL可以得到</p><p><img src="MyBatis%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-20230531151419612.png" alt="image-20230531151419612" loading="lazy"></p><p>但是这样是得不到Teacher对象的值的，只能得到null，所以我需要编写ResutlMap</p><pre><code class="language-java">&lt;resultMap id=&quot;ST&quot; type=&quot;org.example.Bean.Student&quot;&gt;    &lt;id property=&quot;id&quot; column=&quot;id&quot;/&gt;    &lt;result property=&quot;name&quot; column=&quot;sName&quot;/&gt;    &lt;association property=&quot;teacher&quot; javaType=&quot;org.example.Bean.Teacher&quot;&gt;        &lt;id property=&quot;id&quot; column=&quot;tid&quot;&gt;&lt;/id&gt;        &lt;result property=&quot;name&quot; column=&quot;tName&quot;/&gt;    &lt;/association&gt;&lt;/resultMap</code></pre><p>这段代码是一个 MyBatis 的结果映射配置（resultMap），用于将查询结果映射到 Java 对象。根据给定的配置，它定义了一个名为 “ST” 的结果映射，将查询结果映射到 org.example.Bean.Student 类型的对象。</p><p>该 resultMap 包含三个映射项：</p><ol><li><p>id 映射：将查询结果的 “id” 列映射到 Student 对象的 “id” 属性。<strong>标记出作为 ID 的结果可以帮助提高整体性能</strong></p></li><li><p>name 映射：将查询结果的 “sName” 列映射到 Student 对象的 “name” 属性。</p></li><li><p>association（关联）映射：将查询结果中的部分字段映射到关联的 Teacher 对象。</p><ul><li><p>id 映射：将查询结果的 “tid” 列映射到 Teacher 对象的 “id” 属性。</p></li><li><p>name 映射：将查询结果的 “tName” 列映射到 Teacher 对象的 “name” 属性。</p></li></ul></li></ol><p>通过使用 resultMap，MyBatis 可以根据查询结果自动将数据填充到 Student 对象中，并可通过访问 Student 对象的属性来获取查询结果的值。此外，关联映射允许将关联表的部分字段映射到关联对象中，以便在查询结果中包含关联对象的相关信息。</p><ul><li><p>编写测试类</p><pre><code class="language-java">public class MapperTest &#123;    @Test    public void test() &#123;        SqlSession sqlSession = MybatisUtil.getSqlSession();        StudentMapper mapper = sqlSession.getMapper(StudentMapper.class);        List&lt;Student&gt; students = mapper.getStudents();        for (Student student : students) &#123;            System.out.println(student.toString());        &#125;        sqlSession.close();    &#125;&#125;</code></pre></li></ul><p>这样我们就可以给Teacher对象赋值了</p><p><img src="MyBatis%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/image-20230531185420784.png" alt="image-20230531185420784" loading="lazy"></p><p>实际项目中我们可能对多个表进行连表查询，比如，我们如何映射下面这个语句？</p><pre><code class="language-xml">&lt;!-- 非常复杂的语句 --&gt;&lt;select id=&quot;selectBlogDetails&quot; resultMap=&quot;detailedBlogResultMap&quot;&gt;  select       B.id as blog_id,       B.title as blog_title,       B.author_id as blog_author_id,       A.id as author_id,       A.username as author_username,       A.password as author_password,       A.email as author_email,       A.bio as author_bio,       A.favourite_section as author_favourite_section,       P.id as post_id,       P.blog_id as post_blog_id,       P.author_id as post_author_id,       P.created_on as post_created_on,       P.section as post_section,       P.subject as post_subject,       P.draft as draft,       P.body as post_body,       C.id as comment_id,       C.post_id as comment_post_id,       C.name as comment_name,       C.comment as comment_text,       T.id as tag_id,       T.name as tag_name  from Blog B       left outer join Author A on B.author_id = A.id       left outer join Post P on B.id = P.blog_id       left outer join Comment C on P.id = C.post_id       left outer join Post_Tag PT on PT.post_id = P.id       left outer join Tag T on PT.tag_id = T.id  where B.id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>你可能想把它映射到一个智能的对象模型，这个对象表示了一篇博客，它由某位作者所写，有很多的博文，每篇博文有零或多条的评论和标签。 我们先来看看下面这个完整的例子，它是一个非常复杂的结果映射（假设作者，博客，博文，评论和标签都是类型别名）。 不用紧张，我们会一步一步地来说明。虽然它看起来令人望而生畏，但其实非常简单。</p><pre><code class="language-xml">&lt;!-- 非常复杂的结果映射 --&gt;&lt;resultMap id=&quot;detailedBlogResultMap&quot; type=&quot;Blog&quot;&gt;  &lt;constructor&gt;    &lt;idArg column=&quot;blog_id&quot; javaType=&quot;int&quot;/&gt;  &lt;/constructor&gt;  &lt;result property=&quot;title&quot; column=&quot;blog_title&quot;/&gt;  &lt;association property=&quot;author&quot; javaType=&quot;Author&quot;&gt;    &lt;id property=&quot;id&quot; column=&quot;author_id&quot;/&gt;    &lt;result property=&quot;username&quot; column=&quot;author_username&quot;/&gt;    &lt;result property=&quot;password&quot; column=&quot;author_password&quot;/&gt;    &lt;result property=&quot;email&quot; column=&quot;author_email&quot;/&gt;    &lt;result property=&quot;bio&quot; column=&quot;author_bio&quot;/&gt;    &lt;result property=&quot;favouriteSection&quot; column=&quot;author_favourite_section&quot;/&gt;  &lt;/association&gt;  &lt;collection property=&quot;posts&quot; ofType=&quot;Post&quot;&gt;    &lt;id property=&quot;id&quot; column=&quot;post_id&quot;/&gt;    &lt;result property=&quot;subject&quot; column=&quot;post_subject&quot;/&gt;    &lt;association property=&quot;author&quot; javaType=&quot;Author&quot;/&gt;    &lt;collection property=&quot;comments&quot; ofType=&quot;Comment&quot;&gt;      &lt;id property=&quot;id&quot; column=&quot;comment_id&quot;/&gt;    &lt;/collection&gt;    &lt;collection property=&quot;tags&quot; ofType=&quot;Tag&quot; &gt;      &lt;id property=&quot;id&quot; column=&quot;tag_id&quot;/&gt;    &lt;/collection&gt;    &lt;discriminator javaType=&quot;int&quot; column=&quot;draft&quot;&gt;      &lt;case value=&quot;1&quot; resultType=&quot;DraftPost&quot;/&gt;    &lt;/discriminator&gt;  &lt;/collection&gt;&lt;/resultMap&gt;</code></pre><p><code>resultMap</code> 元素有很多子元素和一个值得深入探讨的结构。 下面是<code>resultMap</code> 元素的概念视图。</p><h4 id="结果映射（resultMap）">结果映射（resultMap）</h4><ul><li><p><code>constructor</code>- 用于在实例化类时，注入结果到构造方法中</p><ul><li><code>idArg</code> - ID 参数；标记出作为 ID 的结果可以帮助提高整体性能</li><li><code>arg</code> - 将被注入到构造方法的一个普通结果</li></ul></li><li><p><code>id</code> – 一个 ID 结果；标记出作为 ID 的结果可以帮助提高整体性能</p></li><li><p><code>result</code> – 注入到字段或 JavaBean 属性的普通结果</p></li><li><p><code>association</code>– 一个复杂类型的关联；许多结果将包装成这种类型</p><ul><li>嵌套结果映射 – 关联可以是 <code>resultMap</code> 元素，或是对其它结果映射的引用</li></ul></li><li><p><code>collection</code>– 一个复杂类型的集合</p><ul><li>嵌套结果映射 – 集合可以是 <code>resultMap</code> 元素，或是对其它结果映射的引用</li></ul></li><li><p><code>discriminator</code>– 使用结果值来决定使用哪个<code>resultMap</code></p><ul><li><code>case</code>– 基于某些值的结果映射<ul><li>嵌套结果映射 – <code>case</code> 也是一个结果映射，因此具有相同的结构和元素；或者引用其它的结果映射</li></ul></li></ul></li></ul><p><strong>ResultMap 的属性列表</strong></p><table><thead><tr><th style="text-align:left">属性</th><th style="text-align:left">描述</th></tr></thead><tbody><tr><td style="text-align:left"><code>id</code></td><td style="text-align:left">当前命名空间中的一个唯一标识，用于标识一个结果映射。</td></tr><tr><td style="text-align:left"><code>type</code></td><td style="text-align:left">类的完全限定名, 或者一个类型别名（关于内置的类型别名，可以参考上面的表格）。</td></tr><tr><td style="text-align:left"><code>autoMapping</code></td><td style="text-align:left">如果设置这个属性，MyBatis 将会为本结果映射开启或者关闭自动映射。 这个属性会覆盖全局的属性 autoMappingBehavior。默认值：未设置（unset）。</td></tr></tbody></table><blockquote><p>最好逐步建立结果映射。单元测试可以在这个过程中起到很大帮助。 如果你尝试一次性创建像上面示例那么巨大的结果映射，不仅容易出错，难度也会直线上升。 所以，从最简单的形态开始，逐步迭代。而且别忘了单元测试！ 有时候，框架的行为像是一个黑盒子（无论是否开源）。因此，为了确保实现的行为与你的期望相一致，最好编写单元测试。 并且单元测试在提交 bug 时也能起到很大的作用。</p></blockquote><h3 id="下一部分将详细说明每个元素。"><strong>下一部分将详细说明每个元素。</strong></h3><h4 id="id-result">id &amp; result</h4><pre><code class="language-xml">&lt;id property=&quot;id&quot; column=&quot;post_id&quot;/&gt;&lt;result property=&quot;subject&quot; column=&quot;post_subject&quot;/&gt;</code></pre><p>这些元素是结果映射的基础。<em>id</em> 和 <em>result</em> 元素都将一个列的值映射到一个简单数据类型（String, int, double, Date 等）的属性或字段。</p><p>这两者之间的唯一不同是，<em>id</em> 元素对应的属性会被标记为对象的标识符，在比较对象实例时使用。 这样可以提高整体的性能，尤其是进行缓存和嵌套结果映射（也就是连接映射）的时候。</p><p>两个元素都有一些属性：</p><table><thead><tr><th style="text-align:left"><strong>属性</strong></th><th style="text-align:left"><strong>描述</strong></th></tr></thead><tbody><tr><td style="text-align:left"><strong><code>property</code></strong></td><td style="text-align:left"><strong>映射到列结果的字段或属性。如果 JavaBean 有这个名字的属性（property），会先使用该属性。否则 MyBatis 将会寻找给定名称的字段（field）。 无论是哪一种情形，你都可以使用常见的点式分隔形式进行复杂属性导航。 比如，你可以这样映射一些简单的东西：“username”，或者映射到一些复杂的东西上：“address.street.number”。</strong></td></tr><tr><td style="text-align:left"><strong><code>column</code></strong></td><td style="text-align:left"><strong>数据库中的列名，或者是列的别名。一般情况下，这和传递给 <code>resultSet.getString(columnName)</code> 方法的参数一样。</strong></td></tr><tr><td style="text-align:left"><code>*javaType</code>*</td><td style="text-align:left"><em>一个 Java 类的全限定名，或一个类型别名（关于内置的类型别名，可以参考上面的表格）。 如果你映射到一个 JavaBean，MyBatis 通常可以推断类型。然而，如果你映射到的是 HashMap，那么你应该明确地指定 javaType 来保证行为与期望的相一致。</em></td></tr><tr><td style="text-align:left"><em><code>jdbcType</code></em></td><td style="text-align:left"><em>JDBC 类型</em></td></tr><tr><td style="text-align:left"><em><code>typeHandler</code></em></td><td style="text-align:left"><em>我们在前面讨论过默认的类型处理器。使用这个属性，你可以覆盖默认的类型处理器。 这个属性值是一个类型处理器实现类的全限定名，或者是类型别名。</em></td></tr></tbody></table><h4 id="构造方法">构造方法</h4><p>构造方法注入允许你在初始化时为类设置属性的值，而不用暴露出公有方法。MyBatis 也支持私有属性和私有 JavaBean 属性来完成注入，但有一些人更青睐于通过构造方法进行注入。 <em>constructor</em> 元素就是为此而生的。</p><p>看看下面这个构造方法:</p><pre><code class="language-java">public class User &#123;   //...   public User(Integer id, String username, int age) &#123;     //...  &#125;//...&#125;</code></pre><p>为了将结果注入构造方法，MyBatis 需要通过某种方式定位相应的构造方法。 在下面的例子中，MyBatis 搜索一个声明了三个形参的构造方法，参数类型以 <code>java.lang.Integer</code>, <code>java.lang.String</code> 和 <code>int</code> 的顺序给出。</p><pre><code class="language-xml">&lt;constructor&gt;   &lt;idArg column=&quot;id&quot; javaType=&quot;int&quot;/&gt;   &lt;arg column=&quot;username&quot; javaType=&quot;String&quot;/&gt;   &lt;arg column=&quot;age&quot; javaType=&quot;_int&quot;/&gt;&lt;/constructor&gt;</code></pre><p>当你在处理一个带有多个形参的构造方法时，很容易搞乱 arg 元素的顺序。 从版本 3.4.3 开始，可以在指定参数名称的前提下，以任意顺序编写 arg 元素。 为了通过名称来引用构造方法参数，你可以添加 <code>@Param</code> 注解。</p><p>下面是一个等价的例子，尽管函数签名中第二和第三个形参的顺序与 constructor 元素中参数声明的顺序不匹配。</p><pre><code class="language-xml">&lt;constructor&gt;   &lt;idArg column=&quot;id&quot; javaType=&quot;int&quot; name=&quot;id&quot; /&gt;   &lt;arg column=&quot;age&quot; javaType=&quot;_int&quot; name=&quot;age&quot; /&gt;   &lt;arg column=&quot;username&quot; javaType=&quot;String&quot; name=&quot;username&quot; /&gt;&lt;/constructor&gt;</code></pre><p>如果存在名称和类型相同的可写属性，那么可以省略 <code>javaType</code> 。</p><p>剩余的属性和规则和普通的 id 和 result 元素是一样的。</p><h4 id="关联">关联</h4><pre><code class="language-xml">&lt;association property=&quot;author&quot; column=&quot;blog_author_id&quot; javaType=&quot;Author&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;author_id&quot;/&gt;  &lt;result property=&quot;username&quot; column=&quot;author_username&quot;/&gt;&lt;/association&gt;</code></pre><p>关联（association）元素处理“有一个”类型的关系。 比如，在我们的示例中，一个博客有一个用户。关联结果映射和其它类型的映射工作方式差不多。 你需要指定目标属性名以及属性的<code>javaType</code>（很多时候 MyBatis 可以自己推断出来）。</p><p>关联的不同之处是，你需要告诉 MyBatis 如何加载关联。MyBatis 有两种不同的方式加载关联：</p><ul><li>嵌套 Select 查询：通过执行另外一个 SQL 映射语句来加载期望的复杂类型。</li><li>嵌套结果映射：使用嵌套的结果映射来处理连接结果的重复子集。</li></ul><p>首先，先让我们来看看这个元素的属性。你将会发现，和普通的结果映射相比，它只在 select 和 resultMap 属性上有所不同。</p><table><thead><tr><th style="text-align:left">属性</th><th style="text-align:left">描述</th></tr></thead><tbody><tr><td style="text-align:left"><code>column</code></td><td style="text-align:left">数据库中的列名，或者是列的别名。一般情况下，这和传递给 <code>resultSet.getString(columnName)</code> 方法的参数一样。</td></tr><tr><td style="text-align:left"><code>javaType</code></td><td style="text-align:left">一个 Java 类的完全限定名，或一个类型别名（关于内置的类型别名，可以参考上面的表格）。 如果你映射到一个 JavaBean，MyBatis 通常可以推断类型。然而，如果你映射到的是 HashMap，那么你应该明确地指定 javaType 来保证行为与期望的相一致。</td></tr><tr><td style="text-align:left"><code>jdbcType</code></td><td style="text-align:left">JDBC 类型</td></tr><tr><td style="text-align:left"><code>typeHandler</code></td><td style="text-align:left"><em>我们在前面讨论过默认的类型处理器。使用这个属性，你可以覆盖默认的类型处理器。 这个属性值是一个类型处理器实现类的完全限定名，或者是类型别名。</em></td></tr><tr><td style="text-align:left"><code>select</code></td><td style="text-align:left">用于加载复杂类型属性的映射语句的 ID，它会从 column 属性中指定的列检索数据，作为参数传递给此 select 语句。具体请参考关联元素。</td></tr><tr><td style="text-align:left"><code>resultMap</code></td><td style="text-align:left">结果映射的 ID，可以将嵌套的结果集映射到一个合适的对象树中。 它可以作为使用额外 select 语句的替代方案。它可以将多表连接操作的结果映射成一个单一的 <code>ResultSet</code>。这样的 <code>ResultSet</code> 将会将包含重复或部分数据重复的结果集。为了将结果集正确地映射到嵌套的对象树中，MyBatis 允许你 “串联”结果映射，以便解决嵌套结果集的问题。想了解更多内容，请参考下面的关联元素。</td></tr><tr><td style="text-align:left"><code>name</code></td><td style="text-align:left">构造方法形参的名字。从 3.4.3 版本开始，通过指定具体的参数名，你可以以任意顺序写入 arg 元素。参看上面的解释。</td></tr></tbody></table><h4 id="关联的嵌套-Select-查询">关联的嵌套 Select 查询</h4><table><thead><tr><th style="text-align:left">属性</th><th style="text-align:left">描述</th></tr></thead><tbody><tr><td style="text-align:left"><code>column</code></td><td style="text-align:left">数据库中的列名，或者是列的别名。一般情况下，这和传递给 <code>resultSet.getString(columnName)</code> 方法的参数一样。 注意：在使用复合主键的时候，你可以使用 <code>column=&quot;&#123;prop1=col1,prop2=col2&#125;&quot;</code> 这样的语法来指定多个传递给嵌套 Select 查询语句的列名。这会使得 <code>prop1</code> 和 <code>prop2</code> 作为参数对象，被设置为对应嵌套 Select 语句的参数。</td></tr><tr><td style="text-align:left"><code>select</code></td><td style="text-align:left">用于加载复杂类型属性的映射语句的 ID，它会从 column 属性指定的列中检索数据，作为参数传递给目标 select 语句。 具体请参考下面的例子。注意：在使用复合主键的时候，你可以使用 <code>column=&quot;&#123;prop1=col1,prop2=col2&#125;&quot;</code> 这样的语法来指定多个传递给嵌套 Select 查询语句的列名。这会使得 <code>prop1</code> 和 <code>prop2</code> 作为参数对象，被设置为对应嵌套 Select 语句的参数。</td></tr><tr><td style="text-align:left"><code>fetchType</code></td><td style="text-align:left">可选的。有效值为 <code>lazy</code> 和 <code>eager</code>。 指定属性后，将在映射中忽略全局配置参数 <code>lazyLoadingEnabled</code>，使用属性的值。</td></tr></tbody></table><p>示例：</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;association property=&quot;author&quot; column=&quot;author_id&quot; javaType=&quot;Author&quot; select=&quot;selectAuthor&quot;/&gt;&lt;/resultMap&gt;&lt;select id=&quot;selectBlog&quot; resultMap=&quot;blogResult&quot;&gt;  SELECT * FROM BLOG WHERE ID = #&#123;id&#125;&lt;/select&gt;&lt;select id=&quot;selectAuthor&quot; resultType=&quot;Author&quot;&gt;  SELECT * FROM AUTHOR WHERE ID = #&#123;id&#125;&lt;/select&gt;</code></pre><p>就是这么简单。我们有两个 select 查询语句：一个用来加载博客（Blog），另外一个用来加载作者（Author），而且博客的结果映射描述了应该使用 <code>selectAuthor</code> 语句加载它的 author 属性。</p><p>其它所有的属性将会被自动加载，只要它们的列名和属性名相匹配。</p><p><strong>这种方式虽然很简单，但在大型数据集或大型数据表上表现不佳</strong>。这个问题被称为“<strong>N+1 查询问题</strong>”。 概括地讲，N+1 查询问题是这样子的：</p><ul><li>你执行了一个单独的 SQL 语句来获取结果的一个列表（就是“+1”）。</li><li>对列表返回的每条记录，你执行一个 select 查询语句来为每条记录加载详细信息（就是“N”）。</li></ul><p>这个问题会导致成百上千的 SQL 语句被执行。有时候，我们不希望产生这样的后果。</p><p>好消息是，MyBatis 能够对这样的查询进行延迟加载，因此可以将大量语句同时运行的开销分散开来。 然而，如果你加载记录列表之后立刻就遍历列表以获取嵌套的数据，就会触发所有的延迟加载查询，性能可能会变得很糟糕。</p><p>所以还有另外一种方法。</p><h4 id="关联的嵌套结果映射">关联的嵌套结果映射</h4><table><thead><tr><th style="text-align:left">属性</th><th style="text-align:left">描述</th></tr></thead><tbody><tr><td style="text-align:left"><code>resultMap</code></td><td style="text-align:left">结果映射的 ID，可以将此关联的嵌套结果集映射到一个合适的对象树中。 它可以作为使用额外 select 语句的替代方案。它可以将多表连接操作的结果映射成一个单一的 <code>ResultSet</code>。这样的 <code>ResultSet</code> 有部分数据是重复的。 为了将结果集正确地映射到嵌套的对象树中, MyBatis 允许你“串联”结果映射，以便解决嵌套结果集的问题。使用嵌套结果映射的一个例子在表格以后。</td></tr><tr><td style="text-align:left"><code>columnPrefix</code></td><td style="text-align:left">当连接多个表时，你可能会不得不使用列别名来避免在 <code>ResultSet</code> 中产生重复的列名。指定 columnPrefix 列名前缀允许你将带有这些前缀的列映射到一个外部的结果映射中。 详细说明请参考后面的例子。</td></tr><tr><td style="text-align:left"><code>notNullColumn</code></td><td style="text-align:left">默认情况下，在至少一个被映射到属性的列不为空时，子对象才会被创建。 你可以在这个属性上指定非空的列来改变默认行为，指定后，Mybatis 将只在这些列中任意一列非空时才创建一个子对象。可以使用逗号分隔来指定多个列。默认值：未设置（unset）。</td></tr><tr><td style="text-align:left"><code>autoMapping</code></td><td style="text-align:left">如果设置这个属性，MyBatis 将会为本结果映射开启或者关闭自动映射。 这个属性会覆盖全局的属性 autoMappingBehavior。注意，本属性对外部的结果映射无效，所以不能搭配 <code>select</code> 或 <code>resultMap</code> 元素使用。默认值：未设置（unset）。</td></tr></tbody></table><p>之前，你已经看到了一个非常复杂的嵌套关联的例子。 下面的例子则是一个非常简单的例子，用于演示嵌套结果映射如何工作。 现在我们将博客表和作者表连接在一起，而不是执行一个独立的查询语句，就像这样：</p><pre><code class="language-sql">&lt;select id=&quot;selectBlog&quot; resultMap=&quot;blogResult&quot;&gt;  select    B.id            as blog_id,    B.title         as blog_title,    B.author_id     as blog_author_id,    A.id            as author_id,    A.username      as author_username,    A.password      as author_password,    A.email         as author_email,    A.bio           as author_bio  from Blog B left outer join Author A on B.author_id = A.id  where B.id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>注意查询中的连接，以及为确保结果能够拥有唯一且清晰的名字，我们设置的别名。 这使得进行映射非常简单。现在我们可以映射这个结果：</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;blog_id&quot; /&gt;  &lt;result property=&quot;title&quot; column=&quot;blog_title&quot;/&gt;  &lt;association property=&quot;author&quot; column=&quot;blog_author_id&quot; javaType=&quot;Author&quot; resultMap=&quot;authorResult&quot;/&gt;&lt;/resultMap&gt;&lt;resultMap id=&quot;authorResult&quot; type=&quot;Author&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;author_id&quot;/&gt;  &lt;result property=&quot;username&quot; column=&quot;author_username&quot;/&gt;  &lt;result property=&quot;password&quot; column=&quot;author_password&quot;/&gt;  &lt;result property=&quot;email&quot; column=&quot;author_email&quot;/&gt;  &lt;result property=&quot;bio&quot; column=&quot;author_bio&quot;/&gt;&lt;/resultMap&gt;</code></pre><p>在上面的例子中，你可以看到，博客（Blog）作者（author）的关联元素委托名为 “authorResult” 的结果映射来加载作者对象的实例。</p><p><strong>非常重要： id 元素在嵌套结果映射中扮演着非常重要的角色。你应该总是指定一个或多个可以唯一标识结果的属性。</strong> 虽然，即使不指定这个属性，MyBatis 仍然可以工作，但是会产生严重的性能问题。 只需要指定可以唯一标识结果的最少属性。显然，你可以选择主键（复合主键也可以）。</p><p>现在，上面的示例使用了外部的结果映射元素来映射关联。这使得 Author 的结果映射可以被重用。 然而，如果你不打算重用它，或者你更喜欢将你所有的结果映射放在一个具有描述性的结果映射元素中。 你可以直接将结果映射作为子元素嵌套在内。这里给出使用这种方式的等效例子：</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;blog_id&quot; /&gt;  &lt;result property=&quot;title&quot; column=&quot;blog_title&quot;/&gt;  &lt;association property=&quot;author&quot; javaType=&quot;Author&quot;&gt;    &lt;id property=&quot;id&quot; column=&quot;author_id&quot;/&gt;    &lt;result property=&quot;username&quot; column=&quot;author_username&quot;/&gt;    &lt;result property=&quot;password&quot; column=&quot;author_password&quot;/&gt;    &lt;result property=&quot;email&quot; column=&quot;author_email&quot;/&gt;    &lt;result property=&quot;bio&quot; column=&quot;author_bio&quot;/&gt;  &lt;/association&gt;&lt;/resultMap&gt;</code></pre><p>那如果博客（blog）有一个共同作者（co-author）该怎么办？select 语句看起来会是这样的：</p><pre><code class="language-sql">&lt;select id=&quot;selectBlog&quot; resultMap=&quot;blogResult&quot;&gt;  select    B.id            as blog_id,    B.title         as blog_title,    A.id            as author_id,    A.username      as author_username,    A.password      as author_password,    A.email         as author_email,    A.bio           as author_bio,    CA.id           as co_author_id,    CA.username     as co_author_username,    CA.password     as co_author_password,    CA.email        as co_author_email,    CA.bio          as co_author_bio  from Blog B  left outer join Author A on B.author_id = A.id  left outer join Author CA on B.co_author_id = CA.id  where B.id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>回忆一下，Author 的结果映射定义如下：</p><pre><code class="language-xml">&lt;resultMap id=&quot;authorResult&quot; type=&quot;Author&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;author_id&quot;/&gt;  &lt;result property=&quot;username&quot; column=&quot;author_username&quot;/&gt;  &lt;result property=&quot;password&quot; column=&quot;author_password&quot;/&gt;  &lt;result property=&quot;email&quot; column=&quot;author_email&quot;/&gt;  &lt;result property=&quot;bio&quot; column=&quot;author_bio&quot;/&gt;&lt;/resultMap&gt;</code></pre><p>由于结果中的列名与结果映射中的列名不同。你需要指定 <code>columnPrefix</code> 以便重复使用该结果映射来映射 co-author 的结果。</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;blog_id&quot; /&gt;  &lt;result property=&quot;title&quot; column=&quot;blog_title&quot;/&gt;  &lt;association property=&quot;author&quot;    resultMap=&quot;authorResult&quot; /&gt;  &lt;association property=&quot;coAuthor&quot;    resultMap=&quot;authorResult&quot;    columnPrefix=&quot;co_&quot; /&gt;&lt;/resultMap&gt;</code></pre><h4 id="关联的多结果集（ResultSet）">关联的多结果集（ResultSet）</h4><table><thead><tr><th style="text-align:left">属性</th><th style="text-align:left">描述</th></tr></thead><tbody><tr><td style="text-align:left"><code>column</code></td><td style="text-align:left">当使用多个结果集时，该属性指定结果集中用于与 <code>foreignColumn</code> 匹配的列（多个列名以逗号隔开），以识别关系中的父类型与子类型。</td></tr><tr><td style="text-align:left"><code>foreignColumn</code></td><td style="text-align:left">指定外键对应的列名，指定的列将与父类型中 <code>column</code> 的给出的列进行匹配。</td></tr><tr><td style="text-align:left"><code>resultSet</code></td><td style="text-align:left">指定用于加载复杂类型的结果集名字。</td></tr></tbody></table><p>从版本 3.2.3 开始，MyBatis 提供了另一种解决 N+1 查询问题的方法。</p><p>某些数据库允许存储过程返回多个结果集，或一次性执行多个语句，每个语句返回一个结果集。 我们可以利用这个特性，在不使用连接的情况下，只访问数据库一次就能获得相关数据。</p><p>在例子中，存储过程执行下面的查询并返回两个结果集。第一个结果集会返回博客（Blog）的结果，第二个则返回作者（Author）的结果。</p><pre><code class="language-sql">SELECT * FROM BLOG WHERE ID = #&#123;id&#125;SELECT * FROM AUTHOR WHERE ID = #&#123;id&#125;</code></pre><p>在映射语句中，必须通过 <code>resultSets</code> 属性为每个结果集指定一个名字，多个名字使用逗号隔开。</p><pre><code class="language-xml">&lt;select id=&quot;selectBlog&quot; resultSets=&quot;blogs,authors&quot; resultMap=&quot;blogResult&quot; statementType=&quot;CALLABLE&quot;&gt;  &#123;call getBlogsAndAuthors(#&#123;id,jdbcType=INTEGER,mode=IN&#125;)&#125;&lt;/select&gt;</code></pre><p>现在我们可以指定使用 “authors” 结果集的数据来填充 “author” 关联：</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;id&quot; /&gt;  &lt;result property=&quot;title&quot; column=&quot;title&quot;/&gt;  &lt;association property=&quot;author&quot; javaType=&quot;Author&quot; resultSet=&quot;authors&quot; column=&quot;author_id&quot; foreignColumn=&quot;id&quot;&gt;    &lt;id property=&quot;id&quot; column=&quot;id&quot;/&gt;    &lt;result property=&quot;username&quot; column=&quot;username&quot;/&gt;    &lt;result property=&quot;password&quot; column=&quot;password&quot;/&gt;    &lt;result property=&quot;email&quot; column=&quot;email&quot;/&gt;    &lt;result property=&quot;bio&quot; column=&quot;bio&quot;/&gt;  &lt;/association&gt;&lt;/resultMap&gt;</code></pre><p>你已经在上面看到了如何处理“有一个”类型的关联。但是该怎么处理“有很多个”类型的关联呢？这就是我们接下来要介绍的。</p><h2 id="一对多处理之集合">一对多处理之集合</h2><pre><code class="language-xml">&lt;collection property=&quot;posts&quot; ofType=&quot;domain.blog.Post&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;post_id&quot;/&gt;  &lt;result property=&quot;subject&quot; column=&quot;post_subject&quot;/&gt;  &lt;result property=&quot;body&quot; column=&quot;post_body&quot;/&gt;&lt;/collection&gt;</code></pre><p>集合元素和关联元素几乎是一样的，它们相似的程度之高，以致于没有必要再介绍集合元素的相似部分。 所以让我们来关注它们的不同之处吧。</p><p>我们来继续上面的示例，一个博客（Blog）只有一个作者（Author)。但一个博客有很多文章（Post)。 在博客类中，这可以用下面的写法来表示：</p><pre><code class="language-java">private List&lt;Post&gt; posts;</code></pre><p>要像上面这样，映射嵌套结果集合到一个 List 中，可以使用集合元素。 和关联元素一样，我们可以使用嵌套 Select 查询，或基于连接的嵌套结果映射集合。</p><h4 id="集合的嵌套-Select-查询">集合的嵌套 Select 查询</h4><p>首先，让我们看看如何使用嵌套 Select 查询来为博客加载文章。</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;collection property=&quot;posts&quot; javaType=&quot;ArrayList&quot; column=&quot;id&quot; ofType=&quot;Post&quot; select=&quot;selectPostsForBlog&quot;/&gt;&lt;/resultMap&gt;&lt;select id=&quot;selectBlog&quot; resultMap=&quot;blogResult&quot;&gt;  SELECT * FROM BLOG WHERE ID = #&#123;id&#125;&lt;/select&gt;&lt;select id=&quot;selectPostsForBlog&quot; resultType=&quot;Post&quot;&gt;  SELECT * FROM POST WHERE BLOG_ID = #&#123;id&#125;&lt;/select&gt;</code></pre><p>你可能会立刻注意到几个不同，但大部分都和我们上面学习过的关联元素非常相似。 首先，你会注意到我们使用的是集合元素。 接<strong>下来你会注意到有一个新的 “ofType” 属性。这个属性非常重要，它用来将 JavaBean（或字段）属性的类型和集合存储的类型区分开来。</strong> 所以你可以按照下面这样来阅读映射：</p><pre><code class="language-xnl">&lt;collection property=&quot;posts&quot; javaType=&quot;ArrayList&quot; column=&quot;id&quot; ofType=&quot;Post&quot; select=&quot;selectPostsForBlog&quot;/&gt;</code></pre><p>读作： “posts 是一个存储 Post 的 ArrayList 集合”</p><p>在一般情况下，MyBatis 可以推断 javaType 属性，因此并不需要填写。所以很多时候你可以简略成：</p><pre><code class="language-xml">&lt;collection property=&quot;posts&quot; column=&quot;id&quot; ofType=&quot;Post&quot; select=&quot;selectPostsForBlog&quot;/&gt;</code></pre><h4 id="集合的嵌套结果映射">集合的嵌套结果映射</h4><p>现在你可能已经猜到了集合的嵌套结果映射是怎样工作的——除了新增的 “ofType” 属性，它和关联的完全相同。</p><p>首先, 让我们看看对应的 SQL 语句：</p><pre><code class="language-sql">&lt;select id=&quot;selectBlog&quot; resultMap=&quot;blogResult&quot;&gt;  select  B.id as blog_id,  B.title as blog_title,  B.author_id as blog_author_id,  P.id as post_id,  P.subject as post_subject,  P.body as post_body,  from Blog B  left outer join Post P on B.id = P.blog_id  where B.id = #&#123;id&#125;&lt;/select&gt;</code></pre><p>我们再次连接了博客表和文章表，并且为每一列都赋予了一个有意义的别名，以便映射保持简单。 要映射博客里面的文章集合，就这么简单：</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;blog_id&quot; /&gt;  &lt;result property=&quot;title&quot; column=&quot;blog_title&quot;/&gt;  &lt;collection property=&quot;posts&quot; ofType=&quot;Post&quot;&gt;    &lt;id property=&quot;id&quot; column=&quot;post_id&quot;/&gt;    &lt;result property=&quot;subject&quot; column=&quot;post_subject&quot;/&gt;    &lt;result property=&quot;body&quot; column=&quot;post_body&quot;/&gt;  &lt;/collection&gt;&lt;/resultMap&gt;</code></pre><p>再提醒一次，要记得上面 id 元素的重要性，如果你不记得了，请阅读关联部分的相关部分。</p><p>如果你喜欢更详略的、可重用的结果映射，你可以使用下面的等价形式：</p><pre><code class="language-xml">&lt;resultMap id=&quot;blogResult&quot; type=&quot;Blog&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;blog_id&quot; /&gt;  &lt;result property=&quot;title&quot; column=&quot;blog_title&quot;/&gt;  &lt;collection property=&quot;posts&quot; ofType=&quot;Post&quot; resultMap=&quot;blogPostResult&quot; columnPrefix=&quot;post_&quot;/&gt;&lt;/resultMap&gt;&lt;resultMap id=&quot;blogPostResult&quot; type=&quot;Post&quot;&gt;  &lt;id property=&quot;id&quot; column=&quot;id&quot;/&gt;  &lt;result property=&quot;subject&quot; column=&quot;subject&quot;/&gt;  &lt;result property=&quot;body&quot; column=&quot;body&quot;/&gt;&lt;/resultMap&gt;</code></pre><h2 id="缓存">缓存</h2><p>MyBatis 内置了一个强大的事务性查询缓存机制，它可以非常方便地配置和定制。 为了使它更加强大而且易于配置，我们对 MyBatis 3 中的缓存实现进行了许多改进。</p><p>默认情况下，只启用了本地的会话缓存，它仅仅对一个会话中的数据进行缓存。 要启用全局的二级缓存，只需要在你的 SQL 映射文件中添加一行：</p><pre><code class="language-xml">&lt;cache/&gt;</code></pre><p>基本上就是这样。这个简单语句的效果如下:</p><ul><li>映射语句文件中的所有 select 语句的结果将会被缓存。</li><li>映射语句文件中的所有 insert、update 和 delete 语句会刷新缓存。</li><li>缓存会使用最近最少使用算法（LRU, Least Recently Used）算法来清除不需要的缓存。</li><li>缓存不会定时进行刷新（也就是说，没有刷新间隔）。</li><li>缓存会保存列表或对象（无论查询方法返回哪种）的 1024 个引用。</li><li>缓存会被视为读/写缓存，这意味着获取到的对象并不是共享的，可以安全地被调用者修改，而不干扰其他调用者或线程所做的潜在修改。</li></ul><p><strong>提示</strong> 缓存只作用于 cache 标签所在的映射文件中的语句。如果你混合使用 Java API 和 XML 映射文件，在共用接口中的语句将不会被默认缓存。你需要使用 @CacheNamespaceRef 注解指定缓存作用域。</p><p>这些属性可以通过 cache 元素的属性来修改。比如：</p><pre><code class="language-xml">&lt;cache  eviction=&quot;FIFO&quot;  flushInterval=&quot;60000&quot;  size=&quot;512&quot;  readOnly=&quot;true&quot;/&gt;</code></pre><p>这个更高级的配置创建了一个 FIFO 缓存，每隔 60 秒刷新，最多可以存储结果对象或列表的 512 个引用，而且返回的对象被认为是只读的，因此对它们进行修改可能会在不同线程中的调用者产生冲突。</p><p>可用的清除策略有：</p><ul><li><code>LRU</code> – 最近最少使用：移除最长时间不被使用的对象。</li><li><code>FIFO</code> – 先进先出：按对象进入缓存的顺序来移除它们。</li><li><code>SOFT</code> – 软引用：基于垃圾回收器状态和软引用规则移除对象。</li><li><code>WEAK</code> – 弱引用：更积极地基于垃圾收集器状态和弱引用规则移除对象。</li></ul><p>默认的清除策略是 LRU。</p><p>flushInterval（刷新间隔）属性可以被设置为任意的正整数，设置的值应该是一个以毫秒为单位的合理时间量。 默认情况是不设置，也就是没有刷新间隔，缓存仅仅会在调用语句时刷新。</p><p>size（引用数目）属性可以被设置为任意正整数，要注意欲缓存对象的大小和运行环境中可用的内存资源。默认值是 1024。</p><p>readOnly（只读）属性可以被设置为 true 或 false。只读的缓存会给所有调用者返回缓存对象的相同实例。 因此这些对象不能被修改。这就提供了可观的性能提升。而可读写的缓存会（通过序列化）返回缓存对象的拷贝。 速度上会慢一些，但是更安全，因此默认值是 false。</p><p><strong>提示</strong> 二级缓存是事务性的。这意味着，当 SqlSession 完成并提交时，或是完成并回滚，但没有执行 flushCache=true 的 insert/delete/update 语句时，缓存会获得更新。</p><h4 id="使用自定义缓存">使用自定义缓存</h4><p>除了上述自定义缓存的方式，你也可以通过实现你自己的缓存，或为其他第三方缓存方案创建适配器，来完全覆盖缓存行为。</p><pre><code class="language-xml">&lt;cache type=&quot;com.domain.something.MyCustomCache&quot;/&gt;</code></pre><p>这个示例展示了如何使用一个自定义的缓存实现。type 属性指定的类必须实现 org.apache.ibatis.cache.Cache 接口，且提供一个接受 String 参数作为 id 的构造器。 这个接口是 MyBatis 框架中许多复杂的接口之一，但是行为却非常简单。</p><pre><code class="language-java">public interface Cache &#123;  String getId();  int getSize();  void putObject(Object key, Object value);  Object getObject(Object key);  boolean hasKey(Object key);  Object removeObject(Object key);  void clear();&#125;</code></pre><p>为了对你的缓存进行配置，只需要简单地在你的缓存实现中添加公有的 JavaBean 属性，然后通过 cache 元素传递属性值，例如，下面的例子将在你的缓存实现上调用一个名为 <code>setCacheFile(String file)</code> 的方法：</p><pre><code class="language-xml">&lt;cache type=&quot;com.domain.something.MyCustomCache&quot;&gt;  &lt;property name=&quot;cacheFile&quot; value=&quot;/tmp/my-custom-cache.tmp&quot;/&gt;&lt;/cache&gt;</code></pre><p>你可以使用所有简单类型作为 JavaBean 属性的类型，MyBatis 会进行转换。 你也可以使用占位符（如 <code>$&#123;cache.file&#125;</code>），以便替换成在<a href="https://mybatis.org/mybatis-3/zh/configuration.html#properties">配置文件属性</a>中定义的值。</p><p>从版本 3.4.2 开始，MyBatis 已经支持在所有属性设置完毕之后，调用一个初始化方法。 如果想要使用这个特性，请在你的自定义缓存类里实现 <code>org.apache.ibatis.builder.InitializingObject</code> 接口。</p><pre><code class="language-java">public interface InitializingObject &#123;  void initialize() throws Exception;&#125;</code></pre><p><strong>提示</strong> 上一节中对缓存的配置（如清除策略、可读或可读写等），不能应用于自定义缓存。</p><p>请注意，缓存的配置和缓存实例会被绑定到 SQL 映射文件的命名空间中。 因此，同一命名空间中的所有语句和缓存将通过命名空间绑定在一起。 每条语句可以自定义与缓存交互的方式，或将它们完全排除于缓存之外，这可以通过在每条语句上使用两个简单属性来达成。 默认情况下，语句会这样来配置：</p><pre><code class="language-xml">&lt;select ... flushCache=&quot;false&quot; useCache=&quot;true&quot;/&gt;&lt;insert ... flushCache=&quot;true&quot;/&gt;&lt;update ... flushCache=&quot;true&quot;/&gt;&lt;delete ... flushCache=&quot;true&quot;/&gt;</code></pre><p>鉴于这是默认行为，显然你永远不应该以这样的方式显式配置一条语句。但如果你想改变默认的行为，只需要设置 flushCache 和 useCache 属性。比如，某些情况下你可能希望特定 select 语句的结果排除于缓存之外，或希望一条 select 语句清空缓存。类似地，你可能希望某些 update 语句执行时不要刷新缓存。</p><h4 id="cache-ref">cache-ref</h4><p>回想一下上一节的内容，对某一命名空间的语句，只会使用该命名空间的缓存进行缓存或刷新。 但你可能会想要在多个命名空间中共享相同的缓存配置和实例。要实现这种需求，你可以使用 cache-ref 元素来引用另一个缓存。</p><pre><code class="language-xml">&lt;cache-ref namespace=&quot;com.someone.application.data.SomeMapper&quot;/&gt;</code></pre>]]></content>
    
    
    <summary type="html">MyBatis学习笔记</summary>
    
    
    
    <category term="java web" scheme="https://trevoewu.github.io/categories/java-web/"/>
    
    
    <category term="MyBatis" scheme="https://trevoewu.github.io/tags/MyBatis/"/>
    
    <category term="Java Web" scheme="https://trevoewu.github.io/tags/Java-Web/"/>
    
  </entry>
  
  <entry>
    <title>超市管理系统</title>
    <link href="https://trevoewu.github.io/2023/05/15/%E8%B6%85%E5%B8%82%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/"/>
    <id>https://trevoewu.github.io/2023/05/15/%E8%B6%85%E5%B8%82%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/</id>
    <published>2023-05-15T13:15:33.000Z</published>
    <updated>2023-05-19T13:42:46.033Z</updated>
    
    <content type="html"><![CDATA[<h2 id="实战-超市订单管理系统">实战: 超市订单管理系统</h2><p>为了方便表述和理解, 在编写MVC三层对应的代码时, 使用自顶向下, 但在实际的项目中, 编写代码工作往往是自底向上的.</p><h3 id="搭建项目框架">搭建项目框架</h3><p><img src="%E8%B6%85%E5%B8%82%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/%E6%9C%AA%E5%91%BD%E5%90%8D%E6%96%87%E4%BB%B6.png" alt="未命名文件" loading="lazy"></p><h4 id="编写数据库操作的基础公共类DB">编写数据库操作的基础公共类DB</h4><ul><li><p>数据库配置文件，这是一个资源文件，应该创建在maven项目的resources文件中</p><pre><code class="language-properties">DRIVER=com.mysql.jdbc.DriverURL=jdbc:mysql://localhost:3307/smbms?useUnicode=true&amp;characterEncoding=utf-8USERNAME=rootPASSWORD=</code></pre></li><li><p>使用静态代码块实现初始化参数</p><p>该类为数据库访问工具类，提供了获取数据库连接、执行查询和更新等常用操作的方法。</p><ol><li><p>类名：DB</p></li><li><p>属性：</p></li></ol><ul><li>DRIVER：String类型，表示数据库驱动名。</li><li>URL：String类型，表示数据库连接地址。</li><li>USERNAME：String类型，表示连接数据库的用户名。</li><li>PASSWORD：String类型，表示连接数据库的密码。</li></ul><ol start="3"><li>方法：</li></ol><ul><li>close：用于释放数据库连接、预处理语句和结果集资源。</li><li>getConnection：用于获取数据库连接。</li><li>executeQuery：用于执行查询操作，返回一个结果集。</li><li>executeUpdate：用于执行更新操作，返回受影响的行数。</li></ul><ol start="4"><li>方法的参数：</li></ol><ul><li>conn：Connection类型，表示数据库连接。</li><li>pstmt：PreparedStatement类型，表示预处理语句。</li><li>resSet：ResultSet类型，表示结果集。</li><li>query：String类型，表示要执行的SQL语句。</li><li>params：Object[]类型，表示SQL语句中的参数值。</li></ul><p>在类中还定义了一个静态代码块，该代码块在类加载时会被执行。静态代码块中的主要作用是读取配置文件中的数据库连接参数，并将其赋值给类的静态属性。具体实现如下：</p><ul><li>创建Properties对象。</li><li>通过类加载器加载配置文件为字节输入流。</li><li>使用Properties对象的load()方法加载字节输入流中的内容。</li><li>从Properties对象中获取数据库连接参数，并将其赋值给类的静态属性。</li></ul><p>通过这种方式，可以在不修改Java代码的情况下，更改数据库连接参数，提高了代码的灵活性和可维护性。</p><pre><code class="language-java">public class DB &#123;    private static String DRIVER;    private static String URL;    private static String USERNAME;    private static String PASSWORD;    //静态代码块， 类加载的时候执行    static &#123;        //读取配置文件        //1、创建properties对象        Properties properties = new Properties();        //2、通过类加载器加载资源文件为字节输入流        InputStream in = DB.class.getClassLoader().getResourceAsStream(&quot;db.properties&quot;);        try &#123;            properties.load(in);        &#125; catch (IOException e) &#123;            e.printStackTrace();        &#125;        DRIVER = properties.getProperty(&quot;DRIVER&quot;);        URL = properties.getProperty(&quot;URL&quot;);        USERNAME = properties.getProperty(&quot;USERNAME&quot;);        PASSWORD = properties.getProperty(&quot;PASSWORD&quot;);    &#125;&#125;</code></pre></li><li><p>编写数据库操作的公共方法</p><p>该类为数据库访问工具类，提供了获取数据库连接、执行查询和更新等常用操作的方法。</p><ol><li><p>类名：DB</p></li><li><p>属性：</p></li></ol><ul><li>DRIVER：String类型，表示数据库驱动名。</li><li>URL：String类型，表示数据库连接地址。</li><li>USERNAME：String类型，表示连接数据库的用户名。</li><li>PASSWORD：String类型，表示连接数据库的密码。</li></ul><ol start="3"><li>方法：</li></ol><ul><li>close：用于释放数据库连接、预处理语句和结果集资源。</li><li>getConnection：用于获取数据库连接。</li><li>executeQuery：用于执行查询操作，返回一个结果集。</li><li>executeUpdate：用于执行更新操作，返回受影响的行数。</li></ul><ol start="4"><li>方法的参数：</li></ol><ul><li>conn：Connection类型，表示数据库连接。</li><li>pstmt：PreparedStatement类型，表示预处理语句。</li><li>resSet：ResultSet类型，表示结果集。</li><li>query：String类型，表示要执行的SQL语句。</li><li>params：Object[]类型，表示SQL语句中的参数值。</li></ul><p>该类还包含一个静态代码块，用于初始化数据库连接参数，但代码块的内容未给出，需要进行补充。</p><p><em>同时需要注意，该类并没有实现单例模式，每次获取数据库连接都会重新创建一个新的连接，这可能会导致性能问题。</em></p></li></ul><pre><code class="language-java">package util;public class DB &#123;    private static String DRIVER;    private static String URL;    private static String USERNAME;    private static String PASSWORD;  /*      静态代码块， 类加载的时候执行  */    //释放链接资源    public static void close(Connection conn, PreparedStatement pstmt, ResultSet resSet) throws SQLException &#123;        if(resSet != null)&#123;            resSet.close();        &#125;        if(pstmt != null)&#123;            pstmt.close();        &#125;        if(conn != null) &#123;            conn.close();        &#125;    &#125;    //获取数据库链接    public static Connection getConnection() throws SQLException, ClassNotFoundException &#123;        Class.forName(DRIVER);        String url = URL;        String username = USERNAME;        String password = PASSWORD;        // 2.连接数据库,代表数据库        Connection connection = DriverManager.getConnection(url, username, password);        return  connection;    &#125;    //执行查询, 返回结果集    public static ResultSet executeQuery(Connection conn,String query,Object[] params) throws SQLException, ClassNotFoundException&#123;        ResultSet resultSet = null;        if(conn != null &amp;&amp; !query.isEmpty() &amp;&amp; params != null)&#123;            //预处理            PreparedStatement statement = conn.prepareStatement(query);            for (int i = 0; i &lt; params.length;i++)&#123;                //设置SQL 语句中的参数                if(params[i] instanceof String)&#123;                    statement.setString(i+1,params[i].toString());                &#125; else if(params[i] instanceof Integer)&#123;                    statement.setInt(i+1,Integer.parseInt(params[i].toString()));                &#125; else &#123;                    statement.setObject(i+1,params[i]);                &#125;            &#125;            //执行            resultSet = statement.executeQuery();        &#125;        return resultSet;    &#125;    //执行更新， 返回影响行数    public static int executeUpdate(Connection conn,String query,Object[] params) throws SQLException &#123;        int affectedRows = 0;        if(conn != null &amp;&amp; !query.isEmpty() &amp;&amp; params != null)&#123;            //预处理            PreparedStatement statement = conn.prepareStatement(query);            for (int i = 0; i &lt; params.length;i++)&#123;                //设置SQL 语句中的参数                if(params[i] instanceof String)&#123;                    statement.setString(i+1,params[i].toString());                &#125; else if(params[i] instanceof Integer)&#123;                    statement.setInt(i+1,Integer.parseInt(params[i].toString()));                &#125; else &#123;                    statement.setObject(i+1,params[i]);                &#125;            &#125;            //执行             affectedRows = statement.executeUpdate();        &#125;        return affectedRows;    &#125;&#125;</code></pre><h3 id="登录功能实现">登录功能实现</h3><h4 id="前端页面编写">前端页面编写</h4><p>前端页面由kuang提供</p><pre><code class="language-jsp">&lt;%-- header--%&gt;&lt;form class=&quot;loginForm&quot; action=&quot;$&#123;pageContext.request.contextPath&#125;/login.do&quot; name=&quot;actionForm&quot; id=&quot;actionForm&quot; method=&quot;post&quot;&gt;  &lt;div class=&quot;info&quot;&gt;$&#123;error&#125;&lt;/div&gt;  &lt;div class=&quot;inputbox&quot;&gt;      &lt;label&gt;用户名：&lt;/label&gt;      &lt;input type=&quot;text&quot; class=&quot;input-text&quot; id=&quot;userCode&quot; name=&quot;userCode&quot; placeholder=&quot;请输入用户名&quot; required/&gt;  &lt;/div&gt;  &lt;div class=&quot;inputbox&quot;&gt;      &lt;label&gt;密码：&lt;/label&gt;      &lt;input type=&quot;password&quot; id=&quot;userPassword&quot; name=&quot;userPassword&quot; placeholder=&quot;请输入密码&quot; required/&gt;  &lt;/div&gt;  &lt;div class=&quot;subBtn&quot;&gt;      &lt;input type=&quot;submit&quot; value=&quot;登录&quot;/&gt;      &lt;input type=&quot;reset&quot; value=&quot;重置&quot;/&gt;  &lt;/div&gt;&lt;/form&gt;&lt;%-- footer--%&gt;</code></pre><h4 id="登录处理Servlet实现">登录处理Servlet实现</h4><p>在前端form表单的action属性值我们可以看到提交的URL, 所以, 我们需要编写对应的Servlet来处理这个请求</p><pre><code class="language-jsp">action=&quot;$&#123;pageContext.request.contextPath&#125;/login.do&quot;</code></pre><ul><li><p>创建登录处理Servlet</p><p>以下是一个名为“Login”的Java servlet类，用于处理HTTP GET请求。该类包括对“UserService”实例的引用，该实例用于从数据库中检索用户信息。</p><p>在“doGet”方法中，servlet从HTTP请求中检索userCode和password参数。然后它创建一个“UserServiceImpl”类的新实例（该类实现了“UserService”接口），并调用其“findByUserCode”方法，以根据给定的用户代码从数据库中检索用户对象。</p><p>如果用户对象不为null，则servlet检查用户密码是否与给定密码匹配。如果匹配，则servlet使用用户对象设置名为“USER_SESSION”的会话属性，并将用户重定向到“/jsp/frame.jsp”。如果密码不匹配，则servlet使用错误消息设置名为“error”的会话属性，并将用户重定向回“/login.jsp”。</p><p><em>请注意，此代码片段不完整，因为它缺少有关导入，它所依赖的接口和类，以及servlet配置的信息。</em></p><pre><code class="language-java">public class Login extends HttpServlet &#123;    private UserService service;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        String userCode = req.getParameter(&quot;userCode&quot;);        String password = req.getParameter(&quot;userPassword&quot;);        service = new UserServiceImpl();        User user = null;        user = service.findByUserCode(userCode);        if(user != null) &#123;            if(user.getUserPassword().equals(password)) &#123;                req.getSession().setAttribute(Constant.USER_SESSION,user);                resp.sendRedirect(&quot;/jsp/frame.jsp&quot;);            &#125; else &#123;                req.getSession().setAttribute(&quot;error&quot;,&quot;username or password incorrect&quot;);                resp.sendRedirect(&quot;/login.jsp&quot;);            &#125;        &#125;    &#125;</code></pre></li><li><p>映射 servlet</p><p>这段代码是一个 servlet 配置的 XML 部分，它用于将请求的 URL 映射到特定的 servlet 上。这段配置指定了一个名为 “Login” 的 servlet，使用了完整的类名 <code>servlet.Login</code>，并将它映射到 URL “/login.do” 上。</p><p>这意味着，当用户在浏览器中访问 “/login.do” 时，容器将调用 <code>servlet.Login</code> 的 service() 方法来处理该请求。映射 servlet 的操作是 web 应用的一部分，它在 web.xml 文件中进行配置。</p><pre><code class="language-xml">&lt;servlet&gt;  &lt;servlet-name&gt;Login&lt;/servlet-name&gt;  &lt;servlet-class&gt;servlet.Login&lt;/servlet-class&gt;&lt;/servlet&gt;&lt;servlet-mapping&gt;  &lt;servlet-name&gt;Login&lt;/servlet-name&gt;  &lt;url-pattern&gt;/login.do&lt;/url-pattern&gt;&lt;/servlet-mapping&gt;</code></pre></li><li><p>编写Service层代码</p><p>该代码是一个 Java 类 <code>UserServiceImpl</code>，实现了一个接口 <code>UserService</code>。该类包含了两个方法：</p><ol><li><code>findByUserCode(String userCode)</code>：查找数据库中对应用户的信息，该方法返回一个 <code>User</code> 对象。</li><li><code>updateUser(Integer id, String key, String value)</code>：更新数据库中用户的信息，该方法返回一个 <code>boolean</code> 类型的值，表示更新是否成功。</li></ol><p>在这个类中，私有成员变量 <code>dao</code> 是一个 <code>UserDao</code> 类型的变量，通过该变量调用 <code>UserDaoImpl</code> 类中的方法来访问数据库。在 <code>findByUserCode</code> 和 <code>updateUser</code> 方法中，都需要先通过 <code>dao</code> 实例化一个 <code>UserDaoImpl</code> 对象来调用相应的方法。</p><pre><code class="language-java">public class UserServiceImpl implements UserService &#123;    private UserDao dao;    @Override    public User findByUserCode(String userCode)&#123;        dao = new UserDaoImpl();        User user = dao.findByUserCode(userCode);        return  user;    &#125;    @Override    public boolean updateUser(Integer id, String key, String value)  &#123;        dao = new UserDaoImpl();        int i = 0;        i = dao.updateUser(id, key, value);       return i &gt; 0 ? true : false;    &#125;&#125;</code></pre></li><li><p>DAO层代码</p><p>这段代码是一个Dao层的实现类，用于和数据库交互，提供了对User表的查询和更新方法。下面是代码的分析：</p><ul><li><code>findByUserCode(String userCode)</code> 方法：根据传入的用户名参数 <code>userCode</code> 查询对应的用户记录，并返回一个User对象。该方法使用了数据库连接池技术，在执行完查询后会关闭连接。方法中首先初始化参数，然后执行SQL语句查询结果集，遍历结果集将数据封装到User对象中，最后关闭连接和结果集，返回User对象。</li><li><code>updateUser(Integer id, String key,String value)</code> 方法：根据传入的参数更新User表中的数据。该方法也使用了数据库连接池技术，在执行完更新后会关闭连接。方法中首先判断传入的参数是否为空，然后执行SQL语句更新数据，最后关闭连接，返回更新结果的影响行数。</li></ul><pre><code class="language-java">public class UserDaoImpl implements UserDao&#123;    private User user;    private Connection conn;    private String sql;    @Override    public User findByUserCode(String userCode)&#123;        //2. 初始化参数        Object[] params = &#123;userCode&#125;;        ResultSet set;        sql = &quot;SELECT * FROM smbms_user Where userCode = ?&quot;;        try &#123;            conn = DB.getConnection();            set = DB.executeQuery(conn, sql,params);            if(set != null)&#123;                user = new User();                while (set.next()) &#123;                    user.setUserPassword(set.getString(&quot;userPassword&quot;));                    //一大串赋值代码...                    user.setWorkPicPath(&quot;workPicPath&quot;);                &#125;            &#125; else &#123;                return null;            &#125;            //关闭链接和结果集            DB.close(conn,null,set);        &#125; catch (SQLException e) &#123;            throw new RuntimeException(e);        &#125; catch (ClassNotFoundException e) &#123;            throw new RuntimeException(e);        &#125;        return user;    &#125;    @Override    public int updateUser(Integer id, String key,String value) &#123;        int affectedRow = 0;        if(id != null&amp;&amp; value != null &amp;&amp; key != null)&#123;            Connection conn = null;            sql = &quot;UPDATE smbms_user SET &quot;+key+&quot; = ? WHERE id = ? &quot;;            Object[] params = &#123;value,id&#125;;            try &#123;                conn = DB.getConnection();                affectedRow = DB.executeUpdate(conn, sql, params);                DB.close(conn,null,null);            &#125; catch (SQLException e) &#123;                throw new RuntimeException(e);            &#125; catch (ClassNotFoundException e) &#123;                throw new RuntimeException(e);            &#125;        &#125;        return affectedRow;    &#125;</code></pre></li></ul><h3 id="密码修改实现">密码修改实现</h3><h4 id="前端页面">前端页面</h4><p><strong>JSP部分: 页面显示</strong></p><pre><code class="language-jsp">&lt;%@ page contentType=&quot;text/html;charset=UTF-8&quot; language=&quot;java&quot; %&gt;&lt;%@include file=&quot;/jsp/common/head.jsp&quot; %&gt;&lt;div class=&quot;right&quot;&gt;    &lt;div class=&quot;location&quot;&gt;        &lt;strong&gt;你现在所在的位置是:&lt;/strong&gt;        &lt;span&gt;密码修改页面&lt;/span&gt;    &lt;/div&gt;    &lt;div class=&quot;providerAdd&quot;&gt;        &lt;form id=&quot;userForm&quot; name=&quot;userForm&quot; method=&quot;post&quot; action=&quot;$&#123;pageContext.request.contextPath &#125;/jsp/user.do&quot;&gt;            &lt;input type=&quot;hidden&quot; name=&quot;method&quot; value=&quot;savepwd&quot;/&gt;            &lt;!--div的class 为error是验证错误，ok是验证成功--&gt;            &lt;div class=&quot;info&quot;&gt;$&#123;message&#125;&lt;/div&gt;            &lt;div class=&quot;&quot;&gt;                &lt;label for=&quot;oldPassword&quot;&gt;旧密码：&lt;/label&gt;                &lt;input type=&quot;password&quot; name=&quot;oldpassword&quot; id=&quot;oldpassword&quot; value=&quot;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label for=&quot;newPassword&quot;&gt;新密码：&lt;/label&gt;                &lt;input type=&quot;password&quot; name=&quot;newpassword&quot; id=&quot;newpassword&quot; value=&quot;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label for=&quot;newPassword&quot;&gt;确认新密码：&lt;/label&gt;                &lt;input type=&quot;password&quot; name=&quot;rnewpassword&quot; id=&quot;rnewpassword&quot; value=&quot;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div class=&quot;providerAddBtn&quot;&gt;                &lt;!--&lt;a href=&quot;#&quot;&gt;保存&lt;/a&gt;--&gt;                &lt;input type=&quot;button&quot; name=&quot;save&quot; id=&quot;save&quot; value=&quot;保存&quot; class=&quot;input-button&quot;&gt;            &lt;/div&gt;        &lt;/form&gt;    &lt;/div&gt;&lt;/div&gt;&lt;/section&gt;&lt;%@include file=&quot;/jsp/common/foot.jsp&quot; %&gt;&lt;script type=&quot;text/javascript&quot; src=&quot;$&#123;pageContext.request.contextPath &#125;/statics/js/pwdmodify.js&quot;&gt;&lt;/script&gt;</code></pre><p>这是一个用 JSP（JavaServer Pages）编写的网页代码，用于修改密码。页面包括一个表单，用户可以在其中输入旧密码、新密码和确认新密码。表单使用POST方法提交到服务器，并使用“/jsp/user.do” URL。</p><p>页面包括一个消息区域，用于显示任何错误消息。页面还包括指向名为“pwdmodify.js”的 JavaScript 文件的链接。这个文件应该包含表单的客户端验证逻辑。</p><p>页面使用 JSP 表达式语言（EL）将动态内容插入到 HTML 中。例如，页面使用表达式 “${pageContext.request.contextPath}/statics/js/pwdmodify.js” 插入一个链接到 CSS 文件。“${pageContext.request.contextPath}” 表达式计算为 web 应用程序的上下文路径，用于构造 CSS 文件的完整 URL。</p><p><strong>JavaScript 表单的客户端验证逻辑</strong></p><pre><code class="language-js">var oldpassword = null;var newpassword = null;var rnewpassword = null;var saveBtn = null;$(function()&#123;   oldpassword = $(&quot;#oldpassword&quot;);   newpassword = $(&quot;#newpassword&quot;);   rnewpassword = $(&quot;#rnewpassword&quot;);   saveBtn = $(&quot;#save&quot;);   oldpassword.next().html(&quot;*&quot;);   newpassword.next().html(&quot;*&quot;);   rnewpassword.next().html(&quot;*&quot;);   oldpassword.on(&quot;blur&quot;,function()&#123;      $.ajax(&#123;         type:&quot;GET&quot;,         url:path+&quot;/jsp/user.do&quot;,         data:&#123;method:&quot;pwdmodify&quot;,oldpassword:oldpassword.val()&#125;,         dataType:&quot;json&quot;,         success:function(data)&#123;            if(data.result == &quot;true&quot;)&#123;//旧密码正确               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);            &#125;else if(data.result == &quot;false&quot;)&#123;//旧密码输入不正确               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 原密码输入不正确&quot;,false);            &#125;else if(data.result == &quot;sessionerror&quot;)&#123;//当前用户session过期，请重新登录               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 当前用户session过期，请重新登录&quot;,false);            &#125;else if(data.result == &quot;error&quot;)&#123;//旧密码输入为空               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 请输入旧密码&quot;,false);            &#125;         &#125;,         error:function(data)&#123;            //请求出错            validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 请求错误&quot;,false);         &#125;      &#125;);   &#125;).on(&quot;focus&quot;,function()&#123;      validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 请输入原密码&quot;,false);   &#125;);   newpassword.on(&quot;focus&quot;,function()&#123;      validateTip(newpassword.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 密码长度必须是大于6小于20&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      if(newpassword.val() != null &amp;&amp; newpassword.val().length &gt; 5         &amp;&amp; newpassword.val().length &lt; 20 )&#123;         validateTip(newpassword.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(newpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 密码输入不符合规范，请重新输入&quot;,false);      &#125;   &#125;);   rnewpassword.on(&quot;focus&quot;,function()&#123;      validateTip(rnewpassword.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 请输入与上面一致的密码&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      if(rnewpassword.val() != null &amp;&amp; rnewpassword.val().length &gt; 5         &amp;&amp; rnewpassword.val().length &lt; 20 &amp;&amp; newpassword.val() == rnewpassword.val())&#123;         validateTip(rnewpassword.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(rnewpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 两次密码输入不一致，请重新输入&quot;,false);      &#125;   &#125;);   saveBtn.on(&quot;click&quot;,function()&#123;      oldpassword.blur();      newpassword.blur();      rnewpassword.blur();      if(         oldpassword.attr(&quot;validateStatus&quot;) == &quot;true&quot; &amp;&amp;          newpassword.attr(&quot;validateStatus&quot;) == &quot;true&quot;         &amp;&amp; rnewpassword.attr(&quot;validateStatus&quot;) == &quot;true&quot;)&#123;         if(confirm(&quot;确定要修改密码？&quot;))&#123;            $(&quot;#userForm&quot;).submit();         &#125;      &#125;   &#125;);&#125;);</code></pre><p>这是一个 JavaScript 代码段，它使用 jQuery 库为一个包含密码修改表单的网页添加了客户端验证逻辑。代码使用 AJAX 技术从服务器获取 JSON 格式的响应数据，以验证用户输入的旧密码是否正确，并在页面上显示相关提示信息。</p><p>代码中首先定义了四个变量，分别是旧密码、新密码、确认新密码和提交按钮的 jQuery 对象。然后使用 $() 函数将代码包装在一个回调函数中，确保代码在文档加载完成后才执行。回调函数内部做了以下工作：</p><ol><li><p>通过选择器获取页面上的旧密码输入框、新密码输入框、确认新密码输入框和提交按钮，并将它们的 jQuery 对象赋值给前面定义的四个变量。</p></li><li><p>为旧密码输入框绑定 blur 事件处理函数，当输入框失去焦点时，使用 $.ajax() 函数向服务器发送 GET 请求，验证用户输入的旧密码是否正确，并根据服务器返回的响应数据显示相应的提示信息。在成功回调函数中，根据 data.result 的值判断旧密码是否正确，并通过 validateTip() 函数显示相应的提示信息。在失败回调函数中，显示请求出错的提示信息。</p></li><li><p>为旧密码输入框绑定 focus 事件处理函数，当输入框获得焦点时，显示输入提示信息。</p></li><li><p>为新密码输入框绑定 focus 和 blur 事件处理函数，当输入框获得焦点时，显示输入提示信息；当输入框失去焦点时，判断用户输入的新密码是否符合规范，如果符合则显示正确提示，否则显示错误提示。</p></li><li><p>为确认新密码输入框绑定 focus 和 blur 事件处理函数，当输入框获得焦点时，显示输入提示信息；当输入框失去焦点时，判断用户输入的确认新密码是否与新密码一致，如果一致则显示正确提示，否则显示错误提示。</p></li><li><p>为提交按钮绑定 click 事件处理函数，当按钮被点击时，先让旧密码、新密码和确认新密码输入框失去焦点，然后判断它们的验证状态是否都为 true，如果是，则显示确认修改密码的提示框，如果用户点击确定，则提交表单，将新密码保存到服务器上。</p></li></ol><h4 id="编写处理Servlet">编写处理Servlet</h4><pre><code class="language-java">public class UserDo extends HttpServlet &#123;    private UserService service;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        String method = req.getParameter(&quot;method&quot;);        if(&quot;savepwd&quot;.equals(method))&#123;            savePwd(req, resp);        &#125; else if(&quot;pwdmodify&quot;.equals(method))&#123;            pwdModify(req, resp);        &#125;    &#125;    protected void savePwd(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        //拿到USer Session , 用于得到用户id        HttpSession session = req.getSession();        User user = (User) session.getAttribute(Constant.USER_SESSION);        String newPwd = req.getParameter(&quot;newpassword&quot;);        System.out.println(&quot;用户输入的：new password: &quot;+newPwd);        System.out.println(&quot;Session password: &quot;+user.getUserPassword()+&quot;, id: &quot;+ user.getId());        Integer id = user.getId();        boolean flag;        //method 值为  savepwd, 表示用户执行修改密码操作        if(newPwd != null)&#123;            service = new UserServiceImpl();            flag = service.updateUser(id, &quot;userPassword&quot;, newPwd);            //修改成功            if(flag)&#123;                System.out.println(&quot;修改成功&quot;);                req.setAttribute(&quot;message&quot;,&quot;密码修改成功， 请重新登录&quot;);                //  移除Session                session.removeAttribute(Constant.USER_SESSION);            &#125; else &#123;                System.out.println(&quot;密码修改失败&quot;);                req.setAttribute(&quot;message&quot;,&quot;密码修改失败&quot;);            &#125;        &#125; else &#123;            req.setAttribute(&quot;message&quot;,&quot;新密码格式错误&quot;);        &#125;        //转发到当前页面, 不用重定向而使用转发是为了传统参数（attribute) 通知前端密码修改结果（by attribute message）        req.getRequestDispatcher(&quot;/jsp/pwdmodify.jsp&quot;).forward(req,resp);    &#125;    /*    由ajax发起的密码查询请求， 用户进行密码修改时触发    放回前端json， ajax用于比对旧密码正确性,     */    protected void pwdModify(HttpServletRequest req, HttpServletResponse resp)&#123;        HttpSession session = req.getSession();        Object user = session.getAttribute(Constant.USER_SESSION);        Map&lt;String, String&gt; map = new HashMap&lt;String, String&gt;();        if(user != null)&#123;            String password = ((User) user).getUserPassword();            String inputPwd = req.getParameter(&quot;oldpassword&quot;);            if(inputPwd != null &amp;&amp; inputPwd.length() &gt; 0)&#123;                //旧密码正确                if(inputPwd.equals(password))&#123;                    map.put(&quot;result&quot;,&quot;true&quot;);                &#125; else &#123;                    //旧密码不正确                    map.put(&quot;result&quot;,&quot;false&quot;);                &#125;            &#125; else &#123;                //密码输入为空                map.put(&quot;result&quot;,&quot;error&quot;);            &#125;        &#125; else &#123;            //session timeout            map.put(&quot;result&quot;,&quot;sessionerror&quot;);        &#125;        //返回前端json数据        resp.setContentType(&quot;application/json&quot;);        try &#123;            PrintWriter writer = resp.getWriter();            writer.write(JSONArray.toJSONString(map));            writer.flush();            writer.close();        &#125; catch (IOException e) &#123;            throw new RuntimeException(e);        &#125;    &#125;&#125;</code></pre><p>这段代码是一个Java Web应用程序中的Servlet，用于处理用户密码修改操作。</p><p>在doGet()方法中，根据请求参数&quot;method&quot;的值进行不同的操作。如果&quot;method&quot;的值是&quot;savepwd&quot;，则调用savePwd()方法，保存新密码；如果&quot;method&quot;的值是&quot;pwdmodify&quot;，则调用pwdModify()方法，用于检查旧密码是否正确。</p><p>在savePwd()方法中，首先从Session中获取当前用户的信息，然后获取新密码，并使用UserService接口的实现类UserServiceImpl来更新用户密码。如果更新成功，将信息存储在请求属性中，并移除Session中的用户信息，最后将请求转发到密码修改页面。</p><p>在pwdModify()方法中，首先获取当前用户的信息，然后获取旧密码。如果旧密码不为空，则将输入的旧密码与Session中存储的旧密码进行比较。如果输入的旧密码与Session中的旧密码相同，则返回前端JSON数据，表示旧密码正确；否则返回JSON数据，表示旧密码不正确。</p><p><strong>需要注意的是，在返回JSON数据之前，需要设置响应的Content-Type为&quot;application/json&quot;。</strong></p><h5 id="映射Servlet">映射Servlet</h5><pre><code class="language-xml">  &lt;servlet&gt;    &lt;servlet-name&gt;userDo&lt;/servlet-name&gt;    &lt;servlet-class&gt;servlet.UserDo&lt;/servlet-class&gt;  &lt;/servlet&gt;  &lt;servlet-mapping&gt;    &lt;servlet-name&gt;userDo&lt;/servlet-name&gt;    &lt;url-pattern&gt;/jsp/user.do&lt;/url-pattern&gt;  &lt;/servlet-mapping&gt;</code></pre><h3 id="用户管理底层实现">用户管理底层实现</h3><h3 id="分页实现">分页实现</h3><ol><li><p>导入分页支持</p><pre><code class="language-java">package util;public class PageSupport &#123;    //当前页码-来自于用户输入， 1表示第一页， 用于sql做limit查询 limit startPage, length    private int currentPageNo = 1;    //总数量（表）    private int totalCount = 0;    //页面容量,每一页展示多少数据    private int pageSize = 0;    //总页数: totalCount/pageSize（+1）    private int totalPageCount = 1;      public int getCurrentPageNo() &#123;        return currentPageNo;    &#125;    public void setCurrentPageNo(int currentPageNo) &#123;        if(currentPageNo &gt; 0)&#123;            this.currentPageNo = currentPageNo;        &#125;    &#125;    public int getTotalCount() &#123;        return totalCount;    &#125;    public void setTotalCount(int totalCount) &#123;        if(totalCount &gt; 0)&#123;            this.totalCount = totalCount;            //设置总页数            this.setTotalPageCountByRs();        &#125;    &#125;    public int getPageSize() &#123;        return pageSize;    &#125;    public void setPageSize(int pageSize) &#123;        if(pageSize &gt; 0)&#123;            this.pageSize = pageSize;        &#125;    &#125;    public int getTotalPageCount() &#123;        return totalPageCount;    &#125;    public void setTotalPageCount(int totalPageCount) &#123;        this.totalPageCount = totalPageCount;    &#125;    public void setTotalPageCountByRs()&#123;        if(this.totalCount % this.pageSize == 0)&#123;            this.totalPageCount = this.totalCount / this.pageSize;        &#125;else if(this.totalCount % this.pageSize &gt; 0)&#123;            this.totalPageCount = this.totalCount / this.pageSize + 1;        &#125;else&#123;            this.totalPageCount = 0;        &#125;    &#125;&#125;</code></pre><p>这段代码是一个用于分页的工具类 <code>PageSupport</code>。它包含了用于处理分页逻辑的属性和方法。</p><p>让我们逐个分析这些属性和方法：</p><ol><li><p><code>currentPageNo</code>（当前页码）: 保存当前页的页码，默认为1（表示第一页）。用户可以通过调用 <code>setCurrentPageNo()</code> 方法来设置当前页码。</p></li><li><p><code>totalCount</code>（总数量）: 表示总的数据量。用户可以通过调用 <code>setTotalCount()</code> 方法来设置总数量。</p></li><li><p><code>pageSize</code>（页面容量）: 每一页显示的数据数量。用户可以通过调用 <code>setPageSize()</code> 方法来设置页面容量。</p></li><li><p><code>totalPageCount</code>（总页数）: 表示总的页数，根据总数量和页面容量计算得出。用户可以通过调用 <code>getTotalPageCount()</code> 方法来获取总页数。</p></li><li><p><code>setTotalPageCountByRs()</code> 方法: 根据总数量和页面容量计算总页数，并将结果设置给 <code>totalPageCount</code> 属性。</p></li></ol><p>此外，对于一些属性的设置，代码中进行了一些限制条件的判断：</p><ul><li><p><code>currentPageNo</code> 和 <code>pageSize</code> 必须大于0，否则将不进行设置。</p></li><li><p><code>totalCount</code> 必须大于0，否则将不进行设置，并且会将 <code>totalPageCount</code> 设置为0。</p></li></ul><p>这个工具类主要用于在分页查询中计算总页数和进行当前页码的设置。</p></li></ol><ul><li><p>前端</p><p><strong>Header下用户管理二级菜单</strong></p><pre><code class="language-jsp">&lt;li&gt;&lt;a href=&quot;$&#123;pageContext.request.contextPath &#125;/jsp/user.do?method=query&quot;&gt;用户管理&lt;/a&gt;&lt;/li&gt;</code></pre><p>点击这个菜单, 跳转到<code>/jsp/user.do?method=query</code>这个请求,<code> user.do</code>是对应的处理请求的Servlet, 问号后面是通过请求传递的参数, 为<code>method = query, Servlet</code>可以根据这个参数对请求的不同类型做对应的处理, 实现Servlet的复用.</p><p><strong>用户管理界面:</strong></p><pre><code class="language-jsp">    &lt;div class=&quot;search&quot;&gt;        &lt;form method=&quot;get&quot; action=&quot;$&#123;pageContext.request.contextPath &#125;/jsp/user.do&quot;&gt;            &lt;input name=&quot;method&quot; value=&quot;query&quot; class=&quot;input-text&quot; type=&quot;hidden&quot;&gt;            &lt;span&gt;用户名：&lt;/span&gt;            &lt;input name=&quot;queryname&quot; class=&quot;input-text&quot; type=&quot;text&quot; value=&quot;$&#123;queryUserName &#125;&quot;&gt;            &lt;span&gt;用户角色：&lt;/span&gt;            &lt;select name=&quot;queryUserRole&quot;&gt;                &lt;c:if test=&quot;$&#123;roleList != null &#125;&quot;&gt;                    &lt;option value=&quot;0&quot;&gt;--请选择--&lt;/option&gt;                    &lt;c:forEach var=&quot;role&quot; items=&quot;$&#123;roleList&#125;&quot;&gt;                        &lt;option &lt;c:if test=&quot;$&#123;role.id == queryUserRole &#125;&quot;&gt;selected=&quot;selected&quot;&lt;/c:if&gt;                                value=&quot;$&#123;role.id&#125;&quot;&gt;$&#123;role.roleName&#125;&lt;/option&gt;                    &lt;/c:forEach&gt;                &lt;/c:if&gt;            &lt;/select&gt;            &lt;input type=&quot;hidden&quot; name=&quot;pageIndex&quot; value=&quot;1&quot;/&gt;            &lt;input value=&quot;查 询&quot; type=&quot;submit&quot; id=&quot;searchbutton&quot;&gt;        &lt;/form&gt;    &lt;/div&gt;    &lt;!--用户--&gt;/*用户的mapping代码*/    &lt;input type=&quot;hidden&quot; id=&quot;totalPageCount&quot; value=&quot;$&#123;totalPageCount&#125;&quot;/&gt;    &lt;c:import url=&quot;rollpage.jsp&quot;&gt;        &lt;c:param name=&quot;totalCount&quot; value=&quot;$&#123;totalCount&#125;&quot;/&gt;        &lt;c:param name=&quot;currentPageNo&quot; value=&quot;$&#123;currentPageNo&#125;&quot;/&gt;        &lt;c:param name=&quot;totalPageCount&quot; value=&quot;$&#123;totalPageCount&#125;&quot;/&gt;    &lt;/c:import&gt;&lt;/div&gt;&lt;/section&gt;</code></pre></li></ul><p>这段代码展示了一个搜索表单和与分页相关的部分。</p><ol><li><code>&lt;div class=&quot;search&quot;&gt;</code> 标签: 定义一个搜索的区域。</li><li><code>&lt;form&gt;</code> 标签: 定义了一个表单，使用 GET 方法提交到 <code>$&#123;pageContext.request.contextPath&#125;/jsp/user.do</code>。</li><li><code>&lt;input&gt;</code> 标签: 设置了一个隐藏字段 <code>method</code>，值为 <code>&quot;query&quot;</code>，用于指定查询的方法。</li><li><code>&lt;input&gt;</code> 标签: 创建了一个文本输入框，名称为 <code>queryname</code>，值为 <code>$&#123;queryUserName&#125;</code>，显示了之前查询的用户名。</li><li><code>&lt;select&gt;</code> 标签: 创建了一个下拉选择框，名称为 <code>queryUserRole</code>。使用了 JSTL 标签 <code>&lt;c:if&gt;</code> 进行条件判断，如果 <code>roleList</code> 不为 null，将会显示选项。</li><li><code>&lt;option&gt;</code> 标签: 设置了一个默认选项 <code>&lt;option value=&quot;0&quot;&gt;--请选择--&lt;/option&gt;</code>，然后使用 <code>&lt;c:forEach&gt;</code> 遍历 <code>roleList</code> 中的角色，并将角色的名称和 ID 设置为选项的值。使用 <code>&lt;c:if&gt;</code> 判断当前角色是否与 <code>queryUserRole</code> 相等，如果相等，则设置该选项为选中状态。</li><li><code>&lt;input&gt;</code> 标签: 设置了一个隐藏字段 <code>pageIndex</code>，值为 1</li><li><code>&lt;input&gt;</code> 标签: 设置了一个隐藏字段 <code>totalPageCount</code>，值为 <code>$&#123;totalPageCount&#125;</code>。</li><li><code>&lt;c:import&gt;</code> 标签: 导入了一个名为 <code>rollpage.jsp</code> 的页面，并传递了三个参数 <code>totalCount</code>、<code>currentPageNo</code> 和 <code>totalPageCount</code> 的值。</li></ol><p>这段代码展示了一个搜索表单，用户可以输入用户名和选择用户角色进行查询。同时，还包含了与分页相关的部分，包括当前页码、总页数和总数量的显示。</p><p>我们观察这段代码, 这个form表单的一部分代码, 分别展示了提交的目的地 <code>$&#123;pageContext.request.contextPath &#125;/jsp/user.do</code>  以及 一个隐藏域, 包含一个熟悉 <code>method</code> 值为 <code>query</code>, 是不是, 很熟悉, 没错, 这个Servlet和Header菜单进入管理用户界面所发起的请求是一样的, 也就是说, 是一个Servlet的相同处理过程. 为什么会这样呢?  这是因为查询和展示用户信息(未筛选时) 本质是一样的.</p><pre><code class="language-jsp">    &lt;form method=&quot;get&quot; action=&quot;$&#123;pageContext.request.contextPath &#125;/jsp/user.do&quot;&gt;        &lt;input name=&quot;method&quot; value=&quot;query&quot; class=&quot;input-text&quot; type=&quot;hidden&quot;&gt;</code></pre><ul><li><p>DAO层</p><p><code>int getUserCount()</code></p><pre><code class="language-java">public int getUserCount(Connection conn, String userName, int userRole) &#123;    int count = 0;    StringBuffer sql = new StringBuffer();    sql.append(&quot;SELECT count(1) as count from smbms_user s, smbms_role r where s.userRole = r.id&quot; );    ArrayList&lt;Object&gt; list = new ArrayList&lt;Object&gt;();    ResultSet rs =  null;    if(userName != null)&#123;        sql.append(&quot; and userName like ? &quot;);        list.add(&quot;%&quot;+userName+&quot;%&quot;);    &#125;    if(userRole &gt; 0)&#123;        sql.append(&quot; and userRole like ? &quot;);        list.add(&quot;%&quot;+userRole+&quot;%&quot;);    &#125;    System.out.println(&quot;sql----&gt; &quot;+sql.toString());    try &#123;        rs = DB.executeQuery(conn, sql.toString(), list.toArray());        while (rs.next()) &#123;            count = rs.getInt(&quot;count&quot;);        &#125;        DB.close(null,null,rs);    &#125; catch (SQLException e) &#123;        throw new RuntimeException(e);    &#125; catch (ClassNotFoundException e) &#123;        throw new RuntimeException(e);    &#125;    return count;&#125;</code></pre><p>这段代码是一个方法 <code>getUserCount</code>，用于查询符合条件的用户数量。</p><p>让我们逐行解释这段代码：</p><ol><li><p><code>int getUserCount(Connection conn, String userName, int userRole)</code>: 这是一个方法声明，它接受一个数据库连接对象 <code>conn</code>、一个用户名 <code>userName</code> 和一个用户角色 <code>userRole</code> 作为参数，并返回一个整数表示符合条件的用户数量。</p></li><li><p><code>int count = 0;</code>: 初始化一个计数器变量 <code>count</code>，用于存储查询结果的数量。</p></li><li><p><code>StringBuffer sql = new StringBuffer();</code>: 创建一个 <code>StringBuffer</code> 对象 <code>sql</code>，用于构建 SQL 查询语句。</p></li><li><p><code>sql.append(&quot;SELECT count(1) as count from smbms_user s, smbms_role r where s.userRole = r.id&quot;);</code>: 将基本的查询语句添加到 <code>sql</code> 对象中，查询用户表 <code>smbms_user</code> 和角色表 <code>smbms_role</code>，并通过 <code>userRole</code> 字段进行关联。</p></li><li><p><code>ArrayList&lt;Object&gt; list = new ArrayList&lt;Object&gt;();</code>: 创建一个 <code>ArrayList</code> 对象 <code>list</code>，用于存储查询条件的参数值。</p></li><li><p><code>ResultSet rs =  null;</code>: 初始化一个 <code>ResultSet</code> 对象 <code>rs</code>，用于存储查询结果集。</p></li><li><p><code>if(userName != null) &#123; ... &#125;</code>: 如果 <code>userName</code> 不为 null，则将一个条件子句添加到 <code>sql</code> 对象中，使用 <code>LIKE</code> 运算符进行模糊匹配，并将匹配参数添加到 <code>list</code> 中。</p></li><li><p><code>if(userRole &gt; 0) &#123; ... &#125;</code>: 如果 <code>userRole</code> 大于 0，则将另一个条件子句添加到 <code>sql</code> 对象中，同样使用 <code>LIKE</code> 运算符进行匹配，并将匹配参数添加到 <code>list</code> 中。</p></li><li><p><code>System.out.println(&quot;sql----&gt; &quot;+sql.toString());</code>: 打印输出最终生成的 SQL 查询语句，方便调试和查看。</p></li><li><p><code>rs = DB.executeQuery(conn, sql.toString(), list.toArray());</code>: 执行 SQL 查询，并将结果存储在 <code>rs</code> 中。<code>DB.executeQuery()</code> 是一个执行查询语句的方法，接受数据库连接对象、SQL 查询语句和参数值数组作为参数。</p></li><li><p><code>while (rs.next()) &#123; ... &#125;</code>: 遍历查询结果集，将每行的计数值存储到 <code>count</code> 变量中。</p></li><li><p><code>DB.close(null,null,rs);</code>: 关闭结果集、语句和连接对象，释放资源。<code>DB.close()</code> 是一个关闭数据库资源的方法，接受三个参数：结果集、语句和连接对象。</p></li><li><p><code>return count;</code>: 返回符合条件的用户数量。</p></li></ol><p>该方法的作用是查询数据库中符合给定用户名和用户角色条件的用户数量，并将结果返回。</p><p>==getUserList==</p><pre><code class="language-java">public List&lt;User&gt; getUserList(Connection conn, String userName, int userRole,int currPage, int pageSize) &#123;        List&lt;User&gt; userList = new ArrayList&lt;&gt;();        if(conn != null) &#123;            //准备要使用的对象            ArrayList&lt;Object&gt; params = new ArrayList&lt;&gt;();            ResultSet rs = null;            StringBuffer sql = new StringBuffer();            try &#123;                //编写sql                sql.append(&quot;SELECT * from smbms_user u ,smbms_role r Where u.userRole = r.id &quot;);                //如果用户名不为空， 表示有用户名查询要求，追加限定条件                if(userName != null)&#123;                    sql.append(&quot; AND userName like ? &quot;);                    params.add(&quot;%&quot;+userName+&quot;%&quot;);                &#125;                //同上...                if(userRole != 0)&#123;                    sql.append(&quot; and userRole = ?&quot;);                    params.add(userRole);                &#125;                sql.append(&quot; ORDER BY u.creationDate DESC LIMIT ?,? &quot;);//在sql最后追加一个排序和分页                //5                //1     5                //2     10                //3     15                currPage = (currPage-1)*pageSize;//减一的原因就是MYSQL分页的index从0开始                params.add(currPage);//从哪一个下标开始                params.add(pageSize);//从currentPageNo连续取几个                System.out.println(&quot;sql---&gt;&quot;+sql.toString());                //执行sql， 返回结果集, ⚠️需要把sql转化为字符串， params 转化为数组                rs = DB.executeQuery(conn,sql.toString(),params.toArray());                if(rs != null)&#123;                    while (rs.next())&#123;                        User user = new User();                        user.setUserPassword(rs.getString(&quot;userPassword&quot;));                        user.setId(rs.getInt(&quot;id&quot;));                        user.setUserCode(rs.getString(&quot;userCode&quot;));                        user.setUserName(rs.getString(&quot;userName&quot;));                        user.setAddress(rs.getString(&quot;address&quot;));                        user.setBirthday((Date) rs.getObject(&quot;birthday&quot;));                        user.setGender(rs.getInt(&quot;gender&quot;));                        user.setPhone(rs.getString(&quot;phone&quot;));                        user.setIdPicPath(rs.getString(&quot;idPicPath&quot;));                        user.setUserRole(rs.getInt(&quot;userRole&quot;));                        user.setCreatedBy(rs.getInt(&quot;createdBy&quot;));                        user.setCreationDate( rs.getDate(&quot;creationDate&quot;));                        user.setModifyBy(rs.getInt(&quot;modifyBy&quot;));                        user.setModifyDate(rs.getDate(&quot;modifyDate&quot;));                        user.setUserRoleName(rs.getString(&quot;roleName&quot;));                        userList.add(user);//                        System.out.println(user.toString());                    &#125;                &#125;                DB.close(null,null,rs);            &#125; catch (SQLException e) &#123;                throw new RuntimeException(e);            &#125; catch (ClassNotFoundException e) &#123;                throw new RuntimeException(e);            &#125;        &#125; else &#123;            return null;        &#125;        return userList;    &#125;</code></pre></li></ul><p>这段代码是一个方法 <code>getUserList</code>，用于根据给定的用户名、用户角色、当前页码和页面大小获取符合条件的用户列表。</p><p>让我们逐行解释这段代码：</p><ol><li><p><code>public List&lt;User&gt; getUserList(Connection conn, String userName, int userRole, int currPage, int pageSize)</code>: 这是一个方法声明，它接受一个数据库连接对象 <code>conn</code>、一个用户名 <code>userName</code>、一个用户角色 <code>userRole</code>、当前页码 <code>currPage</code> 和页面大小 <code>pageSize</code> 作为参数，并返回一个 <code>List&lt;User&gt;</code> 类型的用户列表。</p></li><li><p><code>List&lt;User&gt; userList = new ArrayList&lt;&gt;();</code>: 创建一个 <code>ArrayList</code> 对象 <code>userList</code>，用于存储用户列表。</p></li><li><p><code>if(conn != null) &#123; ... &#125; else &#123; return null; &#125;</code>: 检查数据库连接对象是否为 null，如果为 null，则直接返回 null。</p></li><li><p><code>ArrayList&lt;Object&gt; params = new ArrayList&lt;&gt;();</code>: 创建一个 <code>ArrayList</code> 对象 <code>params</code>，用于存储查询条件的参数值。</p></li><li><p><code>ResultSet rs = null;</code>: 初始化一个 <code>ResultSet</code> 对象 <code>rs</code>，用于存储查询结果集。</p></li><li><p><code>StringBuffer sql = new StringBuffer();</code>: 创建一个 <code>StringBuffer</code> 对象 <code>sql</code>，用于构建 SQL 查询语句。</p></li><li><p><code>sql.append(&quot;SELECT * from smbms_user u ,smbms_role r Where u.userRole = r.id &quot;);</code>: 将基本的查询语句添加到 <code>sql</code> 对象中，查询用户表 <code>smbms_user</code>，和角色表<code>smbms_role </code>, 这里进行了多表的联合查询, 是为了给user(Bean)的RoleNa m e赋值, 以便前端使用.</p></li><li><p><code>if(userName != null) &#123; ... &#125;</code>: 如果 <code>userName</code> 不为 null，则将一个条件子句添加到 <code>sql</code> 对象中，使用 <code>LIKE</code> 运算符进行模糊匹配，并将匹配参数添加到 <code>params</code> 中。</p></li><li><p><code>if(userRole != 0) &#123; ... &#125;</code>: 如果 <code>userRole</code> 不为 0，则将另一个条件子句添加到 <code>sql</code> 对象中，限定用户角色，并将角色参数添加到 <code>params</code> 中。</p></li><li><p><code>sql.append(&quot; ORDER BY u.creationDate DESC LIMIT ?,? &quot;);</code>: 在查询语句的末尾追加一个排序和分页的部分，按照 <code>creationDate</code> 字段降序排列，并使用 <code>LIMIT</code> 限制查询结果的起始位置和数量。</p></li><li><p><code>currPage = (currPage-1)*pageSize;</code>: 计算实际的起始位置，根据当前页码和页面大小，减一是因为 MYSQL 分页的索引从 0 开始。</p></li><li><p><code>params.add(currPage);</code>: 将起始位置参数添加到 <code>params</code> 中。</p></li><li><p><code>params.add(pageSize);</code>: 将页面大小参数添加到 <code>params</code> 中。</p></li><li><p><code>System.out.println(&quot;sql---&gt;&quot;+sql.toString());</code>: 打印输出最终生成的 SQL 查询语句，方便调试和查看。</p></li><li><p><code>rs = DB.executeQuery(conn, sql.toString(), params.toArray());</code>: 执行 SQL 查询，并将结果存储在 <code>rs</code> 中。`</p></li></ol><p>​==getRoleList==</p><pre><code class="language-java">public List&lt;Role&gt; getRolelist(Connection conn, int id) &#123;    //prevent roleList    List&lt;Role&gt; roleList = new ArrayList&lt;&gt;();    if(conn != null)&#123;        try &#123;            ResultSet rs = null;            List&lt;Object&gt; params = new ArrayList&lt;Object&gt;();            StringBuffer sql = new StringBuffer();            //edit sql expression            sql.append(&quot;SELECT * FROM smbms_role &quot;);            //输入0表查询全表， 输入&gt;0 为sql拼接条件， 同时准备参数列表            if(id &gt; 0)&#123;                sql.append(&quot; WHERE id = ? &quot;);                params.add(id);            &#125;            System.out.println(&quot;sql----&gt;&quot;+sql.toString());            rs = DB.executeQuery(conn, sql.toString(), params.toArray());            if(rs != null)&#123;                while (rs.next()) &#123;                    //赋值                    Role role = new Role();                    role.setId(rs.getInt(&quot;id&quot;));                    role.setRoleCode(rs.getString(&quot;roleCode&quot;));                    role.setRoleName(rs.getString(&quot;roleName&quot;));                    role.setCreatedBy(rs.getInt(&quot;createdBy&quot;));                    role.setModifyDate(rs.getDate(&quot;creationDate&quot;));                    role.setCreationDate(rs.getDate(&quot;creationDate&quot;));                    role.setModifyBy(rs.getInt(&quot;modifyBy&quot;));                    roleList.add(role);                &#125;            &#125;        &#125; catch (SQLException e) &#123;            throw new RuntimeException(e);        &#125; catch (ClassNotFoundException e) &#123;            throw new RuntimeException(e);        &#125;    &#125;else &#123;        return null;    &#125;    return roleList;&#125;</code></pre><ul><li><p>Service层</p><p>==getUserCount==</p><pre><code class="language-java">public int getUserCount(String userName, int UserRole) &#123;        int count = 0;        Connection conn = null;        try &#123;            conn = DB.getConnection();            dao = new UserDaoImpl();            count = dao.getUserCount(conn, userName, UserRole);            DB.close(conn,null,null);        &#125; catch (SQLException e) &#123;            throw new RuntimeException(e);        &#125; catch (ClassNotFoundException e) &#123;            throw new RuntimeException(e);        &#125;        return count;    &#125;</code></pre><p>==getUserlist==</p><pre><code class="language-java">public List&lt;User&gt; getUserlist(String userName, int userRole,int currPage, int pageSize) &#123;    List&lt;User&gt; usersList = new ArrayList&lt;&gt;();    try &#123;        Connection conn = DB.getConnection();        dao = new UserDaoImpl();        usersList = dao.getUserList(conn, userName, userRole,currPage, pageSize);        DB.close(conn,null,null);    &#125; catch (SQLException e) &#123;        throw new RuntimeException(e);    &#125; catch (ClassNotFoundException e) &#123;        throw new RuntimeException(e);    &#125;    return usersList;&#125;</code></pre><p>==getRoleList==</p><pre><code class="language-java">public List&lt;Role&gt; getRoleList(int id) &#123;    List&lt;Role&gt; roles = new ArrayList&lt;&gt;();    try &#123;        Connection conn = DB.getConnection();        RoleDao dao = new RoleDaoImpl();        roles = dao.getRolelist(conn, id);    &#125; catch (SQLException e) &#123;        throw new RuntimeException(e);    &#125; catch (ClassNotFoundException e) &#123;        throw new RuntimeException(e);    &#125;    return roles;&#125;</code></pre></li><li><p>Servlet层</p><p>类加载读取配置文件： 目前需要读取的数据有：PAGE_SIZE 用于设置页面大小</p><pre><code class="language-java">static &#123;    //读取配置文件    //1、创建properties对象    Properties properties = new Properties();    //2、通过类加载器加载资源文件为字节输入流    InputStream in = DB.class.getClassLoader().getResourceAsStream(&quot;db.properties&quot;);    try &#123;        properties.load(in);    &#125; catch (IOException e) &#123;        e.printStackTrace();    &#125;    PAGE_SIZE = Integer.parseInt(properties.getProperty(&quot;PAGE_SIZE&quot;));&#125;</code></pre><pre><code class="language-java">protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;    String method = req.getParameter(&quot;method&quot;);    if(&quot;savepwd&quot;.equals(method))&#123;        savePwd(req, resp);    &#125; else if(&quot;pwdmodify&quot;.equals(method))&#123;        pwdModify(req, resp);    &#125; else if (&quot;query&quot;.equals(method)) &#123;        query(req, resp);    &#125;&#125;</code></pre><p>这段代码的作用是根据不同的<code>method</code>值调用不同的方法处理GET请求，实现不同的功能操作。</p></li></ul><p>具体实现:</p><pre><code class="language-java">protected void query(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        //1. 从前端获取用户输入的参数 queryname -&gt; userName，queryUserRole -&gt; userRole,pageIndex        String userName = req.getParameter(&quot;queryname&quot;);        String userRole = req.getParameter(&quot;queryUserRole&quot;);        String pageIndex = req.getParameter(&quot;pageIndex&quot;);        // 检验参数的合法性        if( userName == null)&#123;            userName =&quot;&quot;;        &#125;        if(userRole == null || userRole.equals(&quot;&quot;))&#123;            userRole = &quot;0&quot;;        &#125;        if(pageIndex == null || pageIndex.equals(&quot;&quot;))&#123;            pageIndex = &quot;1&quot;;        &#125;        //3. 准备前端需要的参数： queryUserName，queryUserRole ,roleList，userList,        String queryUserName = userName;        String queryUserRole = userRole;        List&lt;Role&gt; roleList;        List&lt;User&gt; userList;        // 以及下列参数： totalPageCount（总页数），totalCount（查询到的用户数），currentPageNo（当前页）        // 这些参数的获取需要先调用userService.getUserCount查询用户数， 然后调用PageSupport提供分页支持        UserServiceImpl userService = new UserServiceImpl();        RoleServiceImpl roleService = new RoleServiceImpl();        int totalCount = userService.getUserCount(userName, Integer.parseInt(userRole));        PageSupport pageSupport = new PageSupport();        pageSupport.setPageSize(PAGE_SIZE);        pageSupport.setTotalCount(totalCount);        pageSupport.setTotalPageCountByRs();        pageSupport.setCurrentPageNo(Integer.parseInt(pageIndex));        int totalPageCount = pageSupport.getTotalPageCount();        // 查询用户和角色        userList = userService.getUserlist(userName, Integer.parseInt(userRole), Integer.parseInt(pageIndex), PAGE_SIZE);        roleList = roleService.getRoleList(0);        //2. 根据参数返回用户表和角色表        req.setAttribute(&quot;roleList&quot;,roleList);        req.setAttribute(&quot;userList&quot;,userList);        //3. 返回用户需要的参数        req.setAttribute(&quot;queryUserName&quot;,queryUserName);        req.setAttribute(&quot;queryUserRole&quot;,queryUserRole);        req.setAttribute(&quot;totalPageCount&quot;, totalPageCount);        req.setAttribute(&quot;totalCount&quot;,totalCount);        req.setAttribute(&quot;currentPageNo&quot;,pageIndex);        //4. 转发到jsp/userlist.jsp， 要使用转发而不是重定向， 便于携带参数        req.getRequestDispatcher(&quot;userlist.jsp&quot;).forward(req,resp);    &#125;</code></pre><p>这段代码实现了一个查询用户的功能，以下是代码实现的步骤：</p><ol><li><p>从前端获取用户输入的参数：userName（用户名），userRole（用户角色），pageIndex（当前页码）。</p></li><li><p>根据参数返回用户表和角色表，并将其设置为请求的属性，供jsp文件使用。</p></li><li><p>准备前端需要的参数： queryUserName（查询用户名），queryUserRole（查询用户角色），roleList（角色列表），userList（用户列表）,totalPageCount（总页数），totalCount（查询到的用户数），currentPageNo（当前页）。</p></li><li><p>查询用户数，并设置pageSupport的totalCount，currentPageNo，pageSize属性。</p></li><li><p>查询用户和角色，并将其设置为请求的属性，供jsp文件使用。</p></li><li><p>将前端需要的参数设置为请求的属性，供jsp文件使用。</p></li><li><p>将请求转发到userlist.jsp文件，便于携带参数。</p></li></ol><p>其中，代码中调用了UserService类和RoleService类的方法实现具体的查询操作。同时，代码中也对获取的参数进行了一些合法性的检验和设置默认值的操作。最终，将查询到的结果设置为请求的属性，通过转发的方式返回到jsp文件中供前端展示。</p><h3 id="用户删除实现">用户删除实现</h3><p>在前端页面找到用户删除的按钮(锚), href是: <code>javascript:</code>,</p><pre><code class="language-jsp">&lt;td&gt;    &lt;span&gt;&lt;a class=&quot;viewUser&quot; href=&quot;javascript:;&quot; userid=$&#123;user.id &#125; username=$&#123;user.userName &#125;&gt;&lt;img src=&quot;$&#123;pageContext.request.contextPath &#125;/statics/images/read.png&quot; alt=&quot;查看&quot; title=&quot;查看&quot;/&gt;&lt;/a&gt;&lt;/span&gt;    &lt;span&gt;&lt;a class=&quot;modifyUser&quot; href=&quot;javascript:;&quot; userid=$&#123;user.id &#125; username=$&#123;user.userName &#125;&gt;&lt;img src=&quot;$&#123;pageContext.request.contextPath &#125;/statics/images/xiugai.png&quot; alt=&quot;修改&quot; title=&quot;修改&quot;/&gt;&lt;/a&gt;&lt;/span&gt;    &lt;span&gt;&lt;a class=&quot;deleteUser&quot; href=&quot;javascript:;&quot; userid=$&#123;user.id &#125; username=$&#123;user.userName &#125;&gt;&lt;img src=&quot;$&#123;pageContext.request.contextPath &#125;/statics/images/schu.png&quot; alt=&quot;删除&quot; title=&quot;删除&quot;/&gt;&lt;/a&gt;&lt;/span&gt;&lt;/td&gt;</code></pre><p>找到对应的javascript</p><pre><code class="language-js">$(&quot;.deleteUser&quot;).on(&quot;click&quot;,function()&#123;userObj = $(this);changeDLGContent(&quot;你确定要删除用户【&quot;+userObj.attr(&quot;username&quot;)+&quot;】吗？&quot;);openYesOrNoDLG();&#125;);</code></pre><p>当删除被点击后, js调用函数<code>userObj</code>, 接着我们去看这个函数</p><pre><code class="language-js">function deleteUser(obj)&#123;   $.ajax(&#123;      type:&quot;GET&quot;,      url:path+&quot;/jsp/user.do&quot;,      data:&#123;method:&quot;deluser&quot;,uid:obj.attr(&quot;userid&quot;)&#125;,      dataType:&quot;json&quot;,      success:function(data)&#123;         if(data.delResult == &quot;true&quot;)&#123;//删除成功：移除删除行            cancleBtn();            obj.parents(&quot;tr&quot;).remove();         &#125;else if(data.delResult == &quot;false&quot;)&#123;//删除失败            changeDLGContent(&quot;对不起，删除用户【&quot;+obj.attr(&quot;username&quot;)+&quot;】失败&quot;);         &#125;else if(data.delResult == &quot;notexist&quot;)&#123;            changeDLGContent(&quot;对不起，用户【&quot;+obj.attr(&quot;username&quot;)+&quot;】不存在&quot;);         &#125;      &#125;,      error:function(data)&#123;         changeDLGContent(&quot;对不起，删除失败&quot;);      &#125;   &#125;);&#125;</code></pre><p>这段代码是一个 JavaScript 函数，它使用 jQuery 的 <code>$.ajax</code> 方法发送一个 GET 请求到服务器。以下是对代码的分析：</p><ol><li><p><code>function deleteUser(obj)</code>：这是一个命名为 <code>deleteUser</code> 的函数，它接受一个参数 <code>obj</code>，表示要删除的用户对象。</p></li><li><p><code>$.ajax</code>：这是 jQuery 提供的一个函数，用于发送 AJAX 请求到服务器。</p></li><li><p>AJAX 请求的配置选项包括：</p><ul><li><code>type: &quot;GET&quot;</code>：指定请求类型为 GET。</li><li><code>url: path + &quot;/jsp/user.do&quot;</code>：指定请求的 URL 地址，其中 <code>path</code> 是一个变量或常量，表示服务器路径。</li><li><code>data: &#123;method: &quot;deluser&quot;, uid: obj.attr(&quot;userid&quot;)&#125;</code>：指定请求发送的数据，包括 <code>method</code> 和 <code>uid</code> 两个参数。<code>method</code> 的值为 <code>&quot;deluser&quot;</code>，<code>uid</code> 的值通过 <code>obj.attr(&quot;userid&quot;)</code> 获取。</li><li><code>dataType: &quot;json&quot;</code>：指定预期的响应数据类型为 JSON。</li></ul></li><li><p><code>success</code>：这是一个成功回调函数，在服务器成功响应时被调用。它接受一个参数 <code>data</code>，表示从服务器返回的数据。函数内部根据返回的数据执行不同的操作：</p><ul><li>如果 <code>data.delResult</code> 的值为 <code>&quot;true&quot;</code>，表示删除成功。函数调用 <code>cancleBtn()</code> 取消按钮，并移除 <code>obj</code> 对应的父级 <code>&lt;tr&gt;</code> 行。</li><li>如果 <code>data.delResult</code> 的值为 <code>&quot;false&quot;</code>，表示删除失败。函数调用 <code>changeDLGContent</code> 修改对话框的内容，显示删除失败的提示信息。</li><li>如果 <code>data.delResult</code> 的值为 <code>&quot;notexist&quot;</code>，表示用户不存在。函数调用 <code>changeDLGContent</code> 修改对话框的内容，显示用户不存在的提示信息。</li></ul></li><li><p><code>error</code>：这是一个错误回调函数，在请求发生错误时被调用。它接受一个参数 <code>data</code>，表示错误信息。函数调用 <code>changeDLGContent</code> 修改对话框的内容，显示删除失败的提示信息。</p></li></ol><p>这段代码用于通过 AJAX 请求向服务器发送删除用户的请求，并根据服务器返回的结果执行相应的操作。成功时移除对应的行，失败时显示错误提示信息。</p><p>通过这些信息,我们可以得知如何编写我们的后台程序</p><ol><li>编写对应的Servlet, 根据method值不同, 调用delUser函数</li><li>编写请求处理函数delUser</li><li>delUser调用Service删除用户, 并返回结果<ol><li>如果删除用户成功, 返回true或者受影响行数为1, 通过响应向前端返回数据delResult = true</li><li>如果Service返回false或者受影响行数为0, 向前端返回delResult = no exist, 表示删除的用户不存在.</li></ol></li><li>出现异常则返回delResult = false(在这里是用户权限不够时返回会false)</li></ol><pre><code class="language-java">protected void delUser(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException&#123;    //1. 获取用户角色， 如果用户不是系统管理员， 则没有删除用户的权限    User o = (User) req.getSession().getAttribute(Constant.USER_SESSION);    Map&lt;String, String&gt; map = new HashMap&lt;&gt;();    if(o.getUserRole() == 1)&#123;        String uid = req.getParameter(&quot;uid&quot;);        service = new UserServiceImpl();        boolean res = service.delUser(Integer.parseInt(uid));        if(res)&#123;            map.put(&quot;delResult&quot;, &quot;true&quot;);        &#125; else &#123;            map.put(&quot;delResult&quot;, &quot;notexist&quot;);        &#125;    &#125; else &#123;        map.put(&quot;delResult&quot;,&quot;false&quot;);    &#125;    //封装json数据， 并写到前端    PrintWriter writer = resp.getWriter();    writer.write(JSONArray.toJSONString(map));    writer.flush();&#125;</code></pre><p>接下来, 我们还需要编写对应的Service层以及Dao层代码, 由于service实际上并没有做什么事, 这里只展示Dao层代码</p><pre><code class="language-java">public int delUser(Connection conn, int id) &#123;    int affectedRow = 0;    if(conn != null)&#123;        try &#123;            String sql = &quot;Delete from smbms_user where id = ?&quot;;            Object[] params = &#123;id&#125;;            affectedRow = DB.executeUpdate(conn, sql, params);        &#125; catch (SQLException e) &#123;            throw new RuntimeException(e);        &#125;    &#125;    return affectedRow;&#125;</code></pre><p>这段代码及其简单, 只需要写一段sql, 然后交给DB去执行就好了, DB这个类会预处理sql, 以及给占位符赋值, 最后执行sql返回结果.</p><h3 id="查看用户实现">查看用户实现</h3><p>和删除用户类似, 找到对应的js请求</p><pre><code class="language-js">$(&quot;.viewUser&quot;).on(&quot;click&quot;,function()&#123;   //将被绑定的元素（a）转换成jquery对象，可以使用jquery方法   var obj = $(this);   window.location.href=path+&quot;/jsp/user.do?method=view&amp;uid=&quot;+ obj.attr(&quot;userid&quot;);&#125;);</code></pre><p>编写Servlet</p><pre><code class="language-java">protected void view(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException&#123;    String uid = req.getParameter(&quot;uid&quot;);    service = new UserServiceImpl();    User user = service.findById(Integer.parseInt(uid));    req.setAttribute(&quot;user&quot;, user);    req.getRequestDispatcher(req.getContextPath()+&quot;/jsp/userview.jsp&quot;).forward(req, resp);&#125;</code></pre><p>编写dao层, service层实现</p><pre><code class="language-java">public User findById(int id) &#123;    //2. 初始化参数    Object[] params = &#123;id&#125;;    ResultSet set;    sql = &quot;SELECT * FROM smbms_user,smbms_role Where smbms_user.userRole = smbms_role.id and smbms_user.id = ?&quot;;    try &#123;        conn = DB.getConnection();        set = DB.executeQuery(conn, sql,params);        if(set != null)&#123;            user = new User();            while (set.next()) &#123;                user.setUserRoleName(set.getString(&quot;roleName&quot;));                user.setUserPassword(set.getString(&quot;userPassword&quot;));                user.setId(set.getInt(&quot;id&quot;));                user.setUserCode(set.getString(&quot;userCode&quot;));                user.setUserName(set.getString(&quot;userName&quot;));                user.setAddress(set.getString(&quot;address&quot;));                user.setBirthday((Date) set.getObject(&quot;birthday&quot;));                user.setGender(set.getInt(&quot;gender&quot;));                user.setPhone(set.getString(&quot;phone&quot;));                user.setIdPicPath(set.getString(&quot;idPicPath&quot;));                user.setUserRole(set.getInt(&quot;userRole&quot;));                user.setCreatedBy(set.getInt(&quot;createdBy&quot;));                user.setCreationDate( set.getDate(&quot;creationDate&quot;));                user.setModifyBy(set.getInt(&quot;modifyBy&quot;));                user.setModifyDate(set.getDate(&quot;modifyDate&quot;));                user.setWorkPicPath(&quot;workPicPath&quot;);                System.out.println(user.toString());            &#125;        &#125; else &#123;            return null;        &#125;        //关闭链接和结果集        DB.close(conn,null,set);    &#125; catch (SQLException e) &#123;        throw new RuntimeException(e);    &#125; catch (ClassNotFoundException e) &#123;        throw new RuntimeException(e);    &#125;    return user;&#125;</code></pre><h3 id="修改用户实现">修改用户实现</h3><p>重复重复再重复…</p><pre><code class="language-js">$(&quot;.modifyUser&quot;).on(&quot;click&quot;,function()&#123;   var obj = $(this);   window.location.href=path+&quot;/jsp/user.do?method=modify&amp;uid=&quot;+ obj.attr(&quot;userid&quot;);&#125;);</code></pre><pre><code class="language-java">protected void modify(HttpServletRequest req, HttpServletResponse resp)throws ServletException, IOException&#123;    String uid = req.getParameter(&quot;uid&quot;);    service = new UserServiceImpl();    User user = service.findById(Integer.parseInt(uid));    req.setAttribute(&quot;user&quot;, user);    req.getRequestDispatcher(req.getContextPath()+&quot;/jsp/usermodify.jsp&quot;).forward(req, resp);&#125;</code></pre><p>和之前不同的是, 这次修改用户指向一个新的jsp页面, 也就是修改用户的页面. 也就是说, 删除用户的请求提交以后, 我们需要把这个请求转发到<code>/jsp/usermodify.jsp</code>这个URL对应的jsp页面去, 由于这个jsp页面需要修改前的user数据, 我们需要查询对应的id,得到user数据后把数据传递给<code>usermodify.jsp</code>这个jsp页面, jsp页面需要这些数据来向用户展示修改前的数据. 下面我们去看这个jsp页面</p><pre><code class="language-jsp">&lt;%@ page contentType=&quot;text/html;charset=UTF-8&quot; language=&quot;java&quot; %&gt;&lt;%@include file=&quot;/jsp/common/head.jsp&quot;%&gt;&lt;div class=&quot;right&quot;&gt;    &lt;div class=&quot;location&quot;&gt;        &lt;strong&gt;你现在所在的位置是:&lt;/strong&gt;        &lt;span&gt;用户管理页面 &gt;&gt; 用户修改页面&lt;/span&gt;    &lt;/div&gt;    &lt;div class=&quot;providerAdd&quot;&gt;        &lt;form id=&quot;userForm&quot; name=&quot;userForm&quot; method=&quot;post&quot; action=&quot;$&#123;pageContext.request.contextPath &#125;/jsp/user.do&quot;&gt;            &lt;input type=&quot;hidden&quot; name=&quot;method&quot; value=&quot;modifyexe&quot;&gt;            &lt;input type=&quot;hidden&quot; name=&quot;uid&quot; value=&quot;$&#123;user.id &#125;&quot;/&gt;            &lt;div&gt;                &lt;label for=&quot;userName&quot;&gt;用户名称：&lt;/label&gt;                &lt;input type=&quot;text&quot; name=&quot;userName&quot; id=&quot;userName&quot; value=&quot;$&#123;user.userName &#125;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label &gt;用户性别：&lt;/label&gt;                &lt;select name=&quot;gender&quot; id=&quot;gender&quot;&gt;                    &lt;c:choose&gt;                        &lt;c:when test=&quot;$&#123;user.gender == 1 &#125;&quot;&gt;                            &lt;option value=&quot;1&quot; selected=&quot;selected&quot;&gt;男&lt;/option&gt;                            &lt;option value=&quot;2&quot;&gt;女&lt;/option&gt;                        &lt;/c:when&gt;                        &lt;c:otherwise&gt;                            &lt;option value=&quot;1&quot;&gt;男&lt;/option&gt;                            &lt;option value=&quot;2&quot; selected=&quot;selected&quot;&gt;女&lt;/option&gt;                        &lt;/c:otherwise&gt;                    &lt;/c:choose&gt;                &lt;/select&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label &gt;出生日期：&lt;/label&gt;                &lt;input type=&quot;text&quot; Class=&quot;Wdate&quot; id=&quot;birthday&quot; name=&quot;birthday&quot; value=&quot;$&#123;user.birthday &#125;&quot;                       readonly=&quot;readonly&quot; onclick=&quot;WdatePicker();&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label &gt;用户电话：&lt;/label&gt;                &lt;input type=&quot;text&quot; name=&quot;phone&quot; id=&quot;phone&quot; value=&quot;$&#123;user.phone &#125;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label &gt;用户地址：&lt;/label&gt;                &lt;input type=&quot;text&quot; name=&quot;address&quot; id=&quot;address&quot; value=&quot;$&#123;user.address &#125;&quot;&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label &gt;用户角色：&lt;/label&gt;                &lt;!-- 列出所有的角色分类 --&gt;                &lt;input type=&quot;hidden&quot; value=&quot;$&#123;user.userRole&#125;&quot; id=&quot;rid&quot; /&gt;                &lt;select name=&quot;userRole&quot; id=&quot;userRole&quot;&gt;&lt;/select&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div class=&quot;providerAddBtn&quot;&gt;                &lt;input type=&quot;button&quot; name=&quot;save&quot; id=&quot;save&quot; value=&quot;保存&quot; /&gt;                &lt;input type=&quot;button&quot; id=&quot;back&quot; name=&quot;back&quot; value=&quot;返回&quot;/&gt;            &lt;/div&gt;        &lt;/form&gt;    &lt;/div&gt;&lt;/div&gt;&lt;/section&gt;&lt;%@include file=&quot;/jsp/common/foot.jsp&quot; %&gt;&lt;script type=&quot;text/javascript&quot; src=&quot;$&#123;pageContext.request.contextPath &#125;/statics/js/usermodify.js&quot;&gt;&lt;/script&gt;</code></pre><p>先观察表单提交的目的地, <code>&#123;pageContext.request.contextPath &#125;/jsp/user.do</code>, 对应一个Servlet, 但是这里并没有提交的按钮, 但是我发现这里有一个<code>usermodify.js</code>的脚本文件, 定位到这个脚本文件</p><pre><code class="language-js">var userName = null;var birthday = null;var phone = null;var userRole = null;var saveBtn = null;var backBtn = null;$(function()&#123;   userName = $(&quot;#userName&quot;);   birthday = $(&quot;#birthday&quot;);   phone = $(&quot;#phone&quot;);   userRole = $(&quot;#userRole&quot;);   saveBtn = $(&quot;#save&quot;);   backBtn = $(&quot;#back&quot;);      userName.next().html(&quot;*&quot;);   birthday.next().html(&quot;*&quot;);   phone.next().html(&quot;*&quot;);   userRole.next().html(&quot;*&quot;);         $.ajax(&#123;      type:&quot;GET&quot;,//请求类型      url:path+&quot;/jsp/user.do&quot;,//请求的url      data:&#123;method:&quot;getrolelist&quot;&#125;,//请求参数      dataType:&quot;json&quot;,//ajax接口（请求url）返回的数据类型      success:function(data)&#123;//data：返回数据（json对象）         if(data != null)&#123;            var rid = $(&quot;#rid&quot;).val();            userRole.html(&quot;&quot;);            var options = &quot;&lt;option value=\&quot;0\&quot;&gt;请选择&lt;/option&gt;&quot;;            for(var i = 0; i &lt; data.length; i++)&#123;               if(rid != null &amp;&amp; rid != undefined &amp;&amp; data[i].id == rid )&#123;                  options += &quot;&lt;option selected=\&quot;selected\&quot; value=\&quot;&quot;+data[i].id+&quot;\&quot; &gt;&quot;+data[i].roleName+&quot;&lt;/option&gt;&quot;;               &#125;else&#123;                  options += &quot;&lt;option value=\&quot;&quot;+data[i].id+&quot;\&quot; &gt;&quot;+data[i].roleName+&quot;&lt;/option&gt;&quot;;               &#125;                           &#125;            userRole.html(options);         &#125;      &#125;,      error:function(data)&#123;//当访问时候，404，500 等非200的错误状态码         validateTip(userRole.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo+&quot; 获取用户角色列表error&quot;,false);      &#125;   &#125;);         userName.on(&quot;focus&quot;,function()&#123;      validateTip(userName.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 用户名长度必须是大于1小于10的字符&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      if(userName.val() != null &amp;&amp; userName.val().length &gt; 1             &amp;&amp; userName.val().length &lt; 10)&#123;         validateTip(userName.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(userName.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo+&quot; 用户名输入的不符合规范，请重新输入&quot;,false);      &#125;         &#125;);      birthday.on(&quot;focus&quot;,function()&#123;      validateTip(birthday.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 点击输入框，选择日期&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      if(birthday.val() != null &amp;&amp; birthday.val() != &quot;&quot;)&#123;         validateTip(birthday.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(birthday.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 选择的日期不正确,请重新输入&quot;,false);      &#125;   &#125;);      phone.on(&quot;focus&quot;,function()&#123;      validateTip(phone.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 请输入手机号&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      var patrn=/^(13[0-9]|15[0-9]|18[0-9])\d&#123;8&#125;$/;      if(phone.val().match(patrn))&#123;         validateTip(phone.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(phone.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 您输入的手机号格式不正确&quot;,false);      &#125;   &#125;);      userRole.on(&quot;focus&quot;,function()&#123;      validateTip(userRole.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 请选择用户角色&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      if(userRole.val() != null &amp;&amp; userRole.val() &gt; 0)&#123;         validateTip(userRole.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(userRole.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo+&quot; 请重新选择用户角色&quot;,false);      &#125;         &#125;);      saveBtn.on(&quot;click&quot;,function()&#123;      userName.blur();      phone.blur();      birthday.blur();      userRole.blur();      if(userName.attr(&quot;validateStatus&quot;) == &quot;true&quot;          &amp;&amp; phone.attr(&quot;validateStatus&quot;) == &quot;true&quot;         &amp;&amp; birthday.attr(&quot;validateStatus&quot;) == &quot;true&quot;         &amp;&amp; userRole.attr(&quot;validateStatus&quot;) == &quot;true&quot;)&#123;         if(confirm(&quot;是否确认要提交数据？&quot;))&#123;            $(&quot;#userForm&quot;).submit();         &#125;      &#125;   &#125;);      backBtn.on(&quot;click&quot;,function()&#123;      //alert(&quot;modify: &quot;+referer);      if(referer != undefined          &amp;&amp; null != referer          &amp;&amp; &quot;&quot; != referer         &amp;&amp; &quot;null&quot; != referer         &amp;&amp; referer.length &gt; 4)&#123;       window.location.href = referer;      &#125;else&#123;         history.back(-1);      &#125;   &#125;);&#125;);</code></pre><p><strong>直接丢给chatGPT分析:</strong></p><ul><li><p>代码的核心部分是以下部分：</p><pre><code class="language-javascript">$.ajax(&#123;  type: &quot;GET&quot;,  url: path + &quot;/jsp/user.do&quot;,  data: &#123; method: &quot;getrolelist&quot; &#125;,  dataType: &quot;json&quot;,  success: function(data) &#123;    if (data != null) &#123;      var rid = $(&quot;#rid&quot;).val();      userRole.html(&quot;&quot;);      var options = &quot;&lt;option value=\&quot;0\&quot;&gt;请选择&lt;/option&gt;&quot;;      for (var i = 0; i &lt; data.length; i++) &#123;        if (rid != null &amp;&amp; rid != undefined &amp;&amp; data[i].id == rid) &#123;          options += &quot;&lt;option selected=\&quot;selected\&quot; value=\&quot;&quot; + data[i].id + &quot;\&quot; &gt;&quot; + data[i].roleName + &quot;&lt;/option&gt;&quot;;        &#125; else &#123;          options += &quot;&lt;option value=\&quot;&quot; + data[i].id + &quot;\&quot; &gt;&quot; + data[i].roleName + &quot;&lt;/option&gt;&quot;;        &#125;      &#125;      userRole.html(options);    &#125;  &#125;,  error: function(data) &#123;    validateTip(userRole.next(), &#123; &quot;color&quot;: &quot;red&quot; &#125;, imgNo + &quot; 获取用户角色列表error&quot;, false);  &#125;&#125;);</code></pre><p>这部分代码使用了 <code>$.ajax</code> 函数发起一个 GET 请求，请求的目标 URL 是 <code>path + &quot;/jsp/user.do&quot;</code>。请求的数据参数中包含了一个名为 <code>method</code> 的参数，其值为 <code>&quot;getrolelist&quot;</code>。预期的响应数据类型为 JSON。</p><p>在成功回调函数中，首先判断返回的数据 <code>data</code> 是否为非空。如果不为空，则继续执行后续逻辑。</p><p>通过获取 <code>$(&quot;#rid&quot;).val()</code> 获取一个表示角色的 ID 值。</p><p>接下来，清空 <code>userRole</code> 元素的内容，然后使用循环遍历返回的数据数组 <code>data</code>。对于每个数据项，根据其 ID 值和当前选中的角色 ID (<code>rid</code>) 进行比较。如果两者相等，说明该选项应该被选中，否则为普通选项。在循环过程中，动态生成了一段 HTML 字符串 <code>options</code>，包含了所有的选项。</p><p>最后，将生成的选项字符串设置为 <code>userRole</code> 元素的 HTML 内容，从而在页面上显示这些选项。</p><p>在错误回调函数中，对获取用户角色列表出错的情况进行处理，调用 <code>validateTip</code> 函数显示相应的错误提示信息。</p><p>该部分代码的核心功能是通过 AJAX 请求获取用户角色列表，并根据返回的数据动态生成下拉选项，用于用户在表单中选择角色。</p><p>所以, 我们需要一个servlet来处理这段ajax请求, 这个请求需要一个角色表</p></li></ul><p>,整理前端提供的数据, 以及前端需要的数据</p><ul><li><p>提供的数据:</p><ul><li><code>name=&quot;method&quot; value=&quot;modifyexe&quot;</code></li><li><code>name=&quot;uid&quot; value=&quot;$&#123;user.id &#125;</code></li><li><code>name=&quot;userName&quot; value=&quot;$&#123;user.userName &#125;</code></li><li><code>name=&quot;gender&quot;</code></li><li><code>name=&quot;birthday&quot; value=&quot;$&#123;user.birthday &#125;</code></li><li><code>name=&quot;phone&quot; value=&quot;$&#123;user.phone &#125;</code></li><li><code>name=&quot;address&quot;  value=&quot;$&#123;user.address &#125;</code></li><li><code>name=&quot;userRole&quot;</code></li></ul></li><li><p>需要的数据</p><ul><li><code>name=&quot;birthday&quot; value=&quot;$&#123;user.birthday &#125;</code></li><li><code>name=&quot;phone&quot; value=&quot;$&#123;user.phone &#125;</code></li><li><code>name=&quot;address&quot;  value=&quot;$&#123;user.address &#125;</code></li><li><code>name=&quot;uid&quot; value=&quot;$&#123;user.id &#125;</code></li><li><code>name=&quot;userName&quot; value=&quot;$&#123;user.userName &#125;</code></li><li>角色表</li></ul></li></ul><p><strong>下面我们来编写servlet</strong></p><ol><li><p>通过method的值对请求做不同处理, 这里有三个个请求, 分别是</p><ol><li><code>method=modify</code></li><li><code>method = getrolelist</code></li><li><code>metho=modidyexe</code></li></ol></li><li><p><code>method=modify</code> 这个请求当用户点击修改按钮时被触发, 目的是导航到用户修改页面, 以及返回修改用户的信息</p></li><li><p><code>method = getrolelist</code> 这个请求当用户跳转到用户修改页面触发, 目的是通过 AJAX 请求获取用户角色列表，并根据返回的数据动态生成下拉选项，用于用户在表单中选择角色。</p></li><li><p><code>metho=modidyexe</code>, 这个请求当用户提交修改信息的时候触发, 修改数据库内容</p><p>一个复用的Servlet, 用来响应对用户表进行操作的请求. (实际上组合查询了角色表)</p><pre><code class="language-java">protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;    String method = req.getParameter(&quot;method&quot;);    if(&quot;savepwd&quot;.equals(method))&#123;        savePwd(req, resp);    &#125; else if(&quot;pwdmodify&quot;.equals(method))&#123;        pwdModify(req, resp);    &#125; else if (&quot;query&quot;.equals(method)) &#123;        query(req, resp);    &#125; else if (&quot;add&quot;.equals(method)) &#123;        addUser(req, resp);    &#125; else if (&quot;getrolelist&quot;.equals(method)) &#123;        getRoleList(req, resp);    &#125; else if (&quot;ucexist&quot;.equals(method)) &#123;        //ajax后台验证--userCode是否已存在        //    //user.do?method=ucexist&amp;userCode=**        isUserCodeExit(req, resp);    &#125; else if (&quot;view&quot;.equals(method)) &#123;        view(req, resp);    &#125; else if (&quot;deluser&quot;.equals(method)) &#123;        delUser(req, resp);    &#125; else if (&quot;modifyexe&quot;.equals(method)) &#123;        modifyExe(req, resp);    &#125; else if (&quot;modify&quot;.equals(method)) &#123;        modify(req, resp);    &#125;&#125;</code></pre><p>​       ==getRoleList(req, resp)==</p><pre><code class="language-java">protected void getRoleList(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;    RoleServiceImpl roleService = new RoleServiceImpl();    List&lt;Role&gt; roleList = roleService.getRoleList(0);    resp.setContentType(&quot;application/json&quot;);    resp.setCharacterEncoding(&quot;UTF-8&quot;);    PrintWriter writer = resp.getWriter();    writer.write(JSONArray.toJSONString(roleList));    writer.flush();    writer.close();&#125;</code></pre><p>​        ==modify(req, resp);==</p><pre><code class="language-java">protected void modify(HttpServletRequest req, HttpServletResponse resp)throws ServletException, IOException&#123;    String uid = req.getParameter(&quot;uid&quot;);    service = new UserServiceImpl();    User user = service.findById(Integer.parseInt(uid));    req.setAttribute(&quot;user&quot;, user);    req.getRequestDispatcher(req.getContextPath()+&quot;/jsp/usermodify.jsp&quot;).forward(req, resp);&#125;</code></pre><p>​        ==modifyExe(req, resp);==</p><pre><code class="language-java">protected void modifyExe(HttpServletRequest req, HttpServletResponse resp) throws IOException, ServletException &#123;    // 1. 从前端获取参数: userCode userName password gender birthday phone address userRole    Map&lt;String, Object&gt; map = new HashMap&lt;&gt;();    String uid = req.getParameter(&quot;uid&quot;);    map.put(&quot;id&quot;,uid);    map.put(&quot;userName&quot;,req.getParameter(&quot;userName&quot;));    map.put(&quot;gender&quot;,Integer.parseInt(req.getParameter(&quot;gender&quot;)));    map.put(&quot;phone&quot;,req.getParameter(&quot;phone&quot;));    map.put(&quot;address&quot;,req.getParameter(&quot;address&quot;));    map.put(&quot;userRole&quot;,Integer.parseInt(req.getParameter(&quot;userRole&quot;)));    String birthday = req.getParameter(&quot;birthday&quot;);    Date date = null;    try &#123;        date = new SimpleDateFormat(&quot;yyyy-MM-dd&quot;).parse(birthday);        map.put(&quot;birthday&quot;,date);    &#125; catch (ParseException e) &#123;        throw new RuntimeException(e);    &#125;    // 2. 封装数据    User o = (User) req.getSession().getAttribute(Constant.USER_SESSION);    map.put(&quot;modifyBy&quot;,o.getId());    Date modifyDate = new Date();    map.put(&quot;modifyDate&quot;,new Date());    //3. 调用service    service = new UserServiceImpl();    for (String key : map.keySet()) &#123;        boolean res = service.updateUser(Integer.parseInt(uid), key, map.get(key));    &#125;    resp.sendRedirect(req.getContextPath()+&quot;/jsp/user.do?method=query&quot;);&#125;</code></pre><p>剩下的编写Service以及Dao层实现对数据库的操作, 整个功能就实现了. 这里不在叙述.</p><h2 id="尾巴">尾巴</h2><p>​到这里, 这个项目的搭建算是告一段落了. 在这个项目中, 我基本上是从零开始搭建. 说来惭愧, 现在大三下下学期已经过半, 学校的JavaEE也结课两周. 我对整个课程却没什么印象, 这当然也和我根本没去上几次课有关系, 除了实验课我基本就不去了. 这也不是我不想学, 只是觉得要是跟着老师学我会更糟. 老师上课完全不顾学生, 只将电脑的idea投屏到投影仪上便能自顾自的讲一节课. 投影仪本来亮度就不高, idea字体又小,完全不知道怎么听. 听过几次我就是在不想去了.</p><p>​加之又有考研的计划, 基本都呆在图书馆学习高数和计算机网络了, JavaEE基本上没怎么碰了, 连分组的实践也是完全丢下给小组其他同学了(小组实践能不能从大学消失啊). 小组其他几个也都是混子, 这留下我的旁边床铺的室友了, 这让我感觉实在很惭愧. 当然最后的结果也是压根没人管这个小组实践了, 直到deadline的前一天晚上, 这事才被重新提起. 当然也只能从github匆匆找一个应付交作业了.</p><p>​那么如果总结这半学期学到了什么的话, 只剩下把计算机网络算是完完整整的学完了, 不过这也是补了之前欠下的债了. 高数我已经接近一个月没有看过了, 现在也已经没什么印象了, 不知道考研要怎么办了.  我也已经做好了二战的准备了.</p><p>​写这个项目的契机, 也是学校又莫名其妙多了个课程设计, 相比学校的什么破实验实践, 我很喜欢学校的课程设计, 上次操作系统的实践设计是关于xv6的, 那次让我对操作系统的理解提升了很多, 现在我都不舍得删掉电脑里的qemu模拟器. 不过这和老师没什么关系, 老师基本上只是把课题扔在那, 我们自己独立完成. 但也同时我有了更多的时间去完成这个课程设计, 没有其他课程任务的干扰, 我可以全心全意的投入到这个课程设计中去. 这很符合我自己的学习习惯, 我更喜欢把一个课程学完了, 或者是一个阶段的任务完成了, 才去开始下一项学习计划. 相比于去课堂上听无意义的课程, 这种把时间留给学生自己的做法, 在这个大学老师基本上起不到什么帮助的破大学里, 显然是更值得的.</p><p>​说回写这个项目的契机, 也是学校的课程设计. 到这个时候所以的课程也已经结束了, 我本以为可以完完全全的开始自己的学习计划的时候, 来这么一个任务一开始我是蛮不开心的. 这次的任务是写一个图书管理系统, 狂神这个课程写了一个超市管理系统, 但是这些什么学生管理系统啊, 图书管理系统之类的, 都没区别, 随便改一改就可以.</p><p>​这次的课程设计分为了几个小组, 和其他小组不同的是, 我们小组的老师要求每个同学独立完成, 这让我很喜这个老师的做法. 而我们每天上课的内容也就是在自习室写自己的项目罢了, 除了早上八点要爬上11楼外, 我没什么抱怨的.</p><p>​接下来的学习计划, 我准备把CSAPP的内容补上, 这个从大一开始便一直计划要学的内容, 到现在也没开始, 我真是废物啊. 如果可以穿越回大一到话, 当然是带着现在的记忆穿越回去, 不然结局也是淹没在大学的无意义的课程中了. 不过带着记忆的话, 与其幻想这个不切实际的梦, 这个幻想其本质和我接下把自己缺失的补上是一样的.</p><p>​即使我对自己的能力不满意, 但我也确实没什么可以抱怨的, 毕竟这就是我, 这才是我. 除了暑假的时间顾着玩了, 大学的生活我的学习步伐一直都是踏踏实实的. 我不得不说我已经够努力了, 靠着我一个人, 在一个远离家乡的城市. 这个指望不上老师的地方, 我做到现在我已经和满意了, 从大一到现在的我, 以及学会了太多太多, 完完全全成长了很多, 学到很多. 剩下的遗憾, 留下没有一个好的环境, 一个良师益友引导我. 不知道这个遗憾能不能在研究生生活中补上呢, 至少现在先考上一个好多大学, 补完这些遗憾吧. 加油啊, 自己.</p><p>以下是改为图书管理系统的字段替换:</p><pre><code>供应商表-&gt; 图书ISBN：-&gt;provider.proCode书名：-&gt;provider.proName作者：-&gt;provider.proContact定价：-&gt;provider.proPhone 出版社-&gt;provider.proFax 分类：-&gt;provider.proDesc订单管理-&gt;借阅记录管理未归还 -&gt; queryIsPayment == 1 已归还-&gt; queryIsPayment == 2 用户：-&gt;queryProductName借阅记录编码-&gt;billCode图书 -&gt;providerName金额-&gt;totalPrice</code></pre></li></ol>]]></content>
    
    
    <summary type="html">写这个项目的契机, 也是学校又莫名其妙多了个课程设计, 相比学校的什么破实验实践, 我很喜欢学校的课程设计, 上次操作系统的实践设计是关于xv6的, 那次让我对操作系统的理解提升了很多, 现在我都不舍得删掉电脑里的qemu模拟器. 不过这和老师没什么关系, 老师基本上只是把课题扔在那, 我们自己独立完成. 但也同时我有了更多的时间去完成这个课程设计, 没有其他课程任务的干扰, 我可以全心全意的投入到这个课程设计中去. 这很符合我自己的学习习惯, 我更喜欢把一个课程学完了, 或者是一个阶段的任务完成了, 才去开始下一项学习计划. 相比于去课堂上听无意义的课程, 这种把时间留给学生自己的做法, 在这个大学老师基本上起不到什么帮助的破大学里, 显然是更值得的.</summary>
    
    
    
    <category term="Java web" scheme="https://trevoewu.github.io/categories/Java-web/"/>
    
    
    <category term="java web" scheme="https://trevoewu.github.io/tags/java-web/"/>
    
    <category term="jsp" scheme="https://trevoewu.github.io/tags/jsp/"/>
    
    <category term="servlet" scheme="https://trevoewu.github.io/tags/servlet/"/>
    
    <category term="jdbc" scheme="https://trevoewu.github.io/tags/jdbc/"/>
    
    <category term="fullstack" scheme="https://trevoewu.github.io/tags/fullstack/"/>
    
    <category term="smbms" scheme="https://trevoewu.github.io/tags/smbms/"/>
    
  </entry>
  
  <entry>
    <title>Java Web 快速入门</title>
    <link href="https://trevoewu.github.io/2023/05/12/Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/"/>
    <id>https://trevoewu.github.io/2023/05/12/Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/</id>
    <published>2023-05-12T09:45:29.000Z</published>
    <updated>2023-06-10T12:58:44.545Z</updated>
    
    <content type="html"><![CDATA[<h2 id="静态web和动态web">静态web和动态web</h2><p>静态Web和动态Web都是Web应用程序的不同类型，它们的主要区别在于页面内容是如何生成和呈现的。</p><p>静态Web是由静态网页组成的Web应用程序，网页的内容在页面请求时已经存在，并且不会随着用户的请求而发生变化。网页内容通常是由HTML、CSS、JavaScript等静态文件组成的，这些文件都是在服务器端预先生成的，当用户访问这些网页时，服务器只需将这些文件发送给用户的浏览器，浏览器将文件解析并显示给用户即可。静态Web通常包含少量的网页，内容不会发生频繁变化，适合于展示企业宣传网站、个人博客等简单的网站。</p><p>动态Web是由动态网页组成的Web应用程序，网页内容是在用户请求时根据请求参数和服务器端的处理生成的。网页内容可以根据用户的不同请求参数、数据库查询结果等实时生成。动态Web需要使用服务器端脚本语言（如Java、PHP、Python Servlet/JSP等）来生成HTML页面内容，通常使用数据库来存储和管理数据。动态Web可以实现更复杂的功能，例如在线商城、社交网络、新闻门户等。</p><p>总的来说，静态Web适合于内容不会频繁变化，网站规模较小的场景，可以使用简单的HTML、CSS、JavaScript等前端技术来实现；而动态Web适合于内容需要频繁变化、网站规模较大的场景，需要使用服务器端脚本语言和数据库来实现。</p><h2 id="web服务器">web服务器</h2><h3 id="Tomcat">Tomcat</h3><p>Web服务器是一种软件程序，主要用于处理客户端和服务器之间的HTTP请求和响应。它可以接收来自客户端（通常是Web浏览器）的HTTP请求，并返回处理后的HTTP响应。Web服务器通常运行在互联网上，提供各种各样的服务，例如Web网站、电子邮件服务、文件传输等。</p><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20230512175355319.png" alt="image-20230512175355319" loading="lazy"></p><p><a href="https://tomcat.apache.org/">Tomcat</a>是一种流行的开源Web服务器，它实现了Java Servlet和JavaServer Pages (JSP) 规范。Tomcat支持多种操作系统，包括Windows、Linux和Mac OS等，它提供了一个简单的、易于使用的管理界面，允许管理员对服务器进行配置和管理。</p><p>Tomcat的主要特点包括：</p><ol><li><p>支持Java Servlet和JSP规范：Tomcat可以运行和处理Java Web应用程序，例如Java Servlet、JSP和JavaServer Faces (JSF) 等。</p></li><li><p>可扩展性：Tomcat支持各种插件和扩展，可以根据需要添加新功能或修改现有功能。</p></li><li><p>易于配置和管理：Tomcat提供了一个简单的管理界面，允许管理员轻松地配置和管理Web服务器，包括添加或删除Web应用程序、配置访问权限等。</p></li><li><p>开源免费：Tomcat是一个开源项目，可以免费使用和修改。这意味着用户可以根据自己的需要进行定制和修改，而不需要支付高昂的授权费用。</p></li></ol><p>总的来说，Tomcat是一种功能强大的Web服务器，非常适合运行Java Web应用程序。它具有可扩展性、易于配置和管理、开源免费等优点，因此在企业和个人用户中非常受欢迎。</p><h4 id="Tomcat文件结构">Tomcat文件结构</h4><pre><code class="language-shell">cd ~/tomcat //tomcart 安装目录</code></pre><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20230512175943702.png" alt="image-20230512175943702" loading="lazy"></p><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20230512180405774.png" alt="image-20230512180405774" loading="lazy"></p><p>访问测试：<a href="http://localhost:8080">http://localhost:8080</a></p><p>默认端口8080</p><h4 id="配置文件">配置文件</h4><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20230512180642215.png" alt="image-20230512180642215" loading="lazy"></p><ul><li>可以修改配置的启动端口号, 默认为8080</li></ul><pre><code class="language-xml">    &lt;Connector port=&quot;8080&quot; protocol=&quot;HTTP/1.1&quot;               connectionTimeout=&quot;20000&quot;               redirectPort=&quot;8443&quot; /&gt;</code></pre><ul><li><p>可以配置主机的名称, 默认网站的存放位置</p><ul><li>name=“locahost” 默认主机名对于的ip地址 127.0.0.1</li><li>默认网站存放地址: webapps</li></ul><pre><code class="language-xml"> &lt;Host name=&quot;localhost&quot;  appBase=&quot;webapps&quot;            unpackWARs=&quot;true&quot; autoDeploy=&quot;true&quot;&gt;</code></pre></li></ul><h4 id="发布一个网站">发布一个网站</h4><ul><li>将自己写的网站，放到服务器(Tomcat)中指定的web应用的文件夹（webapps）下，就可以访问了。</li></ul><h3 id="HTTP">HTTP</h3><p>HTPP 请求</p><pre><code class="language-json">Request URL:https://www.baidu.com/   请求地址Request Method:GET    get方法/post方法Status Code:200 OK    状态码：200Remote（远程） Address:14.215.177.39:443Accept:text/html  客户端可以接受的语言Accept-Encoding:gzip, deflate, br 编码Accept-Language:zh-CN,zh;q=0.9    语言Cache-Control:max-age=0 是否设置缓存Connection:keep-alive 是否为持续性链接</code></pre><h2 id="Servlet">Servlet</h2><h3 id="简介">简介</h3><ul><li>Servlet就是sun公司开发动态web的一门技术。</li><li>Sun在这些API中提供一个接口叫做：Servlet，如果你想开发一个Servlet程序，只需要完成两个小步骤：<ul><li>编写一个类，实现Servlet接口。</li><li>把开发好的Java类部署到web服务器中。</li></ul></li><li><strong>把实现了Servlet接口的Java程序叫做，Servlet</strong>。</li></ul><h3 id="第一个Servlet程序-HelloServlet">第一个Servlet程序 HelloServlet</h3><ol><li>修改web.xml为最新的</li></ol><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;web-app xmlns=&quot;http://xmlns.jcp.org/xml/ns/javaee&quot;         xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;         xsi:schemaLocation=&quot;http://xmlns.jcp.org/xml/ns/javaee                      http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd&quot;         version=&quot;4.0&quot;         metadata-complete=&quot;true&quot;&gt;&lt;/web-app&gt;</code></pre><ol start="2"><li>编写一个Servlet程序。</li></ol><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20230512182111384.png" alt="image-20230512182111384" loading="lazy"></p><ol start="3"><li><p>编写一个普通类Bean</p></li><li><p>实现servlet接口, 这里我们直接继承HttpServlet</p><pre><code class="language-java">public class HelloServlet extends HttpServlet &#123;    // 由于get或post只是请求实现的不同的方式，可以相互调用，业务逻辑都是一样；  //HttpServletRequest代表客户端的请求，用户通过Http协议访问服务器，//HTTP请求中的所有信息会被封装到HttpServletRequest，//通过这个HttpServletRequest的方法，获得客户端的所有信息；    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        PrintWriter writer = resp.getWriter();  // 响应流        writer.print(&quot;Hello,Serlvet&quot;);      writer.flush()    &#125;</code></pre></li></ol><h4 id="write-和println">write( )和println( )</h4><p><code>servletResponse.getWriter().write()``servletResponse.getWriter().println()</code>都是用于将字符串写入到HTTP响应中的方法，但是它们之间有一些关键的区别。</p><p><code>servletResponse.getWriter().write()</code>方法是将指定的字符串写入到HTTP响应输出流中，它不会在字符串末尾添加任何换行符或其他字符。如果需要在输出的字符串后面添加换行符或其他字符，就需要在字符串后面显式添加这些字符。</p><p><code>servletResponse.getWriter().println()</code>方法也是将指定的字符串写入到HTTP响应输出流中，但是它会在字符串末尾自动添加一个换行符。这个换行符的样式和操作系统相关，通常是<code>\n</code>或<code>\r\n</code>。使用<code>println()</code>方法可以让每个输出的字符串都独占一行，使得输出更易于阅读和处理。</p><p>因此，<code>write()</code>方法适合于需要将多个字符串写入到同一行的情况，例如输出CSV格式的数据或者HTML代码的片段。而<code>println()</code>方法适合于需要输出多行字符串的情况，例如输出日志信息或HTML代码的整个页面。</p><p>需要注意的是，无论使用哪种方法，都需要在使用完之后调用<code>servletResponse.getWriter().flush()</code>方法将输出流刷新并将数据发送到客户端。如果不调用<code>flush()</code>方法，可能会导致响应数据无法正常发送到客户端。</p><ol start="5"><li><p>编写Servlet的映射</p><ul><li>为什么需要映射：我们写的是JAVA程序，但是要通过浏览器访问，而浏览器需要连接web服务器，所以我们需要再web服务中注册我们写的Servlet，还需给他一个浏览器能够访问的路径；</li></ul><pre><code class="language-xml">    &lt;!--注册Servlet--&gt;    &lt;servlet&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;servlet-class&gt;cn.loulan.servlet.HelloServlet&lt;/servlet-class&gt;    &lt;/servlet&gt;    &lt;!--Servlet的请求路径--&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;</code></pre></li></ol><h4 id="Servlet-Mapping">Servlet Mapping</h4><ol><li><p>一个Servlet可以指定一个映射路径。</p><pre><code class="language-xml">    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;</code></pre></li><li><p>一个Servlet可以指定多个映射路径。</p><pre><code class="language-xml">    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello2&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello3&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello4&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello5&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;</code></pre></li><li><p>一个Servlet可以指定通用映射路径。</p><pre><code class="language-xml">    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/hello/*&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;</code></pre></li><li><p>默认请求路径。</p><pre><code class="language-xml">    &lt;!--默认请求路径--&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;/*&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;</code></pre></li><li><p>指定一些后缀或者前缀等等….</p><pre><code class="language-xml">    &lt;!-- 可以自定义后缀实现请求映射         注意点，*前面不能加项目映射的路径         hello/subei.github        --&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;hello&lt;/servlet-name&gt;        &lt;url-pattern&gt;*.github&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;</code></pre></li><li><p>优先级问题。</p><ul><li>指定了固有的映射路径优先级最高，如果找不到就会走默认的处理请求；</li></ul></li></ol><h3 id="Servlet-Context">Servlet Context</h3><ul><li>web容器在启动的时候，它会为每个web程序都创建一个对应的ServletContext对象，它代表了当前的web应用(Application)；</li></ul><p>ServletContext是Java Web应用程序的一个接口，它提供了一个Web应用程序的运行环境，可以访问应用程序的配置信息、资源、请求和会话等。每个Web应用程序都有一个ServletContext实例，可以通过getServletContext()方法获取。</p><p>ServletContext提供了以下功能：</p><ol><li><p>应用程序作用域：ServletContext对象是一个应用程序级别的对象，可以在整个应用程序范围内共享数据。</p></li><li><p>应用程序配置信息：ServletContext对象提供了访问Web应用程序的配置信息的方法，例如Web.xml中配置的上下文参数，初始化参数等。</p></li><li><p>资源访问：ServletContext对象提供了一些方法，例如getRealPath()和getResourceAsStream()，可以访问Web应用程序中的资源文件。</p></li><li><p>请求转发：ServletContext对象提供了getRequestDispatcher()方法，可以将请求转发到其他Web组件，例如Servlet或JSP。</p></li><li><p>会话管理：ServletContext对象提供了创建和管理会话的方法，例如getSession()和getSessionCookieConfig()。</p></li></ol><p>ServletContex为Java Web应用程序提供了一个环境，使开发人员可以访问应用程序的配置信息、资源、请求和会话等，并且可以将请求转发到其他Web组件。这使得Java Web应用程序开发变得更加容易和灵活。</p><h4 id="利用servletContext实现不同Servlet之前的数据共享">利用servletContext实现不同Servlet之前的数据共享</h4><ul><li>在这个Servlet中保存的数据，可以在另外一个servlet中拿到；</li></ul><pre><code class="language-java">public class HelloServlet extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        ServletContext context = this.getServletContext();        String name = &quot;学习超好&quot;;   // 数据        context.setAttribute(&quot;name&quot;,name);    &#125;&#125;public class GetServlet extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        ServletContext context = this.getServletContext();        String name = (String)context.getAttribute(&quot;name&quot;);        resp.setContentType(&quot;text/html&quot;);        resp.setCharacterEncoding(&quot;utf-8&quot;);        resp.getWriter().print(&quot;名字:&quot;+name);    &#125;</code></pre><h4 id="利用Servlet获取初始化参数">利用Servlet获取初始化参数</h4><pre><code class="language-xml">&lt;!-- 配置一些Web应用初始化参数 --&gt;    &lt;context-param&gt;        &lt;param-name&gt;url&lt;/param-name&gt;        &lt;param-value&gt;jdbc:mysql://localhost:3306/mybatis&lt;/param-value&gt;    &lt;/context-param&gt;    &lt;servlet&gt;        &lt;servlet-name&gt;gp&lt;/servlet-name&gt;        &lt;servlet-class&gt;com.github.servlet.ServletDemo03&lt;/servlet-class&gt;    &lt;/servlet&gt;    &lt;servlet-mapping&gt;        &lt;servlet-name&gt;gp&lt;/servlet-name&gt;        &lt;url-pattern&gt;/gp&lt;/url-pattern&gt;    &lt;/servlet-mapping&gt;</code></pre><pre><code class="language-java">public class ServletDemo03 extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        ServletContext context = this.getServletContext();        String url = context.getInitParameter(&quot;url&quot;);        resp.getWriter().print(url);    &#125;</code></pre><h4 id="实现请求的转发">实现请求的转发</h4><ol start="2"><li>利用ServletContext实现请求的转发</li></ol><pre><code class="language-java">public class ServletDemo04 extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        ServletContext context = this.getServletContext();//        调用forward实现请求转发;//        requestDispatcher.forward(req,resp);      context.getRequestDispatcher(&quot;url&quot;).forward(req,resp);    &#125;</code></pre><h4 id="用来读取资源文件">用来读取资源文件</h4><ul><li><p>在resources目录下新建properties</p></li><li><p>思路：需要一个文件流；</p></li></ul><pre><code class="language-properties">username=&quot;楼兰&quot;</code></pre><pre><code class="language-java">public class ServletDemo05 extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws IOException &#123;       InputStream stream = this.getServletContext().getResourceAsStream(&quot;WEB-INF/classes/db.properties&quot;);        Properties prop = new Properties();        prop.load(is); String user = prop.getProperty(&quot;username&quot;);//        但是如果配置文件中如果包含中文，就会出现乱码，所以可以通过中转的形式优化一下：        prop.load(new InputStreamReader(stream,&quot;UTF-8&quot;));        resp.getWriter().print(user);    &#125;</code></pre><h3 id="HttpServletResponse">HttpServletResponse</h3><ul><li>web服务器接收到客户端的http请求，针对这个请求，分别创建一个代表请求的HttpServletRequest对象，代表响应的一个HttpServletResponse；<ul><li>如果要获取客户端请求过来的参数：找HttpServletRequest；</li><li>如果要给客户端响应一些信息：找HttpServletResponse。</li></ul></li></ul><h4 id="1-简单分类">1.简单分类</h4><ul><li>负责向浏览器发送数据的方法。</li></ul><pre><code class="language-java">ServletOutputStream getOutputStream();PrintWriter getWriter();</code></pre><ul><li>负责向浏览器发送响应头的方法。</li></ul><pre><code class="language-java">    void setCharacterEncoding(String var1);    void setContentType(String var1);</code></pre><ul><li>响应的状态码。</li></ul><pre><code class="language-java">    int SC_OK = 200;    int SC_MOVED_PERMANENTLY = 301;    int SC_NOT_FOUND = 404;    int SC_INTERNAL_SERVER_ERROR = 500;</code></pre><ul><li>200 - 请求成功</li><li>301 - 资源（网页等）被永久转移到其它URL</li><li>404 - 请求的资源（网页等）不存在</li><li>500 - 内部服务器错误</li></ul><h4 id="2-下载文件">2.下载文件</h4><ol><li>向浏览器输出消息；</li><li>下载文件：<ol><li>要获取下载文件的路径；</li><li>下载的文件名是啥？</li><li>设置想办法让浏览器能够支持下载我们需要的东西；</li><li>获取下载文件的输入流；</li><li>创建缓冲区；</li><li>获取OutputStream对象；</li><li>将FileOutputStream流写入到buffer缓冲区；</li><li>使用OutputStream将缓冲区中的数据输出到客户端！</li></ol></li></ol><pre><code class="language-java">public class FIleServlet extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;//        1. 获取下载文件的路径；    String url = &quot;/Users/trevorwu/Maven_03/HelloServlet/src/main/resources/Peekaboo.png&quot;;//        2. 获取文件名        String fileName = &quot;downFile.png&quot;;//        3. 让浏览器能够支持下载我们需要的东西；//        这段代码是用来设置 HTTP 响应头，将服务器返回的内容作为文件附件（attachment）下载，而非直接在浏览器中打开。//        具体地，`resp.setHeader()` 方法设置了一个名为 &quot;Content-Disposition&quot; 的响应头，//        其值为 &quot;attachment;filename=xxxxx&quot;，其中 &quot;xxxxx&quot; 是要下载的文件名。//        这个文件名可以是服务器上的文件名，也可以是通过代码生成的文件名。//        这个文件名决定了用户下载得到的文件名， 而非服务器上的文件名， 服务器的文件名在下载文件的地址中指定        resp.setHeader(&quot;Content-Disposition&quot;,&quot;attachment;filename=&quot;                + URLEncoder.encode(fileName,&quot;UTF-8&quot;));//        4. 获取下载文件的输入流；        FileInputStream in = new FileInputStream(url);//        5. 创建缓冲区；        int len = 0;        byte buffer[] = new byte[1024];//        6. 获取OutputStream对象；        ServletOutputStream out = resp.getOutputStream();//        7. 将FileOutputStream流写入到buffer缓冲区；//        这段代码是将输入流 `in` 中的数据读取到缓冲区 `buffer` 中，再通过输出流 `out` 输出到目标位置。//        具体地，循环读取操作会在输入流中读取数据，并将数据写入到缓冲区中，//        当读取到的数据长度为0时，说明已经读取完毕，退出循环。//在每次循环中，通过调用 `out.write(buffer, 0, len)` 方法，将缓冲区中的数据写入输出流中。// 这里的 `buffer` 是一个字节数组，`len` 是每次读取的字节数。// 第二个参数 `0` 表示从缓冲区的第一个字节开始写入，而不是从数组的第一个元素开始写入。//通过这段代码，我们可以实现从输入流中读取数据，然后将数据输出到目标位置的操作，通常用于文件下载或数据传输等场景。        while ((len=in.read(buffer)) &gt; 0)&#123;            out.write(buffer,0,len);        &#125;//        close input output stream        in.close();        out.close();    &#125;</code></pre><h4 id="3-验证码功能">3.验证码功能*</h4><ul><li>验证怎么来的？<ul><li>前端实现；</li><li>后端实现，需要用到 Java 的图片类，生产一个图片。</li></ul></li></ul><pre><code class="language-java">public class ImageServlet extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws IOException &#123;//        如何让浏览器3秒自动刷新一次;        resp.setHeader(&quot;refresh&quot;,&quot;3&quot;);//        在内存中创建一个图片        BufferedImage image = new BufferedImage(90,40,BufferedImage.TYPE_INT_RGB);//        得到图片,笔        Graphics2D g = (Graphics2D) image.getGraphics();//        设置图片的背景颜色        g.setColor(Color.white);        g.fillRect(0,0,90,40);//        给图片写数据        g.setColor(Color.RED);        g.setFont(new Font(null,Font.BOLD,20));        g.drawString(makeNum(),8,30);//        告诉浏览器，这个请求用图片的方式打开        resp.setContentType(&quot;image/jpeg&quot;);//        网站存在缓存，不让浏览器缓存        resp.setDateHeader(&quot;expires&quot;,-1);        resp.setHeader(&quot;Cache-Control&quot;,&quot;no-cache&quot;);        resp.setHeader(&quot;Pragma&quot;,&quot;no-cache&quot;);//        把图片写给浏览器        ImageIO.write(image,&quot;jpg&quot;, resp.getOutputStream());    &#125;//    生成随机数    private String makeNum()&#123;        Random random = new Random();        String num = random.nextInt(9999999) + &quot;&quot;;        StringBuffer sb = new StringBuffer();        for (int i = 0; i &lt; 7-num.length() ; i++) &#123;            sb.append(&quot;0&quot;);        &#125;        num = sb.toString() + num;        return num;    &#125;</code></pre><h4 id="4-实现重定向">4.实现重定向</h4><p>之前我们学过利用Servlet实现请求的转发, 这里我们利用response来实现请求重定向</p><ol><li>重定向和转发的区别</li></ol><p>重定向和转发是在Java Servlet编程中常用的两种将请求从一个Servlet转发到另一个Servlet或JSP的方式，它们有一些关键的区别。</p><p>重定向（Redirect）是指当一个Servlet收到一个请求时，它会发送一个HTTP响应告诉浏览器发送一个新的请求到指定的URL。这个URL可以是一个Servlet、JSP、HTML页面或其他任何类型的资源。因此，重定向会导致浏览器向指定的URL发起一个全新的请求，而这个新请求的路径是由浏览器在重定向时自动发送的，不再是原始请求的路径。在重定向中，所有请求参数都会丢失，因此需要通过查询字符串或Session等方式传递参数。重定向可以用于将用户重定向到一个新的页面，或者将用户从一个应用程序重定向到另一个应用程序。</p><p>转发（Forward）是指当一个Servlet收到一个请求时，它会将请求转发到另一个Servlet或JSP来处理。在转发中，请求和响应对象是同一个，只是Servlet容器将请求转发给了另一个资源来生成响应。因此，在转发中，所有请求参数都会保留，并且可以在目标Servlet或JSP中访问。转发通常用于将请求从一个Servlet传递到另一个Servlet或JSP来生成响应，以便可以在请求和响应之间共享数据。</p><p>因此，重定向和转发都可以将请求从一个Servlet转发到另一个Servlet或JSP来生成响应，但它们的区别在于重定向会发送一个新的请求到指定的URL，而转发则将请求转发到另一个Servlet或JSP来处理，响应由目标Servlet或JSP生成。重定向导致的性能开销比转发更高，因为它需要向浏览器发送一个新的请求，而转发则在服务器内部进行，无需向浏览器发送任何内容。</p><pre><code class="language-java">public class RedirectServlet extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        resp.sendRedirect(&quot;/response/img&quot;);    // 重定向    &#125;</code></pre><h4 id="5-简单实现登录重定向">5.简单实现登录重定向</h4><pre><code class="language-java">public class Log extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123; this.getServletContext().getRequestDispatcher(&quot;/hello&quot;).forward(req, resp);    &#125;</code></pre><p>这段代码用于Java Servlet编程中，将一个请求从一个Servlet转发到同一Web应用程序中的另一个Servlet或JSP（Java服务器页面）。</p><p><code>getRequestDispatcher()</code>方法的参数是请求转发到的资源的相对或绝对路径。在本例中，路径为“/hello”，这是另一个Servlet.</p><p>然后调用<code>RequestDispatcher</code>对象的<code>forward()</code>方法，传递<code>req</code>和<code>resp</code>参数。这些是传递到原始Servlet的<code>doGet()</code>或<code>doPost()</code>方法中的<code>HttpServletRequest</code>和<code>HttpServletResponse</code>对象。<code>forward()</code>方法将这些对象发送到指定的资源，并允许该资源生成要发送回客户端的响应。</p><p>总之，这段代码将一个请求从当前Servlet转发到同一Web应用程序中的另一个Servlet或JSP，同时传递<code>HttpServletRequest</code>和<code>HttpServletResponse</code>对象。目标资源将生成要发送回客户端的响应。</p><h3 id="HttpServletRequest">HttpServletRequest</h3><ul><li>HttpServletRequest代表客户端的请求，用户通过Http协议访问服务器，HTTP请求中的所有信息会被封装到HttpServletRequest，通过这个HttpServletRequest的方法，获得客户端的所有信息；</li></ul><pre><code class="language-java">//        HttpServletRequest代表客户端的请求，用户通过Http协议访问服务器，//        HTTP请求中的所有信息会被封装到HttpServletRequest，//        通过这个HttpServletRequest的方法，获得客户端的所有信息；        String username = req.getParameter(&quot;username&quot;);        String password = req.getParameter(&quot;password&quot;);        String host = req.getHeader(&quot;Host&quot;);        resp.getWriter().println(&quot;username:&quot;+username+&quot; password:&quot;+password+&quot; host:&quot;+host);//        `req.getContextPath()` 是一个 Java Servlet API 中的方法，它返回当前 Servlet 的上下文路径。//        上下文路径是指 Web 应用程序的 URL 中，主机名和端口号后的第一个路径。//例如，如果 Web 应用程序的 URL 是 `http://example.com:8080/myapp/index.jsp`，// 那么上下文路径就是 `/myapp`。        resp.getWriter().println(&quot;Context path&quot;+req.getContextPath());       </code></pre><h3 id="保存会话的两种技术">保存会话的两种技术</h3><h4 id="Cookies">Cookies</h4><img src="https://cdn4.iconfinder.com/data/icons/web-hosting-pack-vol-1/512/HTTP_Cookie-512.png" alt="Cookie, http, cookies, internet cookies, web cookies icon - Download on  Iconfinder" style="zoom:25%;" / loading="lazy"><p><strong>HTTP cookie</strong>，简称<strong>cookie</strong>，又称“<strong>网站／浏览＋魔饼／魔片</strong>”等，是浏览<a href="https://zh.wikipedia.org/wiki/%E7%BD%91%E7%AB%99">网站</a>时由<a href="https://zh.wikipedia.org/wiki/%E7%BD%91%E7%BB%9C%E6%9C%8D%E5%8A%A1%E5%99%A8">网络服务器</a>创建并由<a href="https://zh.wikipedia.org/wiki/%E7%BD%91%E9%A1%B5%E6%B5%8F%E8%A7%88%E5%99%A8">网页浏览器</a>存放在用户计算机或其他设备的小文本文件。</p><p>Cookie使Web服务器能在用户的设备存储状态信息（如添加到在线商店购物车中的商品）或跟踪用户的浏览活动（如点击特定按钮、登录或记录历史.</p><p>cookie 是服务器在 HTTP 响应中发送的一段数据。客户端（可选）存储 cookie 并在后续请求中返回它。这允许客户端和服务器共享状态。要设置 cookie，服务器会在响应中包含一个 Set-Cookie 标头。cookie 的格式是名称-值对，带有可选属性。例如：</p><pre><code class="language-powershell">Set-Cookie: session-id=1234567</code></pre><p>这是一个带有属性的示例：</p><pre><code class="language-powershell">Set-Cookie: session-id=1234567; max-age=86400; domain=example.com; path=/;</code></pre><p>为了向服务器返回 cookie，客户端在以后的请求中包含一个 Cookie 标头。</p><pre><code class="language-shell">Cookie: session-id=1234567</code></pre><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image1.png" alt="HTTP Cookies in ASP.NET Web API - ASP.NET 4.x | Microsoft Learn" loading="lazy"></p><h4 id="利用Cookies记录用户上一次登录时间">利用Cookies记录用户上一次登录时间</h4><pre><code class="language-java">public class Cookie extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;//get all cookies        javax.servlet.http.Cookie[] cookies = req.getCookies();        for (javax.servlet.http.Cookie cookie : cookies) &#123;            String name = cookie.getName();            if(name.equals(&quot;lastLoginTime&quot;))&#123;                String value = cookie.getValue();                long parseLong = Long.parseLong(cookie.getValue());                Date date = new Date(parseLong);                resp.getWriter().println(&quot;lastLoginTime: &quot;+date.toLocaleString());            &#125;        &#125;        javax.servlet.http.Cookie cookie = new javax.servlet.http.Cookie(&quot;lastLoginTime&quot;, System.currentTimeMillis() + &quot;&quot;);        resp.addCookie(cookie);    &#125;</code></pre><h4 id="session"><strong>session</strong></h4><p>在<a href="https://zh.wikipedia.org/wiki/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%A7%91%E5%AD%A6">计算机科学</a>领域来说，尤其是在<a href="https://zh.wikipedia.org/wiki/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C">网络</a>领域，<strong>会话</strong>（英语：session，<a href="https://zh.wikipedia.org/wiki/Microsoft_Windows">Microsoft Windows</a>译作<strong>工作阶段</strong>、<strong>连线阶段</strong>）是一种持久网络协议，在用户（或用户代理）端和服务器端之间创建关联，从而起到交换数据包的作用机制，session在<a href="https://zh.wikipedia.org/wiki/%E7%BD%91%E7%BB%9C%E5%8D%8F%E8%AE%AE">网络协议</a>（例如<a href="https://zh.wikipedia.org/wiki/Telnet">telnet</a>或<a href="https://zh.wikipedia.org/wiki/FTP">FTP</a>）中是非常重要的部分。</p><p>在不包含<a href="https://zh.wikipedia.org/wiki/%E4%BC%9A%E8%AF%9D%E5%B1%82">会话层</a>（例如<a href="https://zh.wikipedia.org/wiki/%E7%94%A8%E6%88%B7%E6%95%B0%E6%8D%AE%E6%8A%A5%E5%8D%8F%E8%AE%AE">UDP</a>）或者是无法长时间驻留会话层（例如<a href="https://zh.wikipedia.org/wiki/HTTP">HTTP</a>）的传输协议中，会话的维持需要依靠在传输数据中的高级别程序。例如，在浏览器和远程主机之间的HTTP传输中，HTTP cookie就会被用来包含一些相关的信息，例如session ID，参数和权限信息等。</p><ul><li><p>服务器技术，利用这个技术，可以保存用户的会话信息？ 我们可以把信息或者数据放在Session中！</p></li><li><p>服务器会给每一个用户（浏览器）创建一个Seesion对象；</p></li><li><p>一个Seesion独占一个浏览器，只要浏览器没有关闭，这个Session就存在；</p></li><li><p>用户登录之后，整个网站它都可以访问！–&gt; 保存用户的信息；保存购物车的信息……</p></li></ul><h4 id="Session和cookie的区别：">Session和cookie的区别：</h4><p>Cookie和Session是Web应用程序中常用的两种用于保存用户状态信息的机制。</p><p>相同点：</p><ol><li><p>都是用于在Web应用程序中保存用户状态信息的机制。</p></li><li><p>都可以在多个请求之间保持用户的状态信息，使用户在浏览网站时能够保持状态的连续性。</p></li><li><p>都可以在一定程度上提高Web应用程序的性能，减少对服务器的负载。</p></li></ol><p>不同点：</p><ol><li><p>存储位置不同：Cookie保存在客户端的浏览器中，而Session保存在服务器端的内存或者硬盘中。</p></li><li><p>存储容量不同：Cookie的存储容量比Session小。</p></li><li><p>安全性不同：由于Session数据存储在服务器端，因此相对于Cookie更加安全，Cookie数据容易被篡改或盗用。</p></li><li><p>过期时间不同：Session的过期时间可以由开发人员在代码中设置，而Cookie的过期时间可以在创建Cookie时设置，也可以在浏览器中手动删除。</p></li><li><p>使用场景不同：Cookie适用于需要长期保存用户状态信息的场景，例如保存用户的登录状态、偏好设置等；而Session适用于需要在短时间内保存用户状态信息的场景，例如购物车、会话信息等。</p></li></ol><h4 id="利用Session返回客户端一个Person对象-并在浏览器上打印">利用Session返回客户端一个Person对象, 并在浏览器上打印</h4><pre><code class="language-java">public class Session extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        HttpSession session = req.getSession();        session.setAttribute(&quot;person&quot;,new Person(&quot;Alice&quot;, &quot;Alice@gmail.com&quot;));        resp.getWriter().write(&quot;Session id&quot;+session.getId());        Person person = (Person) session.getAttribute(&quot;person&quot;);        resp.getWriter().write(&quot;name&quot;+person.toString());    &#125;          //client 每次访问服务器都会在服务器端维护一个Session, 并向用户以Cookie的方式，发送Session id,//        但是对于client而言， 这个Session id 是以Cookie的形式存储在浏览器上的， 所以更换浏览器访问后Cookie改变了//        所以，服务器会认为这是一个新的Session.//        下一次用户再次访问的时候就会拿着这个Session id 访问Serve， Server就知道这个客户的信息//        1. 拿到Session， 这个Cookie是由客户端发送给服务器的//        Session 可以使用setAttribute方法来保存信息//        不同于Cookie，Session 可以保存一个对象(Object)， 而不是仅仅是一个String</code></pre><p><strong>会话自动过期：web.xml配置</strong>！</p><pre><code class="language-xml">&lt;!--设置Session默认的失效时间--&gt;&lt;session-config&gt;    &lt;!--15分钟后Session自动失效，以分钟为单位--&gt;    &lt;session-timeout&gt;15&lt;/session-timeout&gt;&lt;/session-config&gt;</code></pre><h3 id="JSP-Java-Server-Pages">JSP (Java Server Pages)</h3><p><strong>JSP</strong>（全称<strong>J</strong>akarta <strong>S</strong>erver <strong>P</strong>ages，曾称为<strong>J</strong>ava<strong>S</strong>erver <strong>P</strong>ages）是由<a href="https://zh.wikipedia.org/wiki/Sun_Microsystems">Sun Microsystems</a>公司主导创建的一种动态网页技术标准。</p><p><strong>Java Server Pages ： Java服务器端页面，也和Servlet一样，用于动态Web技术！</strong></p><ul><li>最大的特点：<ul><li>写JSP就像在写HTML；</li><li>区别：<ul><li>HTML只给用户提供静态的数据；</li><li>JSP页面中可以嵌入JAVA代码，为用户提供动态数据；</li></ul></li></ul></li></ul><h4 id="JSP原理">JSP原理</h4><p>思路：JSP到底怎么执行的！</p><ul><li><p>服务器内部工作：</p><ul><li>tomcat中有一个work目录；</li></ul></li><li><p>IDEA中使用Tomcat的会在IDEA的tomcat中生产一个work目录；</p><p>IDEA会为每个Tomcat Webapps创建一个文件夹,用来存放这个APP的配置文件, 工作路径, 在我的MAC路径<code>/Users/trevorwu/Library/Caches/JetBrains/IntelliJIdea2022.3/tomcat/47ad80c0-0fbd-41b1-a4ae-e036ca41705b</code></p><p>我们进入这个路径的最深处</p><p>`cd Catalina/localhost/ROOT/org/apache/jsp</p><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20230512202849320.png" alt="image-20230512202849320" loading="lazy"></p><p>可以看到这里面有两个文件, 一个是<code>index_jsp.class</code> 另一个是<code>index_jsp.java` 很明显, `index_jsp.class`就是</code>index_jsp.java` 编译生成的class文件.</p><p>这是只有一个这样的class, 也就是我们的主页index.jsp</p><img src="Java-Web-快速入门/image-20230512203239731.png" alt="image-20230512203239731" style="zoom:33%;" / loading="lazy"></li></ul><p>也就是说, 如果我们访问index.jsp, 相当于访问根目录<code>localhost:8080/</code> 服务器就会生成对应的.java文件, 以及.class文件用来处理请求. 这个.java文件本质上就是Servlet. 如果这时候我们访问别的页面, 就会生成新的Servlet.</p><ul><li><strong>浏览器向服务器发送请求，不管访问什么资源，其实都是在访问Servlet！</strong></li><li>JSP最终也会被转换成为一个Java类！</li><li><strong>JSP 本质上就是一个Servlet</strong>！</li></ul><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L20wXzQ2MTUzOTQ5,size_16,color_FFFFFF,t_70.png" alt="在这里插入图片描述" loading="lazy"></p><pre><code class="language-java">// 初始化  public void _jspInit() &#123;  &#125;// 销毁  public void _jspDestroy() &#123;  &#125;// JSPService  public void _jspService(final javax.servlet.http.HttpServletRequest request, final javax.servlet.http.HttpServletResponse response)      throws java.io.IOException, javax.servlet.ServletException &#123;   </code></pre><ol><li><p>内置一些对象；</p><pre><code class="language-java">final javax.servlet.jsp.PageContext pageContext;  // 页面上下文javax.servlet.http.HttpSession session = null;    // sessionfinal javax.servlet.ServletContext application;   // applicationContextfinal javax.servlet.ServletConfig config;         // configjavax.servlet.jsp.JspWriter out = null;           // outfinal java.lang.Object page = this;               // page：当前HttpServletRequest request                        // 请求HttpServletResponse response                      // 响应</code></pre></li><li><p>输出页面前增加的代码；</p><pre><code class="language-java">response.setContentType(&quot;text/html&quot;);       // 设置响应的页面类型pageContext = _jspxFactory.getPageContext(this, request, response,                                          null, true, 8192, true);_jspx_page_context = pageContext;application = pageContext.getServletContext();config = pageContext.getServletConfig();session = pageContext.getSession();out = pageContext.getOut();_jspx_out = out;</code></pre></li><li><p>以上的这些个对象我们可以在JSP页面中直接使用！</p></li></ol><ul><li>在JSP页面中：<ul><li>只要是 JAVA代码就会原封不动的输出；</li><li>如果是HTML代码，就会被转换为：</li></ul></li></ul><pre><code class="language-java">      out.write(&quot;&lt;html&gt;\n&quot;);      out.write(&quot;  &lt;head&gt;\n&quot;);      out.write(&quot;    &lt;title&gt;$Title$&lt;/title&gt;\n&quot;);      out.write(&quot;  &lt;/head&gt;\n&quot;);      out.write(&quot;  &lt;body&gt;\n&quot;);      out.write(&quot;  $END$\n&quot;);      out.write(&quot;  &lt;/body&gt;\n&quot;);      out.write(&quot;&lt;/html&gt;\n&quot;);</code></pre><ul><li>这样的格式，输出到前端！</li></ul><h4 id="JSP语法">JSP语法</h4><ul><li>任何语言都有自己的语法，JAVA中有，JSP 作为java技术的一种应用，它拥有一些自己扩充的语法（了解，知道即可！），Java所有语法都支持！</li><li>配置必需的maven环境：</li></ul><pre><code class="language-xml">&lt;!--        JSP 依赖   --&gt;        &lt;dependency&gt;            &lt;groupId&gt;javax.servlet.jsp&lt;/groupId&gt;            &lt;artifactId&gt;javax.servlet.jsp-api&lt;/artifactId&gt;            &lt;version&gt;2.3.3&lt;/version&gt;        &lt;/dependency&gt;&lt;!--        JSTL表达式的依赖--&gt;        &lt;dependency&gt;            &lt;groupId&gt;javax.servlet.jsp.jstl&lt;/groupId&gt;            &lt;artifactId&gt;jstl-api&lt;/artifactId&gt;            &lt;version&gt;1.2&lt;/version&gt;        &lt;/dependency&gt;&lt;!--        standard标签库--&gt;        &lt;dependency&gt;            &lt;groupId&gt;taglibs&lt;/groupId&gt;            &lt;artifactId&gt;standard&lt;/artifactId&gt;            &lt;version&gt;1.1.2&lt;/version&gt;        &lt;/dependency&gt;</code></pre><h5 id="JSP表达式">JSP表达式</h5><pre><code class="language-jsp">  &lt;body&gt;  &lt;%--JSP表达式              作用：用来将程序的输出，输出到客户端              &lt;%= 变量或者表达式%&gt;  --%&gt;  &lt;%= new java.util.Date()%&gt;  &lt;/body&gt;</code></pre><h5 id="jsp脚本片段">jsp脚本片段</h5><pre><code class="language-jsp">  &lt;body&gt;  &lt;%--jsp脚本片段--%&gt;  &lt;%    int sum = 0;    for (int i = 1; i &lt;=100 ; i++) &#123;      sum+=i;    &#125;    out.println(&quot;&lt;h1&gt;Sum=&quot;+sum+&quot;&lt;/h1&gt;&quot;);  %&gt;  &lt;/body&gt;</code></pre><ul><li>JSP声明</li><li>&lt;%! code %&gt;</li></ul><pre><code class="language-jsp">  &lt;body&gt;  &lt;%!    static &#123;      System.out.println(&quot;Loading Servlet!&quot;);    &#125;    private int globalVar = 0;    public void guo()&#123;      System.out.println(&quot;进入了方法guo！&quot;);    &#125;  %&gt;  &lt;/body&gt;</code></pre><ul><li>JSP声明：会被编译到JSP生成Java的类中！其他的，就会被生成到_jspService方法中！</li></ul><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L20wXzQ2MTUzOTQ5,size_16,color_FFFFFF,t_70-20230512204121432.png" alt="在这里插入图片描述" loading="lazy"></p><h5 id="JSP指令-404与500页面实现">JSP指令: 404与500页面实现</h5><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20230512204634669.png" alt="image-20230512204634669" loading="lazy"></p><pre><code class="language-xml">&lt;!--    自定义错误页面--&gt;&lt;!--    配置之后记得重启Tomcat服务， 修改tomcat配置文件web.xml热部署不生效--&gt;&lt;!--    自由修改jsp文件才能热部署--&gt;    &lt;error-page&gt;        &lt;error-code&gt;404&lt;/error-code&gt;        &lt;location&gt;/error/404.jsp&lt;/location&gt;    &lt;/error-page&gt;    &lt;error-page&gt;        &lt;error-code&gt;500&lt;/error-code&gt;        &lt;location&gt;/error/500.jsp&lt;/location&gt;    &lt;/error-page&gt;</code></pre><h6 id="头部和尾部页面拼接">头部和尾部页面拼接</h6><pre><code class="language-jsp">&lt;body&gt;    &lt;%-- @include会将两个页面合二为一 --%&gt;    &lt;%@include file=&quot;common/header.jsp&quot;%&gt;    &lt;h1&gt;网页主体&lt;/h1&gt;    &lt;%@include file=&quot;common/footer.jsp&quot;%&gt;    &lt;hr&gt;    &lt;%--        jsp标签        jsp:include：拼接页面，本质还是三个    --%&gt;    &lt;jsp:include page=&quot;/common/header.jsp&quot;/&gt;    &lt;h1&gt;网页主体&lt;/h1&gt;    &lt;jsp:include page=&quot;/common/footer.jsp&quot;/&gt;&lt;/body&gt;</code></pre><h4 id="内置对象">内置对象</h4><ul><li>PageContext 存东西</li><li>Request 存东西</li><li>Response</li><li>Session 存东西</li><li>Application 【SerlvetContext】 存东西</li><li>config 【SerlvetConfig】</li><li>out</li><li>page ，不用了解</li><li>exception</li></ul><pre><code class="language-java">&lt;body&gt;&lt;%--内置对象--%&gt;&lt;%    pageContext.setAttribute(&quot;name1&quot;,&quot;天启1号&quot;); // 保存的数据只在一个页面中有效    request.setAttribute(&quot;name2&quot;,&quot;天启2号&quot;); // 保存的数据只在一次请求中有效，请求转发会携带这个数据    session.setAttribute(&quot;name3&quot;,&quot;天启3号&quot;); // 保存的数据只在一次会话中有效，从打开浏览器到关闭浏览器    application.setAttribute(&quot;name4&quot;,&quot;天启4号&quot;);  // 保存的数据只在服务器中有效，从打开服务器到关闭服务器%&gt;&lt;%--    脚本片段中的代码，会被原封不动生成到.jsp.java    要求：这里面的代码，必须保证Java语法的正确性--%&gt;&lt;%    // 从pageContent取出，我们通过寻找的方式来    // 从底层到高层（作用域）:    String name1 = (String) pageContext.findAttribute(&quot;name1&quot;);    String name2 = (String) pageContext.findAttribute(&quot;name2&quot;);    String name3 = (String) pageContext.findAttribute(&quot;name3&quot;);    String name4 = (String) pageContext.findAttribute(&quot;name4&quot;);    String name5 = (String) pageContext.findAttribute(&quot;name5&quot;); // 作用域%&gt;&lt;%--使用EL表达式输出 $&#123;&#125; --%&gt;&lt;h1&gt;取出的值:&lt;/h1&gt;&lt;h3&gt;$&#123;name1&#125;&lt;/h3&gt;&lt;h3&gt;$&#123;name2&#125;&lt;/h3&gt;&lt;h3&gt;$&#123;name3&#125;&lt;/h3&gt;&lt;h3&gt;$&#123;name4&#125;&lt;/h3&gt;&lt;h3&gt; &lt;%=name5%&gt; &lt;/h3&gt;&lt;/body&gt;&lt;/html&gt;</code></pre><ul><li>request：客户端向服务器发送请求，产生的数据，用户看完就没用了，比如：新闻，用户看完没用的！</li><li>session：客户端向服务器发送请求，产生的数据，用户用完一会还有用，比如：购物车；</li><li>application：客户端向服务器发送请求，产生的数据，一个用户用完了，其他用户还可能使用，比如：聊天数据；</li></ul><h4 id="JSP标签-JSTL标签-EL表达式">JSP标签.JSTL标签.EL表达式</h4><ul><li>EL表达式： ${ }<ul><li><strong>获取数据</strong></li><li><strong>执行运算</strong></li><li><strong>获取web开发的常用对象</strong></li></ul></li></ul><pre><code class="language-jsp">&lt;!-- JSTL表达式的依赖 --&gt;&lt;dependency&gt;    &lt;groupId&gt;javax.servlet.jsp.jstl&lt;/groupId&gt;    &lt;artifactId&gt;jstl-api&lt;/artifactId&gt;    &lt;version&gt;1.2&lt;/version&gt;&lt;/dependency&gt;&lt;!-- standard标签库 --&gt;&lt;dependency&gt;    &lt;groupId&gt;taglibs&lt;/groupId&gt;    &lt;artifactId&gt;standard&lt;/artifactId&gt;    &lt;version&gt;1.1.2&lt;/version&gt;&lt;/dependency&gt;</code></pre><ul><li><p><strong>JSP标签</strong></p></li><li><p>利用JSP标签实现请求转发, 并设置参数</p><p>&lt;jsp:forward page=“url”&gt;</p></li></ul><pre><code class="language-jsp">&lt;body&gt;&lt;%--jsp:include--%&gt;&lt;jsp:forward page=&quot;/jspTag2.jsp&quot;&gt;    &lt;jsp:param name=&quot;name&quot; value=&quot;subeiLY&quot;/&gt;    &lt;jsp:param name=&quot;age&quot; value=&quot;18&quot;/&gt;&lt;/jsp:forward&gt;&lt;/body&gt;</code></pre><ul><li>再转发的目标Servlet取参数</li></ul><pre><code class="language-jsp">&lt;body&gt;&lt;%--取出参数--%&gt;名字:&lt;%=request.getParameter(&quot;name&quot;)%&gt;年龄:&lt;%=request.getParameter(&quot;age&quot;)%&gt;&lt;/body&gt;</code></pre><ul><li><p><strong>JSTL表达式</strong></p><ul><li>JSTL标签库的使用就是为了弥补HTML标签的不足；它自定义许多标签，可以供我们使用，标签的功能和Java代码一样！</li></ul></li><li><p><strong>格式化标签</strong></p></li><li><p><strong>SQL标签</strong></p></li><li><p><strong>XML 标签</strong></p></li><li><p><em><strong>核心标签</strong> （掌握部分）</em></p></li><li><p><strong>JSTL标签库使用步骤</strong>：</p><ul><li>引入对应的 taglib；</li><li>使用其中的方法；</li><li><strong>在Tomcat 的lib目录下也需要引入 jstl-api-1.2.jar、standard-1.1.2.jar的包，否则会报错：JSTL解析错误</strong>；</li></ul></li><li><p>c：if标签 EL表达式获取表单中的数据</p><pre><code class="language-jsp">&lt;body&gt;&lt;form action=&quot;core-if.jsp&quot; method=&quot;get&quot;&gt;    &lt;%--        EL表达式获取表单中的数据        $&#123;param.参数名&#125;    --%&gt;    &lt;input type=&quot;text&quot; name=&quot;username&quot; value=&quot;$&#123;param.username&#125;&quot;&gt;    &lt;input type=&quot;submit&quot; value=&quot;登录&quot;&gt;&lt;/form&gt;&lt;%--判断如果提交的用户名是管理员，则登录成功--%&gt;&lt;c:if test=&quot;$&#123;param.username=='admin'&#125;&quot; var=&quot;isAdmin&quot;&gt;    &lt;c:out value=&quot;管理员欢迎您！&quot;/&gt;&lt;/c:if&gt;  &lt;c:out value=&quot;$&#123;isAdmin&#125;&quot;/&gt;&lt;/body&gt;</code></pre><ul><li>c:choose c:when 实现成绩判断(Switch case)</li></ul><pre><code class="language-jsp">&lt;body&gt;&lt;%--定义一个变量score，值为85--%&gt;&lt;c:set var=&quot;score&quot; value=&quot;65&quot;/&gt;&lt;c:choose&gt;    &lt;c:when test=&quot;$&#123;score&gt;=90&#125;&quot;&gt;        你的成绩为优秀    &lt;/c:when&gt;    &lt;c:when test=&quot;$&#123;score&gt;=80&#125;&quot;&gt;        你的成绩为一般    &lt;/c:when&gt;    &lt;c:when test=&quot;$&#123;score&gt;=70&#125;&quot;&gt;        你的成绩为良好    &lt;/c:when&gt;    &lt;c:when test=&quot;$&#123;score&lt;=60&#125;&quot;&gt;        你的成绩为不及格    &lt;/c:when&gt;&lt;/c:choose&gt;&lt;/body&gt;</code></pre><ul><li>c:forEach</li></ul><pre><code class="language-jsp">&lt;body&gt;&lt;%    ArrayList&lt;String&gt; people = new ArrayList&lt;&gt;();    people.add(0,&quot;Alice&quot;);    people.add(1,&quot;Bob&quot;);    people.add(2,&quot;楼兰&quot;);    people.add(3,&quot;Goobus&quot;);    people.add(4,&quot;Kris&quot;);    request.setAttribute(&quot;list&quot;,people);%&gt;&lt;%--var , 每一次遍历出来的变量items, 要遍历的对象begin,   哪里开始end,     到哪里step,   步长 (i++)--%&gt;&lt;c:forEach var=&quot;people&quot; items=&quot;$&#123;list&#125;&quot;&gt;    &lt;c:out value=&quot;$&#123;people&#125;&quot;/&gt; &lt;br&gt;&lt;/c:forEach&gt;&lt;hr&gt;&lt;c:forEach var=&quot;people&quot; items=&quot;$&#123;list&#125;&quot; begin=&quot;1&quot; end=&quot;3&quot; step=&quot;1&quot; &gt;    &lt;c:out value=&quot;$&#123;people&#125;&quot;/&gt; &lt;br&gt;&lt;/c:forEach&gt;&lt;/body&gt;</code></pre></li></ul><h2 id="JavaBean">JavaBean</h2><p>实体类，JavaBean有特定的写法：</p><ul><li>必须要有一个无参构造</li><li>属性必须私有化</li><li>必须有对应的get/set方法；</li></ul><p>一般用来和数据库的字段做映射 ORM；</p><p>ORM ：对象关系映射</p><ul><li>表—&gt;类</li><li>字段–&gt;属性</li><li>行记录----&gt;对象</li></ul><pre><code class="language-jsp">&lt;jsp:useBean id=&quot;people&quot; class=&quot;cn.loulan.Bean.People&quot; /&gt;&lt;jsp:setProperty name=&quot;people&quot; property=&quot;address&quot; value=&quot;成都&quot;/&gt;&lt;jsp:setProperty name=&quot;people&quot; property=&quot;id&quot; value=&quot;1&quot;/&gt;&lt;jsp:setProperty name=&quot;people&quot; property=&quot;age&quot; value=&quot;2&quot;/&gt;&lt;jsp:setProperty name=&quot;people&quot; property=&quot;name&quot; value=&quot;哇哈哈AD钙&quot;/&gt;姓名:&lt;jsp:getProperty name=&quot;people&quot; property=&quot;name&quot;/&gt; ID:&lt;jsp:getProperty name=&quot;people&quot; property=&quot;id&quot;/&gt;年龄:&lt;jsp:getProperty name=&quot;people&quot; property=&quot;age&quot;/&gt;地址:&lt;jsp:getProperty name=&quot;people&quot; property=&quot;address&quot;/&gt;</code></pre><h2 id="MVC三层架构">MVC三层架构</h2><ul><li>什么是MVC： Model view Controller 模型.视图.控制器</li></ul><h3 id="1-早些年">1.早些年</h3><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L20wXzQ2MTUzOTQ5,size_16,color_FFFFFF,t_70-20230512210458691.png" alt="在这里插入图片描述" loading="lazy"></p><ul><li>用户直接访问控制层，控制层就可以直接操作数据库；</li></ul><pre><code class="language-java">servlet--CRUD--&gt;数据库弊端：程序十分臃肿，不利于维护  servlet的代码中：处理请求.响应.视图跳转.处理JDBC.处理业务代码.处理逻辑代码架构：没有什么是加一层解决不了的！程序猿调用|JDBC|Mysql Oracle SqlServer ....</code></pre><h3 id="2-MVC三层架构">2.MVC三层架构</h3><p><img src="Java-Web-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L20wXzQ2MTUzOTQ5,size_16,color_FFFFFF,t_70-20230512210458822.png" alt="在这里插入图片描述" loading="lazy"></p><p>Model</p><ul><li>业务处理 ：业务逻辑（Service）</li><li>数据持久层：CRUD （Dao）</li></ul><p>View</p><ul><li>展示数据</li><li>提供链接发起Servlet请求 （a，form，img…）</li></ul><p>Controller （Servlet）</p><ul><li><p>接收用户的请求 ：（req：请求参数.Session信息….）</p></li><li><p>交给业务层处理对应的代码</p></li><li><p>控制视图的跳转</p><pre><code class="language-java">登录---&gt;接收用户的登录请求---&gt;处理用户的请求（获取用户登录的参数，username，password）----&gt;交给业务层处理登录业务（判断用户名密码是否正确：事务）---&gt;Dao层查询用户名和密码是否正确--&gt;数据库</code></pre></li></ul><h2 id="Filter-重点">Filter(重点)</h2><p>Filter：过滤器 ，用来过滤网站的数据；</p><ul><li>处理中文乱码</li><li>登录验证….</li></ul><p>Filter开发步骤：</p><ol><li>导包</li><li>编写过滤器</li></ol><pre><code class="language-xml">        &lt;!--   Servlet 依赖   --&gt;        &lt;dependency&gt;            &lt;groupId&gt;javax.servlet&lt;/groupId&gt;            &lt;artifactId&gt;servlet-api&lt;/artifactId&gt;            &lt;version&gt;2.5&lt;/version&gt;        &lt;/dependency&gt;        &lt;!--        JSP 依赖   --&gt;        &lt;dependency&gt;            &lt;groupId&gt;javax.servlet.jsp&lt;/groupId&gt;            &lt;artifactId&gt;javax.servlet.jsp-api&lt;/artifactId&gt;            &lt;version&gt;2.3.3&lt;/version&gt;        &lt;/dependency&gt;        &lt;!--        JSTL表达式的依赖--&gt;        &lt;dependency&gt;            &lt;groupId&gt;javax.servlet.jsp.jstl&lt;/groupId&gt;            &lt;artifactId&gt;jstl-api&lt;/artifactId&gt;            &lt;version&gt;1.2&lt;/version&gt;        &lt;/dependency&gt;        &lt;!--        standard标签库--&gt;        &lt;dependency&gt;            &lt;groupId&gt;taglibs&lt;/groupId&gt;            &lt;artifactId&gt;standard&lt;/artifactId&gt;            &lt;version&gt;1.1.2&lt;/version&gt;        &lt;/dependency&gt;        &lt;!--    连接数据库--&gt;        &lt;dependency&gt;            &lt;groupId&gt;mysql&lt;/groupId&gt;            &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;            &lt;version&gt;5.1.47&lt;/version&gt;        &lt;/dependency&gt;</code></pre><ol start="3"><li>实现Filter接口，重写对应的方法即可；</li></ol><p>使用Filter设置编码, 防止乱码</p><pre><code class="language-java">public class Filter implements javax.servlet.Filter &#123;    @Override    public void init(FilterConfig filterConfig) throws ServletException &#123;        //项目启动的时候被调用        System.out.println(&quot;init filter&quot;);    &#125;    @Override    public void doFilter(ServletRequest servletRequest, ServletResponse servletResponse, FilterChain filterChain) throws IOException, ServletException &#123;        servletRequest.setCharacterEncoding(&quot;UTF-8&quot;);        servletResponse.setCharacterEncoding(&quot;UTF-8&quot;);        filterChain.doFilter(servletRequest,servletResponse);    &#125;</code></pre><p>这段代码是Java Servlet中用于实现Filter的接口方法<code>doFilter()</code>的签名。</p><p>Filter是一种用于在Servlet容器中对请求和响应进行预处理和后处理的组件。Filter可以截取请求和响应，修改请求参数或响应内容，或者根据某些条件决定是否允许请求继续处理。</p><p><code>doFilter()</code>方法接受三个参数：</p><p><code>ServletRequest servletRequest</code>、<br><code>ServletResponse servletResponse</code><br><code>FilterChain filterChain</code></p><p><code>ServletRequest</code>表示接收到的HTTP请求，<code>ServletResponse</code>表示将要发送的HTTP响应，<code>FilterChain</code>表示Filter链。</p><p>在<code>doFilter()</code>方法中，Filter可以对请求和响应进行任何必要的操作。</p><p>例如，可以检查请求参数、修改请求头、记录请求日志等。</p><p>然后，Filter必须调用<code>filterChain.doFilter()</code>方法，将请求传递给下一个Filter或目标Servlet来进行处理。</p><p>如果不调用<code>filterChain.doFilter()</code>方法，请求将不会传递到下一个组件，也不会生成响应。在最后一个Filter或目标Servlet中，必须生成响应并将其发送回客户端。</p><p>在Filter链中的每个Filter都必须调用<code>filterChain.doFilter()</code>方法，否则请求将停止在当前Filter，不会传递到后续的Filter或目标Servlet。</p><p>如果在Filter链中的某个Filter中调用了<code>filterChain.doFilter()</code>方法之前或之后生成了响应，或者没有调用<code>filterChain.doFilter()</code>方法，都会导致请求失败或无法正常响应。</p><ol start="4"><li>在web.xml中配置 Filter；</li></ol><pre><code class="language-xml">&lt;!--mapping filter--&gt;    &lt;filter&gt;        &lt;filter-name&gt;encode&lt;/filter-name&gt;        &lt;filter-class&gt;cn.loulan.servlet.Filter&lt;/filter-class&gt;    &lt;/filter&gt;    &lt;filter-mapping&gt;        &lt;filter-name&gt;encode&lt;/filter-name&gt;       &lt;!--只要是 /filter的任何请求，会经过这个过滤器--&gt;        &lt;url-pattern&gt;/filter&lt;/url-pattern&gt;    &lt;/filter-mapping&gt;</code></pre><h2 id="监听器-Listener">监听器(Listener)</h2><p>实现一个监听器的接口；（有N种）</p><ol><li><p>编写一个监听器；</p><p>实现监听器的接口…</p><pre><code class="language-java">public class OnlineCountListener implements HttpSessionListener &#123;//    我们使用一个Integer对象来表示当前在线用户数，//    并且在每次Session创建时，都将在线用户数加1。//    然后，我们将在线用户数存储在ServletContext中，//    以便在整个Web应用程序中共享和使用。    @Override    public void sessionCreated(HttpSessionEvent httpSessionEvent) &#123;        ServletContext servletContext = httpSessionEvent.getSession().getServletContext();        Integer onlineCounter = (Integer) servletContext.getAttribute(&quot;onlineCounter&quot;);        if(onlineCounter == null)&#123;            onlineCounter = Integer.valueOf(1);        &#125; else &#123;            onlineCounter = Integer.valueOf(onlineCounter.intValue() + 1);        &#125;        servletContext.setAttribute(&quot;onlineCounter&quot;, onlineCounter);    &#125;    @Override    public void sessionDestroyed(HttpSessionEvent httpSessionEvent) &#123;        Integer onlineCounter = (Integer) httpSessionEvent.getSession().getServletContext().getAttribute(&quot;onlineCounter&quot;);        if(onlineCounter == null)&#123;            onlineCounter = Integer.valueOf(0);        &#125;else &#123;            onlineCounter = Integer.valueOf(onlineCounter.intValue()- 1);        &#125;        httpSessionEvent.getSession().setAttribute(&quot;onlineCounter&quot;, onlineCounter);    &#125;&#125;</code></pre><ol start="2"><li><p>注册监听器</p><pre><code class="language-xml">&lt;!--    Listener--&gt;    &lt;listener&gt;        &lt;listener-class&gt;cn.loulan.servlet.OnlineCountListener&lt;/listener-class&gt;    &lt;/listener&gt;</code></pre></li></ol></li></ol><h3 id="过滤器-监听器常见应用">过滤器.监听器常见应用</h3><ul><li><h4 id="监听器：GUI编程中经常使用；"><strong>监听器：GUI编程中经常使用；</strong></h4><pre><code class="language-java">public class TestPanel &#123;    public static void main(String[] args) &#123;        // 新建一个窗体        Frame frame = new Frame(&quot;Happy Day&quot;);        // 面板        Panel panel = new Panel(null);        // 设置窗体的布局        frame.setLayout(null);        frame.setBounds(300,300,500,500);        // 设置背景颜色1        frame.setBackground(Color.lightGray);        // 设置背景颜色2        panel.setBackground(Color.orange);        panel.setBounds(frame.getX()/2, frame.getY()/2, frame.getWidth()/2, frame.getHeight()/2);        frame.add(panel);        frame.setVisible(true);        // 监听事件，监听关闭事件        frame.addWindowListener(new WindowAdapter() &#123;            @Override            public void windowClosing(WindowEvent e) &#123;                    frame.dispose();            &#125;        &#125;);    &#125;&#125;</code></pre><h4 id="过滤器：用户登录之后才能进入主页！用户注销后就不能进入主页了">过滤器：用户登录之后才能进入主页！用户注销后就不能进入主页了</h4><ol><li>用户登录之后，向Sesison中放入用户的数据</li><li>进入主页的时候要判断用户是否已经登录；要求：在过滤器中实现！</li></ol><pre><code class="language-java">public class Log extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        String username = req.getParameter(&quot;username&quot;);        String password = req.getParameter(&quot;password&quot;);        HttpSession session = req.getSession();        if(&quot;admin&quot;.equals(username) &amp;&amp; &quot;root&quot;.equals(password)) &#123;            session.setAttribute(Constants.USER_SESSION,new Person(username,password));            resp.sendRedirect(&quot;/pages/success.jsp&quot;);        &#125; else &#123;            resp.sendRedirect(&quot;/&quot;);        &#125;    &#125;</code></pre><p>LoginFilter</p><pre><code class="language-java">public class LoginFilter extends Filter&#123;    @Override    public void doFilter(ServletRequest servletRequest, ServletResponse servletResponse, FilterChain filterChain) throws IOException, ServletException &#123;// ServletRequest 转化为 HttpServletRequest // 目的是拿到Session, 以便获取Attribute        HttpServletRequest request = (HttpServletRequest) servletRequest;        HttpServletResponse response = (HttpServletResponse) servletResponse;        Object o = request.getSession().getAttribute(Constants.USER_SESSION);        if(o == null) &#123;            response.sendRedirect(&quot;/&quot;);        &#125;        filterChain.doFilter(servletRequest, servletResponse);    &#125;&#125;</code></pre><p>LogoutServlet</p><pre><code class="language-java">,public class Logout extends HttpServlet &#123;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;     req.getSession().setAttribute(Constants.USER_SESSION,null);        resp.sendRedirect(&quot;/&quot;);    &#125;,&#125;</code></pre><p>通过设置Session的Attribute(Constants.USER_SESSION)值为空来实现用户的登录, 这种方式不需要服务器重复创建和销毁Session就可以实现用户的注销, 减轻了服务器负担.</p><img src="Java-Web-快速入门/Kapture 2023-05-13 at 11.38.21-3950252.gif" alt="Kapture 2023-05-13 at 11.38.21" style="zoom:80%;" / loading="lazy"></li></ul><h2 id="JDBC">JDBC</h2><ul><li>什么是JDBC ： Java连接数据库！</li><li>需要jar包的支持：<ul><li>java.sql</li><li>javax.sql</li><li>mysql-connecter-java… 连接驱动（必须要导入）</li></ul></li></ul><ol><li><s>加载驱动</s></li><li>连接数据库,代表数据库</li><li>编写SQL （根据业务，不同的SQL）</li><li>预编译</li><li>执行SQL</li><li>关闭连接</li></ol><pre><code class="language-java">public static void main(String[] args) throws ClassNotFoundException, SQLException &#123;    // 配置信息    // useUnicode=true&amp;characterEncoding=utf-8 解决中文乱码    String url=&quot;jdbc:mysql://localhost:3307/JDBC?useUnicode=true&amp;characterEncoding=utf-8&quot;;    String username = &quot;root&quot;;    String password = &quot;&quot;;    // 2.连接数据库,代表数据库    Connection connection = DriverManager.getConnection(url, username, password);    // 3.编写SQL    String sql = &quot;select * from user&quot;;//     4. 预编译    PreparedStatement preparedStatement = connection.prepareStatement(sql);    // 5.执行查询SQL，返回一个 ResultSet  ： 结果集    ResultSet rs = preparedStatement.executeQuery(sql);    while (rs.next()) &#123;        System.out.println(rs.getObject(&quot;name&quot;));        System.out.println(rs.getObject(&quot;email&quot;));        System.out.println(rs.getObject(&quot;password&quot;));        System.out.println(rs.getObject(&quot;role&quot;));    &#125;    // 6.关闭连接，释放资源（一定要做） 先开后关    rs.close();    preparedStatement.close();    connection.close();&#125;</code></pre><h4 id="预编译-使用占位符">预编译: 使用占位符</h4><p>在 JDBC 中，预编译 SQL 的主要目的是<strong>提高执行 SQL 语句的效率</strong>。当一个 SQL 语句被预编译后，JDBC 驱动程序会将该语句存储在数据库中，并将其与一个预定义的执行计划关联起来。这样，在执行该 SQL 语句时，JDBC 驱动程序可以直接使用预定义的执行计划，而<strong>不必重新解析 SQL 语句和生成执行计划</strong>，从而提高了执行效率。</p><p>另外，预编译 SQL 还可以提高代码的<strong>可维护性</strong>和<strong>安全性</strong>。通过使用预编译 SQL，我们可以将 SQL 语句与 Java 代码分离开来，使得代码更易于维护。此外，预编译 SQL 可以避免 <strong>SQL 注入</strong>等安全问题。</p><p>具体地，预编译 SQL 的过程包括两个步骤：首先，使用 <code>Connection</code> 接口中的 <code>prepareStatement()</code> 方法创建一个 <code>PreparedStatement</code> 对象；然后，使用 <code>PreparedStatement</code> 对象的 <code>setXXX()</code> 方法设置 SQL 语句中的参数，并使用 <code>execute()</code> 或 <code>executeUpdate()</code> 方法执行 SQL 语句。</p><p>以下是一个使用预编译 SQL 的示例代码：</p><pre><code>String sql = &quot;SELECT * FROM users WHERE name = ?&quot;;PreparedStatement pstmt = conn.prepareStatement(sql);pstmt.setString(1, &quot;John&quot;);ResultSet rs = pstmt.executeQuery();while (rs.next()) &#123;    // process the result set&#125;</code></pre><p>在上述示例代码中，我们首先使用 <code>prepareStatement()</code> 方法创建了一个 <code>PreparedStatement</code> 对象，然后使用 <code>setString()</code> 方法设置了 SQL 语句中的参数，并最终使用 <code>executeQuery()</code> 方法执行 SQL 语句。这样，在执行 SQL 语句时，JDBC 驱动程序会使用预定义的执行计划，从而提高了执行效率。</p><h4 id="SQL注入">SQL注入</h4><p>SQL 注入（SQL Injection）是一种常见的网络安全漏洞，它允许攻击者在执行 SQL 查询时向查询中插入恶意代码，从而获得对数据库的非授权访问。攻击者可以通过构造特定的 SQL 查询语句，从而绕过身份验证、窃取数据或破坏数据库。</p><p>例如，考虑下面的 SQL 查询语句：</p><pre><code>SELECT * FROM users WHERE username = '&lt;input&gt;';</code></pre><p>如果用户输入 <code>&lt;input&gt;</code> 的值为 <code>' or '1'='1</code>，那么最终的 SQL 查询语句就会变成：</p><pre><code>SELECT * FROM users WHERE username = '' or '1'='1';</code></pre><p>这样就会返回所有用户的记录，而不仅仅是指定的用户名。攻击者可以通过这种方式获取到数据库中的敏感数据或者执行其他恶意操作。</p><h2 id="实战-超市订单管理系统">实战: 超市订单管理系统</h2><p>为了方便表述和理解, 在编写MVC三层对应的代码时, 使用自顶向下, 但在实际的项目中, 编写代码工作往往是自底向上的.</p><h3 id="搭建项目框架">搭建项目框架</h3><h4 id="编写数据库操作的基础公共类DB">编写数据库操作的基础公共类DB</h4><ul><li><p>数据库配置文件，这是一个资源文件，应该创建在maven项目的resources文件中</p><pre><code class="language-properties">DRIVER=com.mysql.jdbc.DriverURL=jdbc:mysql://localhost:3307/smbms?useUnicode=true&amp;characterEncoding=utf-8USERNAME=rootPASSWORD=</code></pre></li><li><p>使用静态代码块实现初始化参数</p><p>该类为数据库访问工具类，提供了获取数据库连接、执行查询和更新等常用操作的方法。</p><ol><li><p>类名：DB</p></li><li><p>属性：</p></li></ol><ul><li>DRIVER：String类型，表示数据库驱动名。</li><li>URL：String类型，表示数据库连接地址。</li><li>USERNAME：String类型，表示连接数据库的用户名。</li><li>PASSWORD：String类型，表示连接数据库的密码。</li></ul><ol start="3"><li>方法：</li></ol><ul><li>close：用于释放数据库连接、预处理语句和结果集资源。</li><li>getConnection：用于获取数据库连接。</li><li>executeQuery：用于执行查询操作，返回一个结果集。</li><li>executeUpdate：用于执行更新操作，返回受影响的行数。</li></ul><ol start="4"><li>方法的参数：</li></ol><ul><li>conn：Connection类型，表示数据库连接。</li><li>pstmt：PreparedStatement类型，表示预处理语句。</li><li>resSet：ResultSet类型，表示结果集。</li><li>query：String类型，表示要执行的SQL语句。</li><li>params：Object[]类型，表示SQL语句中的参数值。</li></ul><p>在类中还定义了一个静态代码块，该代码块在类加载时会被执行。静态代码块中的主要作用是读取配置文件中的数据库连接参数，并将其赋值给类的静态属性。具体实现如下：</p><ul><li>创建Properties对象。</li><li>通过类加载器加载配置文件为字节输入流。</li><li>使用Properties对象的load()方法加载字节输入流中的内容。</li><li>从Properties对象中获取数据库连接参数，并将其赋值给类的静态属性。</li></ul><p>通过这种方式，可以在不修改Java代码的情况下，更改数据库连接参数，提高了代码的灵活性和可维护性。</p><pre><code class="language-java">public class DB &#123;    private static String DRIVER;    private static String URL;    private static String USERNAME;    private static String PASSWORD;    //静态代码块， 类加载的时候执行    static &#123;        //读取配置文件        //1、创建properties对象        Properties properties = new Properties();        //2、通过类加载器加载资源文件为字节输入流        InputStream in = DB.class.getClassLoader().getResourceAsStream(&quot;db.properties&quot;);        try &#123;            properties.load(in);        &#125; catch (IOException e) &#123;            e.printStackTrace();        &#125;        DRIVER = properties.getProperty(&quot;DRIVER&quot;);        URL = properties.getProperty(&quot;URL&quot;);        USERNAME = properties.getProperty(&quot;USERNAME&quot;);        PASSWORD = properties.getProperty(&quot;PASSWORD&quot;);    &#125;&#125;</code></pre></li><li><p>编写数据库操作的公共方法</p><p>该类为数据库访问工具类，提供了获取数据库连接、执行查询和更新等常用操作的方法。</p><ol><li><p>类名：DB</p></li><li><p>属性：</p></li></ol><ul><li>DRIVER：String类型，表示数据库驱动名。</li><li>URL：String类型，表示数据库连接地址。</li><li>USERNAME：String类型，表示连接数据库的用户名。</li><li>PASSWORD：String类型，表示连接数据库的密码。</li></ul><ol start="3"><li>方法：</li></ol><ul><li>close：用于释放数据库连接、预处理语句和结果集资源。</li><li>getConnection：用于获取数据库连接。</li><li>executeQuery：用于执行查询操作，返回一个结果集。</li><li>executeUpdate：用于执行更新操作，返回受影响的行数。</li></ul><ol start="4"><li>方法的参数：</li></ol><ul><li>conn：Connection类型，表示数据库连接。</li><li>pstmt：PreparedStatement类型，表示预处理语句。</li><li>resSet：ResultSet类型，表示结果集。</li><li>query：String类型，表示要执行的SQL语句。</li><li>params：Object[]类型，表示SQL语句中的参数值。</li></ul><p>该类还包含一个静态代码块，用于初始化数据库连接参数，但代码块的内容未给出，需要进行补充。</p><p><em>同时需要注意，该类并没有实现单例模式，每次获取数据库连接都会重新创建一个新的连接，这可能会导致性能问题。</em></p></li></ul><pre><code class="language-java">public class DB &#123;    private static String DRIVER;    private static String URL;    private static String USERNAME;    private static String PASSWORD;     //插入静态代码块(need to fininsh)      //释放链接资源    public static void close(Connection conn, PreparedStatement pstmt, ResultSet resSet) throws SQLException &#123;        if(resSet != null)&#123;            resSet.close();        &#125;        if(pstmt != null)&#123;            pstmt.close();        &#125;        if(conn != null) &#123;            conn.close();        &#125;    &#125;    //获取数据库链接    public static Connection getConnection() throws SQLException, ClassNotFoundException &#123;        Class.forName(DRIVER);        String url = URL;        String username = USERNAME;        String password = PASSWORD;        // 2.连接数据库,代表数据库        Connection connection = DriverManager.getConnection(url, username, password);        return  connection;    &#125;    //执行查询, 返回结果集    public static ResultSet executeQuery(Connection conn,String query,Object[] params) throws SQLException, ClassNotFoundException&#123;        ResultSet resultSet = null;        if(conn != null &amp;&amp; !query.isEmpty() &amp;&amp; params != null)&#123;            //预处理            PreparedStatement statement = conn.prepareStatement(query);            for (int i = 0; i &lt; params.length;i++)&#123;                //设置SQL 语句中的参数                statement.setString(i+1,params[i].toString());            &#125;            //执行            resultSet = statement.executeQuery();        &#125;        return resultSet;    &#125;    //执行更新， 返回影响行数    public static int executeUpdate(Connection conn,String query,Object[] params) throws SQLException &#123;        int affectedRows = 0;        if(conn != null &amp;&amp; !query.isEmpty() &amp;&amp; params != null)&#123;            //预处理            PreparedStatement statement = conn.prepareStatement(query);            for (int i = 0; i &lt; params.length;i++)&#123;                //设置SQL 语句中的参数                statement.setString(i+1,params[i].toString());            &#125;            //执行             affectedRows = statement.executeUpdate();        &#125;        return affectedRows;    &#125;&#125;</code></pre><h3 id="登录功能实现">登录功能实现</h3><h4 id="前端页面编写">前端页面编写</h4><p>前端页面由kuang提供</p><pre><code class="language-jsp">&lt;%-- header--%&gt;&lt;form class=&quot;loginForm&quot; action=&quot;$&#123;pageContext.request.contextPath&#125;/login.do&quot; name=&quot;actionForm&quot; id=&quot;actionForm&quot; method=&quot;post&quot;&gt;  &lt;div class=&quot;info&quot;&gt;$&#123;error&#125;&lt;/div&gt;  &lt;div class=&quot;inputbox&quot;&gt;      &lt;label&gt;用户名：&lt;/label&gt;      &lt;input type=&quot;text&quot; class=&quot;input-text&quot; id=&quot;userCode&quot; name=&quot;userCode&quot; placeholder=&quot;请输入用户名&quot; required/&gt;  &lt;/div&gt;  &lt;div class=&quot;inputbox&quot;&gt;      &lt;label&gt;密码：&lt;/label&gt;      &lt;input type=&quot;password&quot; id=&quot;userPassword&quot; name=&quot;userPassword&quot; placeholder=&quot;请输入密码&quot; required/&gt;  &lt;/div&gt;  &lt;div class=&quot;subBtn&quot;&gt;      &lt;input type=&quot;submit&quot; value=&quot;登录&quot;/&gt;      &lt;input type=&quot;reset&quot; value=&quot;重置&quot;/&gt;  &lt;/div&gt;&lt;/form&gt;&lt;%-- footer--%&gt;</code></pre><h4 id="登录处理Servlet实现">登录处理Servlet实现</h4><p>在前端form表单的action属性值我们可以看到提交的URL, 所以, 我们需要编写对应的Servlet来处理这个请求</p><pre><code class="language-jsp">action=&quot;$&#123;pageContext.request.contextPath&#125;/login.do&quot;</code></pre><ul><li><p>创建登录处理Servlet</p><p>以下是一个名为“Login”的Java servlet类，用于处理HTTP GET请求。该类包括对“UserService”实例的引用，该实例用于从数据库中检索用户信息。</p><p>在“doGet”方法中，servlet从HTTP请求中检索userCode和password参数。然后它创建一个“UserServiceImpl”类的新实例（该类实现了“UserService”接口），并调用其“findByUserCode”方法，以根据给定的用户代码从数据库中检索用户对象。</p><p>如果用户对象不为null，则servlet检查用户密码是否与给定密码匹配。如果匹配，则servlet使用用户对象设置名为“USER_SESSION”的会话属性，并将用户重定向到“/jsp/frame.jsp”。如果密码不匹配，则servlet使用错误消息设置名为“error”的会话属性，并将用户重定向回“/login.jsp”。</p><p><em>请注意，此代码片段不完整，因为它缺少有关导入，它所依赖的接口和类，以及servlet配置的信息。</em></p><pre><code class="language-java">public class Login extends HttpServlet &#123;    private UserService service;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        String userCode = req.getParameter(&quot;userCode&quot;);        String password = req.getParameter(&quot;userPassword&quot;);        service = new UserServiceImpl();        User user = null;        user = service.findByUserCode(userCode);        if(user != null) &#123;            if(user.getUserPassword().equals(password)) &#123;                req.getSession().setAttribute(Constant.USER_SESSION,user);                resp.sendRedirect(&quot;/jsp/frame.jsp&quot;);            &#125; else &#123;                req.getSession().setAttribute(&quot;error&quot;,&quot;username or password incorrect&quot;);                resp.sendRedirect(&quot;/login.jsp&quot;);            &#125;        &#125;    &#125;</code></pre></li><li><p>映射 servlet</p><p>这段代码是一个 servlet 配置的 XML 部分，它用于将请求的 URL 映射到特定的 servlet 上。这段配置指定了一个名为 “Login” 的 servlet，使用了完整的类名 <code>servlet.Login</code>，并将它映射到 URL “/login.do” 上。</p><p>这意味着，当用户在浏览器中访问 “/login.do” 时，容器将调用 <code>servlet.Login</code> 的 service() 方法来处理该请求。映射 servlet 的操作是 web 应用的一部分，它在 web.xml 文件中进行配置。</p><pre><code class="language-xml">&lt;servlet&gt;  &lt;servlet-name&gt;Login&lt;/servlet-name&gt;  &lt;servlet-class&gt;servlet.Login&lt;/servlet-class&gt;&lt;/servlet&gt;&lt;servlet-mapping&gt;  &lt;servlet-name&gt;Login&lt;/servlet-name&gt;  &lt;url-pattern&gt;/login.do&lt;/url-pattern&gt;&lt;/servlet-mapping&gt;</code></pre></li><li><p>编写Service层代码</p><p>该代码是一个 Java 类 <code>UserServiceImpl</code>，实现了一个接口 <code>UserService</code>。该类包含了两个方法：</p><ol><li><code>findByUserCode(String userCode)</code>：查找数据库中对应用户的信息，该方法返回一个 <code>User</code> 对象。</li><li><code>updateUser(Integer id, String key, String value)</code>：更新数据库中用户的信息，该方法返回一个 <code>boolean</code> 类型的值，表示更新是否成功。</li></ol><p>在这个类中，私有成员变量 <code>dao</code> 是一个 <code>UserDao</code> 类型的变量，通过该变量调用 <code>UserDaoImpl</code> 类中的方法来访问数据库。在 <code>findByUserCode</code> 和 <code>updateUser</code> 方法中，都需要先通过 <code>dao</code> 实例化一个 <code>UserDaoImpl</code> 对象来调用相应的方法。</p><pre><code class="language-java">public class UserServiceImpl implements UserService &#123;    private UserDao dao;    @Override    public User findByUserCode(String userCode)&#123;        dao = new UserDaoImpl();        User user = dao.findByUserCode(userCode);        return  user;    &#125;    @Override    public boolean updateUser(Integer id, String key, String value)  &#123;        dao = new UserDaoImpl();        int i = 0;        i = dao.updateUser(id, key, value);       return i &gt; 0 ? true : false;    &#125;&#125;</code></pre></li><li><p>DAO层代码</p><p>这段代码是一个Dao层的实现类，用于和数据库交互，提供了对User表的查询和更新方法。下面是代码的分析：</p><ul><li><code>findByUserCode(String userCode)</code> 方法：根据传入的用户名参数 <code>userCode</code> 查询对应的用户记录，并返回一个User对象。该方法使用了数据库连接池技术，在执行完查询后会关闭连接。方法中首先初始化参数，然后执行SQL语句查询结果集，遍历结果集将数据封装到User对象中，最后关闭连接和结果集，返回User对象。</li><li><code>updateUser(Integer id, String key,String value)</code> 方法：根据传入的参数更新User表中的数据。该方法也使用了数据库连接池技术，在执行完更新后会关闭连接。方法中首先判断传入的参数是否为空，然后执行SQL语句更新数据，最后关闭连接，返回更新结果的影响行数。</li></ul><pre><code class="language-java">public class UserDaoImpl implements UserDao&#123;    private User user;    private Connection conn;    private String sql;    @Override    public User findByUserCode(String userCode)&#123;        //2. 初始化参数        Object[] params = &#123;userCode&#125;;        ResultSet set;        sql = &quot;SELECT * FROM smbms_user Where userCode = ?&quot;;        try &#123;            conn = DB.getConnection();            set = DB.executeQuery(conn, sql,params);            if(set != null)&#123;                user = new User();                while (set.next()) &#123;                    user.setUserPassword(set.getString(&quot;userPassword&quot;));                    //一大串赋值代码...                    user.setWorkPicPath(&quot;workPicPath&quot;);                &#125;            &#125; else &#123;                return null;            &#125;            //关闭链接和结果集            DB.close(conn,null,set);        &#125; catch (SQLException e) &#123;            throw new RuntimeException(e);        &#125; catch (ClassNotFoundException e) &#123;            throw new RuntimeException(e);        &#125;        return user;    &#125;    @Override    public int updateUser(Integer id, String key,String value) &#123;        int affectedRow = 0;        if(id != null&amp;&amp; value != null &amp;&amp; key != null)&#123;            Connection conn = null;            sql = &quot;UPDATE smbms_user SET &quot;+key+&quot; = ? WHERE id = ? &quot;;            Object[] params = &#123;value,id&#125;;            try &#123;                conn = DB.getConnection();                affectedRow = DB.executeUpdate(conn, sql, params);                DB.close(conn,null,null);            &#125; catch (SQLException e) &#123;                throw new RuntimeException(e);            &#125; catch (ClassNotFoundException e) &#123;                throw new RuntimeException(e);            &#125;        &#125;        return affectedRow;    &#125;</code></pre></li></ul><h3 id="密码修改实现">密码修改实现</h3><h4 id="前端页面">前端页面</h4><p><strong>JSP部分: 页面显示</strong></p><pre><code class="language-jsp">&lt;%@ page contentType=&quot;text/html;charset=UTF-8&quot; language=&quot;java&quot; %&gt;&lt;%@include file=&quot;/jsp/common/head.jsp&quot; %&gt;&lt;div class=&quot;right&quot;&gt;    &lt;div class=&quot;location&quot;&gt;        &lt;strong&gt;你现在所在的位置是:&lt;/strong&gt;        &lt;span&gt;密码修改页面&lt;/span&gt;    &lt;/div&gt;    &lt;div class=&quot;providerAdd&quot;&gt;        &lt;form id=&quot;userForm&quot; name=&quot;userForm&quot; method=&quot;post&quot; action=&quot;$&#123;pageContext.request.contextPath &#125;/jsp/user.do&quot;&gt;            &lt;input type=&quot;hidden&quot; name=&quot;method&quot; value=&quot;savepwd&quot;/&gt;            &lt;!--div的class 为error是验证错误，ok是验证成功--&gt;            &lt;div class=&quot;info&quot;&gt;$&#123;message&#125;&lt;/div&gt;            &lt;div class=&quot;&quot;&gt;                &lt;label for=&quot;oldPassword&quot;&gt;旧密码：&lt;/label&gt;                &lt;input type=&quot;password&quot; name=&quot;oldpassword&quot; id=&quot;oldpassword&quot; value=&quot;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label for=&quot;newPassword&quot;&gt;新密码：&lt;/label&gt;                &lt;input type=&quot;password&quot; name=&quot;newpassword&quot; id=&quot;newpassword&quot; value=&quot;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div&gt;                &lt;label for=&quot;newPassword&quot;&gt;确认新密码：&lt;/label&gt;                &lt;input type=&quot;password&quot; name=&quot;rnewpassword&quot; id=&quot;rnewpassword&quot; value=&quot;&quot;&gt;                &lt;font color=&quot;red&quot;&gt;&lt;/font&gt;            &lt;/div&gt;            &lt;div class=&quot;providerAddBtn&quot;&gt;                &lt;!--&lt;a href=&quot;#&quot;&gt;保存&lt;/a&gt;--&gt;                &lt;input type=&quot;button&quot; name=&quot;save&quot; id=&quot;save&quot; value=&quot;保存&quot; class=&quot;input-button&quot;&gt;            &lt;/div&gt;        &lt;/form&gt;    &lt;/div&gt;&lt;/div&gt;&lt;/section&gt;&lt;%@include file=&quot;/jsp/common/foot.jsp&quot; %&gt;&lt;script type=&quot;text/javascript&quot; src=&quot;$&#123;pageContext.request.contextPath &#125;/statics/js/pwdmodify.js&quot;&gt;&lt;/script&gt;</code></pre><p>这是一个用 JSP（JavaServer Pages）编写的网页代码，用于修改密码。页面包括一个表单，用户可以在其中输入旧密码、新密码和确认新密码。表单使用POST方法提交到服务器，并使用“/jsp/user.do” URL。</p><p>页面包括一个消息区域，用于显示任何错误消息。页面还包括指向名为“pwdmodify.js”的 JavaScript 文件的链接。这个文件应该包含表单的客户端验证逻辑。</p><p>页面使用 JSP 表达式语言（EL）将动态内容插入到 HTML 中。例如，页面使用表达式 “${pageContext.request.contextPath}/statics/js/pwdmodify.js” 插入一个链接到 CSS 文件。“${pageContext.request.contextPath}” 表达式计算为 web 应用程序的上下文路径，用于构造 CSS 文件的完整 URL。</p><p><strong>JavaScript 表单的客户端验证逻辑</strong></p><pre><code class="language-js">var oldpassword = null;var newpassword = null;var rnewpassword = null;var saveBtn = null;$(function()&#123;   oldpassword = $(&quot;#oldpassword&quot;);   newpassword = $(&quot;#newpassword&quot;);   rnewpassword = $(&quot;#rnewpassword&quot;);   saveBtn = $(&quot;#save&quot;);   oldpassword.next().html(&quot;*&quot;);   newpassword.next().html(&quot;*&quot;);   rnewpassword.next().html(&quot;*&quot;);   oldpassword.on(&quot;blur&quot;,function()&#123;      $.ajax(&#123;         type:&quot;GET&quot;,         url:path+&quot;/jsp/user.do&quot;,         data:&#123;method:&quot;pwdmodify&quot;,oldpassword:oldpassword.val()&#125;,         dataType:&quot;json&quot;,         success:function(data)&#123;            if(data.result == &quot;true&quot;)&#123;//旧密码正确               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);            &#125;else if(data.result == &quot;false&quot;)&#123;//旧密码输入不正确               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 原密码输入不正确&quot;,false);            &#125;else if(data.result == &quot;sessionerror&quot;)&#123;//当前用户session过期，请重新登录               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 当前用户session过期，请重新登录&quot;,false);            &#125;else if(data.result == &quot;error&quot;)&#123;//旧密码输入为空               validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 请输入旧密码&quot;,false);            &#125;         &#125;,         error:function(data)&#123;            //请求出错            validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 请求错误&quot;,false);         &#125;      &#125;);   &#125;).on(&quot;focus&quot;,function()&#123;      validateTip(oldpassword.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 请输入原密码&quot;,false);   &#125;);   newpassword.on(&quot;focus&quot;,function()&#123;      validateTip(newpassword.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 密码长度必须是大于6小于20&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      if(newpassword.val() != null &amp;&amp; newpassword.val().length &gt; 5         &amp;&amp; newpassword.val().length &lt; 20 )&#123;         validateTip(newpassword.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(newpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 密码输入不符合规范，请重新输入&quot;,false);      &#125;   &#125;);   rnewpassword.on(&quot;focus&quot;,function()&#123;      validateTip(rnewpassword.next(),&#123;&quot;color&quot;:&quot;#666666&quot;&#125;,&quot;* 请输入与上面一致的密码&quot;,false);   &#125;).on(&quot;blur&quot;,function()&#123;      if(rnewpassword.val() != null &amp;&amp; rnewpassword.val().length &gt; 5         &amp;&amp; rnewpassword.val().length &lt; 20 &amp;&amp; newpassword.val() == rnewpassword.val())&#123;         validateTip(rnewpassword.next(),&#123;&quot;color&quot;:&quot;green&quot;&#125;,imgYes,true);      &#125;else&#123;         validateTip(rnewpassword.next(),&#123;&quot;color&quot;:&quot;red&quot;&#125;,imgNo + &quot; 两次密码输入不一致，请重新输入&quot;,false);      &#125;   &#125;);   saveBtn.on(&quot;click&quot;,function()&#123;      oldpassword.blur();      newpassword.blur();      rnewpassword.blur();      if(         oldpassword.attr(&quot;validateStatus&quot;) == &quot;true&quot; &amp;&amp;          newpassword.attr(&quot;validateStatus&quot;) == &quot;true&quot;         &amp;&amp; rnewpassword.attr(&quot;validateStatus&quot;) == &quot;true&quot;)&#123;         if(confirm(&quot;确定要修改密码？&quot;))&#123;            $(&quot;#userForm&quot;).submit();         &#125;      &#125;   &#125;);&#125;);</code></pre><p>这是一个 JavaScript 代码段，它使用 jQuery 库为一个包含密码修改表单的网页添加了客户端验证逻辑。代码使用 AJAX 技术从服务器获取 JSON 格式的响应数据，以验证用户输入的旧密码是否正确，并在页面上显示相关提示信息。</p><p>代码中首先定义了四个变量，分别是旧密码、新密码、确认新密码和提交按钮的 jQuery 对象。然后使用 $() 函数将代码包装在一个回调函数中，确保代码在文档加载完成后才执行。回调函数内部做了以下工作：</p><ol><li><p>通过选择器获取页面上的旧密码输入框、新密码输入框、确认新密码输入框和提交按钮，并将它们的 jQuery 对象赋值给前面定义的四个变量。</p></li><li><p>为旧密码输入框绑定 blur 事件处理函数，当输入框失去焦点时，使用 $.ajax() 函数向服务器发送 GET 请求，验证用户输入的旧密码是否正确，并根据服务器返回的响应数据显示相应的提示信息。在成功回调函数中，根据 data.result 的值判断旧密码是否正确，并通过 validateTip() 函数显示相应的提示信息。在失败回调函数中，显示请求出错的提示信息。</p></li><li><p>为旧密码输入框绑定 focus 事件处理函数，当输入框获得焦点时，显示输入提示信息。</p></li><li><p>为新密码输入框绑定 focus 和 blur 事件处理函数，当输入框获得焦点时，显示输入提示信息；当输入框失去焦点时，判断用户输入的新密码是否符合规范，如果符合则显示正确提示，否则显示错误提示。</p></li><li><p>为确认新密码输入框绑定 focus 和 blur 事件处理函数，当输入框获得焦点时，显示输入提示信息；当输入框失去焦点时，判断用户输入的确认新密码是否与新密码一致，如果一致则显示正确提示，否则显示错误提示。</p></li><li><p>为提交按钮绑定 click 事件处理函数，当按钮被点击时，先让旧密码、新密码和确认新密码输入框失去焦点，然后判断它们的验证状态是否都为 true，如果是，则显示确认修改密码的提示框，如果用户点击确定，则提交表单，将新密码保存到服务器上。</p></li></ol><h4 id="编写处理Servlet">编写处理Servlet</h4><pre><code class="language-java">public class UserDo extends HttpServlet &#123;    private UserService service;    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        String method = req.getParameter(&quot;method&quot;);        if(&quot;savepwd&quot;.equals(method))&#123;            savePwd(req, resp);        &#125; else if(&quot;pwdmodify&quot;.equals(method))&#123;            pwdModify(req, resp);        &#125;    &#125;    protected void savePwd(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException &#123;        //拿到USer Session , 用于得到用户id        HttpSession session = req.getSession();        User user = (User) session.getAttribute(Constant.USER_SESSION);        String newPwd = req.getParameter(&quot;newpassword&quot;);        System.out.println(&quot;用户输入的：new password: &quot;+newPwd);        System.out.println(&quot;Session password: &quot;+user.getUserPassword()+&quot;, id: &quot;+ user.getId());        Integer id = user.getId();        boolean flag;        //method 值为  savepwd, 表示用户执行修改密码操作        if(newPwd != null)&#123;            service = new UserServiceImpl();            flag = service.updateUser(id, &quot;userPassword&quot;, newPwd);            //修改成功            if(flag)&#123;                System.out.println(&quot;修改成功&quot;);                req.setAttribute(&quot;message&quot;,&quot;密码修改成功， 请重新登录&quot;);                //  移除Session                session.removeAttribute(Constant.USER_SESSION);            &#125; else &#123;                System.out.println(&quot;密码修改失败&quot;);                req.setAttribute(&quot;message&quot;,&quot;密码修改失败&quot;);            &#125;        &#125; else &#123;            req.setAttribute(&quot;message&quot;,&quot;新密码格式错误&quot;);        &#125;        //转发到当前页面, 不用重定向而使用转发是为了传统参数（attribute) 通知前端密码修改结果（by attribute message）        req.getRequestDispatcher(&quot;/jsp/pwdmodify.jsp&quot;).forward(req,resp);    &#125;    /*    由ajax发起的密码查询请求， 用户进行密码修改时触发    放回前端json， ajax用于比对旧密码正确性,     */    protected void pwdModify(HttpServletRequest req, HttpServletResponse resp)&#123;        HttpSession session = req.getSession();        Object user = session.getAttribute(Constant.USER_SESSION);        Map&lt;String, String&gt; map = new HashMap&lt;String, String&gt;();        if(user != null)&#123;            String password = ((User) user).getUserPassword();            String inputPwd = req.getParameter(&quot;oldpassword&quot;);            if(inputPwd != null &amp;&amp; inputPwd.length() &gt; 0)&#123;                //旧密码正确                if(inputPwd.equals(password))&#123;                    map.put(&quot;result&quot;,&quot;true&quot;);                &#125; else &#123;                    //旧密码不正确                    map.put(&quot;result&quot;,&quot;false&quot;);                &#125;            &#125; else &#123;                //密码输入为空                map.put(&quot;result&quot;,&quot;error&quot;);            &#125;        &#125; else &#123;            //session timeout            map.put(&quot;result&quot;,&quot;sessionerror&quot;);        &#125;        //返回前端json数据        resp.setContentType(&quot;application/json&quot;);        try &#123;            PrintWriter writer = resp.getWriter();            writer.write(JSONArray.toJSONString(map));            writer.flush();            writer.close();        &#125; catch (IOException e) &#123;            throw new RuntimeException(e);        &#125;    &#125;&#125;</code></pre><p>这段代码是一个Java Web应用程序中的Servlet，用于处理用户密码修改操作。</p><p>在doGet()方法中，根据请求参数&quot;method&quot;的值进行不同的操作。如果&quot;method&quot;的值是&quot;savepwd&quot;，则调用savePwd()方法，保存新密码；如果&quot;method&quot;的值是&quot;pwdmodify&quot;，则调用pwdModify()方法，用于检查旧密码是否正确。</p><p>在savePwd()方法中，首先从Session中获取当前用户的信息，然后获取新密码，并使用UserService接口的实现类UserServiceImpl来更新用户密码。如果更新成功，将信息存储在请求属性中，并移除Session中的用户信息，最后将请求转发到密码修改页面。</p><p>在pwdModify()方法中，首先获取当前用户的信息，然后获取旧密码。如果旧密码不为空，则将输入的旧密码与Session中存储的旧密码进行比较。如果输入的旧密码与Session中的旧密码相同，则返回前端JSON数据，表示旧密码正确；否则返回JSON数据，表示旧密码不正确。</p><p><strong>需要注意的是，在返回JSON数据之前，需要设置响应的Content-Type为&quot;application/json&quot;。</strong></p><h5 id="映射Servlet">映射Servlet</h5><pre><code class="language-xml">  &lt;servlet&gt;    &lt;servlet-name&gt;userDo&lt;/servlet-name&gt;    &lt;servlet-class&gt;servlet.UserDo&lt;/servlet-class&gt;  &lt;/servlet&gt;  &lt;servlet-mapping&gt;    &lt;servlet-name&gt;userDo&lt;/servlet-name&gt;    &lt;url-pattern&gt;/jsp/user.do&lt;/url-pattern&gt;  &lt;/servlet-mapping&gt;</code></pre>]]></content>
    
    
    <summary type="html">狂神说JavaWeb快速入门笔记</summary>
    
    
    
    <category term="Java web" scheme="https://trevoewu.github.io/categories/Java-web/"/>
    
    
    <category term="JavaWeb" scheme="https://trevoewu.github.io/tags/JavaWeb/"/>
    
    <category term="学习笔记" scheme="https://trevoewu.github.io/tags/%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    
  </entry>
  
  <entry>
    <title>大学生就业指导课程心得体会</title>
    <link href="https://trevoewu.github.io/2023/05/11/%E5%AD%A6%E4%B9%A0%E5%BF%83%E5%BE%97/"/>
    <id>https://trevoewu.github.io/2023/05/11/%E5%AD%A6%E4%B9%A0%E5%BF%83%E5%BE%97/</id>
    <published>2023-05-11T10:36:45.000Z</published>
    <updated>2023-05-11T09:05:21.910Z</updated>
    
    <content type="html"><![CDATA[<p>大学生就业指导课程是为了帮助大学生更好地了解职业发展、掌握求职技巧、提高职场竞争力而设计的一门课程。学习这门课程，可以让学生更好地了解自己、掌握自己的优势和劣势，规划自己的职业道路，提高就业竞争力。</p><span id="more"></span><ol><li><p>自我认知非常重要。在课程中，我们学习了如何通过自我分析、职业测评等方式，了解自己的性格、兴趣、能力和价值观等，这对于选择适合自己的职业非常重要。因此，在学习课程时，我认为要重视自我认知，认真分析自己的优势和劣势，了解自己的职业目标和规划。</p></li><li><p>掌握求职技巧。在课程中，我们学习了如何制作简历、撰写求职信、面试技巧等，这些技巧对于我们成功找到一份工作非常重要。因此，在学习课程时，我认为要认真学习这些技巧，并在实践中不断提高。</p></li><li><p>学习职业规划。在课程中，我们学习了如何规划自己的职业道路，了解职业发展方向、职业发展阶段等。在我看来，这些知识对于大学生来说非常有价值，因为我们需要在职业生涯中不断发展自己，提高自己的职业素养和能力，才能更好地适应职业发展的要求。</p></li><li><p>积极参与课程活动。在课程中，我们参加了模拟面试、实习招聘会等活动，这些活动让我更好地了解了职业发展的现状和要求，也提高了我对职业发展的认识和信心。因此，我认为积极参与课程活动是非常重要的。</p></li><li><p>注重实践和经验积累。除了学习理论知识，我们还需要注重实践和经验积累。在课程中，我们可以通过实习、兼职、志愿活动等方式，积累实践经验，丰富自己的简历和求职经历。同时，在实践中也可以不断提高自己的职业素养和能力。</p></li><li><p>保持乐观心态和自信。在职业发展的过程中，我们难免会遇到挫折和困难，但是保持乐观心态和自信非常重要。在课程中，我们学习了如何应对挫折和压力，如何调整自己的心态和情绪。我认为，只有保持乐观心态和自信，才能充满信心地面对职业发展的挑战和机遇。</p></li><li><p>建立个人品牌。在现今竞争激烈的职场中，个人品牌的建立非常重要。在大学生就业指导课程中，我们学习了如何打造自己的个人品牌，包括简历的编写、面试技巧、自我介绍等。建立良好的个人品牌可以让我们在众多求职者中脱颖而出，增加求职的成功率。</p></li><li><p>不断学习和提升自己。职场是一个不断学习和成长的过程，我们需要不断学习和提升自己的能力和素养。在大学生就业指导课程中，我们学习了如何制定职业发展计划，如何选择职业发展方向，如何评估自己的职业发展进展等。通过不断学习和提升自己，我们可以更好地适应职场的变化和挑战，实现职业发展的跨越式提升。</p></li></ol><p>大学生就业指导课程对于大学生职业发展非常重要，通过学习这门课程，我们可以更好地了解自己、掌握求职技巧、规划职业发展，提高职业竞争力。我认为，只有认真学习和实践，才能真正掌握这些知识和技能，为自己的职业生涯打下坚实的基础。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;大学生就业指导课程是为了帮助大学生更好地了解职业发展、掌握求职技巧、提高职场竞争力而设计的一门课程。学习这门课程，可以让学生更好地了解自己、掌握自己的优势和劣势，规划自己的职业道路，提高就业竞争力。&lt;/p&gt;</summary>
    
    
    
    
    <category term="水课论文" scheme="https://trevoewu.github.io/tags/%E6%B0%B4%E8%AF%BE%E8%AE%BA%E6%96%87/"/>
    
  </entry>
  
  <entry>
    <title>Wireshark Lab: Ethernet and ARP</title>
    <link href="https://trevoewu.github.io/2023/05/08/Wireshark-Lab-Ethernet-and-ARP/"/>
    <id>https://trevoewu.github.io/2023/05/08/Wireshark-Lab-Ethernet-and-ARP/</id>
    <published>2023-05-08T11:02:02.000Z</published>
    <updated>2023-05-08T13:26:30.036Z</updated>
    
    <content type="html"><![CDATA[<p><strong>官方英文文档：<a href="Wireshark_Intro_v6.01.pdf">Wireshark_Intro_v6.01.pdf</a></strong></p><p><strong>以下内容为笔者翻译：</strong></p><hr><h2 id="Wireshark-实验-Ethernet-and-ARP-v7-0">Wireshark 实验:  Ethernet and ARP v7.0</h2><p><strong>《计算机网络：自顶向下方法（第6版）》补充材料，J.F. Kurose and K.W. Ross</strong></p><p>“不闻不若闻之，闻之不若见之，见之不若知之，知之不若行之。” ——中国谚语</p><p>© 2005-2012, J.F Kurose and K.W. Ross, All Rights Reserved</p><hr><p>在本实验中，我们将研究以太网协议和 ARP 协议。在开始实验之前， 您可以查看课本的 6.4.1 节（链路层地址和 ARP）和 6.4.2（以太网）, 您也可以去看 RFC 826(<a href="ftp://ftp.rfc-editor.org/in-notes/std/std37.txt">ftp://ftp.rfc-editor.org/in-notes/std/std37.txt</a>)了解关于 ARP 的协议详细信息,该协议可以根据 IP 地址获取远程主机的的物理地址。</p><h2 id="捕获和分析以太网帧">捕获和分析以太网帧</h2><p>让我们从捕获一组以太网帧开始研究。 请执行下列操作 :</p><ul><li><p>首先，清除浏览器缓存。 在Firefox 下执行此操作，请选择 历史记录，然后选中清除历史记录。</p></li><li><p>然后启动 Wireshark 数据包嗅探器</p></li><li><p>打开以下 URL <a href="http://gaia.cs.umass.edu/wireshark-labs/HTTP-ethereal-lab-file3.html">http://gaia.cs.umass.edu/wireshark-labs/HTTP-ethereal-lab-file3.html</a> 您的浏览器应显示相当冗长的美国权利法案。</p></li><li><p>接下来停止 Wireshark 数据包捕获，找到您向 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 的 HTTP GET 消 息的数据包编号以及 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 相应您的 HTTP 回应。您的抓包结果应 看起来向下面一样:</p></li></ul><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508193529270.png" alt="image-20230508193529270" loading="lazy"></p><ul><li><p>由于本实验是关于以太网和 ARP 的，我们对 IP 或更高层协议不感兴趣。 因此，让我们更改 Wireshark 的“捕获数据包列表”窗口，以便它仅显示有关 IP 以下协议的信息。 要让 Wireshark 执行此操作，请选择 Analyze-&gt; Enabled Protocols(分析-启用的协议)。 然后取消选中 IP 框(译者注:这里指的 IPV4 协议，下面有搜索)并选择确定。 您现在 Wireshark 窗口应该如下所示:</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508190619603.png" alt="image-20230508190619603" loading="lazy"></p></li><li><p>为了回答以下问题，您需要查看数据包详细信息和数据包内容窗口(Wireshark 中 的中间和下部显示窗口，译者注: 若您看到的不是如此-建议您重置布局(视图-重 置布局))。</p></li><li><p>选择包含 HTTP GET 消息的以太网帧。 (回想一下，HTTP GET 请求是被加上 TCP 头封装到 TCP 段进行传输,TCP 段加上 IP 头被封装到 IP 数据报进行传输,IP 数 据报又被加上以太网头封装成以太网帧进行传输;如果你发现这个封装有点令人困 惑，请重读文本中的第 1.5.2 节)。 在数据包详细信息窗口中展开以太网 II 信息。 请注意，以太网帧的内容(标题以及有效负载)显示在数据包内容窗口中</p></li><li><p>根据包含 HTTP GET 消息的以太网帧进行分析，如果有可能建议您使用标记的方式展现您的答案。</p></li></ul><ol><li>你的电脑 48 位的地址是多少</li></ol><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508195458584.png" alt="image-20230508195458584" loading="lazy"></p><ol><li>以太网帧中的 48 位目标地址是什么?这是 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 的以太网地址 吗?(提示:答案是否定的)。那么它是什么?注意这一题可能会犯错，请 阅读 468-469(中文版 305-308 页)然后理解它</li></ol><p>下一跳路由器的对应接口的MAC地址.</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508194109036.png" alt="image-20230508194109036" loading="lazy"></p><ol><li>以太网帧上层协议 16 进制值是什么?这对应的上层协议是什么?</li></ol><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508194227724.png" alt="image-20230508194227724" loading="lazy"></p><ol><li><p>从以太帧的开始，一直到“GET”中的 ASCII“G”出现在以太网帧中為止，有多</p><p>少字节?</p><p>G 的ascii码为0x47, 对应地址为0x0042, 故在这之前共有0x43字节, 即67Byte</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508194758609.png" alt="image-20230508194758609" loading="lazy"></p></li></ol><p>接下来，根据包含 HTTP 响应消息的第一个字节的以太网帧的内容，回答以下问题</p><ol start="5"><li><p>这个以太网帧中，以太网源地址的值是多少?这是你的计算机的地址，<a href="http://xn--gaia-zi0i364s.cs.umass.edu">还是gaia.cs.umass.edu</a> 的地址(提示:答案是否定的)。拥有这个以太网地址的设备是什么?</p><p>ANS: 是最后一跳路由器网卡的MAC地址</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508195637736.png" alt="image-20230508195637736" loading="lazy"></p></li><li><p>以太网帧中的目的地址是什么?这是您的计算机的以太网地址吗?</p><p>ANS: 是的</p></li></ol><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508195734432.png" alt="image-20230508195734432" loading="lazy"></p><ol start="5"><li>以太网帧上层协议 16 进制值是什么?这对应的上层协议是什么?</li></ol><p>ANS: 0x0800, IPv4</p><ol start="5"><li>从以太帧的开始，一直到“OK”中的 ASCII“O”出现在以太网帧中为止，有多少字节?</li></ol><p>ANS: 0x4f Byte, 79Byte</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508200014667.png" alt="image-20230508200014667" loading="lazy"></p><h2 id="The-Address-Resolution-Protocol-地址解析协议">The Address Resolution Protocol 地址解析协议</h2><p>在本节中，我们将观察 ARP 协议的作用。我们强烈建议您在继续实验之前重读课文 6.4.1 节</p><h3 id="ARP-Caching-ARP-缓存">ARP Caching(ARP 缓存)</h3><p>回想一下，ARP 协议通常在您的计算机上维护 IP 到以太网地址转换对的缓存.arp 命令(在 MSDOS 和 Linux / Unix 中)用于查看和操作此缓存的内容。由于 arp 命 令和 ARP 协议具有相同的名称，因此很容易混淆它们。但请记住，它们是不同的: arp 命令用于查看和操作 ARP 缓存内容，而 ARP 协议定义了发送和接收的消息的 格式和含义，并定义了对消息传输和接收所采取的操作。</p><p>我们来看看您计算机上 ARP 缓存的内容:</p><ul><li><p>MS-DOS:arp 命令位于 c:\windows\system32 中，因此在 MS-DOS 命令行中</p><p>输入“arp”或“c:\windows\system32\arp”(没有引号)</p></li><li><p>Linux/Unix/MacOS. 根据安装位置不同路径而不同，一般有/sbin/arp (linux)</p><p>和/usr/etc/arp (Unix)</p></li></ul><p>没有参数的 Windows arp 命令将显示计算机上 ARP 缓存的内容。运行 ARP 命 令。</p><ol start="9"><li>写下计算机 ARP 缓存的内容。每个列值的含义是什么?</li></ol><pre><code class="language-shell">// mac/linux arp commend by tldr - Show the current ARP table:    arp -a- Delete a specific entry:    arp -d address- Create an entry in the ARP table:    arp -s address mac_address</code></pre><p>IP地址, MAC地址</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508200844492.png" alt="image-20230508200844492" loading="lazy"></p><p>为了观察您的计算机发送和接收 ARP 消息，我们需要清除 ARP 缓存，否则您的计算机很可能在其缓存中找到所需的 IP-Ethernet 地址转换关系，因此不会发送 ARP 消息。</p><ul><li>MS-DOS:MS-DOS arp -d <em>命令将清除 ARP 缓存。-d 标志指示删除操作，</em> *  是表示删除所有表项的通配符。</li><li>Linux/Unix/MacOS: 清除 arp 缓存的 arp –d *需要 root 权限，如果您没有也没办法使用 Windows 系统进行实验，请下载作者的抓包结果。</li></ul><p><em>所有在Linux系统下 <strong>arp -d $ip</strong> 命令只能清除一个IP地址的对应MAC地址缓存，当然可以使用组合命令操作，这也算是Linux的一个优点吧。</em></p><h3 id="Observing-ARP-in-action-抓取-ARP-消息">Observing ARP in action 抓取 ARP 消息</h3><p>请进行以下操作:</p><ul><li><p>清除你的 ARP 缓存，如上所述。</p><pre><code class="language-shell">sudo arp -a -d //clear arp cahce in mac os</code></pre><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508201938321.png" alt="image-20230508201938321" loading="lazy"></p></li><li><p>接下来，确保浏览器的缓存是空的。</p></li><li><p>启动 Wireshark 捕捉封包</p></li><li><p>打开以下 URL，<a href="http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-lab-file3.html%E3%80%82%E4%BD%A0%E7%9A%84%E6%B5%8F%E8%A7%88%E5%99%A8%E5%BA%94%E8%AF%A5%E5%86%8D%E6%AC%A1%E6%98%BE%E7%A4%BA%E7%9B%B8%E5%BD%93%E9%95%BF%E7%9A%84%E7%BE%8E%E5%9B%BD%E6%9D%83%E5%88%A9%E6%B3%95%E6%A1%88">http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-lab-file3.html。你的浏览器应该再次显示相当长的美国权利法案</a></p></li><li><p>同样设置不显示 IP 和更高层协议，请选择 Analyze-&gt; Enabled Protocols(分析 -启用的协议)。 然后取消选中 IP 框(译者注:这里指的 IPV4 协议，下面有 搜索)并选择确定。 您现在 Wireshark 窗口应该如下所示:</p></li></ul><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508191216762.png" alt="image-20230508191216762" loading="lazy"></p><p>图示是作者的抓包结果截图，您可以发现第 1，2，6 帧都包含 ARP 消息</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508203130494.png" alt="image-20230508203130494" loading="lazy"></p><p>Answer the following questions: (回答下列问题)</p><ol start="10"><li>包含 ARP 请求消息的以太网帧中源和目标地址的十六进制值是什么?</li></ol><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508202533299.png" alt="image-20230508202533299" loading="lazy"></p><ol start="10"><li><p>以太网帧上层协议 16 进制值是什么?</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508202601296.png" alt="image-20230508202601296" loading="lazy"></p></li><li><p>下载 ARP 规范(<a href="ftp://ftp.rfc-editor.org/in-notes/std/std37.txt.">ftp://ftp.rfc-editor.org/in-notes/std/std37.txt.</a>),讨论请移步</p><p>(<a href="http://www.erg.abdn.ac.uk/users/gorry/course/inet-pages/arp.html">http://www.erg.abdn.ac.uk/users/gorry/course/inet-pages/arp.html</a>)</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508204313285.png" alt="image-20230508204313285" loading="lazy"></p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508204134165.png" alt="image-20230508204134165" loading="lazy"></p><p>a) ARP 操作码字段开始从以太网帧的最开始有多少字节?</p><p>0x13Byte = 19Byte,</p><p>操作码：1为ARP请求，2为ARP应答，3为<a href="https://zh.wikipedia.org/wiki/%E9%80%86%E5%9C%B0%E5%9D%80%E8%A7%A3%E6%9E%90%E5%8D%8F%E8%AE%AE">RARP</a>请求，4为RARP应答。</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508204930125.png" alt="image-20230508204930125" loading="lazy"></p><p>b) 在进行 ARP 请求的以太网帧的 ARP 负载部分中，操作码字段的值是 多少?</p><p>操作码1为ARP请求</p><p>c) ARP 消息是否包含发送方的 IP 地址?</p><p>不包括.</p><p>d) 在 ARP 请求中从哪里看出我们要查询相应 IP 的以太网地址?</p><p>图中蓝色部分, 可以看到要查询的IP(16进制), 转换为十进制为: 10.51.187.123</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508205742976.png" alt="image-20230508205742976" loading="lazy"></p></li><li><p>找到相应 ARP 请求的而发送 ARP 回复</p></li></ol><pre><code>1. a) ARP 操作码字段开始从以太网帧的最开始有多少字节?2. b)  在进行 ARP 响应的以太网帧的 ARP 负载部分中，操作码字段的值是   多少?   2, 表示ARP响应3. c)   在响应 ARP 中从哪里看出现早期 ARP 请求的答案?   响应ARP中的源MAC地址</code></pre><ol start="12"><li><p>包含 ARP 回复消息的以太网帧中的源地址和目标地址的十六进制值是多 少?</p></li><li><p>在作者抓包结果中，他有两台电脑，一台运行 wireshark 进行抓包，一台没 有，那么为什么运行 wireshark 那台电脑发送 ARP 请求得到了应答，另外一 台电脑的 ARP 请求没有得到应答?(没有相应第 6 帧的 ARP 的请求)</p></li></ol><p>请求ARP的目标MAC地址48位全1, 是广播地址, 所以局域网设备都能收到, 而响应ARP是单播地址, 目标MAC为请求目标的MAC地址, 故只有发起对该IP请求对电脑能收到.</p><h3 id="Extra-Credit-额外实验">Extra Credit 额外实验</h3><p>EX-1. The arp command: arp 命令:</p><pre><code class="language-shell">arp -s InetAddr EtherAddr</code></pre><p>这个命令允许你手动添加 arp 记录到缓存表中。它会把您输入的 IP 地址 (InetAddr)解析为物理地址(EtherAddr)，请问您输入正确 IP 地址但是物理地 址错误会发生什么。</p><p>成功写入了, 类型为permanent, 没有发生什么, 而且可以在ARP table中看到两条同一IP地址的不同MAC地址条目.</p><p><img src="Wireshark-Lab-Ethernet-and-ARP/image-20230508211728737.png" alt="image-20230508211728737" loading="lazy"></p><p>EX-2. 在删除 ARP 缓存之前，请问它们默认的有效时间是多少，您可以通过不定 时的查看缓存内容得出结论或者查询相应的操作系统文档。</p>]]></content>
    
    
    <summary type="html">在本实验中，我们将研究以太网协议和ARP协议</summary>
    
    
    
    <category term="学习记录" scheme="https://trevoewu.github.io/categories/%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/"/>
    
    
    <category term="computer network" scheme="https://trevoewu.github.io/tags/computer-network/"/>
    
  </entry>
  
  <entry>
    <title>Wireshark Lab 802.11</title>
    <link href="https://trevoewu.github.io/2023/05/08/Wireshark-Lab-802-11/"/>
    <id>https://trevoewu.github.io/2023/05/08/Wireshark-Lab-802-11/</id>
    <published>2023-05-08T10:36:45.000Z</published>
    <updated>2023-05-08T11:01:19.094Z</updated>
    
    <content type="html"><![CDATA[<hr><h2 id="Wireshark-实验-802-11">Wireshark 实验:  802.11</h2><p><strong>《计算机网络：自顶向下方法（第6版）》补充材料，J.F. Kurose and K.W. Ross</strong></p><p>“不闻不若闻之，闻之不若见之，见之不若知之，知之不若行之。” ——中国谚语</p><p>© 2005-2012, J.F Kurose and K.W. Ross, All Rights Reserved</p><hr><p>在本实验中，我们将研究 802.11 无线协议。在开始本实验之前，建议您重新阅读课本的中第 7.3 节。因为我们将比课本深入研究 802.11 协议内容，因此您可能需要查看 Pablo Brenner (Breezecom Communications)写的《802.11 协议技术指南》<a href="http://www.sss-mag.com/pdf/802_11tut.pdf">ATechnical Tutorial on the 802.11Protocol </a> 以及 Jim Geier 写的《了解 802.11 帧类型》<a href="http://www.wi-fiplanet.com/tutorials/article.php/1447501">Understanding 802.11 Frame Types</a> 。当然，您还需要查阅 802.11 的圣经-它的标准书<a href="http://gaia.cs.umass.edu/wireshark-labs/802.11-1999.pdf">《ANSI / IEEE 标准 802.11,1999 版(R2003)》(ANSI/IEEE Std 802.11, 1999 Edition (R2003)</a>。您可能会发现标准书第 36 页的表 1 在分析无线跟踪特别有用.</p><p>在除本次实验以外其它的 Wireshark 实验中，我们都是在有线以太网连接进行抓包(捕获帧)。在本实验中，因为 802.11 是无线链路层协议，我们将在“空中”捕获帧。不幸的是，带有 802.11 协议的无线网卡(NIC)设备驱动无法提供钩子将捕获/接收的 802.11 帧用于 Wireshark 实验分析。因此，在本实验中，您可能会使用作 者捕获的 802.11 帧(抓包结果)进行分析。如果您有能力自己捕获，欢迎您自己动手。如果您乐意自己跟踪，或许你可以买一个小的 <a href="www.cacetech.com">USB 网卡 AirPcap 用以捕获 802.1 帧</a></p><h2 id="Getting-Started-开始实验">Getting Started 开始实验</h2><p>从 <a href="http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip">http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip</a> 下载压缩包并且得到 Wireshark_802_11.pcap。本结果由团队的一个作者在家庭网络使用 AirPcap 以及运行 Wireshark 的计算机得到，结果其中包括 Linksys 802.11g 的组合接入点(路由器)，该接入点为两台有线 PC 和一台无线 PC 提供服务。作者幸运邻居也使用无线接入点。在此跟踪文件中，我们能看到在通道 6 上捕获的帧。由于我们需要分析主机和接入点不是使用通道 6 的唯一组合，我们也会看到其他例如邻居接入点使用通道6被作者收集到。跟踪文件中采用的无线主机活动包括:</p><ul><li><p>跟踪开始时，主机已经与 30 Munroe St 接入点关联(associated)。</p></li><li><p>在时间 24.82 时刻，主机向 IP 地址 128.119.245.12 的服务器发送 HTTP 请</p><p>求，请求内容是 <a href="http://gaia.cs.umass.edu/wireshark-labs/alice.txt">http://gaia.cs.umass.edu/wireshark-labs/alice.txt</a> 。</p></li><li><p>在时间 32.82 时刻，主机向 IP 地址 128.119.240.19.的服务器发送 HTTP 请求，请求内容是 <a href="http://www.cs.umass.edu">http://www.cs.umass.edu</a>。</p></li><li><p>在时间 49.58 的时刻，主机断开了与 30 Munroe St 接入点的关联，并且尝试</p></li></ul><p>连接到 linksys_ses_24086 接入点。该接入点不是开放的接入点，因此主机始终没有成功与该接入点关联成功。</p><ul><li>在时间 63.0 时刻，主机放弃尝试关联 linksys_ses_24086 接入点，并且再次连接30 Munroe St 接入点关联。</li></ul><ol><li><p>下载作者的抓包结果后，使用 Wireshark 的 File 菜单打开该文件</p><p>Wireshark_802_11.pcap，您应该看到与图 1 显示相同的结果</p><p><img src="Wireshark-Lab-802-11/image-20230508184630447.png" alt="image-20230508184630447" loading="lazy"></p></li></ol><h2 id="Beacon-Frames-信标帧">Beacon Frames 信标帧</h2><p>回想一下，802.11 接入点使用信标帧表示其存在。要回答下面的问题，您或许应该 展开 IEEE 802.11 帧并在 Wireshark 中间窗口看到它的字段详细信息。请尽量清晰 的展示您的答案，必要时您可以在图中用标记辅以说明。您的答案应该简单可读。</p><ul><li><p>发送最多信标帧的两个接入点的服务集标识符是多少(SSID)?</p></li><li><p>inksys_ses_24086 接入点和 30 Munroe St.接入点的信标帧传输时间是多少?(提示:此时间间隔包含在信标帧本身之中。)</p></li><li><p>请以十六进制表示法找到 30 Munroe St 接入点的源 MAC 地址。回想课本中 图 7-13，并说明源地址，目的地址，基本服务集(BSS)的地址是什么。有 关 802.11 帧结构的详细讨论说明，请参阅 IEEE 802.11 标准文档(上文所 提)中的第 7 节。</p></li><li><p>30 Munroe St 接入点的信标帧目的地址十六进制表示是什么?</p></li><li><p>30 Munroe St 接入点的 BSS ID 地址是什么?</p></li><li><p>来自 30 Munroe St 接入点的信标帧宣告接入点可以支持四种数据速率和八种</p><p>额外的“扩展支持速率”。这些速率是多少?</p></li></ul><h2 id="Data-Transfer-数据传输">Data Transfer 数据传输</h2><p>由于作者抓包开始时，主机已经与 AP 关联，因此在做下面 AP 关联/解除关联之 前，我们首先研究已经主机与关联 802.11 AP 的数据传输。回想一下，在作者抓包 结果中，在时间 24.82 的时刻，主机向 <a href="http://gaia.cs.umass.edu/wireshark-">http://gaia.cs.umass.edu/wireshark-</a> labs/alice.txt 发出 HTTP 请求。 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 的 IP 地址是 128.119.245.12。然 后，在 32.82 时刻，主机向 <a href="http://www.cs.umass.edu">http://www.cs.umass.edu</a> 发出 HTTP 请求。</p><ul><li>找到包含第一个 TCP SYN TCP 报文(下载 alice.txt 会话)的 802.11 帧。 802.11 帧中三个 MAC 地址字段分别是什么?此帧中那个 MAC 地址对应十 六进制的无线主机的 MAC 地址?那个对应接入点 MAC 地址，第一跳路由 器的 MAC 地址?发送此 TCP 报文的无线主机 IP 是什么?目的地 IP 地址是 什么?此目的地是否与主机地址，接入点地址，第一跳路由器地址或者某些 其他网络设备相对应，解释并且说明。</li><li>找到包含此 TCP 会话 SYN ACK 报文的 802.11 帧。该帧中三个 MAC 地址 字段是什么?那个 MAC 地址对应十六进制的无线主机的 MAC 地址?那个 对应接入点 MAC 地址，第一跳路由器的 MAC 地址?帧中的发送方 MAC 地址是否与发送此 TCP 报文的设备的 IP 地址相对应?</li></ul><h2 id="Association-Disassociation-关联-解除关联">Association/Disassociation 关联/解除关联</h2><p>回想一下课本的第 7.3.1 节，主机在发送数据之前必须与接入点关联。802.11 中的关联使用 ASSOCIATE REQUEST 帧(从主机发送到 AP，帧类型 0 和子类型 0，参 见本文中的第 7.3.3 节)和 ASSOCIATE RESPONSE 帧(由 AP 发送给具有主机， 帧类型 0 和子类型 1，响应于接收到的 ASSOCIATE REQUEST)。有关 802.11 帧 中每个字段的详细说明，请参阅 <a href="http://gaia.cs.umass.edu/wireshark-labs/802.11-">http://gaia.cs.umass.edu/wireshark-labs/802.11-</a> 1999.pdf 中 802.11 规范的第 34 页(第 7 节)。</p><ul><li><p>在时间 49 的时刻，主机在跟踪中使用那两个动作(例如:发送帧)解除在 跟踪开始之前就已经连接的 30 Munroe St AP 的关联。(提示:一个是 IP 层 多宗，一个是 802.11 层动作)。在查看 802.11 规范之后，请找出抓包结果 中未显示一个动作帧。</p></li><li><p>在抓包结果中，找到主机发送给无线 AP 的 AUTHENICATION 帧，同时也</p><p>找无线 AP 的回复响应帧。在时间 49 时刻之后，无线主机向 linksys_ses_24086 AP(MAC 地址包括 Cisco_Li_f5:ba:bb)发送了多少 AUTHENTICATION 消息?</p></li><li><p>主机是否希望身份认证和关联 AP?</p></li><li><p>您是否在跟踪中看到来自 linksys_ses_24086 AP 回的 AUTHENTICATION?</p></li><li><p>现在让我们来分析当主机放弃尝试与 linksys_ses_24086 AP 关联并且现在尝</p><p>试与 30 Munroe St AP 关联发生什么。查找从主机发送到 AP 的 AUTHENICATION 帧，同时也找无线 AP 的回复响应帧。在什么时间有一 个主机到 30 Munroe St AP 的帧，在什么时间无线 AP 回主机该帧的回复。 (注意:您可以使用“ wlan.fc.subtype == 11and wlan.fc.type == 0 and wlan.addr == IntelCor_d1:b6:4f ”(不含引号)在此跟踪中仅显示无线主机的 AUTHENTICATION 帧)</p></li><li><p>从主机到 AP 的关联请求，以及相应的 AP 对主机关联请求的响应。在什么 时候有来自主机到 AP 关联请求，什么时候 AP 对主机关联请求响应。(注 意:你可以使用过滤表达式“wlan.fc.subtype &lt; 2 and wlan.fc.type == 0 and wlan.addr == IntelCor_d1:b6:4f”来仅显示 ASSOCIATE REQUEST 和 ASSOCIATE RESPONSE 帧。</p></li><li><p>主机和 AP 愿意使用什么传输速率?要回答此问题，您或许要查看 802.11 无</p><p>线管理帧的字段。</p></li></ul><h2 id="Other-Frame-types-其他帧类型">Other Frame types 其他帧类型</h2><p>作者的抓包结果中含许多 PROBE REQUEST 和 PROBE RESPONSE 帧。</p><ul><li>这些帧中的发送方，接收方和 BSS ID MAC 地址是什么? 这两种帧的目的是什么? (要回答最后一个问题，您需要深入研究本实验前面引用说明的 在线参考资料)。</li></ul>]]></content>
    
    
    <summary type="html">在本实验中，我们将研究 802.11 无线协议。</summary>
    
    
    
    <category term="学习记录" scheme="https://trevoewu.github.io/categories/%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/"/>
    
    
    <category term="computer network" scheme="https://trevoewu.github.io/tags/computer-network/"/>
    
  </entry>
  
  <entry>
    <title>Wireshark Lab: ICMP</title>
    <link href="https://trevoewu.github.io/2023/05/05/Wireshark-Lab-ICMP/"/>
    <id>https://trevoewu.github.io/2023/05/05/Wireshark-Lab-ICMP/</id>
    <published>2023-05-05T02:48:32.000Z</published>
    <updated>2023-05-05T07:21:30.475Z</updated>
    
    <content type="html"><![CDATA[<p>In this lab, we’ll explore several aspects of the ICMP protocol:<br>在本实验中，我们将探讨 ICMP 协议的几个方面:</p><span id="more"></span><ul><li><p>ICMP messages generating by the Ping program; Ping 程序生成的 ICMP 消息;</p></li><li><p>ICMP messages generated by the Traceroute program; Traceroute 程序生成的 ICMP 消息;</p></li><li><p>the format and contents of an ICMP message. ICMP 消息的格式和内容。</p></li></ul><p>Before attacking this lab, you’re encouraged to review the ICMP material in section 5.6<br>of the text1. We present this lab in the context of the Microsoft Windows operating system. However, it is straightforward to translate the lab to a Unix or Linux environment. 在开始本实验之前，我们建议您查看课本的 5.6 节中的 ICMP 章节。此实验是在Windows 下完成的，如果您使用其他系统也不用担心，因为大体都相同。</p><h2 id="ICMP-and-Ping-ICMP-协议和-Ping-程序">ICMP and Ping ICMP 协议和 Ping 程序</h2><p>Let’s begin our ICMP adventure by capturing the packets generated by the Ping program. You may recall that the Ping program is simple tool that allows anyone (for example, a network administrator) to verify if a host is live or not. The Ping program in the source host sends a packet to the target IP address; if the target is live, the Ping program in the target host responds by sending a packet back to the source host. As you might have guessed (given that this lab is about ICMP), both of these Ping packets are ICMP packets. 让我们通过捕获 Ping 程序生成的数据包开始我们的 ICMP 实验。 您可能还记得Ping 程序是一个简单的工具，允许任何人(例如:网络管理员)验证主机是否存在。 源主机中的 Ping 程序将数据包发送到目标 IP 地址; 如果目标是在线的，则目标主机中的 Ping 程序将会发送响应 Ping 数据包证明他在线，这两个 Ping 数据包都是 ICMP 数据包。 因此您可能猜出我们这个实验都是关于 ICMP 的实验了。</p><p>Do the following: 请按照以下动作做:</p><ul><li><p>Let’s begin this adventure by opening the Windows Command Prompt application (which can be found in your Accessories folder).<br>打开 windows 的命令提示符。</p></li><li><p>Start up the Wireshark packet sniffer, and begin Wireshark packet capture. 启动 Wireshark 数据包嗅探器，并开始 Wireshark 数据包捕获。</p></li><li><p>The ping command is in c:\windows\system32, so type either “ping –n 10 hostname” or “c:\windows\system32\ping –n 10 hostname” in the MS-DOS command line (without quotation marks), where hostname is a host on another continent. If you’re outside of Asia, you may want to enter <a href="http://www.ust.hk">www.ust.hk</a> for the Web server at Hong Kong University of Science and Technology. The argument “-n 10” indicates that 10 ping messages should be sent. Then run the Ping program by typing return.</p></li></ul><p>Ping 程序在 c:\windows\system32 目录中，所以您在命令提示符中输入“ping –n 10 hostname” 或 “c:\windows\system32\ping –n 10 hostname”都是正确的 (注意命令是引号里的内容)。其中 hostname 是另一个大陆的主机名。如 您如果在非亚洲地区，建议您访问香港科技大学 <a href="http://www.ust.hk">www.ust.hk</a> ， 参数-n 10 代表发送 10 个 Ping 消息，然后按下回车执行命令。</p><ul><li>When the Ping program terminates, stop the packet capture in Wireshark. 当 Ping 程序终止时，停止在 Wireshark 中捕获数据包。</li></ul><p>At the end of the experiment, your Command Prompt Window should look something like Figure 1. In this example, the source ping program is in Massachusetts and the destination Ping program is in Hong Kong. From this window we see that the source ping program sent 10 query packets and received 10 responses. Note also that for each response, the source calculates the round-trip time (RTT), which for the 10 packets is on average 375 msec.</p><p>在实验结束时，您的命令提示符窗口应如图 1 所示。在此示例中，源 ping 程序位 于马萨诸塞州，目标 Ping 程序位于香港。 从这个窗口我们看到源 ping 程序发送了10 个查询包并收到了 10 个响应。 另请注意，对于每个响应，源计算往返时间 (RTT)，对于 10 个数据包平均为 375 毫秒。</p><p><img src="Wireshark-Lab-ICMP/image-20230505125410545.png" alt="image-20230505125410545" loading="lazy"></p><p>Figure 2 provides a screenshot of the Wireshark output, after “icmp” has been entered into the filter display window. Note that the packet listing shows 20 packets: the 10 Ping queries sent by the source and the 10 Ping responses received by the source. Also note that the source’s IP address is a private address (behind a NAT) of the form 192.168/12; the destination’s IP address is that of the Web server at HKUST. Now let’s zoom in on the first packet (sent by the client); in the figure below, the packet contents area provides information about this packet. We see that the IP datagram within this packet has protocol number 01, which is the protocol number for ICMP. This means that the payload of the IP datagram is an ICMP packet.<br>如图 2，在将“icmp”输入过滤器显示窗口后 Wireshark 输出的屏幕截图。 请注意，</p><p>数据包列表显示 20 个数据包:源发送的 10 个 Ping 查询和源接收的 10 个 Ping 响 应。 另请注意，源的 IP 地址是 192.168 / 12 格式的私有地址(通过 NAT 协议连接 互联网); 目的地的 IP 地址是香港科技大学网络服务器的 IP 地址。 现在让我们点 击显示第一个由客户端发送的数据包; 在图中的下方，数据包内容区域提供有关此 数据包的信息。 我们看到该数据包中的 IP 数据报具有协议号 01，这是 ICMP 的协 议号。 这意味着 IP 数据报的有效载荷是 ICMP 数据包</p><p><img src="Wireshark-Lab-ICMP/image-20230505110353038.png" alt="image-20230505110353038" loading="lazy"></p><center>图 2 在 Wireshark 中过滤 ICMP 协议并且选中某个 ICMP 消息<center/><p>Figure 3 focuses on the same ICMP but has expanded the ICMP protocol information in the packet contents window. Observe that this ICMP packet is of Type 8 and Code 0 - a so-called ICMP “echo request” packet. (See Figure 5.19 of text.) Also note that this ICMP packet contains a checksum, an identifier, and a sequence number.</p><p>图 3 同样使用相同的 ICMP 过滤器，展开可以看到该 ICMP 包的详情信息。 观察到 该 ICMP 数据包是类型 8 和代码 0 ——所谓的 ICMP“回应请求”数据包。 (请参见 书本的图 5.19。)此外，此 ICMP 数据包包含校验和，标识符和序列号</p><p><img src="Wireshark-Lab-ICMP/image-20230505110809611.png" alt="image-20230505110809611" loading="lazy"></p><h2 id="What-to-Hand-In-回答问题">What to Hand In: 回答问题</h2><p>You should hand in a screen shot of the Command Prompt window similar to Figure 1 above. Whenever possible, when answering a question below, you should hand in a printout of the packet(s) within the trace that you used to answer the question asked. Annotate the printout to explain your answer. To print a packet, use File-&gt;Print, choose Selected packet only, choose Packet summary line, and select the minimum amount of packet detail that you need to answer the question.</p><p>请尽量清晰的展示您的答案，必要时您可以在图中用标记辅以说明。您的答案应该<br>简单可读。</p><pre><code class="language-sh">ping -c 10 bing.com // [-c count] </code></pre><p>You should answer the following questions: 请回答以下问题:</p><ol><li>What is the IP address of your host? What is the IP address of the destination host? 您的主机的 IP 地址是多少? 目标主机的 IP 地址是多少?</li></ol><p><img src="Wireshark-Lab-ICMP/image-20230505125749494.png" alt="image-20230505125749494" loading="lazy"></p><ol><li>Why is it that an ICMP packet does not have source and destination port numbers? 为什么 ICMP 数据包没有源端口号和目的端口号?</li></ol><p><strong>大多数TCP/IP实现直接在操作系统中支持ping服务器, 即该服务器不是一个进程.</strong></p><p>ICMP（Internet Control Message Protocol）数据包没有源端口号和目的端口号，因为ICMP不像TCP（传输控制协议）或UDP（用户数据报协议）那样是传输层协议。相反，ICMP是一种直接在IP（Internet Protocol）层之上运行的网络层协议。</p><p>TCP和UDP使用源和目的地端口号来在同一设备上运行的不同应用程序之间进行多路复用和解复用数据。然而，ICMP不用于传输应用程序数据，而是用于在网络设备之间传递错误消息和网络状态信息。因此，ICMP消息并不是针对设备上的特定端口而言的，而是针对设备本身的。</p><p>ICMP消息包含类型、代码和校验和等字段，这些字段用于指示消息的目的和内容。这些字段允许网络设备适当地解释和响应ICMP消息。例如，当从一个设备发送ping命令到另一个设备时，ping数据包包含一个ICMP回显请求消息，这促使接收设备向发送方发送一个ICMP回显回复消息。ICMP数据包中缺少源和目的端口号是由于这些信息对协议的运行不必要。</p><ol><li>Examine one of the ping request packets sent by your host. What are the ICMP type and code numbers? What other fields does this ICMP packet have? How many bytes are the checksum, sequence number and identifier fields? 查看任意的请求 ICMP 数据包， ICMP 类型和代码是什么? 该 ICMP 数据包还有哪些其他字段? 校验和，序列号和标识符字段有多少字节?</li></ol><p><strong>Type: 8 code: 0 回显请求</strong>。还包括<strong>检验和(2 Byte)</strong>, <strong>id(2 Byte)</strong>, <strong>sequence number(2 Byte)</strong></p><p><img src="Wireshark-Lab-ICMP/image-20230505131005238.png" alt="image-20230505131005238" loading="lazy"></p><p><img src="Wireshark-Lab-ICMP/image-20230505140604234.png" alt="image-20230505140604234" loading="lazy"></p><ol><li>Examine the corresponding ping reply packet. What are the ICMP type and code numbers? What other fields does this ICMP packet have? How many bytes are the checksum, sequence number and identifier fields?<br>查看任意的响应 ICMP 数据包， ICMP 类型和代码是什么? 该 ICMP 数据包<br>还有哪些其他字段? 校验和，序列号和标识符字段有多少字节?</li></ol><p>Type: 0(Echo reply)</p><p>Code : 0</p><p>还包括<strong>检验和(2 Byte)</strong>, <strong>id(2 Byte)</strong>, <strong>sequence number</strong>**(2 Byte**)</p><p><img src="Wireshark-Lab-ICMP/image-20230505140719354.png" alt="image-20230505140719354" loading="lazy"></p><h2 id="ICMP-and-Traceroute-ICMP-协议和-Traceroute-命令">ICMP and Traceroute ICMP 协议和 Traceroute 命令</h2><p>Let’s now continue our ICMP adventure by capturing the packets generated by the Traceroute program. You may recall that the Traceroute program can be used to figure out the path a packet takes from source to destination. Traceroute is discussed in Section 1.4 and in Section 5.6 of the text.<br>现在让我们通过捕获 Traceroute 程序生成的数据包继续我们的 ICMP 实验。 您可能<br>还记得 Traceroute 程序可用于确定数据包从源到目的地的路径。 Traceroute 命令可 以在课本中的 1.4 节和 5.6 节中找到</p><p>Traceroute is implemented in different ways in Unix/Linux/MacOS and in Windows. In Unix/Linux, the source sends a series of UDP packets to the target destination using an unlikely destination port number; in Windows, the source sends a series of ICMP packets to the target destination. For both operating systems, the program sends the first packet with TTL=1, the second packet with TTL=2, and so on. Recall that a router will decrement a packet’s TTL value as the packet passes through the router. When a packet arrives at a router with TTL=1, the router sends an ICMP error packet back to the source. In the following, we’ll use the native Windows tracert program. A shareware version of a much nice Windows Traceroute program is pingplotter (<a href="http://www.pingplotter.com">www.pingplotter.com</a>). We’ll use pingplotter in our Wireshark IP lab since it provides additional functionality that we’ll need there.<br>每个系统有不同路由跟踪实现办法，在 Unix / Linux 中，路由跟踪 traceroute 使用 发送不可到达(无使用的)端口的 <strong>UDP</strong> 包来实现，在 Windows 中，路由跟踪 tracert 仅使用 <strong>ICMP</strong> 数据包来实现，但是对于他们来说，都是发送 TTL 增加的数据 包，例如 TTL=1,TTL=2，回想下，每经过一个路由器，TTL 就会减一，<strong>当 TTL=1 的包达到路由器，该路由器会将该包丢弃，并且发送 ICMP 错误给请求的机器</strong>，在 本次实验，我们使用 window 自带的 tracert。有一个跨平台 Windows Traceroute 程序的是 pingplotter(<a href="http://www.pingplotter.com">www.pingplotter.com</a>)(收费但可以试用)。 我们将在 Wireshark IP 实验室中使用 pingplotter，因为它提供了我们在那里需要的其他功能。</p><p>Do the following4: 请执行以下步骤</p><ul><li><p>Let’s begin by opening the Windows Command Prompt application (which can be found in your Accessories folder).<br>打开 windows 的命令提示符。</p></li><li><p>Start up the Wireshark packet sniffer, and begin Wireshark packet capture. 启动 Wireshark 数据包嗅探器，并开始 Wireshark 数据包捕获。</p></li><li><p>The tracert command is in c:\windows\system32, so type either “tracert hostname” or “c:\windows\system32\tracert hostname” in the MS-DOS command line (without quotation marks), where hostname is a host on another continent. (Note that on a Windows machine, the command is “tracert” and not “traceroute”.) If you’re outside of Europe, you may want to enter <a href="http://www.inria.fr">www.inria.fr</a> for the Web server at INRIA, a computer science research institute in France. Then run the Traceroute program by typing return.<br>tracert 程序在 c:\windows\system32 目录中，所以您在命令提示符中输入</p><p>“tracert hostname” 或 “c:\windows\system32\ tracert hostname”都是正确的(注 意命令是引号里的内容)。其中 hostname 是另一个大陆的主机名。如您如 果在非欧洲地区，建议您访问法国 INRIA(计算机科学研究所) <a href="http://www.inria.fr">www.inria.fr</a> 然后按下回车执行命令。</p></li><li><p>When the Traceroute program terminates, stop packet capture in Wireshark. 当 Traceroute 程序终止时，停止在 Wireshark 中捕获数据包。</p><p>At the end of the experiment, your Command Prompt Window should look something like Figure 4. In this figure, the client Traceroute program is in Massachusetts and the target destination is in France. From this figure we see that for each TTL value, the</p></li></ul><p>source program sends three probe packets. Traceroute displays the RTTs for each of the probe packets, as well as the IP address (and possibly the name) of the router that returned the ICMP TTL-exceeded message. 在实验结束时，您的命令提示符窗口应如图 4 所示。在此图中，客户端 Traceroute程序主机位于马萨诸塞州，目标主机位于法国。 从该图中我们看到，对于每个 TTL 值，源程序发送三个探测包。 Traceroute 显示每个探测包的 RTT，以及返回 ICMP TTL 超出消息的路由器的 IP 地址(可能还有名称)。</p><p><img src="Wireshark-Lab-ICMP/image-20230505111220401.png" alt="image-20230505111220401" loading="lazy"></p><p>Figure 5 displays the Wireshark window for an ICMP packet returned by a router. Note that this ICMP error packet contains many more fields than the Ping ICMP messages.<br>图 5 显示了路由器返回的 ICMP 数据包的 Wireshark 窗口。 请注意，此 ICMP 错误数据包包含的字段比 Ping ICMP 消息多得多。</p><h2 id="image-20230505111244920"><img src="Wireshark-Lab-ICMP/image-20230505111244920.png" alt="image-20230505111244920" loading="lazy"></h2><p>If you are unable to run Wireshark live on a computer, you can download the zip file <a href="http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip">http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip</a> and extract the file ICMP-ethereal-trace-2. The traces in this zip file were collected by Wireshark running on one of the author’s computers, while performing the steps indicated in the Wireshark lab. Once you have downloaded the trace, you can load it into Wireshark and view the trace using the File pull down menu, choosing Open, and then selecting the ICMP-ethereal-trace-2 trace file. You can then use this trace file to answer the questions below. 同样如果您无法抓包，建议您下载作者的抓包结果 <a href="http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip">http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip</a> 解压并且使用 Wireshark 打开 ICMP-ethereal-trace-2 进行分析。</p><hr><h2 id="What-to-Hand-In-回答问题-2">What to Hand In: 回答问题</h2><p>For this part of the lab, you should hand in a screen shot of the Command Prompt window. Whenever possible, when answering a question below, you should hand in a printout of the packet(s) within the trace that you used to answer the question asked. Annotate the printout to explain your answer. To print a packet, use File-&gt;Print, choose Selected packet only, choose Packet summary line, and select the minimum amount of packet detail that you need to answer the question. 请尽量清晰的展示您的答案，必要时您可以在图中用标记辅以说明。您的答案应该简单可读</p><p>Answer the following questions: 请回答以下问题:</p><ol><li><p>What is the IP address of your host? What is the IP address of the target destination host?<br>您的主机的 IP 地址是多少? 目标目标主机的 IP 地址是多少?</p></li><li><p>If ICMP sent UDP packets instead (as in Unix/Linux), would the IP protocol number still be 01 for the probe packets? If not, what would it be?<br>如果 ICMP 发送了 UDP 数据包(如在 Unix / Linux 中)，那么探测数据包的 IP 协议号仍然是 01 吗? 如果没有，它会是什么?</p></li></ol><p><img src="Wireshark-Lab-ICMP/image-20230505141720372.png" alt="image-20230505141720372" loading="lazy"></p><ol><li>Examine the ICMP echo packet in your screenshot. Is this different from the ICMP ping query packets in the first half of this lab? If yes, how so? 检查屏幕截图中的 ICMP 响应数据包。 这与本实验的前半部分中的 ICMP ping 查询数据包不同吗? 如果不同，请解释为什么?</li></ol><p><img src="Wireshark-Lab-ICMP/image-20230505141845947.png" alt="image-20230505141845947" loading="lazy"></p><p>该ICMP Message Type:11 Code: 0 , 路由器告诉源主机你的分组由于TTL减为0被我丢掉了(Time-to-live exceeded), 这是Ping和treaceroute程序的工作原理不同导致的, Ping程序发送请求回显报文, 目标主机收到后回复(回显回答), 而traceroute程序通过发送TTL从1递增的UPD数据报来获得路由信息.</p><ol><li>Examine the ICMP error packet in your screenshot. It has more fields than the ICMP echo packet. What is included in those fields?<br>检查屏幕截图中的 ICMP 错误数据包。 它具有比 ICMP 响应数据包更多的字段。 这个数据包含哪些内容?</li></ol><p><img src="Wireshark-Lab-ICMP/image-20230505142419713.png" alt="image-20230505142419713" loading="lazy"></p><p>多了IP头部, IP头部包含了被丢弃的IP数据报的头部字段</p><ol><li>Examine the last three ICMP packets received by the source host. How are these packets different from the ICMP error packets? Why are they different? 检查源主机收到的最后三个 ICMP 数据包。 这些数据包与 ICMP 错误数据包有何不同? 他们为什么不同?</li></ol><p>找不到…最后都没有收到, 但我还是说几句. 这些同为ICMP数据包, 但TYPE和Code字段的字不同, Type:3 Code:3 目标主机告诉源主机目标端口不可达, 这是因为treaceroute程序发送了不可达的端口</p><p><img src="Wireshark-Lab-ICMP/image-20230505143521010.png" alt="image-20230505143521010" loading="lazy"></p><ol><li><p>Within the tracert measurements, is there a link whose delay is significantly longer than others? Refer to the screenshot in Figure 4, is there a link whose delay is significantly longer than others? On the basis of the router names, can you guess the location of the two routers on the end of this link?</p><p>在 tracert 跟踪测量中，是否有一个连接的延迟比其他连接长得多? 请参阅 图 4 中的屏幕截图，是否有连接的延迟明显长于其他连接? 根据路由器名 称，您能猜出这个连接末端的两个路由器的位置吗?</p></li></ol><p>可以发现TTL为6和为10时, 延迟变化很大, 通过best trace程序发现, 这两个路由器的位置分别是中国北京和德国，黑森州，法兰克福，</p><img src="Wireshark-Lab-ICMP/image-20230505144758780.png" alt="image-20230505144758780" style="zoom:50%;" / loading="lazy"><p><img src="Wireshark-Lab-ICMP/image-20230505145146382.png" alt="image-20230505145146382" loading="lazy"></p><h2 id="Extra-Credit-额外问题">Extra Credit 额外问题</h2><p>For one of the programming assignments you created a UDP client ping program. This ping program, unlike the standard ping program, sends UDP probe packets rather than ICMP probe packets. Use the client program to send a UDP packet with an unusual destination port number to some live host. At the same time, use Wireshark to capture any response from the target host. Provide a Wireshark screenshot for the response as well as an analysis of the response.</p><p>对于一个编程任务，您可能创建了一个 UDP 客户端 ping 程序。 与标准 ping 程序 不同，此 ping 程序发送 UDP 探测包而不是 ICMP 探测包。 使用客户端程序将具有 异常目标端口号的 UDP 数据包发送到某个活动主机。 同时，使用 Wireshark 捕获 目标主机的任何响应。 提供响应的 Wireshark 屏幕截图以及响应分析。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;In this lab, we’ll explore several aspects of the ICMP protocol:&lt;br&gt;
在本实验中，我们将探讨 ICMP 协议的几个方面:&lt;/p&gt;</summary>
    
    
    
    <category term="学习" scheme="https://trevoewu.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="computer network" scheme="https://trevoewu.github.io/tags/computer-network/"/>
    
    <category term="lab" scheme="https://trevoewu.github.io/tags/lab/"/>
    
  </entry>
  
  <entry>
    <title>Wireshark Lab: DNS</title>
    <link href="https://trevoewu.github.io/2023/04/21/Wireshark-Lab-DNS/"/>
    <id>https://trevoewu.github.io/2023/04/21/Wireshark-Lab-DNS/</id>
    <published>2023-04-21T13:05:21.000Z</published>
    <updated>2023-04-21T13:18:17.607Z</updated>
    
    <content type="html"><![CDATA[<p><strong>官方英文文档：<a href="Wireshark_Intro_v6.01.pdf">Wireshark_Intro_v6.01.pdf</a></strong></p><p><strong>以下内容为笔者翻译：</strong></p><hr><h2 id="Wireshark-实验-DNS-v6-01">Wireshark 实验:  DNS v6.01</h2><p><strong>《计算机网络：自顶向下方法（第6版）》补充材料，J.F. Kurose and K.W. Ross</strong></p><p>“不闻不若闻之，闻之不若见之，见之不若知之，知之不若行之。” ——中国谚语</p><p>© 2005-2012, J.F Kurose and K.W. Ross, All Rights Reserved</p><hr><p>如书中第2.5节所述，域名系统(DNS)将主机名转换为IP地址，在互联网基础架构中发挥关键作用。在本实验中，我们将仔细查看DNS在客户端的细节。回想一下，客户端在DNS中的角色相对简单——客户端向其本地DNS服务器发送<em>请求</em>，并接收一个<em>响应</em>。如书中的图2.21和2.22所示，由于DNS分层服务器之间相互通信，可以递归地或迭代地解析客户端的DNS查询请求，而大多数操作是不可见的。然而，从DNS客户端的角度来看，协议非常简 ——将查询指向为本地DNS服务器，并从该服务器接收到响应。</p><p>在开始本实验之前，您可能需要阅读书中的第2.5节来了解DNS。另外，您可能需要查看关于<strong>本地DNS服务器，DNS缓存，DNS记录和消息，以及DNS记录中的TYPE字段</strong>的资料。</p><h3 id="1-nslookup">1. nslookup</h3><p>在本实验中，我们将大量使用<em>nslookup</em>工具，这个工具在现在的大多数Linux/Unix和Microsoft平台中都有。要在Linux/Unix中运行<em>nslookup</em>，您只需在命令行中键入<em>nslookup</em>命令即可。要在Windows中运行，请打开命令提示符并在命令行上运行<em>nslookup</em>。</p><p>在这是最基本的操作，<em>nslookup</em>工具允许主机查询任何指定的DNS服务器的DNS记录。DNS服务器可以是根DNS服务器，顶级域DNS服务器，权威DNS服务器或中间DNS服务器（有关这些术语的定义，请参阅书本）。要完成此任务，<em>nslookup</em>将DNS查询发送到指定的DNS服务器，然后接收DNS回复，并显示结果。</p><p>上面的屏幕截图显示了三个不同<em>nslookup</em>命令的结果（显示在Windows命令提示符中）。在此示例中，客户端主机位于布鲁克林理工大学校园，<a href="http://xn--DNSdns-prime-1f5s760cplnk5cu61h2pa6538cej6c.poly.edu">默认本地DNS服务器为dns-prime.poly.edu</a>。运行nslookup时，如果没有指定DNS服务器，则<em>nslookup</em>会将查询发送到默认的DNS服务器（<a href="http://xn--dnsprime-t39lfp892aj4qdp3agi7e3d7c.poly.edu">在这种情况下为dnsprime.poly.edu</a>）。来看第一个命令：</p><p><code>nslookup www.mit.edu</code></p><p>说这个命令是说，请告诉我主机 <a href="http://www.mit.edu">www.mit.edu</a> 的IP地址。如屏幕截图所示，此命令的响应提供两条信息：（1）提供响应的DNS服务器的名称和IP地址；（2）响应本身，即 <a href="http://www.mit.edu">www.mit.edu</a> 的主机名和IP地址。虽然响应来自理工大学的本地DNS服务器，但本地DNS服务器很可能会迭代地联系其他几个DNS服务器来获得结果，如书中第2.4节所述。</p><p>现在来看第二个命令：</p><p><code>nslookup -type=NS mit.edu</code></p><p>在这个例子中，我们添加了选项&quot;-type=NS&quot;和域名&quot;<a href="http://mit.edu">mit.edu</a>&quot;。这将使得<em>nslookup</em>将NS记录发送到默认的本地DNS服务器。换句话说，“请给我发送mit.edu的权威DNS的主机名” （当不使用-type选项时，<em>nslookup</em>使用默认值，即查询A类记录。）上述屏幕截图中，首先显示了提供响应的DNS服务器（这是默认本地DNS服务器）以及三个MIT域名服务器。这些服务器中的每一个确实都是麻省理工学院校园主机的权威DNS服务器。然而，<em>nslookup</em>也表明该响应是非权威的，这意味着这个响应来自某个服务器的缓存，而不是来自权威MIT DNS服务器。最后，响应结果还显示了麻省理工学院权威DNS服务器的IP地址。 （即使<em>nslookup</em>生成的NS类型查询没有明确要求IP地址，本地DNS服务器依然”免费“返回了这些信息，然后被<em>nslookup</em>显示出来。）</p><p>最后来看第三个命令：</p><p><code>nslookup www.aiit.or.kr bitsy.mit.edu</code></p><p>在这个例子中，我们希望将查询请求发送到DNS服务器 <a href="http://bitsy.mit.edu">bitsy.mit.edu</a> ，而不是默认的DNS服务器（<a href="http://dns-prime.poly.edu">dns-prime.poly.edu</a>）。因此，查询和响应事务直接发生在我们的主机和 <a href="http://bitsy.mit.edu">bitsy.mit.edu</a> 之间。在这个例子中，DNS服务器 <a href="http://bitsy.mit.edu">bitsy.mit.edu</a> 提供主机 <a href="http://www.aiit.or.kr">www.aiit.or.kr</a> 的IP地址，它是高级信息技术研究所（韩国）的Web服务器。</p><p>现在我们了解了一些示例，您现在可能想知道<em>nslookup</em>命令的一般语法。语法是：</p><p><code>nslookup -option1 -option2 host-to-find dns-server</code></p><p>一般来说，<em>nslookup</em>可以不添加选项，或者添加一两个甚至更多选项。正如我们在上面的示例中看到的，dns-server也是可选的；如果这项没有提供，查询将发送到默认的本地DNS服务器。</p><p>现在我们提供了总览了<em>nslookup</em>，现在是你自己驾驭它的时候了。执行以下操作（并记下结果）：</p><ol><li><p>运行<em>nslookup</em>以获取一个亚洲的Web服务器的IP地址。该服务器的IP地址是什么？</p><p><img src="Wireshark-Lab-DNS/image-20230421210837297.png" alt="image-20230421210837297" loading="lazy"></p><p>查询了哈尔滨理工大学的ip地址, 通过得到的信息可以知道, 该服务器的IP地址为202.118.201.1</p><p>可以发现这是一个来自DNS服务器182.254.116.116的应答, 搜索发现, 这是腾讯的备用域名服务器.</p><p><img src="Wireshark-Lab-DNS/image-20230421210846142.png" alt="image-20230421210846142" loading="lazy"></p><p>继续查询权威名字服务器, 得到的是非权威的, 这是因为腾讯的DNS服务器不知道哈尔滨理工大学的权威名字服务器, 只能向根DNS→TLD(顶级域DNS)(.cn)→edu.cnDNS→hrbust.edu.cnDNS, 迭代查询获得</p><p>如果我们之间对这个DNS服务器发起DNS查询:</p><p><img src="Wireshark-Lab-DNS/image-20230421210900650.png" alt="image-20230421210900650" loading="lazy"></p><p>可以发现, 这样一条来自权威名字服务器的应答.</p></li><li><p>运行<em>nslookup</em>来确定一个欧洲的大学的权威DNS服务器。</p><p>查询了苏黎世联邦理工大学的权威DNS</p></li></ol><p><img src="Wireshark-Lab-DNS/image-20230421210910755.png" alt="image-20230421210910755" loading="lazy"></p><p>可以发现权威名字服务器为: <a href="http://ns1.ethz.ch">ns1.ethz.ch</a></p><ol><li>运行<em>nslookup</em>，使用问题2中一个已获得的DNS服务器，来查询Yahoo!邮箱的邮件服务器。它的IP地址是什么？</li></ol><p><img src="Wireshark-Lab-DNS/image-20230421210921960.png" alt="image-20230421210921960" loading="lazy"></p><p>查不到好吧…人家是大学的权威DNS, 没有雅虎的邮件服务器地址, 只维护了大学的IP地址查询, 不当然公共DNS的指责, 即不去迭代的查询(个人猜测)</p><p>又到了问聪明的chatGPT的时间了:</p><p><img src="Wireshark-Lab-DNS/image-20230421211212041.png" alt="image-20230421211212041" loading="lazy"></p><p><img src="Wireshark-Lab-DNS/image-20230421210930573.png" alt="image-20230421210930573" loading="lazy"></p><p>使用腾讯的公共DNS服务器我们可以得到Yahoo的邮件服务器地址.</p><p>同时为发现查询<code>www.mail.yahoo.com</code>和mail.yahoo.com的结果不同</p><p><img src="Wireshark-Lab-DNS/image-20230421211245869.png" alt="image-20230421211245869" loading="lazy"></p><p>查询chatGPT得到以下回复:</p><p><img src="Wireshark-Lab-DNS/image-20230421211255078.png" alt="image-20230421211255078" loading="lazy"></p><h3 id="2-ipconfig">2. ipconfig</h3><p><em>ipconfig</em>（对于Windows）和<em>ifconfig</em>（对于Linux / Unix）是主机中最实用的程序，尤其是用于调试网络问题时。这里我们只讨论<em>ipconfig</em>，尽管Linux / Unix的<em>ifconfig</em>与其非常相似。 <em>ipconfig</em>可用于显示您当前的TCP/IP信息，包括您的地址，DNS服务器地址，适配器类型等。例如，您只需进入命令提示符，输入</p><p><code>ipconfig /all</code></p><p><em>ipconfig</em>对于管理主机中存储的DNS信息也非常有用。在第2.5节中，我们了解到主机可以缓存最近获得的DNS记录。要查看这些缓存记录，在 C:\&gt; 提示符后输入以下命令：</p><p><code>ipconfig /displaydns</code></p><p>每个条目显示剩余的生存时间（TTL）（秒）。要清除缓存，请输入</p><p><code>ipconfig /flushdns</code></p><p>清除了所有条目并从hosts文件重新加载条目。</p><h3 id="3-使用Wireshark追踪DNS">3. 使用Wireshark追踪DNS</h3><p>现在，我们熟悉<code>nslookup</code>和<code>ipconfig</code>，我们准备好了一些正经的事情。首先让我们捕获一些由常规上网活动生成的DNS数据包。</p><ul><li>使用<em>ipconfig</em>清空主机中的DNS缓存。</li><li>打开浏览器并清空浏览器缓存。 （若使用Internet Explorer，转到<strong>工具</strong>菜单并选择<strong>Internet选项</strong>；然后在<strong>常规</strong>选项卡中选择删除文件。）</li><li>打开Wireshark，然后在过滤器中输入“ip.addr==your_IP_address”，您可以先使用<em>ipconfig</em>获取你的IP地址。此过滤器将删除既从你主机不发出也不发往你主机的所有数据包。</li><li>在Wireshark中启动数据包捕获。</li><li>使用浏览器访问网页： <a href="http://www.ietf.org">http://www.ietf.org</a></li><li>停止数据包捕获。</li></ul><p>如果您无法在你的网络连接上运行Wireshark，则可以下载一个捕获了数据包的文件，这个文件是本书作者在自己计算机上 按照上述步骤捕获的（原文注：Download the zip file <a href="http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip">http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip</a> and extract the file dnsethereal-trace-1. The traces in this zip file were collected by Wireshark running on one of the author’s computers, while performing the steps indicated in the Wireshark lab. Once you have downloaded the trace, you can load it into Wireshark and view the trace using the File pull down menu, choosing Open, and then selecting the dns-ethereal-trace-1 trace file. ）。回答下列问题。您应该在解答中尽可能展示你使用了哪些你捕获到的数据包，并注释出来（原文注：What do we mean by “annotate”? If you hand in a paper copy, please highlight where in the printout you’ve found the answer and add some text (preferably with a colored pen) noting what you found in what you ‘ve highlight. If you hand in an electronic copy, it would be great if you could also highlight and annotate. ）。若要打印数据包，请使用<em>文件</em>-&gt;<em>打印</em>，只勾选<em>仅选中分组</em>，和<em>概要行</em>，并选中你所需要用于解答问题的数据包。</p><ol start="4"><li><p>找到DNS查询和响应消息。它们是否通过UDP或TCP发送？</p><p><img src="Wireshark-Lab-DNS/image-20230421211313407.png" alt="image-20230421211313407" loading="lazy"></p><p>通过UDP发送</p></li><li><p>DNS查询消息的目标端口是什么？ DNS响应消息的源端口是什么？</p><p><img src="Wireshark-Lab-DNS/image-20230421211321920.png" alt="image-20230421211321920" loading="lazy"></p><p>目标端口通过上一问的图片可以得到为: 53, 响应DNS到源端口也是53</p></li><li><p>DNS查询消息发送到哪个IP地址？使用ipconfig来确定本地DNS服务器的IP地址。这两个IP地址是否相同？</p><p>本地IP 地址: 10.51.172.226</p><p>DNS服务器IP地址: 182.254.116.116</p><p>相同</p><p><img src="Wireshark-Lab-DNS/image-20230421211341608.png" alt="image-20230421211341608" loading="lazy"></p></li><li><p>检查DNS查询消息。DNS查询是什么&quot;Type&quot;的？查询消息是否包含任何&quot;answers&quot;？</p><p><img src="Wireshark-Lab-DNS/image-20230421211351099.png" alt="image-20230421211351099" loading="lazy"></p><p>TYPE: A</p><p>不包含</p></li><li><p>检查DNS响应消息。提供了多少个&quot;answers&quot;？这些答案具体包含什么？</p><p><img src="Wireshark-Lab-DNS/image-20230421211359997.png" alt="image-20230421211359997" loading="lazy"></p><p>三个answer</p><p>第一个answer: TYPE CNAME, 主机别名到规范主机名的对应关系</p><p><img src="Wireshark-Lab-DNS/image-20230421211409785.png" alt="image-20230421211409785" loading="lazy"></p><p>包含NAME(域名),TYPE, Class, TTL, Data. CNAME(规范主机名)</p><p>第二个answer: TYPE A, 域名和IP地址</p><p><img src="Wireshark-Lab-DNS/image-20230421211422241.png" alt="image-20230421211422241" loading="lazy"></p><p>得到了规范主机名的IP地址</p><p>第三个answer:</p><p><img src="Wireshark-Lab-DNS/image-20230421211431684.png" alt="image-20230421211431684" loading="lazy"></p><p>另一个规范主机名的IP地址</p></li><li><p>考虑从您主机发送的后续TCP SYN数据包。 SYN数据包的目的IP地址是否与DNS响应消息中提供的任何IP地址相对应？</p><p>直接过滤ip.addr==104.16.44.99</p><p>结果如下:</p><p><img src="Wireshark-Lab-DNS/image-20230421211445058.png" alt="image-20230421211445058" loading="lazy"></p><p>可以发现我的主机向这个IP地址发起了TCP的链接请求</p></li><li><p>这个网页包含一些图片。在获取每个图片前，您的主机是否都发出了新的DNS查询？</p><p><img src="Wireshark-Lab-DNS/image-20230421211456062.png" alt="image-20230421211456062" loading="lazy"></p><p>有一个对r3.o.lencr.org的DNS查询, 搜索发现这是一个恶意的软件发起的, 目的是给浏览器带来广告(服了…)</p><p><img src="Wireshark-Lab-DNS/image-20230421211504325.png" alt="image-20230421211504325" loading="lazy"></p></li></ol><p>现在让我们玩玩<em>nslookup</em>（原文注：If you are unable to run Wireshark and capture a trace file, use the trace file dns-ethereal-trace-2 in the zip file <a href="http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip">http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip</a>  ）。</p><ul><li>启动数据包捕获。</li><li>使用nslookup查询 <a href="http://www.mit.edu">www.mit.edu</a></li><li>停止数据包捕获。</li></ul><p>我们从上面的屏幕截图看到，<em>nslookup</em>实际上发送了三个DNS查询，并收到了三个DNS响应。只考虑本次实验相关结果，在回答以下问题时，请忽略前两组查询/响应，因为<em>nslookup</em>的一些特殊性，这些查询通常不是由标准网络应用程序生成的。您应该专注于最后一个查询和响应消息。</p><ol start="11"><li><p>DNS查询消息的目标端口是什么？ DNS响应消息的源端口是什么？</p><p>查询:</p><p><img src="Wireshark-Lab-DNS/image-20230421211516046.png" alt="image-20230421211516046" loading="lazy"></p><p>响应:</p><p><img src="Wireshark-Lab-DNS/image-20230421211522148.png" alt="image-20230421211522148" loading="lazy"></p></li><li><p>DNS查询消息的目标IP地址是什么？这是你的默认本地DNS服务器的IP地址吗？</p><p>是的</p><p><img src="Wireshark-Lab-DNS/image-20230421211532903.png" alt="image-20230421211532903" loading="lazy"></p></li><li><p>检查DNS查询消息。DNS查询是什么&quot;Type&quot;的？查询消息是否包含任何&quot;answers&quot;？</p></li><li><p>TYPE A, 不包含</p><p><img src="Wireshark-Lab-DNS/image-20230421211545749.png" alt="image-20230421211545749" loading="lazy"></p></li><li><p>检查DNS响应消息。提供了多少个&quot;answers&quot;？这些答案包含什么？</p><p>三个</p><p><img src="Wireshark-Lab-DNS/image-20230421211557865.png" alt="image-20230421211557865" loading="lazy"></p><p>包含主机别名到规范主机名的转化关系(TYPE:CNAME)</p><p>规范主机名到IP地址到转换关系</p></li></ol><p>现在重复上一个实验，但换成以下命令：</p><p><code>nslookup -type=NS mit.edu</code></p><p>回答下列问题：</p><ol start="16"><li><p>DNS查询消息发送到的IP地址是什么？这是您的默认本地DNS服务器的IP地址吗？</p><p>是的</p><p><img src="Wireshark-Lab-DNS/image-20230421211610171.png" alt="image-20230421211610171" loading="lazy"></p></li><li><p>检查DNS查询消息。DNS查询是什么&quot;Type&quot;的？查询消息是否包含任何&quot;answers&quot;？</p><p><img src="Wireshark-Lab-DNS/image-20230421211621477.png" alt="image-20230421211621477" loading="lazy"></p><p>Type: NS</p><p>不包含</p></li><li><p>检查DNS响应消息。响应消息提供的MIT域名服务器是什么？此响应消息还提供了MIT域名服务器的IP地址吗？</p><p>![image-20230416184356921](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416184356921.png)</p></li></ol><p>现在重复上一个实验，但换成以下命令：</p><p><code>nslookup www.aiit.or.kr bitsy.mit.edu</code></p><p><strong>该DNS服务器已停用</strong>, 使用作者提供的抓包结果dns-ethereal-trace-4</p><p>回答下列问题：</p><ol start="20"><li><p>DNS查询消息发送到的IP地址是什么？这是您的默认本地DNS服务器的IP地址吗？如果不是，这个IP地址是什么？</p><p><img src="Wireshark-Lab-DNS/image-20230421211639835.png" alt="image-20230421211639835" loading="lazy"></p><p>理论上不是, 因为已经指定了对应的DNS服务器,目标IP地址应该是DNS服务器名字<code>bitsy.mit.edu</code> 的IP地址</p></li><li><p>检查DNS查询消息。DNS查询是什么&quot;Type&quot;的？查询消息是否包含任何&quot;answers&quot;？</p><p>第一个查询:</p><p><img src="Wireshark-Lab-DNS/image-20230421211649155.png" alt="image-20230421211649155" loading="lazy"></p><p>TYPE: PTR 通过IP反向查询域名</p><p>第二个查询: TYPE A, 查询<code>www.aiit.or.kr.poly.edu</code>的IP地址, 目标IP地址是18.72.0.3</p><p><img src="Wireshark-Lab-DNS/image-20230421211656347.png" alt="image-20230421211656347" loading="lazy"></p><p>第三个查询: TYPE A, 查询<code>www.aiit.or.kr</code>的IP地址, 目标IP和上一个查询相同.</p><p><img src="Wireshark-Lab-DNS/image-20230421211702635.png" alt="image-20230421211702635" loading="lazy"></p></li><li><p>检查DNS响应消息。提供了多少个&quot;answers&quot;？这些答案包含什么？</p><p>第一个响应: 提供了一个answer</p><p><img src="Wireshark-Lab-DNS/image-20230421211713117.png" alt="image-20230421211713117" loading="lazy"></p><p>TYPE:PTR</p><p>回答里包含IP地址3.0.72.18对应的域名</p><p>第二个响应:</p><p><img src="Wireshark-Lab-DNS/image-20230421211730365.png" alt="image-20230421211730365" loading="lazy"></p><p>TYPE: SOA(Start Of a zone of Authority)</p><p><img src="Wireshark-Lab-DNS/image-20230421211748643.png" alt="image-20230421211748643" loading="lazy"></p><p>响应结果为找不到:</p><p>第三个响应:</p><p><img src="Wireshark-Lab-DNS/image-20230421211757311.png" alt="image-20230421211757311" loading="lazy"></p><p>ansewer为目标域名的IP地址.</p></li></ol>]]></content>
    
    
    <summary type="html">域名系统(DNS)将主机名转换为IP地址，在互联网基础架构中发挥关键作用。在本实验中，我们将仔细查看DNS在客户端的细节。回想一下，客户端在DNS中的角色相对简单——客户端向其本地DNS服务器发送*请求*，并接收一个*响应*。如书中的图2.21和2.22所示，由于DNS分层服务器之间相互通信，可以递归地或迭代地解析客户端的DNS查询请求，而大多数操作是不可见的。然而，从DNS客户端的角度来看，协议非常简 ——将查询指向为本地DNS服务器，并从该服务器接收到响应</summary>
    
    
    
    <category term="学习" scheme="https://trevoewu.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="computer cetwork" scheme="https://trevoewu.github.io/tags/computer-cetwork/"/>
    
  </entry>
  
  <entry>
    <title>Wireshark Lab: TCP</title>
    <link href="https://trevoewu.github.io/2023/04/21/Wireshark-Lab-TCP/"/>
    <id>https://trevoewu.github.io/2023/04/21/Wireshark-Lab-TCP/</id>
    <published>2023-04-21T12:48:28.000Z</published>
    <updated>2023-04-21T13:04:35.920Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>在本实验中，我们将详细研究著名的 TCP 协议的行为。 我们将通过从您的电脑向 远程服务器传输一份 150KB 的文件(一份 Lewis Carrol 的“爱丽丝梦游仙境”文本)，并分析 TCP 传输内容的发送和接收过程来实现。 我们将研究 TCP 对序列和 确认号的使用，以提供可靠的数据传输;我们将看到 TCP 的拥塞控制算法 – 慢启 动和拥塞避免 – 在过程中，我们将看看 TCP 的接收器发送流量控制的机制。 我们还将简要地观察 TCP 连线的设置，我们还会研究计算机和服务器之间 TCP 连线的性能(吞吐量和往返时间)。</p></blockquote><h2 id="A-first-look-at-the-captured-trace">A first look at the captured trace</h2><ul><li><p>将文件传输到 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 的客户端计算机使用的 IP 地址和 TCP 端口号是什么?</p></li><li><p><a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 的 IP 地址是什么? 在哪个端口号上发送和接收此连接的 TCP 段?</p></li></ul><p><img src="Wireshark-Lab-TCP/image-20230421205628132.png" alt="image-20230421205628132" loading="lazy"></p><p>Client IP Addressee: 10.51.172.226 Client Port: 61084<br><a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> IP Address: 128.119.245.12</p><ul><li>客户端计算机将文件传输到 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 所使用的 IP 地址和 TCP 端口号是多少?</li></ul><p><img src="Wireshark-Lab-TCP/image-20230421205643562.png" alt="image-20230421205643562" loading="lazy"></p><p>Client IP Addressee: 10.51.172.226 Client Port: 61084</p><ul><li>用于在客户端计算机和 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 之间启动 TCP 连接的 TCP SYN 段的序列号是什么? 标识为 SYN 段的段有什么功能?</li></ul><p><img src="Wireshark-Lab-TCP/image-20230421205702461.png" alt="image-20230421205702461" loading="lazy"></p><p>如图, SYN序列号为<code>4095602682</code>, 标识为SYN的segment用于建立TCP链接</p><ul><li><a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 发送给客户端计算机以回复 SYN 的 SYNACK 段的序列号是多少?</li></ul><p><code>2017202044</code></p><p><img src="Wireshark-Lab-TCP/image-20230421205716096.png" alt="image-20230421205716096" loading="lazy"></p><p>SYNACK 段中的 Acknowledgment 栏位的值是多少?</p><p>为<code>4095602683</code></p><p><img src="Wireshark-Lab-TCP/image-20230421205729437.png" alt="image-20230421205729437" loading="lazy"></p><p><strong>观察到除了 SYN 标志位，ACK(Acknoledment)标志位也设 1，意思服务器接收到我约连按请求并且发 SYN-ACIK 确认，这是三次握手的第二步。</strong></p><p><a href="http://Gaia.cs.umass.edu">Gaia.cs.umass.edu</a> 是如何确定此 Acknowledgment 的数值的?</p><p>收到的$$seqence number + Len.$$</p><p>在将段标识 为 SYNACK 段的段在连线中有什么功能?</p><p><img src="Wireshark-Lab-TCP/image-20230421205757558.png" alt="image-20230421205757558" loading="lazy"></p><p><strong>用于建立链接, 是三次握手的第二次, 表示同样连接请求</strong></p><ul><li>包含 HTTP POST 命令的 TCP 区段的序列号是多少?</li></ul><p><img src="Wireshark-Lab-TCP/image-20230421205811084.png" alt="image-20230421205811084" loading="lazy"></p><p>如图为<code>4095602683</code>,TCP PSH 字段设为1表示要传送数据.  HTTP的POST方法借助于TCP提供的服务实现, 在TCP中表现为TCP的PUSH字段设为1.</p><ul><li>将包含 HTTP POST 的 TCP 区段视为 TCP 连接中的第一个区段。在这个 TCP 连线中前六个 TCP 区段的序列号是什么(包括包含 HTTP POST的段 每区段发送的时间是什么时候? 收到的每个区段的 ACK 是什么时候?</li></ul><p>鉴于发送每个 TCP 区段的时间与收到确认的时间之间的差异，六个区段中每个区段的 RTT 值是多少?</p><p>如图是前6段的信息:</p><p><img src="Wireshark-Lab-TCP/image-20230421205823688.png" alt="image-20230421205823688" loading="lazy"></p><h3 id="第一段Frame1">第一段Frame1:</h3><p>(发送第一个frame,在 Wireshark中序号为frame69,这是Wireshark捕获到的序号, 由于这是发送数据的第一帧记frame1)</p><p><img src="Wireshark-Lab-TCP/image-20230421205838620.png" alt="image-20230421205838620" loading="lazy"></p><p>RTT:</p><p><em>note: 这里的Timestamps是Wireshark提供的额外的功能, 和TCP段Optionfields 里的Timestamps不同.</em></p><p>通过Wireshark的分析, 可以知道IRTT(initial RTT 表示握手阶段的RTT)为0.39900200s</p><p><img src="Wireshark-Lab-TCP/image-20230421205848826.png" alt="image-20230421205848826" loading="lazy"></p><p>ACK(对第一个frame)的ACK</p><p><img src="Wireshark-Lab-TCP/image-20230421205858659.png" alt="image-20230421205858659" loading="lazy"></p><p>发现这里的ACK number = 4095603435, 而seq1 的Next sequence = 4095603298, 而恰好, frame2的Next sequence = 4095603435</p><p><img src="Wireshark-Lab-TCP/image-20230421205911880.png" alt="image-20230415112704099" loading="lazy"></p><p><strong>所以这是对frame1和frame2的累计确认, 这是因为TCP 采用了延迟到ACK(delay ACK), 具有所期望的序号报文到达时, 等待一段时间(通常为40ms), 如果下一个按序报文到达, 立即发送单个累计ACK, 以确认两个按序到达的报文段. 如果下一个按序报文段没有在这个时间内到达, 则发送一个ACK.</strong></p><p>我们继续观察第一个报文段段Timestamps字段的值:</p><p><img src="Wireshark-Lab-TCP/image-20230421205924206.png" alt="image-20230421205924206" loading="lazy"></p><p>对比ACK1的Timestamps:</p><p><img src="Wireshark-Lab-TCP/image-20230421205931912.png" alt="image-20230421205931912" loading="lazy"></p><p>可以发现, 第一个报文段的TSval和ACK1的TimestampsTSerc相等, 进一步验证了之前的说法, 当接受到连续两个按序到达的报文时, 用于回复的ACK报文段段TSerc值的最早接受到的报文段的TSval, 这是为了对方更够更好的估算RTT.</p><p><strong>时间戳选项主要的功能有两个</strong>：</p><ul><li><p><strong>用来计算往返时间RTT</strong>。</p><p>发送方在发送报文段时把当前时钟的时间值放入时间戳字段，接收方在确认该报文段时把时间戳字段值复制到时间戳回送回答字段。因此，发送方在收到确认报文后，可以准确计算出RTT。时间戳是一个单调增长的值，接收方只需要回显收到的内容，因此是不需要关注时间戳的单元是什么，也不需要连接双发的时钟同步。</p></li><li><p><strong>PAWS:防止回绕的序号</strong>。</p></li></ul><p>我们知道序列号只有32位，而每增加2^32个序列号后就会重复使用原来用过的序列号。假设我们有一条高速网络，通信的主机双方有足够大的带宽用来快速的传输数据。例如1Gb/s（<strong>TCP 的窗口经过窗口缩放可以最高到 1GB（2^30)</strong>）的速率发送报文段，则不到35秒钟数据字节的序列号就会重复。这样对TCP传输带来混乱的情况。这种情况之出现在高速链路上。<strong>而采用时间戳选项，可以很容易的分辨出相同序列号的数据报，哪个是最近发送，哪个是以前发送的。</strong></p><p>参考文章<a href="https://switch-router.gitee.io/blog/tcp-timestamp/">TCP timestamp 选项那点事</a></p><p>接着我们观察ACK1报文的ACK分析(由wireshark提供), RTT为: 0.40989700s</p><p><img src="Wireshark-Lab-TCP/image-20230421205941895.png" alt="image-20230421205941895" loading="lazy"></p><h3 id="第二段"><strong>第二段:</strong></h3><p>我们直接去找, 收到的第二个ACK报文</p><p><img src="Wireshark-Lab-TCP/image-20230421210002459.png" alt="image-20230421210002459" loading="lazy"></p><p>对比sequence的值, 可以发现, 这是对frame75的ACK, 也就是, 收到了对第七个报文段的ACK. 说明服务器已经收到了第七个报文段以及之前的报文.</p><p>我们还发现, 收到了一条[Windows Update]的报文, 这是TCP的流量控制, 用来告诉发送端根据这个值适当的调节发送速率.</p><p><img src="Wireshark-Lab-TCP/image-20230421210012633.png" alt="image-20230421210012633" loading="lazy"></p><p>根据wireshark的分析, 可以知道, RTT为0.409390000s</p><p><img src="Wireshark-Lab-TCP/image-20230421210034300.png" alt="image-20230421210034300" loading="lazy"></p><h3 id="第三段">第三段:</h3><p><img src="Wireshark-Lab-TCP/image-20230421210044526.png" alt="image-20230415135344532" loading="lazy"></p><h3 id="第四段">第四段:</h3><p><img src="Wireshark-Lab-TCP/image-20230421210050923.png" alt="image-20230421210050923" loading="lazy"></p><h3 id="第五段">第五段:</h3><p><img src="Wireshark-Lab-TCP/image-20230421210058078.png" alt="image-20230421210058078" loading="lazy"></p><h3 id="第六段">第六段</h3><p><img src="Wireshark-Lab-TCP/image-20230421210120044.png" alt="image-20230421210120044" loading="lazy"></p><p>收到每个 ACK 后，EstimatedRTT 值(参 见本节中的第 3.5.3 节，第 242 页)是什么?</p><p>假设第一个 EstimatedRTT 的 值等于第一个区段的测量 RTT，然后使用课本第 242 页的 EstimatedRTT 公式计算所有后续区段。</p><p><img src="Wireshark-Lab-TCP/image-20230421210135957.png" alt="image-20230421210135957" loading="lazy"></p><ul><li><p>What is the length of each of the first six TCP segments?4 前六个 TCP 区段的长度是多少?</p><p><img src="Wireshark-Lab-TCP/image-20230421210144892.png" alt="image-20230421210144892" loading="lazy"></p><p><img src="Wireshark-Lab-TCP/image-20230421210152513.png" alt="image-20230421210152513" loading="lazy"></p></li><li><p>What is the minimum amount of available buffer space advertised at the received for the entire trace? Does the lack of receiver buffer space ever throttle the sender? 对于整个跟踪包，收到的最小可用缓冲区空间量是多少? 缺少接收缓冲区空间是否会限制发送方传送 TCP 区段?</p><p><img src="Wireshark-Lab-TCP/image-20230421210204402.png" alt="image-20230421210204402" loading="lazy"></p><p>大部分Win都是很大的值, 太多包了, 懒得分析了.通过检查此跟踪，发送器永远不会因为接收器缓冲区空间不足而受到抑制。</p></li><li><p>Are there any retransmitted segments in the trace file? What did you check for (in the trace) in order to answer this question?<br>在跟踪文件中是否有重传的段? 为了回答这个问题，您检查了什么(在跟踪包中)?</p><p>观察序列号的变化:</p><p>可以发现, 序列号一直在增加, 所以没有重传的段.</p><p><img src="Wireshark-Lab-TCP/image-20230421210221963.png" alt="image-20230421210221963" loading="lazy"></p><p>通常到收到了不同的两个冗余ACK, 但这不一定意味着丢包, 这是因为分组乱序到达接受方导致的.</p><p><img src="Wireshark-Lab-TCP/image-20230421210234982.png" alt="image-20230421210234982" loading="lazy"></p></li><li><p>How much data does the receiver typically acknowledge in an ACK? Can you identify cases where the receiver is ACKing every other received segment (see Table 3.2 on page 250 in the text).<br>接收方通常在 ACK 中确认多少数据? 您是否可以识别接收方每隔一个接收到的区段才发送确认的情况(参见本文第 250 页的表 3.2)</p><p>按照表3-2的说明, 接受方最多确认两个段, 但是观察了捕获结果, 发现很多事确认了超过两个段段情况, 比如在下图中, 收到的第一个ACK确认了69,70两段. 这是一次典型的delay ACK</p><p><img src="Wireshark-Lab-TCP/image-20230421210252350.png" alt="image-20230421210252350" loading="lazy"></p><p>但是观察收到的第二个ACK, 是对75段及之前段的累积确认, 这同时累积确认了71,72,73,74,75这五个段. 且观察后续的ACK, 除了在上一问题上出现的冗余ACK外, 无其他冗余ACK.</p><p>如果严格按照表3-2的叙述来看, 这只可能是部分ACK丢失导致的, 这就意味着网络拥塞, 但很明显这是不可能的, 因为没有发现任何一个重传的段, 网络很通畅.</p><p>让我们问问万能的chatGPT吧:</p><p><img src="Wireshark-Lab-TCP/image-20230421210304585.png" alt="image-20230421210304585" loading="lazy"></p><p><strong>最多可以合并2~5个报文段哦!!! chatGPT yyds!</strong></p><p><img src="Wireshark-Lab-TCP/image-20230421210315184.png" alt="image-20230421210315184" loading="lazy"></p></li><li><p>What is the throughput (bytes transferred per unit time) for the TCP connection? Explain how you calculated this value.TCP 连接的吞吐量(每单位时间传输的节数)是多少? 解释你如何计算 这个值。</p><p>TCP吞吐量的计算在很大程度上取决于平均时间段的选择。作为一种常见的吞吐量计算，在本问题中，我们选择平均时间周期作为整个连接时间。然后，该TCP连接的平均吞吐量被计算为总数据量与总传输时间之间的比率。可以通过第一个TCP数据段的序列号和最后一个确认确认的序列号之间的差来计算发送的总数据量。</p><p>最后一个由我发给服务器的最后一个ACK的seqence numerber为4095755619, 我发送的报文段第一个序列号为: 4095602682</p><p>所以接受方的接受到的总数据为: 4095755619- 4095602682   =  152936Byte</p><p><img src="Wireshark-Lab-TCP/image-20230421210352640.png" alt="image-20230421210352640" loading="lazy"></p><p><img src="Wireshark-Lab-TCP/image-20230421210406967.png" alt="image-20230421210406967" loading="lazy"></p><p>总传输时间为5.455830-0.026477=2.50933秒。</p><p>因此，tcp连接的吞吐量计算为152936/2.50933=60946.945997537191202B/s = 60KB/s。</p><p><img src="Wireshark-Lab-TCP/image-20230421210418067.png" alt="image-20230421210418067" loading="lazy"></p></li></ul><p><em>图由wireshark产生</em></p><blockquote><p>4tcp-ethereal-trace-1 跟踪文件中的 TCP 区段都小于 1460 􏰁节。 这是因为收集跟踪 的计算机具有以太网卡，该最大 IP 数据包的长度限制为 1500 􏰁节(40 􏰁节的 TCP / IP 报头数据和 1460 􏰁节的 TCP 有效负载)。此 1500 􏰁节值是以太网允许 的标准最大长度。 如果您的跟踪包内容指示 TCP 区段长度大于 1500 􏰁节，并且 您的计算机使用以太网连接网路，则是 Wireshark 报告了错误的 TCP 区段长度;它 也可能是只显示一个大的 TCP 段而不是多个较小的区段，而您的计算机确实可能 正在发送多个较小的区段，这可以由收到的 ACK 区段来证明。 报告的区段长度的 这种不一致是由于以太网驱动程序和 Wireshark 软件之间的交互作用所导致。 如果 您有这种不一致情形，我们建议您使用本文所提供的跟踪文件来执行本实验的分析 内容。</p></blockquote><h2 id="TCP-congestion-control-in-action-TCP-拥塞控制">TCP congestion control in action (TCP 拥塞控制)</h2><blockquote><p>现在让我们检查从客户端服务器的每单位时间发送的数据量。 而不是(繁琐 地!)从 Wireshark 窗口中的原始数据计算这些数值，我们将使用 Wireshark 的 TCP 图形工具 – 时序图(Stevens) - 来绘制数据。</p></blockquote><ul><li><p>使用时序图(Stevens)绘图工具查看从客户端发送到 <a href="http://gaia.cs.umass.edu">gaia.cs.umass.edu</a> 服务 器的区段的序列号与时间关系图。您能否确定 TCP 的慢启动阶段的开始和 结束位置，以及拥塞避免接管的位置? 评论测量数据与我们在文本中研究 的 TCP 的理想化行为的不同之处。</p><p><img src="Wireshark-Lab-TCP/image-20230421210435578.png" alt="image-20230421210435578" loading="lazy"></p></li></ul><p>计算每轮发送的段数量分别为: 12,17,36,87 呈指数增长, 一只在慢启动阶段.</p>]]></content>
    
    
    <summary type="html">在本实验中，我们将详细研究著名的 TCP 协议的行为。 我们将通过从您的电脑向 远程服务器传输一份 150KB 的文件，并分析 TCP 传输内容的发送和接收过程来实现。 我们将研究 TCP 对序列和 确认号的使用，以提供可靠的数据传输;我们将看到 TCP 的拥塞控制算法 – 慢启 动和拥塞避免 – 在过程中，我们将看看 TCP 的接收器发送流量控制的机制。 我们还将简要地观察 TCP 连线的设置，我们还会研究计算机和服务器之间 TCP 连线的性能。</summary>
    
    
    
    <category term="学习" scheme="https://trevoewu.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="computer network" scheme="https://trevoewu.github.io/tags/computer-network/"/>
    
  </entry>
  
  <entry>
    <title>MAC M1 安装Oracle数据库教程</title>
    <link href="https://trevoewu.github.io/2023/04/21/MAC-M1-%E5%AE%89%E8%A3%85Oracle%E6%95%B0%E6%8D%AE%E5%BA%93%E6%95%99%E7%A8%8B/"/>
    <id>https://trevoewu.github.io/2023/04/21/MAC-M1-%E5%AE%89%E8%A3%85Oracle%E6%95%B0%E6%8D%AE%E5%BA%93%E6%95%99%E7%A8%8B/</id>
    <published>2023-04-21T12:35:34.000Z</published>
    <updated>2023-04-21T12:37:22.477Z</updated>
    
    <content type="html"><![CDATA[<h1>MAC M1 安装Oracle数据库教程</h1><p><strong>oracle目前还没有支持M1的版本, 本教程使用SSH来远程登录oracle服务器</strong></p><ol><li>在开始之前, 请确保你的mac安装了parallels Desktop, 并安装windows系统</li><li>如果你身边有windows环境, 比如你的室友的电脑, 你可以跳过1.</li><li>在windows电脑中安装Oracle Database Software.</li></ol><h2 id="安装OpenSSH服务">安装OpenSSH服务</h2><p>在window设置中</p><p><em>适用于 Windows Server 2019、Windows 10,11、Windows Server 2022</em></p><p>OpenSSH 是一款用于远程登录的连接工具，它使用 SSH 协议。 它会加密客户端与服务器之间的所有流量，从而遏止窃听、连接劫持和其他攻击。</p><p>OpenSSH 可用于将安装了 OpenSSH 客户端的 Window 10（版本 1809 及更高版本）或 Windows Server 2019 设备连接到那些安装了 OpenSSH 服务器的设备。</p><h3 id="使用windous设置安装OpenSSSH">使用windous设置安装OpenSSSH</h3><p>可以使用 Windows Server 2019 和 Windows 10 设备上的 Windows 设置安装这两个 OpenSSH 组件。</p><p>若要安装 OpenSSH 组件：</p><ol><li>打开“设置”，选择“应用”&gt;“应用和功能”，然后选择“可选功能” 。</li><li>扫描列表，查看是否已安装 OpenSSH。 如果未安装，请在页面顶部选择“添加功能”，然后：<ul><li>查找“OpenSSH 客户端”，再单击“安装”</li><li>查找“OpenSSH 服务器”，再单击“安装”</li></ul></li></ol><p>设置完成后，回到“应用”&gt;“应用和功能”和“可选功能”，你应会看到已列出 OpenSSH 。</p><p><em>安装 OpenSSH 服务器将创建并启用一个名为 <code>OpenSSH-Server-In-TCP</code> 的防火墙规则。 这允许端口 22 上的入站 SSH 流量。 如果未启用此规则且未打开此端口，那么连接将被拒绝或重置。</em></p><h3 id="使用PowerShell安装OpenSSH">使用PowerShell安装OpenSSH</h3><p>若要使用 PowerShell 安装 OpenSSH，请先以管理员身份运行 PowerShell。 为了确保 OpenSSH 可用，请运行以下 cmdlet：</p><pre><code class="language-shell">Get-WindowsCapability -Online | Where-Object Name -like 'OpenSSH*'</code></pre><p>如果两者均尚未安装，则此操作应返回以下输出：</p><pre><code class="language-shell">Name  : OpenSSH.Client~~~~0.0.1.0State : NotPresentName  : OpenSSH.Server~~~~0.0.1.0State : NotPresent</code></pre><p>然后，根据需要安装服务器或客户端组件：</p><pre><code class="language-shell"># Install the OpenSSH ClientAdd-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0# Install the OpenSSH ServerAdd-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0</code></pre><p>这两者应该都会返回以下输出：</p><pre><code class="language-shell">Path          :Online        : TrueRestartNeeded : False</code></pre><h2 id="启动并配置-OpenSSH-服务器">启动并配置 OpenSSH 服务器</h2><p>若要启动并配置 OpenSSH 服务器来开启使用，请以管理员身份打开 PowerShell，然后运行以下命令来启动 <code>sshd service</code>：</p><pre><code class="language-shell"># Start the sshd serviceStart-Service sshd# OPTIONAL but recommended:Set-Service -Name sshd -StartupType 'Automatic'# Confirm the Firewall rule is configured. It should be created automatically by setup. Run the following to verifyif (!(Get-NetFirewallRule -Name &quot;OpenSSH-Server-In-TCP&quot; -ErrorAction SilentlyContinue | Select-Object Name, Enabled)) &#123;    Write-Output &quot;Firewall Rule 'OpenSSH-Server-In-TCP' does not exist, creating it...&quot;    New-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -DisplayName 'OpenSSH Server (sshd)' -Enabled True -Direction Inbound -Protocol TCP -Action Allow -LocalPort 22&#125; else &#123;    Write-Output &quot;Firewall rule 'OpenSSH-Server-In-TCP' has been created and exists.&quot;&#125;</code></pre><h2 id="连接到-OpenSSH-服务器">连接到 OpenSSH 服务器</h2><p>mac自带SSH服务, 所以安装后，可以使用MAC连接到 OpenSSH 服务器, 在终端中运行一下命令:</p><pre><code class="language-shell">ssh username@ipadress #username是安装了Oracle的windows电脑的用户名#ipaddress是该电脑的ip地址</code></pre><p>可以通过在windows的PowerShell中运行以下命令来查看ip地址:</p><pre><code class="language-shell">ipconfig</code></pre><p>安装要求输入密码, 注意是微软用户的密码, 不是电脑的开机密码</p><p>连接后，你将看到 Windows 命令行界面提示符：</p><pre><code class="language-shell">domain\username@SERVERNAME C:\Users\username&gt;</code></pre><h2 id="用户登录Oracle数据库">用户登录Oracle数据库</h2><p>c成功链接后, 在终端输入:</p><pre><code class="language-shell">sqlplus / as sysdba</code></pre><p>按照提示输入用户名:</p><pre><code class="language-shell">sys as sysdba </code></pre><p>并按照提示输入口令, 这个口令是在你安装Orecle软件的过程中设置的.</p><p>产生一下输出则链接成功</p><pre><code class="language-shell">连接到: Oracle Database 11g Enterprise Edition Release 11.2.0.1.0 - 64bit ProductionWith the Partitioning, OLAP, Data Mining and Real Application Testing options</code></pre><p>使用<code>ctrl</code>+<code>c</code>退出sql服务, 输入<code>exit</code>退出ssh链接</p><h2 id="用Navicat远程登陆Oracle数据库">用Navicat远程登陆Oracle数据库</h2><p>打开Navicat, 左上角选择, 之后选择Oracle.</p><p>链接名请随意填写, 主机位置填入localhost, 端口默认为1521</p><p>服务名默认为ORCL, 如果你在安装Oracle中没有更改, 则保持不变, 我的改为了ORACLE</p><p>角色选择SYSDBA,用户名为sys, 密码就是之前提到的口令.</p><img src="/Users/trevorwu/Library/Application Support/typora-user-images/image-20220929221736551.png" alt="image-20220929221736551" style="zoom:50%;" / loading="lazy"><p>点击SSH选项</p><img src="/Users/trevorwu/Library/Application Support/typora-user-images/image-20220929221807569.png" alt="image-20220929221807569" style="zoom:50%;" / loading="lazy"><p>主机填入windows主机的IP地址, 端口默认22, 用户名是windows账户的用户名, 密码是该账户的密码</p><p>点击链接测试, 成功链接后点击保存即可.</p>]]></content>
    
    
    <summary type="html">oracle目前还没有支持M1的版本, 本教程使用SSH来远程登录oracle服务器</summary>
    
    
    
    <category term="mac使用指南" scheme="https://trevoewu.github.io/categories/mac%E4%BD%BF%E7%94%A8%E6%8C%87%E5%8D%97/"/>
    
    
    <category term="mac使用指南" scheme="https://trevoewu.github.io/tags/mac%E4%BD%BF%E7%94%A8%E6%8C%87%E5%8D%97/"/>
    
    <category term="oracle" scheme="https://trevoewu.github.io/tags/oracle/"/>
    
    <category term="教程" scheme="https://trevoewu.github.io/tags/%E6%95%99%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>计算机系统漫游</title>
    <link href="https://trevoewu.github.io/2023/04/21/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E6%BC%AB%E6%B8%B8/"/>
    <id>https://trevoewu.github.io/2023/04/21/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E6%BC%AB%E6%B8%B8/</id>
    <published>2023-04-21T12:31:56.000Z</published>
    <updated>2023-04-21T12:33:26.396Z</updated>
    
    <content type="html"><![CDATA[<h2 id="信息就是位-bit-加上下文-context">信息就是位(bit)加上下文(context)</h2><p>我们学习一门新的语言, 往往是从Hello world开始的, 我想没有一个程序员不喜欢这句话, 就像一个刚出生的婴儿, 对着这个陌生的世界微笑一样.</p><p>这次对计算机系统的学习就从一个hello world程序的生命周期开始. 从他被我们键入编辑器, 到在系统运行, 打印hello world最后interminates.</p><pre><code class="language-c">#include&lt;stdio.h&gt;int main()&#123;  printf(&quot;hello world\n&quot;);  return 0;&#125;</code></pre><p>我们的hello world程序生命的开始是源程序(source progam)或者源文件(source file), 以后缀 <code>.c</code> 结尾, 它是由一系列的0,1代码构成, 我们把每一位叫做bit. 每八个bit组成一个块, 我们称之为一个字节byte. 它是计算机处理信息的最小单位. 每一个byte对应着一个字符.</p><p>大多数计算机系统都采用ASCII标准来表示字符, 每一个ASCII码的值都对应着一个唯一的字符. 我们可以在linux或者like-unix系统的终端中键入<code>man ascii</code>来查看对应关系</p><img src="/Users/trevorwu/Library/Application Support/typora-user-images/image-20220928022041369.png" alt="image-20220928022041369" style="zoom:50%;" / loading="lazy"><p>我们把像<code>hello.c</code>这样用ASCII码表示的文件叫做文本文件(text files), 而其他形式的文件叫做二进制文件(binary files)</p><p>在计算机中, 所以的信息都是都是一连串的由0和1组成, 而唯一区分这些信息就是上下文. 在不同上下文中, 相同的一段比特序列可能表示一个整数, 浮点数, 或者一条指令.</p><h2 id="程序被编译器翻译成不同的格式">程序被编译器翻译成不同的格式</h2><p>计算机只认识二进制序列, helloworld这样的程序是不能被计算机理解的. 为了让计算机正确的执行我们的指令, 我们必须把它翻译成以二进制表示的形式, 我们把这样由二进制表示的语言叫做机器语言. 这些指令最后被打包成名叫可执行程序的形式, 存储为磁盘中的二进制文件.</p><p>在unix系统中, 这个由源文件翻译为可执行文件的过程是由一个叫编译器的程序完成.</p><p>整个边缘过程分为4个阶段, 分别是预编译处理, 编译, 汇编, 链接.</p><ul><li>预编译阶段修改由<code>#</code>为开始的语句, 比如这段hello.c程序中的<code>#include&lt;stdio.h&gt;</code>, 预编译阶段会在目标路径中找到对应的<code>stdio.h</code>文件, 并直接插入到程序中, 有点内容复制粘贴到过程.</li></ul><p>​这个<code>stdio.h</code>文件中主要包括函数声明, 宏定义, 以及结构体定义, 这个阶段会生成以<code>.i</code>为后缀的文本文件.</p><p>​默认情况下，预处理器的输出会被导入到标准输出流（也就是显示器），可以利用<code>-o</code>选项把它导入到某个输出文件, 在shell中键入<code>gcc -E hello.c -o hello.i</code>来输出到hello.i文件中</p><pre><code class="language-c">extern int __vsnprintf_chk (char * restrict, size_t, int, size_t,       const char * restrict, va_list);# 400 &quot;/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk/usr/include/stdio.h&quot; 2 3 4# 2 &quot;hello.c&quot; 2int main()&#123;  printf(&quot;hello world\n&quot;);  return 0;&#125;</code></pre><p>产生的代码很长很长, 大概有500行,这里只截取最后一部分. 可以发现, 即使是想helloworld这样极端简单的程序, 编译之后也会比源文件大很多.</p><ul><li>编译阶段把预编译产生的以<code>.i</code>结尾的文件翻译成以汇编语言表示的文本文件, 汇编是一种很有用的低一级的语言, 在不同的编译器编译不同的高级语言中, 它往往作为编译阶段的输出语言.</li></ul><p>我们可以在shell中键入<code>gcc -S hello.c</code>生产汇编程序<code>hello.s</code> :</p><pre><code class="language-assembly">_main:                                  ; @main.cfi_startproc; %bb.0:subsp, sp, #32                     ; =32stpx29, x30, [sp, #16]             ; 16-byte Folded Spilladdx29, sp, #16                    ; =16.cfi_def_cfa w29, 16.cfi_offset w30, -8.cfi_offset w29, -16movw8, #0strw8, [sp, #8]                    ; 4-byte Folded Spillsturwzr, [x29, #-4]adrpx0, l_.str@PAGEaddx0, x0, l_.str@PAGEOFFbl_printfldrw0, [sp, #8]                    ; 4-byte Folded Reloadldpx29, x30, [sp, #16]             ; 16-byte Folded Reloadaddsp, sp, #32                     ; =32ret</code></pre><ul><li><p>汇编阶段</p><p>在汇编阶段编译器会把上一阶段用ASCII表示的文本文件<code>hello.s</code>转化为机器语言表示的指令, 并打包生成叫做可重定位的二进制文件文件, 存储在<code>hello.o</code>的文件中, 如果我们打开这个文件,  文本编辑器会按照ASCII表示成对应的字符, 所以我们将会看到一堆乱码.</p></li><li><p>链接阶段</p></li></ul><p>注意到我们的helloworld程序调用了一个名为printf的函数, 这个函数是C标准库函数的一部分,由编译起提供. 这个函数保持在一个单独的预编译的目标文件中, 叫做<code>printf.o</code>它必须和我们的<code>hello.o</code>合并, 这个工作由链接器完成, 生成和以直接运行可执行文件<code>hello</code>.</p><p>现在, 我们的源程序以及被翻译成了可执行文件, 并存储来磁盘中. 我们可以在shell中键入<code>./hello</code>来运行我们的程序.</p>]]></content>
    
    
    <summary type="html">我们学习一门新的语言, 往往是从Hello world开始的, 我想没有一个程序员不喜欢这句话, 就像一个刚出生的婴儿, 对着这个陌生的世界微笑一样.</summary>
    
    
    
    <category term="学习笔记" scheme="https://trevoewu.github.io/categories/%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    
    
    <category term="computer system" scheme="https://trevoewu.github.io/tags/computer-system/"/>
    
  </entry>
  
  <entry>
    <title>纪念拥有了自己的博客</title>
    <link href="https://trevoewu.github.io/2023/04/21/%E7%BA%AA%E5%BF%B5%E6%8B%A5%E6%9C%89%E4%BA%86%E8%87%AA%E5%B7%B1%E7%9A%84%E5%8D%9A%E5%AE%A2/"/>
    <id>https://trevoewu.github.io/2023/04/21/%E7%BA%AA%E5%BF%B5%E6%8B%A5%E6%9C%89%E4%BA%86%E8%87%AA%E5%B7%B1%E7%9A%84%E5%8D%9A%E5%AE%A2/</id>
    <published>2023-04-21T12:28:51.000Z</published>
    <updated>2023-04-21T12:30:45.584Z</updated>
    
    <content type="html"><![CDATA[<p>当一个由你亲手塑造的东西第一次呈现在你面前的时候, 那种兴奋感以及成就感是难以比拟的.</p><span id="more"></span><p>比如当我第一次用自己的废旧的电脑组装成一台显示器, 当看到显示器作为switch副屏被点亮的时候. 比如自己照着教程给路由器刷插件来破解校园网, 看见Wi-Fi信号出现, 设备连接后, 绕过认证连接上互联网的时候.</p><p>那时候, 我好高兴好高兴, 我很想分享我的成果, 告诉宿舍的每一个人让他们连我的Wi-Fi, 从此实现Wi-Fi自由. 平时沉默不语的我在这时候犹豫了. 我只能强迫自己去做一些别的事来抑制这份心情.</p><p>而用于自己的博客带来的悸动是更胜过这两件事的.</p><p>我很开心当我告诉我的两位异性朋友时, 虽然她们对此并不了解, 仅仅是看见她们好奇和称赞我就好开心.</p><p>第一次听到博客这个词是在初二的时候, 老师给我们发了一份调查问卷, 其中一个问题便是有没有自己的博客, 那时候我还不知道博客是什么, 便以为是微博, 于是填上了是. 想必那时候拿到数据的人会不会惊讶于一个初二学生会拥有自己的博客呢.</p><p>上大学后, 靠搜索学习也让我遇见了很多优秀的博客, 有着精致的页面, 充满个人特色的UI, 以及那些学习和生活的记录, 每次翻阅, 我都觉得他们好优秀.</p><p>好像成为他们那样优秀的人.</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;当一个由你亲手塑造的东西第一次呈现在你面前的时候, 那种兴奋感以及成就感是难以比拟的.&lt;/p&gt;</summary>
    
    
    
    <category term="日常" scheme="https://trevoewu.github.io/categories/%E6%97%A5%E5%B8%B8/"/>
    
    
    <category term="日常" scheme="https://trevoewu.github.io/tags/%E6%97%A5%E5%B8%B8/"/>
    
    <category term="talk" scheme="https://trevoewu.github.io/tags/talk/"/>
    
  </entry>
  
  <entry>
    <title>hexo插入图片问题</title>
    <link href="https://trevoewu.github.io/2023/04/21/hexo%E6%8F%92%E5%85%A5%E5%9B%BE%E7%89%87%E9%97%AE%E9%A2%98/"/>
    <id>https://trevoewu.github.io/2023/04/21/hexo%E6%8F%92%E5%85%A5%E5%9B%BE%E7%89%87%E9%97%AE%E9%A2%98/</id>
    <published>2023-04-21T03:46:44.000Z</published>
    <updated>2023-04-21T04:47:56.326Z</updated>
    
    <content type="html"><![CDATA[<p>昨天刚搭建完博客, 发现直接写markdown, hexo 生成后图片全部都没法显示了, 毕竟图片都在本地, 以绝对路径的方式存储的, markdown的路径的hexo的路径不匹配.</p><span id="more"></span><p>以前搭博客的时候这个图片问题就老是困扰着我, 我用typora编写的文章转到html之后, 我还得把图片也引到项目目录来, 然后在一个个的该路径. 体验可以说是很差了, 这也导致哪怕写完文章, 我也懒得发布了, 太麻烦.</p><p>这次用hexo搭建也遇到了同样的问题, 按照老方法, 我能想到的就只有把图片全部拉过来了, 我直接把全部图片<code>mv</code>到了public目录的imag目录下, 部署到github上的时候, 由于图片太多了, 进度条老是卡住, 我只好放弃了. 我直接<code>ctl+c</code>, <code>hexo clean</code>,<code>hexo s</code>.这时候我还不知道<code>hexo clean</code>意味这什么, 只是看教程大家都是这样做的, 结果, 结果hexo把我pubilc目录下的文件全部删了…, 我的图片, 也全没了.</p><p>今天发现一个绝好的解决方案, 将Typora和hexo完美结合起来.</p><h3 id="第一步-修改hexo的站点配置文件-config-yml">第一步 修改hexo的站点配置文件_config.yml</h3><p>首先安装插件:</p><pre><code class="language-shell">npm install hexo-renderer-marked</code></pre><p>在_config.yml搜索<code>post_asset_folder</code>改为true 如下:</p><pre><code class="language-yml">post_asset_folder: truemarked:  prependRoot: true  postAsset: true</code></pre><p>修改之后会开启 Hexo 的文章资源文件管理功能。Hexo 将会在我们每一次通过 <code>hexo new &lt;title&gt;</code> 命令创建新文章时自动创建一个同名文件夹，并在执行<code>hexo g</code>的时候连同图片以前复制到public目录下(网站的根目录).于是我们便可以将文章所引用的相关资源放到这个同名文件夹下，然后通过相对路径引用。例如，你把一个 <code>example.jpg</code> 图片放在了这个同名文件夹中，使用相对路径的常规 markdown 语法 <code>![](example/example.jpg)</code>即可访问 。</p><p>也就是这样</p><p><img src="hexo%E6%8F%92%E5%85%A5%E5%9B%BE%E7%89%87%E9%97%AE%E9%A2%98/image-20230421120557507.png" alt="image-20230421120557507" loading="lazy"></p><p>但是呢, 执行<code>hexo g</code>生成的时候, hexo会把sourec目录下的_posts文件的转化为成html, 并保存在pubilc对应日期的路径中,<img src="hexo%E6%8F%92%E5%85%A5%E5%9B%BE%E7%89%87%E9%97%AE%E9%A2%98/image-20230421121112744.png" alt="image-20230421121112744" loading="lazy"></p><p>可以看到, 此时index中的图片路径和markdown的图片路径是冲突的.</p><pre><code class="language-markdown">typora的路径使用: ![](./example/example.jpg)这样页面上的img路径就会是:/你的文章标题/example/example.jpg正确的图片地址: /example/example.jpg</code></pre><p>这是因为<code>hexo g</code>把文件夹里面图片复制过来了, 但并没有复制整个图片的文件夹.</p><p>所以我只需要拿到当前博客的路径，直接加上图片名字就行.</p><h3 id="第二步-编写路径替换脚本">第二步, 编写路径替换脚本</h3><p>在全局js文件 themes\next\source\js\next-boot.js中，编写img 路径替换脚本</p><pre><code class="language-javascript">// 脚本提供者//http://songpengpeng.comvar images = document.getElementsByTagName(&quot;img&quot;);var loHref = window.location.hreflet baseHref = loHref.substring(0,loHref.lastIndexOf(&quot;/&quot;)+1)for (let i = 0; i &lt;images.length; i++) &#123;    let executeNum = 0;    let src = images[i]['src']    let img = new Image();    img.src = src    img.onload= ()=&gt; &#123;    &#125;    img.onerror= ()=&gt; &#123;        executeNum ++ ;        handAMark(src)        if(executeNum&gt;3)&#123;            return;        &#125;        let fileName = src.substring(src.lastIndexOf(&quot;/&quot;)+1);        images[i].src= baseHref+fileName    &#125;&#125;function  handAMark(src)&#123;    var as = document.getElementsByClassName(&quot;fancybox&quot;);    for (let i = 0; i &lt;as.length; i++) &#123;        let executeNum = 0;        let href = as[i]['href']        if(href==src)&#123;            let img = new Image();            img.src = src            img.onload= ()=&gt; &#123;            &#125;            img.onerror= ()=&gt; &#123;                executeNum ++ ;                if(executeNum&gt;3)&#123;                    return;                &#125;                let fileName = src.substring(src.lastIndexOf(&quot;/&quot;)+1);                as[i].href= baseHref+fileName            &#125;        &#125;    &#125;&#125;</code></pre><p>这样, 使用<code>hexo g</code>生成的时候, 就会自动将图片的路径替换.</p><h3 id="第三步-搭配typora才是最佳">第三步, 搭配typora才是最佳</h3><p>但是还是不够方便啊, 我们得先把图片放到文章的资源文件夹中. Typora对插图片的支持非常好, 可以在复制图片的同时, 把物品保存在对应的文件夹中</p><p>打开Typora, <code>⌘+,</code>进入系统偏好设置, 点击image选项卡, 修改为以下格式:</p><p>这样当插入图片的时候, 会自动把图片放到当前目录的markdown相名的文件夹中, 恰好, Hexo 在我们每一次通过 <code>hexo new &lt;title&gt;</code> 命令创建新文章时自动创建一个同名文件夹.</p><p>完美!</p><p><img src="hexo%E6%8F%92%E5%85%A5%E5%9B%BE%E7%89%87%E9%97%AE%E9%A2%98/image-20230421124001298.png" alt="image-20230421124001298" loading="lazy"></p><p>这样, 我们在Typora上写文章, 完全不用担心插入图片问题, 而且文章的图片也会有序的存放, 方面日后的管理.</p><p>现在我们只需要<code>hexo n 'title'</code>然后在Typora上写文章, 就可以直接推到博客了, 照片也可以正常显示.</p><p>你看, 我写完这篇文章的时候, hexo已经完美的生成网页了.</p><p>这太棒了, 不是吗?</p><p><img src="hexo%E6%8F%92%E5%85%A5%E5%9B%BE%E7%89%87%E9%97%AE%E9%A2%98/image-20230421124655253.png" alt="image-20230421124655253" loading="lazy"></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;昨天刚搭建完博客, 发现直接写markdown, hexo 生成后图片全部都没法显示了, 毕竟图片都在本地, 以绝对路径的方式存储的, markdown的路径的hexo的路径不匹配.&lt;/p&gt;</summary>
    
    
    
    
    <category term="日常" scheme="https://trevoewu.github.io/tags/%E6%97%A5%E5%B8%B8/"/>
    
  </entry>
  
  <entry>
    <title>使用hexo重新搭建博客</title>
    <link href="https://trevoewu.github.io/2023/04/21/%E4%BD%BF%E7%94%A8hexo%E9%87%8D%E6%96%B0%E6%90%AD%E5%BB%BA%E5%8D%9A%E5%AE%A2/"/>
    <id>https://trevoewu.github.io/2023/04/21/%E4%BD%BF%E7%94%A8hexo%E9%87%8D%E6%96%B0%E6%90%AD%E5%BB%BA%E5%8D%9A%E5%AE%A2/</id>
    <published>2023-04-20T16:52:00.000Z</published>
    <updated>2023-04-20T16:58:45.077Z</updated>
    
    <content type="html"><![CDATA[<p>之前的博客是学web的时候手搓的, 导致后期管理困难, 每次发布文章我甚至得手写HTML. 后来发现大家都是用hexo搭建博客, 我也尝试了一下.</p><p>我真的好喜欢自己之前博客的风格<a href="https://trevoewu.github.io/Peekaboo/">Peekaboo的部落格</a></p><p>现在就先用next主题吧, 这个主题虽然不怎么好看, 但是帮助文档太友好了, 感谢感谢.</p>]]></content>
    
    
    <summary type="html">之前的博客是学web的时候手搓的, 导致后期管理困难, 每次发布文章我甚至得手写HTML. 后来发现大家都是用hexo搭建博客, 我也尝试了一下.</summary>
    
    
    
    
    <category term="Talk" scheme="https://trevoewu.github.io/tags/Talk/"/>
    
  </entry>
  
  <entry>
    <title>Wireshark lab HTTP</title>
    <link href="https://trevoewu.github.io/2023/04/20/Wireshark-lab-HTTP/"/>
    <id>https://trevoewu.github.io/2023/04/20/Wireshark-lab-HTTP/</id>
    <published>2023-04-20T15:01:49.000Z</published>
    <updated>2023-05-15T13:16:02.833Z</updated>
    
    <content type="html"><![CDATA[<h1>Wireshark实验 - HTTP</h1><p><strong>官方英文文档：[<a href="http://www-net.cs.umass.edu/wireshark-labs/Wireshark_HTTP_v7.0.pdf">Wireshark_HTTP_v7.0</a>](Wireshark_HTTP_v7.0)</strong></p><p><strong>以下内容为笔者翻译：</strong></p><hr><p><strong>《计算机网络：自顶向下方法（第6版）》补充材料，J.F. Kurose and K.W. Ross</strong></p><p>“不闻不若闻之，闻之不若见之，见之不若知之，知之不若行之。” ——中国谚语</p><p>© 2005-2012, J.F Kurose and K.W. Ross, All Rights Reserved</p><hr><p>在介Wireshark实验-入门里，我们已经初步使用了Wireshark包嗅探器，我们现在可以操作Wireshark来查看网络协议。在这个实验中，我们会探索HTTP协议的几个方面：基本的GET/response交互，HTTP消息格式，检索大型HTML文件，检索具有嵌入对象的HTML文件，HTTP认证和安全性。在开始这些实验之前，您可能想查看书中第2.2节。</p><h3 id="1-基本HTTP-GET-response交互">1.基本HTTP GET/response交互</h3><p>我们开始探索HTTP，方法是下载一个非常简单的HTML文件<br>非常短，并且不包含嵌入的对象。执行以下操作：</p><ol><li>启动您的浏览器。</li><li>启动Wireshark数据包嗅探器，如Wireshark实验-入门所述（还没开始数据包捕获）。在display-filter-specification窗口中输入“http”（只是字母，不含引号标记），这样就在稍后的分组列表窗口中只捕获HTTP消息。（我们只对HTTP协议感兴趣，不想看到其他所有的混乱的数据包）。</li><li>稍等一会儿（我们将会明白为什么不久），然后开始Wireshark数据包捕获。</li><li>在浏览器中输入以下内容 <a href="http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file1.html">http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file1.html</a> 您的浏览器应显示非常简单的单行HTML文件。</li><li>停止Wireshark数据包捕获。</li></ol><p>你的Wireshark窗口应该类似于图1所示的窗口。如果你无法连接网络并运行Wireshark，您可以根据后面的步骤下载已捕获的数据包：</p><p>下载zip文件 <a href="http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip">http://gaia.cs.umass.edu/wireshark-labs/wireshark-traces.zip</a></p><p>解压缩文件 http-ethereal-trace-1。这个zip文件中的数据是由本书作者之一使用Wireshark在作者电脑上收集的，并且是按照Wireshark实验中的步骤做的。 如果你下载了数据文件，你可以将其加载到Wireshark中，并使用文件菜单选择打开并查看数据，然后选择http-ethereal-trace-1文件。 结果显示应与图1类似。（在不同的操作系统上，或不同的Wireshark版本上，Wireshark的界面会不同）。</p><p><strong>图1：Wireshark显示 <a href="http://gaia.cs.umass.edu/wireshark-labs/HTTPwireshark-file1.html">http://gaia.cs.umass.edu/wireshark-labs/HTTPwireshark-file1.html</a> 已被您的浏览器打开</strong></p><p>图1中的示例在分组列表窗口中显示了两个被捕获的HTTP消息：GET消息（<a href="http://xn--gaia-z25f82pn9csshz01ap32a0fv906c05q.cs.umass.edu">从您的浏览器发送到gaia.cs.umass.edu</a> 的web服务器）和从服务器到浏览器的响应消息。分组内容窗口显示所选消息的详细信息（在这种情况下为HTTP OK消息，其在分组列表窗口中高亮显示）。回想一下，因为HTTP消息被装载在TCP报文段内，该报文段是在IP分组封装吗，进而在以太网帧，和帧中封装，所以界面中显示了帧，以太网，IP，TCP分组信息以及HTTP报文信息。我们想最小化非HTTP数据的显示（我们这里只对HTTP感兴趣，这些其他协议将在以后的实验中研究），所以确保帧，以太网，IP和TCP行的信息被隐藏，注意左边有一个加号或一个向右的三角形（这意味着有信息被隐藏），而HTTP行具有减号或向下三角形（表示显示有关HTTP消息的所有信息）。</p><p>（注意：您应该忽略与favicon.ico相关的任何HTTP GET和response。 如果你看到一个关于这个文件的引用，这是你的浏览器自动询问服务器是否有一个图标文件应显示在浏览器的URL旁边。 我们会忽略这个引起麻烦的引用。）</p><p>通过查看HTTP GET和响应消息中的信息，回答以下问题。 在回答以下问题时，您应该打印出GET和响应消息（请参阅Wireshark-入门实验以获取信息），并指出您在消息中的哪个具体位置找到了回答以下问题的信息。 当您上交作业时，请注明输出，显示您在哪些地方表示了您的答案（例如，对于我们的课程，我们要求学生用笔标记纸质副本，或用彩色字体在电子副本的中注释文本）。</p><ol><li><p>您的浏览器是否运行HTTP版本1.0或1.1？服务器运行什么版本的HTTP？</p><p>HTTP/1.1</p><p>![image-20230416143533797](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416143533797.png)</p></li><li><p>您的浏览器会从接服务器接受哪种语言（如果有的话）？</p><p>![image-20230416144437584](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416144437584.png)</p></li><li><p>您的计算机的IP地址是什么？ gaia.cs.umass.edu服务器地址呢？</p><p>![image-20230416143642203](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416143642203.png)</p></li><li><p>服务器返回到浏览器的状态代码是什么？</p><p>![image-20230416144507009](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416144507009.png)</p></li><li><p>服务器上HTML文件的最近一次修改是什么时候？</p><p>![image-20230416144603595](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416144603595.png)</p></li><li><p>服务器返回多少字节的内容到您的浏览器？</p><p>![image-20230416144632313](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416144632313.png)</p></li><li><p>通过检查数据包内容窗口中的原始数据，你是否看到有协议头在数据包列表窗口中未显示？ 如果是，请举一个例子。</p><p>没有</p></li></ol><p>在您对上述问题5的回答中，您可能会惊讶地发现您刚才检索的文档在下载文档之前最近一次修改是一分钟前。 那是因为（对于这个特定文件），gaia.cs.umass.edu服务器将文件的最后修改时间设置为当前时间，并且每分钟执行一次。 因此，如果您在两次访问之间等待一分钟，则该文件看起来已被修改，因此您的浏览器将下载文档的“新”副本。</p><h3 id="2-HTTP条件Get-response交互">2.HTTP条件Get/response交互</h3><p>回顾书的第2.2.5节，大多数Web浏览器使用对象缓存，从而在检索HTTP对象时执行条件GET。执行以下步骤之前，请确保浏览器的缓存为空。（要在Firefox下执行此操作，请选择“工具” - &gt; “清除最近历史记录”，然后检查缓存框，对于Internet Explorer，选择“工具” - &gt;“Internet选项” - &gt;“删除文件”；这些操作将从浏览器缓存中删除缓存文件。 现在按下列步骤操作：</p><ul><li>启动您的浏览器，并确保您的浏览器的缓存被清除，如上所述。</li><li>启动Wireshark数据包嗅探器。</li><li>在浏览器中输入以下URL <a href="http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file2.html">http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file2.html</a> 您的浏览器应显示一个非常简单的五行HTML文件。</li><li>再次快速地将相同的URL输入到浏览器中（或者只需在浏览器中点击刷新按钮）。</li><li>停止Wireshark数据包捕获，并在display-filter-specification窗口中输入“http”，以便只捕获HTTP消息，并在数据包列表窗口中显示。</li><li>（注意：如果无法连接网络并运行Wireshark，则可以使用http-ethereal-trace-2数据包跟踪来回答以下问题；请参见上文注释。）</li></ul><p>回答下列问题：</p><ol start="8"><li><p>检查第一个从您浏览器到服务器的HTTP GET请求的内容。您在HTTP GET中看到了“IF-MODIFIED-SINCE”行吗？</p><p>没有</p></li><li><p>检查服务器响应的内容。服务器是否显式返回文件的内容？ 你是怎么知道的？</p><p>是的,![image-20230416150206500](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416150206500.png)</p></li><li><p>现在，检查第二个HTTP GET请求的内容。 您在HTTP GET中看到了“IF-MODIFIED-SINCE:”行吗？ 如果是，“IF-MODIFIED-SINCE:”头后面包含哪些信息？</p><p>看见了. 日期</p><p>![image-20230416151334645](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416151334645.png)</p></li><li><p>针对第二个HTTP GET，从服务器响应的HTTP状态码和短语是什么？服务器是否明确地返回文件的内容？请解释。</p><p>![image-20230416150249214](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416150249214.png)</p></li></ol><h3 id="3-检索长文件">3.检索长文件</h3><p>在我们到目前为止的例子中，检索的文档是简短的HTML文件。 接下来我们来看看当我们下载一个长的HTML文件时会发生什么。 按以下步骤操作：</p><ul><li>启动您的浏览器，并确保您的浏览器缓存被清除，如上所述。</li><li>启动Wireshark数据包嗅探器</li><li>在您的浏览器中输入以下URL <a href="http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file3.html">http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file3.html</a> 您的浏览器应显示相当冗长的美国权利法案。</li><li>停止Wireshark数据包捕获，并在display-filter-specification窗口中输入“http”，以便只显示捕获的HTTP消息。</li><li>（注意：如果无法连接网络并运行Wireshark，则可以使用http-ethereal-trace-3数据包跟踪来回答以下问题；请参见上文注释。）</li></ul><p>在分组列表窗口中，您应该看到您的HTTP GET消息，然后是对您的HTTP GET请求的多个分组的TCP响应。这个多分组响应值得进行一点解释。回顾第2.2节（见文中的图2.9），HTTP响应消息由状态行组成，后跟标题行，后跟一个空行，后跟实体主体。在我们的HTTP GET这种情况下，响应中的实体主体是整个请求的HTML文件。在我们的例子中，HTML文件相当长，4500字节太大，一个TCP数据包不能容纳。因此，单个HTTP响应消息由TCP分成几个部分，每个部分包含在单独的TCP报文段中（参见书中的图1.24）。在Wireshark的最新版本中，Wireshark将每个TCP报文段指定为独立的数据包，并且单个HTTP响应在多个TCP数据包之间分段的事实由Wireshark显示的Info列中的“重组PDU的TCP段”指示。 Wireshark的早期版本使用“继续”短语表示HTTP消息的整个内容被多个TCP段打断。我们在这里强调，HTTP中没有“继续”消息！</p><p>回答下列问题：</p><ol start="12"><li><p>您的浏览器发送多少HTTP GET请求消息？哪个数据包包含了美国权利法案的消息？</p><p>![image-20230416151626850](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416151626850.png)</p><p>两个, 第一个</p></li><li><p>哪个数据包包含响应HTTP GET请求的状态码和短语？</p><p>![image-20230416151652019](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416151652019.png)</p><p>第二个</p></li><li><p>响应中的状态码和短语是什么？</p><p>200 OK</p></li><li><p>需要多少包含数据的TCP段来执行单个HTTP响应和权利法案文本？</p><p>4个TCP段</p><p>![image-20230416151853132](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416151853132.png)</p></li></ol><h3 id="4-具有嵌入对象的HTML文档">4.具有嵌入对象的HTML文档</h3><p>现在我们已经看到Wireshark如何显示捕获的大型HTML文件的数据包流量，我们可以看看当浏览器使用嵌入的对象下载文件时，会发生什么，即包含其他对象的文件（在下面的例子中是图像文件） 的服务器。<br>执行以下操作：</p><ul><li>启动您的浏览器。</li><li>启动Wireshark数据包嗅探器。</li><li>在浏览器中输入以下URL <a href="http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file4.html">http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file4.html</a> 您的浏览器应显示包含两个图像的短HTML文件。这两个图像在基本HTML文件中被引用。也就是说，图像本身不包含在HTML文件中；相反，图像的URL包含在已下载的HTML文件中。如书中所述，您的浏览器将不得不从指定的网站中检索这些图标。我们的出版社的图标是从 <a href="http://www.aw-bc.com">www.aw-bc.com</a> 网站检索的。而我们第5版（我们最喜欢的封面之一）的封面图像存储在manic.cs.umass.edu服务器。</li><li>停止Wireshark数据包捕获，并在display-filter-specification窗口中输入“http”，以便只显示捕获的HTTP消息。</li><li>（注意：如果无法连接网络并运行Wireshark，则可以使用http-ethereal-trace-4数据包跟踪来回答以下问题；请参见上文注释。）</li></ul><p>回答下列问题：</p><ol start="16"><li><p>您的浏览器发送了几个HTTP GET请求消息？ 这些GET请求发送到哪个IP地址？</p><p>三个</p><p>![image-20230416152201783](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416152201783.png)</p></li><li><p>浏览器从两个网站串行还是并行下载了两张图片？请说明。</p><p>猜测为串行, 因为这两个GET请求发送有时间差</p><p>![image-20230416152416694](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416152416694.png)</p></li></ol><h3 id="5-HTTP认证">5.HTTP认证</h3><p>最后，我们尝试访问受密码保护的网站，并检查网站的HTTP消息交换的序列。URL <a href="http://gaia.cs.umass.edu/wireshark-labs/protected_pages/HTTP-wireshark-file5.html">http://gaia.cs.umass.edu/wireshark-labs/protected_pages/HTTP-wireshark-file5.html</a> 是受密码保护的。用户名是“wireshark-students”（不包含引号），密码是“network”（再次不包含引号）。所以让我们访问这个“安全的”受密码保护的网站。执行以下操作：</p><ul><li>请确保浏览器的缓存被清除，如上所述，然后关闭你的浏览器，再然后启动浏览器</li><li>启动Wireshark数据包嗅探器。</li><li>在浏览器中输入以下URL <a href="http://gaia.cs.umass.edu/wireshark-labs/protected_pages/HTTP-wiresharkfile5.html">http://gaia.cs.umass.edu/wireshark-labs/protected_pages/HTTP-wiresharkfile5.html</a> 在弹出框中键入所请求的用户名和密码。</li><li>停止Wireshark数据包捕获，并在display-filter-specification窗口中输入“http”，以便只显示捕获的HTTP消息。</li><li>（注意：如果无法连接网络并运行Wireshark，则可以使用http-ethereal-trace-5数据包跟踪来回答以下问题；请参见上文注释。）</li></ul><p>现在来看看Wireshark输出。 您可能需要首先阅读HTTP身份验证相关信息，方法是在 <a href="http://frontier.userland.com/stories/storyReader$2159">http://frontier.userland.com/stories/storyReader$2159</a> 上查看“HTTP Access Authentication Framework ”上的易读材料。</p><p>回答下列问题：</p><ol start="18"><li><p>对于您的浏览器的初始HTTP GET消息，服务器响应（状态码和短语）是什么响应？</p><p>![image-20230416152712931](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416152712931.png)</p><p>未授权</p></li><li><p>当您的浏览器第二次发送HTTP GET消息时，HTTP GET消息中包含哪些新字段？</p><p>![image-20230416152814670](/Users/trevorwu/Library/Application Support/typora-user-images/image-20230416152814670.png)</p><p>一个Credentials: 的字段</p></li></ol><p>您输入的用户名（wireshark-students）和密码（network）按照客户端HTTP GET消息中请求头的“Authorization: Basic ”的字符串（d2lyZXNoYXJrLXN0dWRlbnRzOm5ldHdvcms=）编码。虽然您的用户名和密码可能加密，但它们只是以一种称为Base64格式的格式进行编码。用户名和密码并没有加密！要确认这些，请访问 <a href="http://www.motobit.com/util/base64-decoder-encoder.asp">http://www.motobit.com/util/base64-decoder-encoder.asp</a> 并输入base64编码的字符串d2lyZXNoYXJrLXN0dWRlbnRz 并进行解码。瞧！您已从Base64编码转换为ASCII编码，因此应该看到您的用户名！要查看密码，请输入字符串Om5ldHdvcms=的剩余部分，然后按解码。因为任何人都可以下载像Wireshark这样的工具，而且可以通过网络适配器嗅探数据包（不仅仅是自己的），任何人都可以从Base64转换为ASCII（你刚刚就这么做了！），所以你应该很清楚，WWW网站上的简单密码并不安全，除非采取其他措施。</p><p>wireshark自带解码…</p><p>不要害怕！ 正如我们将在第8章中看到的，有一些方法可以使WWW访问更加安全。然而，我们显然需要一些超出基本HTTP认证框架的知识！</p>]]></content>
    
    
    <summary type="html">在这个实验中，我们会探索HTTP协议的几个方面：基本的GET/response交互，HTTP消息格式，检索大型HTML文件，检索具有嵌入对象的HTML文件，HTTP认证和安全性</summary>
    
    
    
    
    <category term="computer network" scheme="https://trevoewu.github.io/tags/computer-network/"/>
    
  </entry>
  
  <entry>
    <title>Wireshark Lab: IP v7.0</title>
    <link href="https://trevoewu.github.io/2023/04/20/Wireshark-Lab-IP-v7-0/"/>
    <id>https://trevoewu.github.io/2023/04/20/Wireshark-Lab-IP-v7-0/</id>
    <published>2023-04-20T10:22:06.000Z</published>
    <updated>2023-04-21T03:44:49.965Z</updated>
    
    <content type="html"><![CDATA[<ul><li>Select the first ICMP Echo Request message sent by your computer, and expand the Internet Protocol part of the packet in the packet details window.(选择您的计算机发送的第一个 ICMP Echo Request 消息，并在数据包详细信息窗口中展开数据包的 Internet 协议部分)<br>不知道为什么, 我的电脑只能收到Time to live exceeded报文段.</li></ul><span id="more"></span><p>结果是因为我在过滤了ICMP, 而<strong>traceroute默认使用UDP而不是ICMP</strong>, 所以只能收到路由器发给我的ICMP time to live exceeded 报文,告诉我的数据包被干掉了.</p><p>tracert 默认使用 ICMP 包探测，traceroute 使用 UDP 包，也可以使用 ICMP 和 TCP 包探测, 带上参数-T即可.</p><pre><code class="language-shell">-I      Use ICMP ECHO instead of UDP datagrams.  (A synonym for &quot;-P icmp&quot;).</code></pre><pre><code class="language-shell">traceroute baidu.com 2000  # 使用baidu测试, 默认使用UDP, 数据报大小为2000byte</code></pre><ul><li><p>What is the IP address of your computer?</p></li><li><p>在 IP header 中，上层协议字段的值是多少?</p><p>我的IP地址: 10.51.172.226</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421113757555.png" alt="image-20230421113757555" loading="lazy"></p><p>这是traceroute发送的第一个数据报, 可以得到以下信息:</p><ul><li>该数据报是IP数据报, 上层协议为UDP</li><li>数据报被分片了, More Fragment字段被设为1</li><li>源IP是10.51.172.226, 这是host IP</li><li>目标IP是110,242,68,66, 这是Baidu.com的IP地址, 我用百度来测试.</li></ul></li><li><p>IP datagram 的有效负载中有多少 bytes? 说明如何 确定 payload bytes 的数。</p></li></ul><p><img src="Wireshark-Lab-IP-v7-0/image-20230421113848009.png" alt="image-20230421113848009" loading="lazy"></p><p>payload = Total Len - header Len = 1500 - 20 = 1480, 是的, 我直接发送了2000byte的报文</p><ul><li><p>此 IP 数据报是否已被分段(fragmented)?解释您如何确定数据报是否已被分段(fragmented)</p><p>是的, 前文有提到, 这里不再赘述.</p></li></ul><blockquote><p>接下来，通过单击 Source 列标题，根据 IP 源地址对跟踪的数据包进行排序，一个小的向下箭头应出现在 Source 旁边，如果箭头指向上方请再次单击“Source column header”。选择计算机发送的第一个 ICMP Echo Request 消息，然后展开“details of selected packet header”窗口中的 Internet 协议部分。在“listing of captured packets”窗 口中，您应该在第一个 ICMP 下面看到所有后续 ICMP 消息(可能还有计算器上运行的其他协议发送的其他散布数据包)，使用向下箭头浏览计算器发送的 ICMP 消息。</p></blockquote><ul><li><p>在您的计算器发送的这一系列 ICMP 消息中，IP 数据报中的哪些字段总有改变</p><p>使用</p><pre><code class="language-sh">traceroute -I gaia.cs.umass.edu -80# 用作者给的网站测试, -I表示使用ICMP# The default probe datagram length is 40 bytes[traceroute manual]# 但是我使用作者提议用56作为数据报(ip datagrame)大小时, 收到以下警告: # traceroute: packet length must be &gt; 71# 所以这里数据报大小设为80, 实际上, 我使用不带数据报大小参数的命令时, 默认数据报大小为1380</code></pre><p>再次抓包, 由于traceroute实际上会发送三个ICMP的数据报, 所以三个为一组.  所以可发现每一组的ID值差3, 当然是因为一组发了三个ICMP数据报啊…</p></li></ul><p>第一个:</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421113947335.png" alt="image-20230421113947335" loading="lazy"></p><p>第二个:</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114009587.png" alt="image-20230421114009587" loading="lazy"></p><p>第三个:</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114026434.png" alt="image-20230421114026434" loading="lazy"></p><p>可以发现, Id, TTL, Header checksum改变了.</p><ul><li><p>哪些字段保持不变? 哪个字段必须保持不变? 哪些字段必须更改? 为什么?</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114245968.png" alt="image-20230421114245968" loading="lazy"></p><ul><li>Version, Header Length , TOS(Differentiate Services Field差异化服务领域), Flag, TTL, Protocol, SIP,DIP保持不变</li><li>Version, Protocol, TOS, Header Len必须保持不变.</li><li>Identification, checksum,TTL必须更改</li></ul></li><li><p>描述您在 IP 数据报的标识字段的值中看到的模式</p></li></ul><p>每发一个ICMP的数据报, ID字段加一(不分片的情况下, 分片时一个IP数据报的不同片具有相同的ID)</p><ul><li>下一步(数据包仍按来源地址排序)查找最近的(第一跳)路由器发送到您的计算器的一系列 ICMP TTL 超出的回复讯息。</li><li>ID 字段和 TTL 字段的值是多少?</li></ul><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114303180.png" alt="image-20230421114303180" loading="lazy"></p><ul><li><p>对于最近(第一跳)路由器发送到您的计算器的所有 ICMP TTL 超出的回复，这些值是否保持不变?为什么?</p><p>TTL保持不变, 但ID改变了. TTL都为64(可以改变, 理论上可以为1, 因为第一台路由器到我的主机一跳可达)</p><p>在不管是发回给我的time to live exceeded还是ICMP reply TTL的值都为64, 可能是默认的值.</p></li></ul><blockquote><p>单击“时间”列，再次按时间对数据包列表进行排序。</p></blockquote><ul><li>在将 pingplotter 中的数据包大小更改为 2000 后，查找计算机发送的第一个 ICMP Echo Request 消息。该消息是否已分片为多个 IP 数据报?</li><li>打印出碎片 IP 数据报的第一个片段。 IP 头中的哪些信息表明数据报已碎片 化? IP 头中的哪些信息表明这是第一个片段还是后一个片段? 这个 IP 数据报有多长?</li><li>打印出碎片 IP 数据报的第二个片段。 IP 标头中的哪些信息表明这不是第一 个数据报片段? 是否还有更多的片段? 你是如何知道的?</li></ul><p>是的, 该消息被分片了, 可以通过flag字段看出, Fragmen Offset 为1480, 而More Fragment为0 ,告诉这是第二个message, 那第一个到哪里去了呢?</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114317964.png" alt="image-20230421114317964" loading="lazy"></p><p>Wireshark告诉我在frame 73, 于是我去寻找frame73</p><p><em>73frame和74frame的payload 和 为 1980, 因为规定了ICMP的IP数据报大小为2000, 但由于IP头部的20byte, 实际的payload恰好为1980byte</em></p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114327978.png" alt="image-20230421114327978" loading="lazy"></p><p>注意到发到第一个message竟然不是ICMPmessage, 而是一个IPdatagram</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114340203.png" alt="image-20230421114340203" loading="lazy"></p><p>打开IP头部, 我们发现, 这仍然是一个ICMP的数据报, 但是在IP的数据部分并没有ICMP头部.</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114350725.png" alt="image-20230421114350725" loading="lazy"></p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114403647.png" alt="image-20230421114403647" loading="lazy"></p><ul><li><p>在第一个和第二个片段中，IP 标头中哪些字段发生了变化?</p><p>第一个fregment:</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114415389.png" alt="image-20230421114415389" loading="lazy"></p><p>第二片:</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114431692.png" alt="image-20230421114431692" loading="lazy"></p><p>注意到红色部分发送了改变, 而蓝色部分没有改变, IP数据报用ID来标识多个片是否属于同一个IP数据报.</p></li></ul><blockquote><p>现在，在将 pingplotter 中的数据包大小更改为 3500 后，找到计算机发送的第一个 ICMP Echo Request 消息。</p></blockquote><ul><li><p>从原始数据报创建了多少个片?</p><p>三个片</p><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114440735.png" alt="image-20230421114440735" loading="lazy"></p></li><li><p>片段中 IP 标头中的哪些字段发生了变化?</p><p>和上一个结论相同, 红色部分发送了改变, 而蓝色部分没有改变, IP数据报用ID来标识多个片是否属于同一个IP数据报.</p></li></ul><p><img src="Wireshark-Lab-IP-v7-0/image-20230421114449611.png" alt="image-20230421114449611" loading="lazy"></p>]]></content>
    
    
    <summary type="html">在本实验中, 我们将研究 IP 协议,重点关注IP datagram. 我们将通过分析在执行 traceroute 程序发送和接收的一系列 IP 数据报的过程来完成这个实验,我们将研究IP datagram 中的各个字段(fields)，并详细研究 IP fragmentation 的方法</summary>
    
    
    
    
    <category term="computer network" scheme="https://trevoewu.github.io/tags/computer-network/"/>
    
    <category term="lab" scheme="https://trevoewu.github.io/tags/lab/"/>
    
  </entry>
  
</feed>
